# 运行时数据（共享后端 SQLite / 旧 JSON 存储），不入库
server/data.sqlite
server/data.sqlite-wal
server/data.sqlite-shm
server/data.json
# 无限画布完整草稿中的图片二进制，是运行时持久数据，不随代码同步。
server/canvas_blobs/
# 本地/服务器密钥配置，不入库
.env
.env.local
# 本地 Python 环境、备份与日志不入库
.venv/
.venv-py*/
backups/
logs/
# Python 缓存
__pycache__/
**/__pycache__/
# 子应用本地持久数据与模型缓存
runtime/
# 服务器备份与详情文档（含真实密钥，禁止提交）
服务器详情.md
服务器数据/
# 系统
.DS_Store
