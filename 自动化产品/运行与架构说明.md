# 星阵运行与架构说明

更新时间：2026-08-13

本文档是星阵项目的“当前事实入口”。新线程和每次版本迭代应优先读取、实时维护本文件；`docs/version.md`、`docs/Problem Document.md` 和 `docs/Server Deployment Log.md` 是按需检索的历史记录，不要求默认全文阅读。

## 当前版本状态

- 当前生产为 v143.0，功能提交 `f95e9f9dcb492022137dbf8b9b8f1f249cd38c6e`，分支 `codex/v141-content-governance`，release/cache `20260813-v1430-batch-durable-start-1`，实际 sibling release `20260813-v1430-batch-durable-start-1-f95e9f9d`。生产由 v143.0 green main/sidecar 和首位精确路由承接流量；v142.9 main/sidecar/routing 仍 active/RW，构成不替换数据库的即时在线代码回滚。发布全程没有停服或转只读；当前 `ready=true / writeReady=true / startupVerified=true`，sidecar 为 `ready=true / readOnly=false / writePolicy=normal`，SQLite `quick_check=ok`。
- v143.0 让图文批次在付费 worker 起跑前依次确认 production、batch 根和 session 都已写入服务器；失败时保留任务并暂停，不再产生“子项存在、刷新找不到批次”。水合只为当前 owner、近十四天、未交付且归属明确的孤立 production 重建原 batchId，服务器确认后续跑未完成项，成功和结果未知项均不重提。每个图文批次只占一个共享图片通道，服务器两路槽位可在成员间公平交错；等待窗口为 `240s`，结果未知边界不变。v142.9 的画布整批耐久登记、v142.8 的多参考图和成功 Blob 权威继续保留。
- 本版目标 Linux 为锁定主服务 `832 passed + 1 approved skip`、sidecar `160/160`、Node `129/129`，release verifier 通过。公网真实无限画布成功一张，生产流量中两条小红书图文各 `3/3` 图片完成且 batch 根/刷新索引完整；root/health/OpenAPI/community posts 各 `20/20` 为 200，Chromium 加载精确 v143.0 cache。保护点 verify/restore-drill 通过，48 表与六类媒体均无减少；代码/静态更新没有覆盖 SQLite、账号、媒体、任务、认证、Nginx 或 provider 私密配置。
- 生产 provider 配置的权威来源仍是 release 外私密 env。新 release 环境必须在绿色进程接流量前继承既有 LLM、图片、TTS、数字人、普通视频配置，再叠加本版 Qianfan/Seedance 2.5 配置；只检查新增 key 或只看 socket health 会造成“页面可打开、生成才报未配置”的半联通状态。验收至少核对主 health 的 LLM 标志、creator-auth 的 image/TTS/video config 和一次必要的真实生成；不得打印、复制到 Git 或写入 release。
- 模型用量审计与 sidecar usage 审计是永久 warning-only：未决 receipt/outbox/spool 继续可观察和记录，但不阻断启动、RW、路由或普通生产。供应商曝光量/观看量、回传链接和权限记录不是这类技术用量审计，仍按原业务事务保存和保护。媒体 exact exception 只接受绑定当前 release、精确缺失集合摘要和数量的证据；原引用和 raw 缺失事实必须保留，未来漂移仍重新告警。

- v142.6 补齐供应商 `team_suppliers` 身份在私有媒体登记中的团队解析，并为内容账号头像建立独立精确读取：母账号可管理团队全部账号头像，子账号只读已分配账号，服务端核对 `accountId → avatarAssetId → asset → media` 完整链路，不获得团队通用媒体权限。标题驱动的批量图文文案在模型提示与确定性后处理两层保证正文加 4–7 个标签合计不超过 `1000` 个 Unicode 字符；账号每日配额继续只统计成功交付，不改配额历史或日账本。目标 Linux 锁定主服务 `819 passed + 1 approved skip`、sidecar `160/160`、Node `129/129` 与 release verifier 已通过；服务器私密环境中的 LLM、图片、TTS 和 Qianfan 最小真实调用均返回 200，Seedance/OmniHuman 配置与可达性为真。

- v142.5 现作为仍在线的首选代码回滚点保留。它保留 v142.4 的 Seedance 参考闭环与图卡 JSON 收口，并修复批量图文全局哈希去重导致的跨账号媒体错绑、首条失败中止整批发布、失败 operation key 不能显式重试，以及无限画布小 logo/中文 donor 指令不兼容的问题。
- v142.5 的批量图片对资产存在性、类型、内容账号、交付状态和文件状态做端到端校验。只对已在客户端确证的 wrong-account/wrong-type/delivered/file-missing 绑定转为缺失项，保留异常原 ID 和其他成功图；局部水合中找不到的资产不自动清空。用户明确重试缺失图时才提升 revision，不在页面启动、水合或轮询时自动重提 provider。
- v142.5 不改 schema、持久路径、权限或 Nginx；生产只新增独立绿色 systemd/路由实例并保留旧实例回滚。目标 Linux 定向回归 `77/77`，无密钥锁定主服务 `816 passed + 1 approved skip`，视频 sidecar `160/160`，Node `129/129`，无限画布 lint/typecheck/build 及 `63 files / 1,768,749 bytes` vendor 闭包已通过。release verifier Phase 0 为 `3c497d0b3a523fb5ba21a5cb964d47c33cb8e77b0d0df7f356166e770c72671f`，ESM closure `77dbdd6882192bc0bd48109f3f42805b87f3ba02b9169cfc9cfe5f08a5da6285`，runtime `65 files / 3,318,796 bytes / c478fbe3aefb64e8c1d273f65b0fa9fb20cea7ee2ad5f5c3acf1ad39dcd2636f`。切换前后 48 表 / 66,978 行和六类媒体文件/字节均无减少；公网、静态闭包和 5 类角色共 22 条只读 API 验收通过，未调用真实付费 provider。

- 单张素描故事板收口后的最新本地证据：主服务定向 `60/60`、视频 sidecar `160/160`、Node `129/129`；本轮定向工作流 `36/36`、视频计费 `7/7` 通过，release verifier Phase 0 `7c03a159ced9f1a1f1e3946873ca3277eb8c9be6e4c677380ca56236defbbcdb`，ESM closure `89d9636bc29044b98a7d132b12723c23acf53271869f424d349f212e82324011`，runtime `65` files / `3,311,104` bytes / `3dc993d95b9b64a3b22f49d253dc37e3ee9e6e3fcf4b3d02eb1ca07c3edef032`。这些是本地代码与 fake-provider 契约证据；方舟 2.5 另有开通后的真实 submit/poll/output 闭环证据。

- v142.3 最新故事板契约为“一张 16:9 横版多格素描故事板 + 一份连续视频提示词”。语言模型生成的 5–10 个 `0–30s` 逻辑分镜只是故事板内部格子；image-2 只执行一次，用户统一参考图参与这张 owner-scoped 故事板生成。Seedance 提交时同时携带故事板、用户统一参考图、视频提示词和显式的 `duration=30` / 默认 `ratio=9:16` / `generate_audio=true`；故事板比例不决定最终视频比例。看板与微调只操作整张素描故事板。
- 真实 API 状态：百度千帆搜索已返回 HTTP 200、`request_id` 和 3 条 references。方舟控制台已显示 `doubao-seedance-2-5-260628` 服务“已开通”，开通后最小真实任务提交 HTTP 200，任务 `cgt-20260811065820-5dj9d` 轮询至 `succeeded` 且包含输出。原 `ModelNotOpen` 阻断已解除；生产切换前仍须由部署线程在目标 Linux 与服务器私密环境验收完整故事板链路，不能用默认 2.0 静默降级。

- v142.3 已按上面的零停机绿色拓扑部署。它累计包含 v142.1 的百度搜索 AI 选题与可折叠工作区，并新增统一视频号、任务级数字人/创意视频选择、image-2 故事版和显式 Seedance 2.5 创意模型路由；本次没有上传或替换服务器 SQLite、账号、成员、媒体、认证、Nginx 或运行数据。
- v142.3 的数字人与创意视频详情只暴露“生成/审核”两节点。批量 production 不新建第二套剪辑数据模型：主服务按当前 owner 验证 production、job 与本地物理成片后，幂等生成视频工坊项目/构图快照并只复制进入 sidecar 受控输出目录；原始 production、job、媒体与账本不改写，桥接过程不调用 provider。视频工坊继续作为唯一剪辑实现，承载画中画、主轨替换、字幕、口播/BGM/音效、转场与外部素材。
- 创意故事版的图片 transport 使用稳定 operation key 和耐久 attempt receipt。上游 `ReadTimeout`、浏览器 Abort 或 `502/503/504` 且已调用 provider 时属于“结果未知”，只能查询回执并保留为 confirming/pending，不能写终态 failed 或自动重提；显式单张微调重生成会提升 revision，因而与原未知调用清晰分离。该恢复协议不放宽真实失败、图片引用归属或媒体存在性门禁。
- 视频号账号只表达发布身份，历史 `subType` 不再决定新任务生成链路。批量计划以 `videoCreationMode=digitalHuman|creativeVideo` 选择数字人或创意视频：数字人继续读取角色版、声线和原有片段链路；创意视频先生成覆盖 `0–30s` 的结构化故事版，再创建一个连续 30 秒音画同出 job。视频工坊与定制发布按账号 `mode=视频` 展示全部未停用视频号；旧账号字段和旧任务不迁移。
- 创意故事版与无限画布、批量图文共享主服务图片配置和前端 `activeProviderFor("image")`，单张请求携带稳定 operation key、原任务参考图和 `16:9` 故事板约束；完成后保存为 owner-scoped 资产，续跑只补这一张未完成故事板。最终视频再组合故事板与原任务参考图，新任务不再生成 A/B 面或前后 15 秒 job；历史 `infoFlow` 数据仍由兼容视图读取，不能为清理 UI 删除旧任务或媒体。
- Seedance 2.5 创意能力使用显式配置 `SEEDANCE_CREATIVE_MODEL=doubao-seedance-2-5-260628`（兼容 `SEEDANCE_25_MODEL`）与 `SEEDANCE_CREATIVE_MAX_DURATION=30`。该模型已在控制台开通并通过最小真实生成；缺少创意模型时主服务仍在计费和 provider 调用前返回 `503`，不使用默认 2.0 兜底。普通视频和数字人继续原模型路由；部署 release 只同步代码，API key 必须安全转存到服务器 release 外的私密环境，不能进入源码或构建产物。
- 百度搜索 AI 选题由 creator-auth 的主服务路由调用官方 `POST /v2/ai_search/web_search`，固定 `baidu_search_v2` 并只接受近一周/近一月。`QIANFAN_SEARCH_API_KEY` 只存在私密运行环境；references 仅保留 HTTP(S) URL、有限正文和来源字段，再交给既有可计费文案模型生成账号级 JSON。账号最多 6 个一组，漏项只逐账号修复，服务端与前端都要求返回账号集合与所选集合完全一致后才允许填入。生成载荷只保留账号 ID/名称/平台/可选产品，不传账号定位、人设、语气或历史风格；小红书标题使用事实匹配的热门标题写法，正文按题目选择干货拆解或自然真人分享。服务端归一化与前端发布规则共同执行小红书 20/1000、视频号 16 字无标点；确认动作只补空白行，不覆盖人工输入。
- 批次 `paused` 是既有 batch 文档的调度状态：worker 在下一条 production 前等待，阶段评估、生成与重试在暂停时不再 dispatch；已经进入上游的单次调用不 abort，仍按原 receipt/checkpoint 保存。整批删除先从服务器精确删除 batch 与未交付 production/job，再更新本地状态，已交付 production 不在删除集合。
- 统一工作区的折叠状态只保存本机 UI 偏好；桌面收起后使用白色图标 rail 与原版蓝色星阵图标，主区变为 `56px + 1fr`。rail 不维护第二份导航定义，而是按 `workspaceNavItems()` 动态隐藏与排序，因而与 Logo 下拉栏共享当前角色/团队的 entitlement、顺序、zone/page 路由；`custom/video` 与 `custom/canvas` 还按 page 独立高亮。展开按钮与搜索按钮在 flex 布局中各占固定宽度，底部只显示当前用户头像。视频声音工作台使用当前成员隔离的独立折叠偏好，收起到 `48px`，主对话区自适应扩展。
- 无限画布不再创建自己的侧栏收起控件或父页面小地图 portal；平台统一 rail 是唯一的全局收起入口，画布 ViewportControls 始终留在 iframe 画布左下角。工作区菜单文字统一 `nowrap`，窄菜单里的“无限画布”仍保持单行。
- 无限画布草稿写入先验证当前 owner/scope 下的全部 Blob 引用，再提交草稿。后置 GC 使用独立 savepoint：已知的历史业务/社区缺失引用只会让本次 GC 延后，不再回滚无关且已验证的用户保存；缺失引用本身及 readiness/media audit 事实不变。相同逻辑草稿的丢响应重试可幂等确认，真实内容差异继续 `409` 并保留本地恢复包。
- 主服务管理的 LLM 请求由浏览器使用覆盖两次服务端尝试的有界窗口；图片调用使用稳定 item operation key，客户端窗口覆盖 `IMAGE_SUBMIT_QUEUE_WAIT_SECONDS` 与上游调用预算。队列等待超过 `30s` 必须在调用 provider 之前返回 `image_queue_busy/providerCalled=false`；浏览器 Abort、`409` 或网络未知只能进入 receipt 核对/确认态，不能直接判定失败或盲目重提。`GET /api/image/operations/{operation_key}` 只读取耐久用量回执，不产生调用。
- 批量图片检查点先保存本地结果，再以 `20s` 专用窗口和受控退避同步单文档；同步暂时失败只表示保存待确认。每个已完成 item 与稳定 operation key 一起保留，重试只续跑有 prompt 的缺失项；空 items/空 prompt 返回起草阶段。成功 review 统一清理旧错误，恢复器与用户重试共用同一幂等判断。
- 旧信息流 A/B 结构化协议仅保留给已有历史任务的恢复与查看；v142.3 新建创意视频使用 `0–30s` 连续故事版协议，首次结构不合格只做一次定向修复，仍不合格即在图片/视频 provider 前终止。两条兼容路径都不能伪造缺失镜头或复用别的 production 媒体。
- 本地启动在 `DATA_DB` 未显式配置时把 SQLite、uploads、composed、canvas blobs 和 video-workshop projects/uploads/outputs 视为同一恢复单元；仅有一个完整恢复点才自动绑定，多个完整点 fail closed。该选择只适用于本地开发，生产 release 永远不得上传或替换服务器 SQLite、账号、媒体、认证、私密配置和持久目录。
- v142.3 当前相关验证为 Python `60/60`、工作区 Node `60/60`，覆盖创意视频计费/模型路由、数字人兼容、未知图片结果恢复、30 秒故事版看板和批量剪辑台幂等桥接；相关 JavaScript 语法和 Python 编译通过。fake provider 验证缺配置零调用、显式模型单条 30 秒 payload、同图像 provider 故事版与新任务无 A/B job，没有触发真实图片、视频、语音或发布调用。最终本地 verifier 为 Phase 0 `c977182cca0c3f9fe9bfa0f0dd5950af77ae89a2cdf9e04a0d011a1f83fbebae`、ESM closure `4c575baae9629492abf29c9f6822afdc8711bf9afd3440f38ef3612c0a5d46dc`、canvas `63` files / `1,768,481` bytes / `6ca04ee8ff6c4fba5bc581c641e542830dc7d7c0d187311b89663bed4f63a018`、runtime `65` files / `3,311,104` bytes / `3dc993d95b9b64a3b22f49d253dc37e3ee9e6e3fcf4b3d02eb1ca07c3edef032`。正式 Linux / CPython 3.12 locked 全量仍由获授权部署流程执行。
- “账号数据”是原数据看板与单号创作的前端聚合入口：左侧先显示数据看板，再列出全部账号；账号点击仍进入原 studio，且 studio 左栏顶部仍保留数据看板返回项。账号、production、权限、发布与配额数据模型均未迁移。首屏上层并排展示总播放量平台环图、发布量平台环图与发布沟通预览，下层整行展示发布趋势；内容弹窗以默认折叠的账号 `details` 聚合供应商权威曝光/播放和分析快照赞藏评分享，并显示账号、创作人及真实回传链接。平台、时间、账号、创作人、日期与播放量范围只影响当前只读视图；本版移除顶栏同步按钮，不在进入看板时主动同步。
- 视频工坊 `running` 项目在既有项目 JSON 中保存 `runStartedAt`：仅从非运行态进入运行态或旧运行项目缺字段时写入，同一运行任务的进度/事件 mutate 不覆盖；终态后下一次运行会获得新的开始时间。前端离开页面只停止本页 interval，重新进入仍优先读取耐久时间，旧项目才使用当前成员 + 项目隔离的 localStorage 首见时间。“思考中”展开区把当前请求 token 作为 pending 边界；进入 production-live 后再以本轮 `runStartedAt` 截断耐久事件，过滤旧通用占位、去重并限制为最多三个不同的当前步骤。问候和独立新任务不继承历史，只有明确承接/修改表达才读取同一会话最近有效任务作为公开摘要。该判断只影响展示文本，不修改 `/api/chat` 载荷、后端会话上下文、项目 JSON、provider 编排或用量结算，也不显示隐藏推理。
- 统一左栏的视频会话三点菜单始终是绝对定位浮层；运行中柔光只提升会话主按钮和三点按钮的层级，不能用直接子元素通配规则覆盖菜单定位。菜单开关、重命名、收藏、分组和删除逻辑保持原实现，本版只修复布局层级。
- 视频工坊对存在历史未决 receipt 的项目继续 fail closed，主服务以结构化 `video_workshop_usage_pending` 告知客户端。客户端不修改旧项目证据、不自动创建新项目，只撤销本次临时思考态、恢复原窗口的文本/附件并重新水合原项目；后台幂等收口后仍在同一会话重试。首次打开会话使用有界底部锁覆盖媒体延迟布局，之后的轮询仍遵守“近底部才跟随、阅读历史不强拉”的规则。
- reviewed usage v2 保留全局精确集合规则，并新增更窄的单 video-workshop project 模式：仅当计划全部绑定同一项目、全部是 `sidecar-submitted-indeterminate`、receipt 与未决集合完全相等且不存在 projection outbox 时，才能把这些无 provider reference/终态证据的提交记录落定为 `indeterminate/calls=0`。该模式不枚举或修改其他项目，apply 前仍必须绑定 fresh backup 与完整 snapshot，二跑必须零写；本地恢复项目的 `3` 条已依此闭合，其他全局未决证据保持原样，生产没有执行该本地计划。
- 本地 `start.command` / `start-shared.command` 以当前 `.env.local` 为第一选择，缺失时通过 Git common dir 解析主检出目录的私密 `.env.local`，不执行其中的 shell 内容；同一文件通过 `FALLBACK_ENV` 交给 sidecar。主服务开放前必须同时具备导演 `LLM_API_KEY`、视频 `SEEDANCE_API_KEY` 与语音 `MINIMAX_API_KEY`，否则 fail fast。生产仍只读取服务器私密环境，不接受本地 env 上传或写入 release。
- 本地恢复环境必须让 `DATA_DB`、`UPLOAD_DIR`、`COMPOSED_DIR`、`CUSTOM_CANVAS_BLOB_DIR` 和 video-workshop 各目录来自同一恢复点。该约束只是本地运行配置校验，不新增持久目录，也不改变 supplier delivery-scoped 私有媒体读取模型；生产始终使用服务器原持久路径，代码/静态更新不得携带本地 SQLite 或媒体目录。
- 供应商交付筛选保存在既有 UI 元数据的 `deliveryFiltersByScope` 中，以角色和成员 ID 隔离，刷新只恢复当前 scope 的结构化筛选；自由搜索词不持久化。该机制不创建服务器筛选表、不改变供应商可见集合，也不修改交付业务文档。创作时间只改变展示精度为分钟，不改写原始时间戳。
- 剪辑台监看器面对已经烧录字幕的历史成片时，只在预览字幕带使用局部遮罩并显示当前唯一可编辑字幕；提交后仍从分段源执行一次 ASS 合成，不把遮罩写进成片。播放头、进度条和时间码由单一 `requestAnimationFrame` 时钟读取媒体真实时间，暂停/结束/关闭时取消，避免多时钟竞争和离散跳动。
- 产品目录 seed 更新为 `20260810-product-db-v5-baige-6-full-infra`，百舸条目由本版受管 seed 覆盖旧持久 seed，来源为 `百度百舸产品信息与数据汇总-2.md`。批量图文、单号、视频口播和图片提示词仍经既有产品命中与禁止幻觉机制读取，不新增外部检索、模型依赖或独立知识库服务。
- 每个新接受的视频制作计划都有稳定 `reference_scope_id`。静态/动态分镜中间文件、分段文件、完成判定、重启恢复和重试缺失集合都由该 scope 命名和校验；同一 project 仅作为会话容器，不再允许上一条 production 的 `scene-N` 文件替代下一条。旧无 scope 计划继续使用原文件名以兼容中断恢复，已生成成片和历史媒体不自动改写。
- 成片剪辑台读取交付版本的 composition 快照并在已有项目内保存结构化 `timeline_edit`：主轨顺序、源文件、入点、时长、逐片段字幕、素材替换、白名单转场、画中画时段/`position_x`/`position_y`/比例/入出场效果、`bgm_selection`、音轨音量和音效 cue 都是显式字段。时间线与监看器遵守 `V1 主画面 < V2 画中画 < T 字幕` 的视觉叠放关系，并把声音拆为 `A1 口播 / A2 配乐 / A3 音效`；图片与视频画中画使用同一直接拖动、缩放和受控效果模型，播放头命中时同步真实视频帧。监看器下方使用独立播放与进度控件，原生视频 controls 不再遮住字幕。重渲染只读取 owner-scoped 的当前交付镜头与项目素材，复用既有 FFmpeg/OpenMontage 合成、ASS 字幕、BGM/SFX、质检和版本化输出；不重新调用画面/口播 provider，不覆盖旧成片。
- 剪辑台外部素材导入使用 owner-scoped `POST /projects/{id}/assets`：只复用现有附件类型、大小、视频探测和预览提取，写入当前 sidecar 项目后立即由主平台同步 owner 绑定与私有媒体登记，不经过导演或生产 pipeline。弹窗边界优先截断文件拖放事件，避免继续冒泡到视频工坊全局对话附件区；只有明确落在 V1 具体片段才替换主画面，图片/视频落在 V2 或其他剪辑台区域默认成为画中画，音频落在 A2 时替换 BGM、其他区域默认进入 A3。主平台只代理 owner 已绑定项目的 `video-editor`、`assets` 与 `timeline-revision`，未授权项目继续拒绝。内置 Kenney CC0 音效通过静态白名单解析，未知路径或跨项目音频不进入渲染。v141.6 没有新增 schema、数据库、持久目录、Nginx/systemd 配置或 npm/Python 依赖。
- v141.4 的视频工坊声音工作台压缩为口播声线、语音生成、音色设计三栏，在当前 resource scope 内输出我的/团队设计音色，并去重合并主服务受控的 MiniMax 真实系统音色；主平台以同源 workspace 协议传入当前成员收藏 ID，子应用按收藏、我的设计、团队设计、MiniMax 系统音色分组。口播和语音生成各自保存声线状态，两个选择器都支持按需试听；音色设计继续调用既有 `/api/tts/voice/design`，候选只按当前成员/团队 scope 保存到 `voicePresets`。导演进度改由无边框的“思考中”状态行按需显示；导演追问不再被 `240` 字符硬截断。
- 会话删除继续遵守 exact creator 权限。针对少量旧 session 的 `docs.owner_id` 漂移，v141.4 只信任 payload owner 或不可变 resource scope owner；删除时同事务解除同 owner batch 的 session 引用并写 `archivedSessionId/sessionDeletedAt`。任务、production、job、发布记录和媒体不删除，恢复器必须跳过带删除标记的批次，避免刷新或版本更新后“复活”会话。v141.5 仍不新增 schema、依赖、媒体迁移、持久路径或 systemd/Nginx 变化。
- v141.3 的功能提交 `1332f8c8e9408c7e9a36f49ab13b48a46ca353f5`、release/cache `20260810-v1413-runtime-finalization-1` 现作为仍在线的旧服务回滚点保留；它不再承接正常生产流量。生产 SQLite、账号、媒体、认证、私密配置和 Nginx 继续使用原服务器持久路径，没有被本地状态覆盖。
- v141.3 的 `media-settle` 以 `effectivePendingRows` 而不是 raw pending 为增量目标，只登记物理存在、owner/project 唯一、未隔离且非公共头像例外的媒体。提交前必须证明 effective/drift 归零且 raw 精确回到既有隔离基线；相同 settlement 二跑零写，之后再出现 pending 则 fail closed。视频工坊为用户实际访问到的 active owner 项目启动 checkpoint finalizer：只观察本地耐久状态变化并复用既有同步、媒体登记与用量闭合，不提交、轮询或重试 provider，terminal/shutdown 后退出。
- v141.2 把账号每日 `2` 条额度定义为“成功发布到供应商清单”，草稿创建与生成不占额度；同团队成员共享权威用量，普通/批量发布和视频工坊/无限画布定制发布都在同一 SQLite 原子事务中最终裁决。百舸别名路由从主题、标题、正文与口播识别“百舸/百舸 6.0/百度百舸”，将产品库已确认事实注入文案、口播和图片提示词，同时保持禁止幻觉边界。
- 无限画布的父子应用发布协议由单图兼容扩展为 `1–20` 张有序图片数组；Mac Command、Windows Ctrl/Shift 和拖框均可多选，主平台按选择顺序逐张运行既有图片精修并生成发布图集。选择动作保持客户端瞬态，不再改变 z-order 或触发草稿写入；真实拖动后才持久化。瞬态同步使用有界自动/手动重试，真实 revision 冲突继续保留本地恢复包与服务器副本。
- v141.3 没有新增 schema、依赖、持久路径或 Nginx 变化；systemd 只增加可回滚的 sibling release 指针。最终目标 Linux 锁定主服务 `781 passed + 1 approved skip`、sidecar `142/142`、Node `127/127`，三套 wheelhouse 离线安装门禁和 release verifier 均通过。真实生产完成媒体 `25`、usage v2 `2`、video usage recovery `13` 的精确 apply 与 fresh-binding 二跑零写，全程没有 provider retry。
- v141.1 数据看板仍包含在本版：总播放量弹窗逐条并排显示供应商权威曝光/播放，筛选在桌面保持一行；互动构成左侧固定标题、账号、平台、快照时间，右侧按列展示播放、点赞、收藏、评论与分享。v141.0 的小红书/视频号文字门禁、静态视频大图压缩、供应商操作列、会话内搜索和批量图文统一图数也继续保留；只有“创建即占配额”的旧口径被 v141.2 明确废止。
- v140.6 恢复三条生成链路：无限画布解析浏览器 source project 到服务端稳定项目后再做 owner/scope 门禁；OmniHuman 通过显式 `PUBLIC_BASE_URL` 向上游提供受签名保护的公网角色图/音频；信息流的分时镜头解析兼容小数秒/中文单位，且所有可达 importer 与 `ai.js` 使用同一新 cache identity，防止 immutable 浏览器缓存继续执行旧解析器。
- 生产锁定主服务仍为 FastAPI 0.68.1 / Starlette 0.14.2 / Pydantic 1.10.26。写门通过该旧栈原生 startup/shutdown 事件武装：`_prime_production_write_gate` 完整通过后才完成 Uvicorn startup；失败不对外服务。`/api/ready` 永远只观察，不写入进程内 gate。
- expand-only schema 当前到 `140010`。`140008/140009/140010`、tenant/resource/canvas/incident/usage settlement 都只允许显式 CLI 和精确 reviewed plan，绑定当前 DB identity、fresh SQLite v2 backup、fresh 20-component snapshot/live media digest；服务启动不会隐式重放，二跑必须零写。
- 常规写路径新增三条强不变量：画布后台 job 文档与 resource scope 同事务；成员入团先全量验证其 personal scope/registry 再原子 adoption；画布 GC 必须保活同 owner succeeded job/已发布 community 引用并在归属不明时整批停止。资产删除以受保护文档检查为第一步，不允许文件删除成功但业务文档删除失败。
- `140010` 已按精确授权计划隔离 `46` 个无可靠原件引用，不删除历史、不伪造文件/归属、不重试 provider；正常审计基线是 raw `missingReferencedFiles=46 / pendingRows=48 / publicAvatarExemptions=2`、`isolated=46 / unisolated=0 / effectivePending=0`。v141.3 已把切换前 raw 73 / effective 25 / drift 1 的新增 `video-output / video-workshop-project` 精确登记，apply 后 raw 回到 48、effective/drift 为 0，二跑零写；历史隔离行没有被登记、删除或改写。
- 已隔离条目保留原 owner/team/resource scope 和全部业务历史，媒体读取精确返回 410，依赖原件的下载/发布/复用拒绝执行。社区列表以一次读快照批量标注隔离媒体；隔离基线建立后，新写路径不得再持久化物理不存在的引用。
- 用量审计同时覆盖中央账本和 sidecar：旧 v1 `operator-confirmed-unknown` 只在不可变身份/hash 精确匹配时作为合法终态；v141.3 对切换前精确 2 条中央未决执行 usage v2、再导入 13 条 sidecar 已成功而中央缺失的记录，两步均 fresh-binding 二跑零写。切换门禁时中央 unresolved/outbox/spool 与 sidecar effective/conflict 均为 0；之后正常业务可以创建新的短暂 pending receipt，观察端点可暂时转红，但不能由 GET 改变已武装 live gate，也不能据此自动停服、切 RO 或猜补用量。
- 生产切换时受保护 gate 为 `ready=true / writeReady=true / startupVerified=true`，SQLite `quick_check=ok`，resource/media/sidecar usage 全部闭合；公网 root、health、OpenAPI、community 和两套子应用静态入口可达。当前进程保持 `ACG_READ_ONLY=0`，一次性 `/api/ready` 只反映当时业务证据，不是负载均衡存活探针；可用性使用 `/api/health` 与服务状态判断。
- 最终目标 Linux release verifier Phase 0 为 `5160b64d7ad75ebbf69fa54dce8146d840b82ee07d061087e32d5ceeb800f2e2`，ESM closure 为 `a6a641e6aa01a2c4a1f3bbdaf6e1c8a950fc6a554992518da38734ca739a25f1`，runtime 为 `58` files / `3,025,471` bytes / `730bf796628acf05cf50e8bbad4e505333d97d5e3f2e5a38702438d48cf00616`。
- fresh pre/post SQLite v2 backup 与 20-component complete snapshot 均已 verify；新保护点的隔离 restore-drill 通过。部署前后均为 48 张表且无行数下降，媒体 inventory digest 与 uploads/composed/canvas/video 六类计数完全不减，供应商曝光/观看/回传相关三张表及账号文档摘要不变。生产 SQLite、账号、媒体、认证、私密环境和 Nginx 未被本地状态覆盖；旧 release、正式快照和代码/服务回滚点保留。
- v140 保留 8787 主服务 + 8765 回环 sidecar + 无限画布静态闭包的拓扑，在 v137/v139 基础上新增 deny-by-default resource scope、私有媒体 owner/team registry、fresh v2 备份事务绑定、完整 runtime snapshot/restore、精确离线依赖和启动前 write gate。RO release 可用于迁移验收；RW 必须在全部生产证据通过后明确 `writeReady=true`。
- 生产 18 项运行锁与 20 项一次性测试锁严格隔离，测试锁只新增 requests/urllib3。视频 sidecar 为 37 项运行锁，显式包含 ctranslate2 所需的 `setuptools==83.0.0`。正式 wheelhouse 除哈希/ABI 验签外，必须从一次性 venv 以 `--no-index --no-deps` 安装并通过 exact-installed 与 `pip check`；测试工具链显式要求 Node、FFmpeg/FFprobe、Git 与 `lsof`。
- complete snapshot 的恢复语义包含普通文件精确 `mtimeNs`：安全解包后逐文件无链接跟随地恢复 manifest 纳秒值，并以 `lstat` 严格复核；文件系统若舍入即 fail closed。原因是生产媒体 inventory digest 同时绑定 path、size、mtimeNs 和内容 SHA-256，不能把“内容一致”误报为“同一恢复点”。`927bd29` 的生产形状副本已暴露最多 120ns 的 tar 恢复漂移，必须从修复后的唯一提交重做完整快照、恢复和第七步 media-preflight。
- v139 起将主服务、无限画布和视频工坊已识别模型入口统一为逐次 upstream attempt 的 durable receipt，并以 completion spool、projection outbox、后台重放和 readiness 指标处理 SQLite 完成写失败；v140 对同进程并发写增加 receipt 专用微批协调，每项保留独立 savepoint，整批提交成功后才授权上游，不使用全局 `store._lock`，跨进程仍由 SQLite 唯一约束裁决。它仍是应用侧事实账本而非供应商账单；历史缺口只能按受保护 recovery plan 从耐久证据恢复，不能猜补 token、调用次数或日期。
- 批量真人/数字人、图文和静态任务统一使用失败终态：上游/准备阶段失败写 `failed` 和可读原因，只有用户明确重试才重新派发；`evaluate`、JobRunner 和刷新恢复都不把 failed 当 pending。旧等待上传状态只在水合兼容层被幂等降为 failed，运行 UI 不再存在等待补图卡或跨任务自动补洞；主动上传仍由任务行、详情、计划参考图和资产入口明确触发。
- 批量刷新恢复以服务端持久化的 batch/production/job 为检查点：图文空计划回到起草、已有提示词继续缺图、图片齐全进入审核、已有 provider job 只继续查询；“思考中”由同一检查点重建。图文提示词计划、上游调用前状态、逐张完成结果及全部图片完成后的 review 终态都使用推进版本的单文档确认；水合发现图片已齐但仍残留 running 时幂等补写 review，不把浏览器内存或延迟整集合回推当权威，也不重提已有上游任务。远程数据按 session/batch、production、job、assets、analytics 顺序渐进返回；工作台在 production/session/batch 到达后即可打开，job 到达后才按累计完整集合启动恢复器，不用重型轮询和资产列表锁住导航。统一左栏的每条批量会话固定显示无边框三点菜单，选中会话只用与画布/视频会话一致的柔和光影流动和轻阴影，不显示“当前”文字或蓝色框架，可访问性仍保留 `aria-current`。思考卡只计入仍在起草/生成/轮询的本轮批次，已审核或失败的历史批次不再累加分母。
- 批量生产目前仍由浏览器执行起草、提示词、生图与视频 job 编排。刷新后可以从已持久化检查点接续，已有 `providerRef` 的任务只查询同一上游任务；但最后一个页面关闭后，浏览器还没有提交的后续步骤不会自动推进。要实现整批关页继续，必须另建 owner-scoped 服务端 durable workflow，以原子 claim、阶段检查点、provider receipt 和积分结算共同保证不重提/不重扣；不得用 service worker、页面保活或模糊重试假装后台可靠。
- 远端水合后会对当前 owner 的视频 running 状态做幂等终态收敛：成片已在则 review，完整 succeeded 分段只执行 compose，失败/缺段/缺输出则 failed。`140005` 的服务端持久账本以 owner + production/timeline 指纹原子裁决 compose；跨标签页只生成一份成片，成功与媒体归属同事务提交，失败可重试。前端 `composing` 仅为展示态，pending 或浏览器停止等待不会误写 failed，下一次水合会查询同一账本。该恢复器不调用 provider、不创建 job、不扣费，并可收敛无 batch、done batch 残留状态和空转 generating batch。
- 供应商下载、缩略图、封面和预览交付媒体时，私有媒体都是“交付关联读”而非团队成员读：请求携带精确 `deliveryId`，并必须同时命中当前 supplier parent/child 可见的持久交付和该交付精确引用的 cover/pack/source/final media。该例外仅作 GET/Range，不授予其他私有 URL、删除或覆盖；任一必备媒体失败都显式显示原因，下载则中止完整包。
- 视频工坊对话自动跟随只允许写 `.conversation-column` 的内部滚动状态。显式发送建立当前项目专属的有界底部锁，覆盖用户消息、公开进度和最终回复的连续高度变化；锁截止前的普通重绘不会恢复旧快照，消息行也不重播入场动画。非发送轮询仅在用户原本接近底部时继续跟随，阅读历史时恢复原 `scrollTop`。禁止对消息节点调用会滚动 iframe 外层祖先的元素定位 API。
- 供应商交付的播放/曝光 triplet 由专用服务端端点维护；泛用资产快照即使携带更新的 `updatedAt` 也不得覆盖计数、时间和操作人。供应商页仅在进入和窗口重新聚焦时轻量拉取权威 `accounts/assets`，更新失败必须显示错误。
- Free 用户的图片、视频和语言对话在调用上游前预占真实积分，成功结算、失败释放，幂等重放不再扣费。“踢出团队”只移除席位，账号和资料保留并回到 Free；ACG 市场部 owner/admin 可停用或恢复创作端账号，停用后 Bearer、私有媒体 cookie 和视频工坊 cookie 均 fail closed。

## 文档维护规则

每次形成新版本号、提交候选或部署候选时，必须同步更新本文件：

1. 更新日期、当前本地版本、主构建标识和线上基线。
2. 同步入口、模块、依赖、权限、数据、启动或部署变化。
3. 架构没有变化时，也要明确记录“本版架构无变化”。
4. 真实部署后立即把线上基线、服务状态和部署边界更新到这里。
5. 发现本文档与代码、Git、服务或服务器不一致时，以实时检查为准，并在当前版本结束前修正文档。

## 默认任务执行与检查规范

除非用户明确缩小或扩大范围，按下面的分级执行。所有检查都以不覆盖用户修改、不写生产数据、不触发真实付费链路为前提。

### 0. 只读、问答与文档评估

- 用户明确要求“只读”“不要改代码”或只是询问架构时，只调查和报告，不修改项目。
- 可以读取相关文件、Git 状态和已有测试结果；不启动真实生成、发布、部署或外部写入。
- 纯文档修改只需要检查 Markdown 内容、链接、Git diff 和敏感信息，不需要启动整套产品。

### 1. 本地快速烟测（代码改动的默认级别）

用户没有额外说明时，完成本地代码修改后默认执行与改动范围匹配的快速烟测：

1. 检查 `git status --short` 和 `git diff --check`，确认没有误碰无关文件。
2. 对改动的 JavaScript 运行 `node --check`；对改动的 Python 运行编译检查。
3. 运行最相关的定向测试，不默认跑与本次无关的全部测试矩阵。
4. 服务已启动时检查相关健康接口；未启动时，仅在启动不会安装未知依赖、覆盖状态或触发外部任务的情况下启动本地服务。
5. 涉及可见前端页面时默认只跑源码与自动布局烟测；只有用户明确要求浏览器检查、视觉回归或手动验收时，才打开浏览器并覆盖指定视口。
6. 明确区分“自动测试通过”“浏览器快速烟测通过”和“用户已手动验收”，不能把前两者写成用户验收完成。

本地快速烟测不包括：真实图片/视频/语音付费生成、真实发布、供应商回传、批量写业务数据、Git 推送、服务器部署或生产数据修改。

### 2. 自动扩大为针对性验证的情况

出现以下任一情况时，应在快速烟测基础上增加相应测试和读写闭环验证：

- 修改登录、游客、成员、团队、供应商或角色权限。
- 修改积分预占/结算、订阅权益、支付展示或模型调用计量。
- 修改 SQLite schema、数据迁移、草稿同步、Blob、上传、删除、发布、回撤或幂等逻辑。
- 修改视频长任务、取消、恢复、重试、媒体合成、字幕或缓存标识。
- 修改无限画布源码、托管 vendor、父子应用消息协议或 iframe 鉴权。
- 修改批量生产与单号生产之间的项目映射、资产归属或交付状态。
- 修复曾经发生过的数据覆盖、权限串数据、白屏、缓存混版或部署差异。

针对性验证应覆盖涉及的单元测试/API、必要的浏览器流程、失败分支和数据非回退检查。涉及真实付费调用或生产写入时仍需用户明确授权，不能因为风险高就自行执行。

### 3. 完整验收与生产操作

- 只有用户明确要求“仔细验收”“完整视觉回归”“真实生成”“发布”“提交/推送”或“部署”时，才进入对应完整流程。
- 完整视觉回归再扩展到多个桌面/移动视口、截图、控制台、网络失败和关键交互矩阵。
- 真实模型调用应控制次数和费用，先验证配置与最小请求，再执行目标任务。
- Git 提交、推送、部署、外部发消息和生产数据变更必须遵守用户授权与交接边界。
- 生产部署必须使用干净、可审计的目标提交和精确资源清单，并验证数据基线、服务健康和纯代码回滚点。

### 检查遇到阻塞时

- 测试或服务无法运行时，说明具体原因、已完成的替代检查和剩余风险，不伪造通过结果。
- 发现工作区已有陌生修改时保留原样；只有与本次文件重叠且无法安全继续时才询问用户。
- 发现文档写法与当前代码不一致时，以实时证据为准，并把文档失真作为本版需要收口的事项。

## 本地预览

项目主目录：

```text
/Users/macbookpro/Desktop/obsidian/知识库/百度/codex自动化产品2/自动化产品
```

推荐启动方式：

```bash
cd /Users/macbookpro/Desktop/obsidian/知识库/百度/codex自动化产品2/自动化产品
./start.command
```

启动器同时运行：

- 主平台：`0.0.0.0:8787`，提供浏览器页面、同源 API、权限、数据和静态子应用入口。
- 视频工坊 sidecar：`127.0.0.1:8765`，只供主平台本机调用，不单独暴露。

浏览器入口：

```text
http://127.0.0.1:8787/
```

共享局域网调试使用 `start-shared.command`；只共享 `8787`，`8765` 仍保持回环访问。不要直接打开 `index.html`，前端使用 ES Modules 并依赖同源 `/api/*`。

首次启动视频工坊时会使用 Python 3.10+ 在 `apps/video-workshop/.venv/` 安装独立依赖。若开发机遗留的是 Python 3.9 虚拟环境，启动器保留它不动，并在 `.venv-py<版本>/` 创建隔离的新环境；也可用 `VIDEO_WORKSHOP_BOOTSTRAP_PYTHON` 明确指定兼容解释器。项目、上传、成片、BGM 和语音识别模型缓存保存在 `runtime/`，不写入子应用代码目录。退出 `8787` 主服务或按 `Ctrl+C` 后，启动器会同步结束本次启动的 sidecar。

本地存活检查（不作为生产部署门禁）：

```bash
curl -fsS http://127.0.0.1:8787/api/health
curl -fsS http://127.0.0.1:8765/api/health
```

## 当前产品入口

- 游客可以进入首页、查看订阅和有限资产入口；真正发起创作时再登录或注册。
- 个人账号拥有视频工坊、无限画布、语音生成和个人整体资产；每日积分按中国时区结算，生成类接口使用“预占、成功结算、失败释放”。
- 团队账号增加数据看板、批量生产、账号和成员协作、供应商交付、发布清单、数据分析和团队管理。
- 订阅管理目前是本地方案与权益展示，没有接入真实支付；ACG 内部团队按现有内部权益处理。
- 首页统一输入可把提示词和附件交接到视频工坊或无限画布；工作区左栏统一管理视频、画布和批量任务的上下文。
- 首页灵感发现读取真实社区帖子，以每页 16 条的复合游标分页和四列密排呈现原始比例媒体；图片懒加载，卡片视频只在接近视口或悬停时挂载，避免社区增长后一次拉取和解码全部媒体。首页与整体资产收藏详情在桌面端固定左侧媒体、仅让右侧长文案滚动，视频由用户点击后有声播放，点赞/收藏在右上角以纯图标和选中色表达。视频工坊、无限画布、发布清单的分享保持源作者和团队归属，以同一作者+规范化 provenance 幂等，旧 URL-only/identity 兼容，已分享产出不得重复发布。
- 整体资产按个人 / 专业 / 团队权限投影草稿、本人媒体、收藏和后台素材；普通个人不获得团队发布账号筛选能力。

## 当前代码结构

```text
index.html
├── js/core/       状态、路由、迁移、远端同步与工具
├── js/domain/     账号、生产任务、资产、交付、分析与语音领域逻辑
├── js/api/        LLM、图片、视频、TTS 与任务适配
├── js/agent/      批量计划、会话、任务编排与任务板
├── js/views/      首页、订阅、工作台、资产、发布、分析、设置与子应用外壳
├── js/ui/         弹窗、图标、登录动效和通用交互
├── styles/        变量、页面布局、工作区与动效
├── docs/          版本、问题、部署记录与跨线程交接指南
├── vendor/        无限画布等由主服务直接托管的静态构建产物
├── apps/          视频工坊运行副本与无限画布可重建源码
├── runtime/       子应用项目、上传、成片、BGM 与模型缓存（不入 Git）
└── server/        主平台 FastAPI、运行配置、SQLite、显式 migrations、备份、权限与 provider 代理
```

主平台前端仍是原生 ES Modules；无限画布源码使用 Next/React/Zustand 并构建为静态子应用。两者通过同源 iframe 和受控消息桥集成，当前不做运行时强行合并。

## 主业务模型

核心生产对象为 `production`：

- 图文：图文创作台 -> 审核 -> 交付。
- 数字人：数字人制作 -> 智能混剪 -> 审核 -> 交付。
- 信息流/素材视频：视频生成与素材编排 -> 智能混剪 -> 审核 -> 交付。
- 批量生产负责账号选择、账号级文案、参考图和批次启动；单号工作台负责具体生产、修正与交付。

账号、成员、团队、供应商、资产、发布和分析等权威业务数据由主服务控制。前端隐藏按钮不能替代服务端权限校验。

## 定制创作边界

### 视频工坊

- 独立 FastAPI sidecar 监听 `127.0.0.1:8765`，隔离新版 FastAPI/Pydantic、`faster-whisper`、FFmpeg 和长时媒体任务。
- 主平台通过 `/custom-video/` 签发当前成员会话、限制 owner 范围并代理 API、附件和成片。
- 项目、上传和输出保存在 `runtime/video-workshop/`；成功镜头、口播和成片支持保留与恢复，不得被代码部署覆盖。
- 动态任务与静态任务使用独立执行链：动态进入 Seedance；静态使用图片分镜、TTS、字幕、BGM 和 FFmpeg 渲染，图片生成最多三路并发，不得把静态任务误送 Seedance。
- 静态分镜按口播语义、导演节拍、附件归属、全片统一风格和负约束规划；约一分钟且语义充足时通常可达 12 张以上，短口播允许更少，3–5 秒仅为软节奏。画面使用固定中心的连续单调慢速放大，不建立精确时长或强制口播约束。
- 每次新的生产请求从最近一次交付边界后重建导演上下文，并使用生产级 `reference_scope_id` 命名连续性参考图；无新附件的下一任务不得读取上一任务的附件或固定文件名 anchor。同一任务的恢复/重试则继续使用自己的 scope。
- 项目顶层 status 只表示当前对话/制作阶段，不覆盖已持久化 delivery/output 的完成状态。用户点击当前或历史成片发布时，必须从该项目的 canonical output ID 解析同项目站内媒体路径；空/失败/未完成 output、外链、穿越和跨项目伪造必须拒绝。
- iframe 与 `postMessage` 是账号、样式、事件和长任务隔离边界；没有真实性能证据和灰度回退方案前不取消。

### 无限画布

- 源码位于 `apps/infinite-canvas-source/`，生产托管产物位于 `vendor/infinite-canvas/`，由主服务在 `/XZ-Design/` 提供。
- React/Zustand 子应用负责画布交互、节点、视口、参考图和浏览器导出；模型密钥、权限、生成、编辑和服务端草稿由主服务负责。
- 项目摘要与本地 checkpoint 按成员分仓；完整草稿同步到服务端，使用 revision、冲突保护和删除 tombstone。
- 新生成图片先固化为 owner-scoped Blob URL，再写入画布 checkpoint；旧内联 Base64 草稿在正常读写时惰性迁移。
- 批量生成最多三路并发，并已有取消、超时、Blob 持久化和旧执行态中断恢复。owner-scoped 后台 job 在浏览器掉线、查询超时或瞬态 429/5xx 时保持可恢复运行，轮询采用可见性调节和有界退避；服务端明确失败仍进入可重试终态，不重提 provider、不重复扣费。最终单张失败的 expanded part 精确重试仍待完成。
- 当前静态产物由 `apps/infinite-canvas-source/` 源码构建，共 63 个文件、`1,768,916` bytes；相对路径、大小和 SHA-256 manifest 已校验，`vendor/infinite-canvas/` 已按该审计闭包精确同步，不再保留历史多余哈希文件。浏览器端供应商 Key 与直连 API 构建路径已禁用，Agent、生成、高清、变换和局部编辑统一经主服务鉴权、配额和 receipt。
- 画布发布支持按 selection 数组顺序提交 `1–20` 张图片；主平台逐张执行既有 `polishImageForPublish` 后再形成同序图集，普通浏览器导出不触发发布精修。多选只改变本地 selection，不写服务端草稿；z-order 仅在真实拖动后更新。瞬态保存失败按有界退避自动重试并可手动重试，409 冲突不自动覆盖任一版本。

## 身份、团队与积分边界

- 游客状态不是服务器成员身份，不得访问团队业务和受保护生成接口。
- 公共注册先建立个人账号；加入团队和团队角色通过后续申请、审批和成员关系决定。
- 团队所有者、管理员、编辑、普通成员和供应商角色的能力由服务端接口逐项校验。
- v140 严格资源模式要求受保护业务记录拥有唯一 personal/team scope；查询、写入和删除按服务端行为人及已持久化 owner/account/upstream scope 判定，`role=admin` 不能作为跨租户旁路。历史歧义在 `140002` 迁移前保持生产只读，不能由前端筛选或默认团队掩盖。
- v140 私有媒体模式要求 uploads、composed、画布 Blob 和视频工坊持久媒体具有唯一 owner/team registry；本人及同团队按归属访问，外部团队管理员拒绝。供应商不是 team member，只能通过其确实可见的交付 ID 精确读取该交付引用的媒体，不获得任意媒体或写/删权限。社区已发布媒体通过独立公共边界读取，不能把公共帖子的例外扩大为任意直接 URL 权限。
- 个人每日积分和外部有限套餐在模型调用前原子预占，成功后按实际用量结算，失败、取消或中断后释放；幂等键必须绑定请求摘要和任务终态。
- 动态视频在成功终态按提交模型与归一化请求时长对应费率结算；静态视频按本轮新生成图片及存在且本轮新生成的 TTS 实际用量结算，历史复用媒体不重复收费。ACG 市场部精确走无限积分旁路。
- 供应商初始凭据只允许在首次受控响应中出现，响应必须禁止缓存；不得写入日志、文档或聊天。

## 字幕与语音边界

- 数字人自动字幕以分段口播原文为内容真源，以每段实测音频或视频时长为时间边界。
- 信息流字幕只读取最终视频提示词中的时间段和可验证说话上下文，排除 UI、道具文字、元数据和导演说明。
- 视频工坊对用户主动上传的外部音频可在 sidecar 内使用 `faster-whisper`；该依赖和模型缓存与主平台字幕链隔离。

## 数据与部署边界

- 主业务数据位于服务端 SQLite、上传目录、合成目录和受控 Blob/runtime 路径；生产必须使用 release 目录外的固定绝对路径，不得随版本切换。
- `.env.local`、服务器环境变量、API Key、认证缓存、数据库、上传、成片、日志、浏览器状态和模型缓存不得提交 Git。
- `runtime/`、`server/canvas_blobs/`、`apps/video-workshop/.venv/` 和生产持久卷不得被静态同步覆盖。
- 当前前向迁移顺序固定为 `137003` schema、`137004` ACG 团队数据、`139001` usage receipt/outbox、`140001` resource scope schema、`140002` 历史资源归属、`140003` private media schema、`140004` 历史媒体归属、`140005` owner-scoped compose claim、`140006` 成员状态/媒体 settlement、`140007` 模型用量 settlement 审计、`140008` 生产恢复账本、`140009` 多来源 usage 裁决账本、`140010` 用户授权的精确缺失媒体隔离账本。生产已到 `140010` 且全部成功，禁止重放；启动服务不会隐式执行历史结算。后续不得裸 SQL、猜测 Token、调用 provider 或重放既有结算。
- `140002` 与 `140004` 都先做只读 preflight；真正 unresolved、ambiguous、缺文件、缺 owner 或 registry conflict 为硬阻断。人工 override 只能精确覆盖 preflight 报告的可复核孤儿，并绑定冻结库 identity、路径/逻辑摘要、schema/user version 和当次 fresh 备份 manifest SHA，不能硬编码生产 ID 或默认收编到 ACG。
- 所有生产 apply 都必须绑定紧接其前生成的 `acg-sqlite-backup-v2`，并在同一 `BEGIN IMMEDIATE` 重新核对当前源库 identity 与逻辑摘要；上一步成功后必须重新备份。完整生产恢复使用 `acg-production-complete-v1` 将 SQLite、uploads、composed、canvas blobs、usage spool、视频 runtime、BGM、模型缓存、环境、systemd 和 Nginx 绑定到同一 snapshot ID，独立验签并恢复到空隔离目录。模型缓存内部文件 symlink 与 Nginx sites-enabled 入口只能按 production 精确组件策略解引用；真实字节纳入哈希，恢复为普通文件，越界、环、dangling 或特殊文件继续 fail closed。
- 主服务与 sidecar 分别以精确 requirements lock 建立目标 Python/ABI wheelhouse；manifest、目标运行时和每个 wheel SHA-256 必须离线验签，并通过一次性 `--no-deps` 安装、exact-installed 与 `pip check` 证明依赖闭包。生产启动的 `installed` 门禁也强制执行 `pip check`。监听 socket 前还要执行 write gate；`/api/ready` 的 RO 通过不等于可写，只有迁移、资源/媒体覆盖、用量、路径、sidecar、画布与 release 门禁全部通过时才可报告 `writeReady=true`。
- v139 的 `MODEL_USAGE_COMPLETION_SPOOL_DIR` 必须位于 release 外持久根并与 SQLite 属于同一 snapshot。readiness 的 unresolved/outbox/spool pending/corrupt/conflict 任一异常都不得切流；历史恢复只能按 `scan -> apply(copy) -> reconcile-copy` 操作隔离数据库副本，禁止指向活动库或生产持久根。
- 生产运行 release 必须以 systemd `ExecStart`/`WorkingDirectory` 和实际进程 cwd 为准；已发现 `/data/.../current` 可能是滞后普通目录，不得把它当作运行代码指针。持久路径仍必须来自经审计的绝对环境变量，运行 release 与批准 SHA 不一致即停止。
- 当前生产代码、目标 Linux 依赖与 release verifier 已验签并运行完整 RW。媒体 raw `missingReferencedFiles=46 / pendingRows=48` 继续作为历史事实显示，精确隔离后 `unisolated=0 / effectivePending=0 / evidence conflicts=0 / audit drift=0`；隔离仍不冒充文件恢复，未来新增缺失会立即重新阻断写门。长期 RW 回滚必须使用理解现行 schema 的前向兼容 release，不能让原始 v120 直接读取演进后的数据库。
- v140 发布/迁移唯一权威方案是 `deploy/README.md`、`docs/本地代码架构优化与服务器迁移部署方案.md`、`docs/ACG市场部生产团队迁移计划.md` 与 `docs/服务器部署交接指南.md`。当前 v140.6 已完成受保护切换、完整 RW 和核心生成链路验收；生产数据与媒体继续使用 release 外持久路径。

## 项目记录读取方式

- `docs/Conversation Thread Handover Guide.md`：交接规则，交接时阅读。
- `docs/version.md`：版本历史，只读顶部最近版本或指定版本。
- `docs/Problem Document.md`：问题档案，按模块、症状和风险关键词检索。
- `docs/Server Deployment Log.md`：实际部署记录，仅部署、回滚和生产核验时读取。
- `deploy/README.md` + `docs/本地代码架构优化与服务器迁移部署方案.md` + `docs/ACG市场部生产团队迁移计划.md` + `docs/服务器部署交接指南.md`：v140 生产发布、迁移、验收与回滚的权威方案。

每个版本必须更新本文件；历史文档继续保留证据，但不作为每次任务的全文前置阅读。

### 什么时候读取历史文档

读取 `docs/version.md`：

- 开始或结束一个有版本号的迭代，需要确认最近版本、构建标识、完成范围和未验证项。
- 回归某个旧功能、准备提交/交接/部署，或需要判断某项能力是否已经上线。
- 默认读取顶部最近 1 至 3 个版本；已知目标版本时直接搜索版本号，不全文读取。

检索 `docs/Problem Document.md`：

- 遇到报错、白屏、缓存异常、布局回退、权限问题、数据覆盖风险、任务恢复失败或本地/服务器不一致。
- 修改高风险模块前，需要确认是否存在同类历史坑。
- 使用模块名、接口名、错误文本、版本号或风险词定向检索；没有相关命中就停止，不为“可能有用”而加载全文。

读取 `docs/Server Deployment Log.md`：

- 只有部署、回滚、生产健康核验、线上版本确认和正式周报取证时读取。
- 默认看目标版本和最近 3 次部署；本地开发任务不需要读取。

读取 `docs/Conversation Thread Handover Guide.md`：

- 只有当前任务需要交给其他 Codex 线程、接收跨线程任务或移交部署责任时读取。
- 普通连续开发不需要重复加载整份交接指南。

## 修改后的最低验证

```bash
git status --short
git diff --check
git branch --show-current

for file in $(find js -type f -name '*.js' -print); do
  node --check "$file"
done

curl -fsS http://127.0.0.1:8787/api/health
curl -fsS http://127.0.0.1:8765/api/health
```

根据改动范围补充 Python 编译、针对性单元测试、无限画布 TypeScript/构建验证和真实浏览器检查。自动化测试通过不等于用户已经完成视觉或业务验收。
