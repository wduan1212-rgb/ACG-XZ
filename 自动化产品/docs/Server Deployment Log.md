# Server Deployment Log

## v143.2 - 2026-08-13（零停机绿色发布、发布日期切割与回传 Excel）

- Source and release: production traffic runs SHA `aa80a16b65564208021c84618cf3b9bf6054c2a9`, release/cache `20260813-v1432-publish-export-1`, sibling release `20260813-v1432-publish-export-1-aa80a16b`. New main/video units listen on private ports `8800/8775`; the v1432 route precedes retained v1431. v1431 main/video/routing stayed active/RW throughout, so removing only v1432 routing returns traffic to the online old code without replacing data.
- Code/config boundary: only the exact verified Git archive was extracted; archive SHA-256 is `42d2ec36dda8c6ce842c0c0d931921925f20b31bc58f790b37e137a597113e0e`. The release-external environment was inherited server-side and changed only for release/root/private ports. SQLite, accounts, members, auth, uploads, composed media, canvas blobs, video runtime, Nginx and provider secrets were not uploaded, overwritten or recorded in Git/release/logs.
- Linux closure: main passed `837 passed + 1 approved skip` in the correct source harness; sidecar passed `160/160`; Node passed `129/129`. Release verifier passed Phase 0 `8db29d229ac79bf7661ee3dd3da70a7babcf2db54a105b305aaa760f7867de7f`, ESM `63 modules / 354 edges`, closure `89d6b9fcbf0312b6039e7d60af5204a2e4da943d81e3e531747a76fb4e4b0ec9`, canvas `63 files / 1,772,848 bytes / e5268fb1907d2397e887491bec0045a053d48c6f05b0af000ea8e0292b565e34`, runtime `65 files / 3,335,927 bytes / 882cbb36837e4cfb57f1f90cda823728ea697fe0d2f2beeefc86832e0f7c2a98`.
- Protection: fresh SQLite v2 backup `/data/dumate-studio/backups/v1432-pre-switch-20260813T101222Z` has database SHA-256 `f1c177a90f4d504bde00bb03a43c056be483af69e5d355f757dcd49eff68da37` and logical SHA-256 `d7e183350cbac7a36de934edbeacd4dfa90c31310516389889a4cc8ead2f30d1`. Manifest verification, byte-identical isolated restore drill and restored `quick_check=ok / 48 tables` passed. The verified prior code/unit/routing/private-env rollback set is `/data/dumate-studio/deployment-backups/v1432-pre-switch-20260813T101609Z`.
- Baseline real acceptance: one off-traffic canvas batch settled `10/10` with ten durable blobs. Two concurrent batch-image groups settled `3/3 + 3/3`, and both vision-copy calls returned parseable output. An authenticated public browser opened a real canvas blob and the batch workspace; the creator delivery export modal selected a returned-link date range and downloaded 161 rows. Reimport with Artifact Tool passed required columns, numeric views and formula-error checks.
- Complex multi-user acceptance: three creator identities simultaneously submitted six canvas batches and three batch-image groups using four references: `4096x128`, `128x4096`, a `48x48` transparent PNG and a normal image. Canvas settled `18/18`; every durable blob was readable, all 18 content hashes were unique, polling had no missing-background-job response, and all three cross-owner reads were denied. Batch image settled seven of eight submitted items; the eighth had already called the provider when a roughly 272-second read timeout produced `IMAGE_PROVIDER_RESULT_UNKNOWN`. The planned ninth image was not submitted and the unknown attempt was not retried. Thus the normal path is green, while extreme mixed-load batch acceptance remains honestly non-green because of provider tail latency, not malformed-image parameters, lost jobs or host CPU.
- Runtime state: protected readiness remains `ready=true / writeReady=true / startupVerified=true / blockers=[]`; main and sidecar are active/RW with `NRestarts=0`, and SQLite remains `quick_check=ok / 48 tables`. Production uses `IMAGE_SUBMIT_CONCURRENCY=2`; recent P50/P90 image latency was about `62.3s/90.4s`, while sampled host CPU was idle. The two shared provider lanes and upstream long tails explain waiting. Contract capacity was not inferred by increasing concurrency. `/data` is about 99% used; no restore evidence was deleted without separate approval.

## v143.1 - 2026-08-13（零停机绿色发布、画布登记边界与创作队列不拒绝）

- Source and release: production traffic runs functional SHA `6e0d0fed7d3f323984f011edc18cfcd58366c4dc` from `codex/v141-content-governance`, release/cache `20260813-v1431-creation-queue-stability-1`, sibling release `20260813-v1431-creation-queue-stability-1-6e0d0fed`. New main/video units listen on private ports `8799/8774`; the v1431 route precedes retained v1430. v1430 main/video/routing stayed active/RW throughout, so there was no stop or read-only interval; removing only v1431 routing returns traffic to the online old code without replacing data.
- Code/config boundary: only the exact Git archive was extracted; archive SHA-256 is `e2d247cb418570ef5d61628ed49b2f1a4eb3b0bd518d40d77d82657404439333`. The release-external environment was inherited server-side and changed only for release/root/private ports. SQLite, accounts, members, auth, uploads, composed media, canvas blobs, video runtime, Nginx and provider secrets were not uploaded, overwritten or recorded in Git/release/logs.
- Linux closure: main passed `835 tests / OK / 1 approved skip` in the correct parent-project/Git-index harness; sidecar passed `160/160`; Node 22.19 passed `129/129`. Release verifier passed Phase 0 `1ec88d63191fe99c8a1b247d340a7462c2e14c8562a5a040fe4ecf3ec00c290f`, ESM closure `5df12b717799c17445cae3145c7b71162432ea32f1ef9eb777a5f437663ffc1c`, canvas `63 files / 1,772,848 bytes / e5268fb1907d2397e887491bec0045a053d48c6f05b0af000ea8e0292b565e34`, runtime `65 files / 3,335,797 bytes / 1a22eb0b69cf1718398f7baed4f552ff21b104c2b2a0beac8ec09e8640e2b121`.
- Protection: fresh SQLite v2 backup `/data/dumate-studio/backups/v1431-pre-switch-20260813T081127Z` has manifest SHA-256 `550345de4a846d55f57b44e48775127eacbf5bb49355fcff6d56d24d6faff667`, database SHA-256 `29b98e9f57685e7dc5e8fc28fc1cfa15e29e0a6c66a795f7a159bbba09b7ef05`, and logical SHA-256 `fede4db2e0bfef5da9321bd56c0a9a64056c3377896cfb986a94cae09fa8fb2d`. Manifest verification, byte-identical isolated restore drill and restored `quick_check=ok / 48 tables` passed. The verified v1430 code/config rollback set is `/data/dumate-studio/deployment-backups/v1431-pre-switch-20260813T081412Z`.
- Real acceptance: one atomic infinite-canvas batch registered ten jobs with two shared references; all settled `10/10 succeeded`, each with one durable Blob and `usedRefs=2`. Two batch-image account groups ran concurrently at `3/3 + 3/3`; all six responses were valid images and none returned 503. Two vision-copy calls returned parseable JSON with title/copy lengths `20/207` and `10/195`. Existing Wang Xuan/Wang Duan tasks were not rerun or overwritten, and no unknown provider result was resubmitted.
- Public/browser acceptance: public root, health, OpenAPI and community posts passed 20 rounds each, `80/80` HTTP 200. A cold Chrome DOM loaded the exact v1431 cache identity. Protected readiness passed three consecutive rounds with `ready=true / writeReady=true / startupVerified=true / blockers=[]`; sidecar was ready/RW with normal write policy, and new/old services retained `NRestarts=0`.
- Data reconciliation: SQLite stayed at 48 tables with `quick_check=ok`; every table was non-decreasing and total rows advanced `77,156→77,343`. Uploads advanced `7,695→7,707`, while composed stayed `1,005`, canvas blobs `741`, and video projects/uploads/outputs `64/205/1,968`; all six media byte totals were non-decreasing. Existing business history, ownership, supplier metrics, return links and raw media evidence were not deleted, fabricated or reassigned.
- Validation notes: the first archive-root test run failed only because the runner lacked the repository parent path/Git index and used system Node 20; the exact source harness with Node 22.19 passed. An initial candidate unit draft also contained mixed source paths and failed before spawning or receiving traffic; exact fields were corrected off-traffic while v1430 continued serving.

## v143.0 - 2026-08-13（零停机绿色发布、图文批次耐久起跑与公平排队）

- Source and release: production traffic runs functional SHA `f95e9f9dcb492022137dbf8b9b8f1f249cd38c6e` from `codex/v141-content-governance`, release/cache `20260813-v1430-batch-durable-start-1`, sibling release `20260813-v1430-batch-durable-start-1-f95e9f9d`. New main/video units listen on private ports `8798/8773`; a persistent v1430 routing rule precedes retained v1429. v1429 main/video/routing stayed active/RW throughout, so there was no stop, read-only interval or 502 window. Removing only v1430 routing returns traffic to the online compatible old code without replacing data.
- Code/config boundary: only the exact Git archive was extracted; archive SHA-256 is `3ddcd28e88fd29340fc75d986061e002bd52ba979fc81c0f2c3260032d85ca09`. The release-external environment was inherited server-side and changed only for release/root/private ports and the non-secret queue wait. SQLite, accounts, members, auth, uploads, composed media, canvas blobs, video runtime, Nginx and provider secrets were not uploaded, overwritten or recorded in Git/release/logs.
- Linux closure: locked main passed `833 collected / 832 passed / 1 approved skip`; sidecar `160/160`; Node `129/129`. Release verifier passed Phase 0 `fdba66b026307306d73065c06d0b6c6cb9529845e6b3aac1dab01ca9cfef43e8`, ESM closure `b572143041cb367925a2e1c136388aa05b3237e04a06097e59c6a6aa05cc2761`, canvas `63 files / 1,772,596 bytes / 6ebf1da77060e6e9752cbb87bed9657afaf2dc0f4e1563a039b8e05bfd3edb72`, runtime `65 files / 3,336,016 bytes / 069c2a8d40ac822881081f4fd86e1bde9099f53d7ea14c7708ee1d54799ec44e`. New and old main/video units remained active/RW; v1430 main/video have `NRestarts=0`.
- Protection: fresh SQLite v2 backup `/data/dumate-studio/backups/v1430-pre-switch-20260813T051106Z` has manifest SHA-256 `977b8884a6c56ddf025a550b18fd0bf5858f432b8acb53a42dda3112dea15105`, database SHA-256 `d2e9e12bca24ca88eafb29d99372d94e753a119844df97d2a199f58b8c1f247a`, and logical SHA-256 `404b7c8598bfba710fddb6dd622464b1f379baf432b60c01c13a3026c9142d14`. Manifest verification, byte-identical isolated restore drill `/data/dumate-studio/restore-drills/v1430-pre-switch-20260813T051106Z.sqlite` and restored `quick_check=ok` passed.
- Real acceptance: a fresh ordinary account completed a public-browser infinite-canvas submission and received one durable Blob with a normal `海报 01` title. Concurrent production traffic completed two XHS image productions at `3/3` each; both retain a server batch root containing the production ID, and image submit/operation reads returned 200. Same-operation 409 responses were safe idempotent replays followed by retrieval of an existing result, not a second provider attempt. The historical Wang Duan orphan group remains unchanged at two successful images and four never-provider-called items; owner-scoped v1430 hydration will register the original batch/session before continuing when that member loads the new cache.
- Public/browser acceptance: public root, health, OpenAPI and `/api/community/posts` passed 20 rounds each, `80/80` HTTP 200. Chromium rendered the application and exact v1430 cache identity. Protected readiness is `ready=true / writeReady=true / startupVerified=true / blockers=[]`; usage and exact historical-media observations remain warning-only and did not stop, downgrade or route away normal production.
- Data reconciliation: SQLite stayed at 48 tables with pre/post `quick_check=ok`; total rows advanced `73,903→73,970` through the controlled browser account, one real canvas output and concurrent normal production. Uploads advanced `7,455→7,461`, composed stayed `1,005`, canvas blobs advanced `726→727`, and video projects/uploads/outputs stayed `64/205/1,968`; all six byte counts were non-decreasing. Existing business history, ownership, supplier metrics, return links and raw media evidence were not deleted, fabricated or reassigned.
- Deployment note: root directly requesting the legacy process bound to `8787` bypasses the owner-UID Nginx routing rule and is not a public-route probe. The final checks used the Nginx worker identity, candidate private port and real public Chromium, all of which reported v1430. The incorrect root-local probe never received user traffic or changed routing.

## v142.9 - 2026-08-13（零停机绿色发布、画布整批耐久登记）

- Source and release: production traffic runs functional SHA `7f4f09501e81c40c9f7122138dcbeacbc4fe73ff` from `codex/v141-content-governance`, release/cache `20260813-v1429-canvas-durable-batch-1`, sibling release `20260813-v1429-canvas-durable-batch-1-7f4f0950`. New main/video units listen on private ports `8797/8772`; a persistent v1429 routing rule precedes retained v1428. v1428 main/video/routing stayed active/RW throughout, so there was no stop, read-only interval or 502 window. Removing only v1429 routing returns traffic to the online compatible old code without replacing data.
- Code/config boundary: only the exact Git archive was extracted; archive SHA-256 is `041cd8b5ca08a284992fcad6ad91216f6b210fa886882e76fa631ab54ef30a1c`. The release-external environment was inherited server-side and changed only for release/root/private ports. SQLite, accounts, members, auth, uploads, composed media, canvas blobs, video runtime, Nginx and provider secrets were not uploaded, overwritten or recorded in Git/release/logs.
- Linux closure: locked main passed `832 collected / 831 passed / 1 approved skip`; sidecar `160/160`; Node `129/129`. Release verifier passed Phase 0 `eff3c8a526e28ac2d83366eb22ddfc795b5f905841198664b6ff5ea62171dc0c`, ESM closure `e00a2266b553cbae2b7a313f9ee2bf0039fb5274a98349cb880d882a490a1eab`, canvas `63 files / 1,772,596 bytes / 6ebf1da77060e6e9752cbb87bed9657afaf2dc0f4e1563a039b8e05bfd3edb72`, runtime `65 files / 3,336,020 bytes / c5b92f62cc9fbd022f624c4d64e2f8b79098088840c4929d834e9b7f2fc3e7ee`.
- Protection: fresh SQLite v2 backup `/data/dumate-studio/backups/v1429-pre-switch-20260813T035333Z` has manifest SHA-256 `129d196f825d5d6e9e232b213b3f73b9684b7377262ad3b75e268fb1b3e24bb6`, database SHA-256 `db40514dd575ae90600e3af347e42d00b95c5b3fd6b4a8bb753bb082a3484749`, and logical SHA-256 `c61dce9012df4230f6154bd4a7dc3cab65703633f3f3fbc88524265cfb73afb6`. Manifest verification, byte-identical isolated restore drill `/data/dumate-studio/restore-drills/v1429-pre-switch-20260813T035333Z.sqlite` and restored `quick_check=ok` passed.
- Real acceptance: one atomic canvas batch registered ten jobs with two shared reference images; all settled `10/10 succeeded`, each with one hashed durable Blob, `usedRefs=2` and `skippedRefs=0`. Exact replay returned the same ten terminal jobs and did not start another provider run. One XHS batch-core 3:4 image used both reference images and returned a valid 1,573,655-byte result; vision-copy returned parseable JSON with a 12-character title and 423-character copy including tags. The admin smoke bypassed product points but used the real upstream adapters. Since candidate startup, generation endpoint aggregation was image `17/17` HTTP 200 and vision-copy `1/1` HTTP 200 with no observed generation 4xx/5xx.
- Public/browser acceptance: public root, health, OpenAPI and community passed 20 rounds, `80/80` HTTP 200. A fresh Chromium page rendered the application and exact v1429 cache identity; anonymous `/api/members/me` remained expected 401. Protected readiness repeated `ready=true / writeReady=true`; the sidecar was `ready=true / readOnly=false / writePolicy=normal`. A single logged h11 `illegal request line` came from an invalid external HTTP request and did not affect a user request, health, restart count or application state.
- Data reconciliation: SQLite stayed at 48 tables with pre/post `quick_check=ok`; total rows advanced `73,097→73,511` through controlled provider work and concurrent normal production, with no table decrease. Uploads advanced `7,356→7,387`, composed stayed `1,005`, canvas blobs advanced `713→725`, and video projects/uploads/outputs stayed `64/205/1,968`; all six byte counts were non-decreasing. Existing history, ownership, supplier metrics, return links and raw media evidence were not deleted, fabricated or reassigned.
- Deployment-only pitfalls: an initial archive-root test run lacked the repository parent/Git-index shape and therefore produced ten path-contract failures; the correct isolated checkout shape passed the full suite. A broad draft unit replacement also produced mixed v1428/v1429 inactive paths, but the candidate had not started or received traffic; exact field mappings were validated before first start. Neither event touched the old active service or production data.

## v142.8 - 2026-08-12（零停机绿色发布、画布/批量/AI 选题/备注闭环）

- Source and release: production traffic runs functional SHA `1eeae4928080206e0784732dd02a6b1c9a62b9f4` from `codex/v141-content-governance`, release/cache `20260812-v1428-canvas-batch-stability-1`, sibling release `20260812-v1428-canvas-batch-stability-1-1eeae492`. New main/video units listen on private ports `8796/8771`; the persistent v1428 routing unit inserts two exact rules before retained v1427.1 rules. Old v1427.1 main/routing remained active/RW throughout, so there was no stop, read-only period or 502 window; removing only the v1428 route returns traffic to the online compatible old code without replacing data.
- Code/config boundary: only the verified Git archive was extracted. Its archive SHA-256 is `c98227a28c5f8cedfcef1566956ac42bf17a84b7b303a100be38601092a29f78`. The release-external environment was inherited server-side with mode `0600`; only release/root/private ports and the non-secret image concurrency value were changed. SQLite, accounts, members, authentication, uploads, composed media, canvas blobs, video runtime, Nginx and provider secrets were not uploaded, overwritten or written into Git/release/logs.
- Linux closure: locked main passed `829 collected / 828 passed / 1 approved skip`; sidecar `160/160`; Node `129/129`. Release verifier passed Phase 0 `fc07f32b452808224c35b70e8ed960afff506779f5ac22b5938ab5cd755a43dc`, ESM graph `d3bc098ae4a34582ae35e628a456b31a58faf353d5f3567d5188b4ec44548e5f`, closure `c02056796787fc3920a20b4a474df2c8f3cff3a2c1f179bf31717a0294eef155`, canvas `63 files / 1,771,558 bytes / 74d95401f2497d73b7bcfd9db66cd8801acd4e5fe17b867fe718ed263780e109`, runtime `65 files / 3,328,588 bytes / 1f8416ccf09a178eff51074118792b9b039a4fb37f9f776432fab48f5e46c45a`.
- Protection: fresh online SQLite v2 backup `/data/dumate-studio/backups/v1428-pre-switch-20260812T224200Z` has manifest SHA-256 `7d69212e61c682855be73f18836d27d11638f703904ca50122793c6494139c99` and database SHA-256 `666d14cc58cdad2e0b551ec0328134bb597279dd85ccbb6425a95ac9b00aee2d`. Manifest verification, byte-identical isolated restore drill and restored `quick_check=ok` passed. The prior main unit/routing/private env rollback set remains at `/data/dumate-studio/deployment-backups/v1428-pre-switch-20260812T224200Z` with verified hashes.
- Off-traffic startup: the first new-main attempt correctly refused startup because it still targeted the old v1427 sidecar. A dedicated v1428 sidecar was then started on `8771`; one inherited public path in the new release-external env still carried the old sibling suffix and was corrected to the already verified v1428 release. Both corrections happened off-traffic while the old public service stayed active. Since the successful `22:51` startup, both services have remained active/RW with no Traceback, fatal error or application-startup failure. Final protected readiness is `ready=true / writeReady=true / startupVerified=true / blockers=[]`; model usage, exact private-media exception and video-workshop receipts remain visible warning-only and did not stop or downgrade production.
- Real acceptance: same-team creator delivery remarks returned 200 while a cross-team creator remained 403. Qianfan topic preview returned 200 with five sources and one complete item. Ten distinct infinite-canvas background jobs were all accepted with 202 and settled `10/10 succeeded`; every result has one durable local canvas URL and no failure label. One XHS batch-core `3:4` image returned 200 through the production MaaS adapter, then vision-copy returned parseable JSON with a 19-character title and 426-character copy. No unknown provider result was resubmitted.
- Public/browser acceptance: root and health returned 200 for `20/20` consecutive checks; OpenAPI/community were 200 and anonymous state was the expected 401. Root and a fresh Chromium DOM loaded the exact v1428 cache identity and rendered the login/batch application DOM. The isolated browser-control screenshot channel produced an empty visual frame, so authenticated remarks/topic/generation claims are based on the same-release authenticated API and durable results rather than overstating a visual screenshot.
- Data reconciliation: SQLite stayed at 48 tables with `quick_check=ok`; total rows advanced `70,997→71,098` through the controlled real provider jobs and receipts, with no abnormal table or business-row decrease. Pre/post-route media counts and bytes were identical: uploads `7,213 / 13,202,672,782`, composed `1,005 / 9,011,672,060`, canvas blobs `691 / 915,749,843`, video projects `64 / 5,183,677`, video uploads `205 / 261,056,856`, video outputs `1,968 / 6,571,985,070`. Existing historical missing-media facts and exact exception remained unchanged; no reference, file, owner, supplier metric or return link was deleted, fabricated or reassigned.

## v142.6 - 2026-08-12（零停机绿色发布、供应商头像与批量文案收口）

- Source and release: production traffic runs functional SHA `f2c35ed1457e635331ce56042cf40b8ef4f78371` from `codex/v141-content-governance`, release/cache `20260812-v1426-supplier-avatar-copy-limit-1`, sibling release `20260812-v1426-supplier-avatar-copy-limit-1-f2c35ed1`. The new main/video units listen on private ports `8793/8769`; the v1426 routing unit inserts two exact rules before retained v1425 rules. v1425 main/video/routing stayed active/RW throughout, so deployment had no stop, read-only period or 502 window; removing only the v1426 routing unit returns traffic to still-online v1425 without replacing data.
- Code/config boundary: only the verified Git archive was extracted into the new release. The v1425 release-external environment was copied server-side with mode `0600`, then only release ID/root and candidate ports were changed. SQLite, accounts, members, auth, uploads, composed media, canvas blobs, video runtime, Nginx and provider secrets were not uploaded or overwritten. Dependency locks were byte-identical to the verified Linux environment; main 18 packages and sidecar 37 packages passed exact-installed and `pip check`.
- Linux closure: locked main passed `820 collected / 819 passed / 1 approved skip`; sidecar `160/160`; Node `129/129`. Release verifier passed Phase 0 `700a304cd0b2277bffdd8613be02ddc2b33d048c0a77d38700136c59ec1d57b4`, ESM graph `cccb6260628b003b3189d05dd3b40632aede87e095179861be4ee75735d5e597`, closure `ca9c31f6e0fa5a5d48f412e6c8940b9f8d401669231e3a4ed365adec15b559e3`, canvas `63 files / 1,768,749 bytes / 1cec16f2cd3fede5021d521cd8d0abcc3f46d9d2409c8f3dad5f63b8d6189d75`, runtime `65 files / 3,322,592 bytes / c935ce1784fda6e1166efca02dbd6f1070ad377df71616060332395591a549fc`.
- Protection: fresh SQLite v2 backup `/data/dumate-studio/backups/v1426-pre-f2c35ed1-20260812T054035Z` has manifest SHA-256 `0b65fbbd3874c50930c954dbe88973e1bb059da616b45722bd6b16fbff98b906` and database SHA-256 `a162639be23e6b90751b17d2b408dfbab00f9eab2dba1f2ddd7ec6eb4b4fbb7f`. Manifest verification, byte-identical isolated restore and restored `quick_check=ok` passed. Exact v1425 units, routing helpers and release-external env remain at `/data/dumate-studio/deployment-backups/v1426-pre-f2c35ed1-20260812T054035Z` with verified `SHA256SUMS`.
- Off-traffic acceptance: main/video were active/RW with `NRestarts=0`; readiness was `ready=true / writeReady=true / startupVerified=true / blockers=[]`. Authenticated LLM, image, video, digital-human, TTS and canvas configs returned 200 and configured/reachable. One minimal real LLM call, one image, one TTS test and one Qianfan topic preview all returned HTTP 200; the image decoded as a real image data URL, while Qianfan returned 10 references and one compliant 465-character copy. A real supplier-parent snapshot returned 200 and one existing content-account avatar returned a valid PNG only with its exact `accountId`; the missing-context request stayed denied.
- Public acceptance: health was 200 for `10/10` consecutive checks; root/OpenAPI/community/infinite-canvas/video-workshop were 200, anonymous auth was 401, and root carried the exact v1426 cache ID. Live authenticated user traffic immediately continued with 200 responses for state, publish quota and delivery metrics. Post-route main HTTP 5xx/Traceback counts were zero, and final v1426/v1425 main/video services all remained active with `NRestarts=0`.
- Data reconciliation: SQLite stayed at 48 tables with stable identity and `quick_check=ok`; total rows advanced `66,982→67,000` under the four controlled model receipts and concurrent normal user reads, with no key-table decrease. `model_usage_receipts` advanced `5,543→5,547` exactly for the four real smokes. Media counts and bytes were unchanged: uploads `6,904 / 12,497,094,668`, composed `1,005 / 9,011,672,060`, canvas `645 / 681,817,927`, video projects `63 / 5,183,309`, uploads `205 / 261,056,856`, outputs `1,968 / 6,571,985,070`. Historical raw missing `47`, pending `48`, isolated `46`, unisolated `1`, effective pending `0` remain visible; model and sidecar usage are warning-only and did not stop or downgrade production.

## v142.5 - 2026-08-12（零停机绿色发布、批量媒体恢复）

- Source and release: production traffic runs functional SHA `f3287b230f8e76d3e744a35bb153307e44e94440` from `codex/v141-content-governance`, release/cache `20260812-v1425-batch-media-recovery-1`, sibling release `20260812-v1425-batch-media-recovery-1-f3287b23`. The v142.5 main/video units listen on private ports `8792/8768`; a persistent routing unit inserts two exact rules before the retained v142.3 rules. v142.3 green and v141.3 base services stayed active/RW throughout, so there was no server stop, read-only interval or 502 window. Removing only the v142.5 routing unit exposes the still-online v142.3 route without replacing data.
- Code and configuration boundary: only the exact Git archive was streamed into the sibling release. The v142.3 external environment was copied server-side with mode `0600`; only release identity, code roots and private candidate ports changed. The deployment did not upload or overwrite SQLite, accounts, members, authentication, uploads, composed media, canvas blobs, video runtime, Nginx or provider secrets. Main/sidecar locks were byte-identical to the verified Linux environment; exact-installed and `pip check` passed for 18 and 37 packages.
- Linux closure: locked main completed `817 collected / 816 passed / 1 approved skip`, sidecar `160/160`, Node `129/129`. Release verification passed Phase 0 `3c497d0b3a523fb5ba21a5cb964d47c33cb8e77b0d0df7f356166e770c72671f`, ESM graph `13043a28971f31c5f1f220243a2669f1920f69c6e656e2df673dd548e60c498b`, closure `77dbdd6882192bc0bd48109f3f42805b87f3ba02b9169cfc9cfe5f08a5da6285`, canvas `1cec16f2cd3fede5021d521cd8d0abcc3f46d9d2409c8f3dad5f63b8d6189d75`, and runtime 65 files / 3,318,796 bytes / `c478fbe3aefb64e8c1d273f65b0fa9fb20cea7ee2ad5f5c3acf1ad39dcd2636f`. The first server full-suite attempt exposed only source-checkout shape assumptions; the final isolated source harness reproduced the repository parent/Git index without adding `.git` to the release and passed the complete suite.
- Protection point: online SQLite v2 backup `/data/dumate-studio/backups/v1425-pre-f3287b23-20260812T033347Z` has manifest SHA-256 `5ffcc0ae3ce27d6b5aa3cff5e8eab621767e963466ff7f5aa865efe4e8f068fb` and database SHA-256 `841e2268ddd7d4a0739f17304004e8f8a6a4f69d6a176277b45fc19112d2d3e9`. Independent manifest verification, byte-identical isolated restore and restored SQLite `quick_check=ok` passed; only the reproducible temporary drill copy was removed afterwards. The previous units, routing scripts and release-external environment remain at `/data/dumate-studio/deployment-backups/v1425-pre-f3287b23-20260812T033347Z`.
- Runtime gate: the off-traffic candidate initially rejected startup because the one retained registry-missing exception was intentionally bound to v142.3. A read-only v142.5 audit proved the exact missing-set SHA `fb472ac2c87397f5fc49018a59d94affb1b606768e6e52deb1b78d81a3c8e523`, unisolated/registry-missing count `1`, effective pending `0`, sidecar health and SQLite integrity were unchanged. Only then was the existing exception release ID rebound to v142.5; the digest, count, media references, files and owners were not changed. Final readiness is `ready=true / writeReady=true / startupVerified=true / blockers=[]`; model-usage and sidecar-usage discrepancies remain warning-only, while supplier business metrics remain protected data.
- Data reconciliation: SQLite stayed at 48 tables / 66,978 total rows with no table decrease and `quick_check=ok`. File counts and bytes did not decrease: uploads 6,904 / 12,497,094,668 bytes; composed 1,005 / 9,011,672,060; canvas blobs 645 / 681,817,927; video projects 63 / 5,183,309; video uploads 205 / 261,056,856; video outputs 1,968 / 6,571,985,070.
- Acceptance: the public health endpoint returned 200 for 40/40 consecutive checks; root loaded the exact v142.5 cache, OpenAPI/community/Nginx root and health returned 200, and anonymous `/api/auth/me` returned 401. Every root-referenced JS/CSS/brand asset plus `/XZ-Design/` and `/custom-video/` returned 200. Existing production sessions completed 22 read-only checks across administrator, creator, Free, supplier parent and supplier child roles, including state, platform accounts, member visibility, provider configs, canvas projects, supplier bindings and delivery metrics. No paid provider generation, publication or supplier write was submitted during deployment. Final main/video services are active/RW with no post-start warning-or-higher journal entries and `NRestarts=0`.

## v142.3 - 2026-08-11（零停机绿色发布、完整 provider 配置继承）

- Source and release: production traffic runs functional SHA `ec0a4daf86502f5cbf5a03806c202942661f5d29` from `codex/v141-content-governance`, release/cache `20260811-v1423-batch-video-editor-1`, sibling release `20260811-v1423-batch-video-editor-1-ec0a4daf`. Persistent green main/video units listen on private alternate ports and an idempotent routing unit applies two exact NAT rules for the public direct port and Nginx worker. The old v141.3 main and sidecar stayed active/RW throughout; stopping only the routing unit immediately returns traffic to the old online service.
- Availability incident and closure: the first green environment was release-complete for paths, readiness, Qianfan and Seedance but replaced rather than inherited the existing provider environment. Users therefore saw HTTP 503 for copy generation and local “image service not configured” before an upstream image call. Traffic was returned to the old service without stopping either generation, then exactly 31 missing existing provider keys for LLM/image/TTS/digital-human/video were copied by key from the old release-external env into the new release-external env, with values never logged and file mode `0600`. Green services restarted off-traffic, main health reported `llm_configured=true`, protected readiness passed, and routing was restored. Subsequent image config reads were 200 and two real image generations returned 200.
- Runtime policy: model usage receipt/outbox/spool and sidecar usage discrepancies remain visible as warnings but never block startup, RW or routing; supplier-entered exposure/view metrics remain ordinary protected business data and were not changed. One current registry-missing media reference is accepted only by an exact release ID, missing-set digest and count binding. Raw missing evidence remains visible; no reference, owner or file was deleted, fabricated or reassigned.
- Linux closure: locked main collected 807 with `806 passed + 1 approved skip`; sidecar `160/160`; Node `129/129`. Main and sidecar wheelhouses passed offline `--no-deps`, exact-installed and `pip check`. Final release verifier passed Phase 0 `862a4553802914ca817541183d54f50ac45c3eb5bc0a8525bb26eadd4b84cabd`, ESM graph `6019e999874c254346ff519c0bfa06eb80de1f5c9da4ad2fe92b19bf5bcb17c6`, closure `89d9636bc29044b98a7d132b12723c23acf53271869f424d349f212e82324011`, canvas `6ca04ee8ff6c4fba5bc581c641e542830dc7d7c0d187311b89663bed4f63a018`, runtime 65 files / 3,313,747 bytes / `b4d5fb1641ec27651fe067fa255a83eaf5afc31d618e7f50d917eb0b1ae71cb5`.
- Protection point: online SQLite backup `/data/dumate-studio/backups/v1423-pre-switch-ec0a4daf-20260811T004158Z.sqlite` is 66,015,232 bytes with SHA-256 `7a91ef6ed1de47209630d1270ffd0d9e8ebcf0a573e94a90068f0cdba2154782`; isolated restore drill `/data/dumate-studio/restore-drills/v1423-pre-switch-ec0a4daf-20260811T004158Z` has the same digest and `quick_check=ok`. The previous release remains the live code fallback; rollback never restores the old SQLite over newer activity.
- Data reconciliation: SQLite stayed at 48 tables and `quick_check=ok`; total rows advanced `64,195→64,237` through normal production with no table decrease. Uploads advanced `6,718→6,720`; composed `1,005`, canvas blobs `638`, and video projects/uploads/outputs `60/203/1,872` did not decrease. The release did not upload or replace SQLite, accounts, members, authentication, uploads, composed media, canvas blobs, video runtime, Nginx or other private configuration.
- Final acceptance: protected readiness is `ready=true / writeReady=true / startupVerified=true / blockers=[]` with warnings `model-usage-unresolved` and `private-media-registry-exact-exception`; sidecar is ready/RW with normal write policy. Public root and health serve the exact release/cache, OpenAPI and guest boundaries behave normally, and a real browser rendered the v142.3 workspace, community media and retained-history placeholder. A server-private Qianfan search returned HTTP 200. No duplicate Seedance paid smoke was submitted during this deployment.
- Deferred product issue: while one task board was still running, a second board received HTTP 200 LLM responses that were not strict JSON and failed before `/api/image/generate`. Later image submissions returned HTTP 200, proving the image provider is connected. This is delegated as a local-only JSON/concurrency resilience fix; it is not repaired by changing production data, retrying paid image/video providers or weakening ownership/checkpoint rules.

## v141.3 - 2026-08-10（滚动切换、媒体与用量终态闭环）

- Source and release: production runs functional SHA `1332f8c8e9408c7e9a36f49ab13b48a46ca353f5` from `codex/v141-content-governance`, release/cache `20260810-v1413-runtime-finalization-1`, active release `20260810-v1413-runtime-finalization-1-1332f8c`. The old v140.6 main and sidecar stayed active/RW during checkout, Linux build, tests and protected settlement. After the old release was explicitly restored and verified at public HTTP 200, the two services rolled to the new sibling release in an approximately nine-second switch window. Both are active/RW with `NRestarts=0`; their actual working directories and public release environment point to the new release, while all persistent data paths remain the original server paths.
- Production-only forward fix: the first v141.3 media plan failed closed because status correctly reported effective 25 but plan selection included all 73 raw candidates. Read-only evidence proved an exact partition: 48 video outputs predated the immutable `140010` isolation baseline and 25 were created after it. The final patch accepts only that complete, single-baseline, `video-output / video-workshop-project` topology and still rejects unsupported kind/provenance, touched history, count mismatch or time drift. Apply registered exactly 25 rows, restored raw pending `73→48`, effective `25→0` and drift `1→0`; a fresh-binding replay returned `applied=false / insertedRows=0`.
- Usage closure: the reviewed usage-v2 plan covered exactly two pre-switch central unresolved records and inserted two terminal rows; replay inserted zero. The video recovery plan then imported exactly 13 sidecar-persisted successful terminal operations still absent centrally; replay again inserted zero. Final protected settlement state was unresolved/outbox/spool/effective sidecar all zero with `quick_check=ok`; no provider submit, poll, retry or inferred cost occurred. A normal post-switch image request later created one fresh `main-provider` pending receipt with `calls=0`; per the operating policy it was left to the live request path rather than manually settled or used to stop/flip the already-armed RW service.
- Linux closure: three wheelhouses passed manifest verification, offline `--no-deps` install-check, exact-installed and `pip check`: main runtime `9093dfce52d392123b803534c21998c3e259a243578921ef796d6c464be62fc7`, main test `d748941d9b0cf6dd78fd24d432df24784e7e1ff6601bce467b5ead58b4957de7`, sidecar `d41503a784a27373bc6069ae7ecd6d9d34183dc58101e5d2a0fe2dc05808d1e2`. Locked main passed `781 + 1 approved skip`, sidecar `142/142`, Node `127/127`. Release verifier passed Phase 0 `5160b64d7ad75ebbf69fa54dce8146d840b82ee07d061087e32d5ceeb800f2e2`, ESM 62/354 with closure `a6a641e6aa01a2c4a1f3bbdaf6e1c8a950fc6a554992518da38734ca739a25f1`, canvas `efd2eff99df225ebbfabe37fa90571f21e430c5ced91cec34168d53daf578dd5`, runtime 58 files / 3,025,471 bytes / `730bf796628acf05cf50e8bbad4e505333d97d5e3f2e5a38702438d48cf00616`.
- Protection and rollback: the verified pre-media backup/snapshot manifests are `f191431e889cd69ec616021d00939f49f85acfc63d8609869da564043d93cfbf` and `d7dc28062cda34ffc75e3670deab772df60f2c45b7d624f682be7dbeb9055af7`; the isolated restore drill passed with restored SQLite `quick_check=ok`. Final post-recovery backup `/data/dumate-studio/migration-backups/v1413-post-video-recovery-1332f8c-20260810T053000Z` has manifest `27bfdacfaf194bf287a4a27df3ff4d8454848a1b417b605061c7a23ff9092efe`, and complete snapshot `/data/dumate-studio/snapshots/v1413-post-video-recovery-1332f8c-20260810T053000Z` has manifest `01dea431bcc489468470383c93b3d3d29f8003a020d75d7313f3d7af78922446`. Pre-switch service configuration is retained at `/data/dumate-studio/config/rollback-pre-v1413-1332f8c`; rollback switches compatible code/service pointers only and must not replace newer business data.
- Data and media reconciliation: tables stayed `48→48`, total rows `58,509→58,595`, and no table decreased. Increases are the exact media/usage settlements plus one legitimate asset written while old RW remained online. Media inventory digest stayed `d0c6af65bf266bc5f314bf0f354912b50361e3b10ae84aca635c9a67f0f0659f`; uploads/composed/canvas blobs/video projects/uploads/outputs stayed `6,240 / 993 / 634 / 58 / 176 / 1,777`. Raw historical media facts remain missing 46 / pending 48 / isolated 46 / public avatar exemptions 2, while unisolated/effective/conflict/drift are zero. No reference or file was deleted, fabricated or reassigned.
- Supplier data protection: `supplier_activity` stayed 2,354 rows with an identical digest, `supplier_account_bindings` stayed 52 and `team_suppliers` stayed 4 with identical digests; all 80 account documents were unchanged. Deployment did not modify exposure counts, view counts, return links, supplier assignments or operator history.
- API and browser acceptance: the switch gate reached `ready=true / writeReady=true / startupVerified=true`; main and sidecar health were ready/RW and SQLite was `ok`. Public root, health, OpenAPI, community, infinite-canvas and video-workshop assets returned 200; unauthenticated protected state returned 401. A real browser loaded the exact v141.3 cache marker, rendered the full home/community UI and range-loaded media, showed the retained-history state for an isolated item, and enforced the guest login boundary for infinite canvas. No paid generation or publication was submitted during deployment; full Linux/Node suites and release/static closure cover v141 features and the existing infinite-canvas, OmniHuman and information-flow integration without claiming a new paid-provider output.
- Availability and cleanup: only completed restore-drill derivative copies reproducible from retained formal snapshots were removed, freeing 82,033,015,545 bytes in total; formal backups, snapshots, releases, business media and the current rollback point remain. Ordinary code-only releases should keep the old release active/RW through sibling build and validation, then use the shortest rolling switch. Protected writer freeze is limited to evidence-bound data settlement, never an open-ended public outage or maintenance mode.

## v140.6 - 2026-08-09（核心生成链路与信息流缓存恢复）

- Source and release: production runs functional SHA `2d48b0efa5d836f8f0d9c50ad52ef8540f73a578` on `codex/v122-team-auth-home`, release/cache `20260809-v140-core-connectivity-3`, active release `20260809-v140-core-connectivity-3-2d48b0e`. Main and video sidecar resolve to that exact release, are active/RW and report `NRestarts=0`. Only audited code/static and systemd release pointers changed; production SQLite, accounts, media, authentication, private environment contents and Nginx were not replaced.
- Root causes and fixes: infinite-canvas job creation now resolves historical browser source IDs to the stable server project before strict owner/scope validation, and immutable media-isolation evidence prevents approved historical missing outputs from being misclassified by GC. OmniHuman public input URLs are supplied through the existing signed public media proxy with explicit `PUBLIC_BASE_URL`; image/audio Range probes returned 206 and real submit/poll requests returned 200. Information-flow prompts already contained complete time-coded shots, but six importers retained the old immutable `ai.js?v=20260727-v118-7`; the final release gives `ai.js` and its complete reachable ESM graph the new cache identity.
- Linux validation: the functional release passed locked main `773 passed + 1 approved skip`, sidecar `140/140` and Node `124/124`. The final cache-only commit additionally passed Linux Python targeted `57/57` and Node `124/124`. Release verifier passed Phase 0 `6f257170c55084814298d4387e397c794d12da9d8879f1639f62f6806d6af96e`, ESM 60 modules / 342 edges, graph `06aa09adc2c0d5d0e2cda8b58fbcf9966ddd191ba1e06cd3fc3193ea0a9b5bb6`, closure `80e5670dca6263206efdb7d2553480b99575f429e9ae4b06cd20b40456bf17f5`, unchanged canvas manifest `59b1d83c6235ad26c4b20c1e0182dec47205c53677a06035f95cde42667d6789`, and runtime 58 files / 3,001,887 bytes / `526ab76ee1d7636711bd4d65dc1b5d479a1e59226b055358278da12b2bb81556`.
- Protection point: pre-switch SQLite v2 backup `pre-v1406-core-connectivity-7c0bff0-20260809T080435Z` has manifest `2e951862723aca70356f7ecd0dc0d34dc09230dbe085c1154612dc8157f119a1` and passed verification. The 20-component complete snapshot `pre-v1406-core-connectivity-7c0bff0-20260809T080833Z` has manifest `53d3e10b44be6b6710c73ed74ecc1e0b9f27ecc6114c8b1362c48814e99b2b2d`, media digest `34beddb4712ce25445b13525c95aba0ace5f0f3c2a537e0581565b8ef4e3e85a`, and passed verify plus empty-target restore drill with SQLite `quick_check=ok`. The prior release and a separate pre-v1407 systemd configuration rollback are retained.
- API and browser acceptance: protected readiness is `ready=true / writeReady=true / startupVerified=true`, database `quick_check=ok`, sidecar normal RW. Resource missing, model-usage unresolved/outbox/spool, unisolated media, evidence conflict and audit drift are all zero. Public root, health, OpenAPI and community return 200; unauthenticated protected config returns 401, and repeated readiness reads do not change the live gate. A real production browser loaded `main.js?v=20260809-v140-core-connectivity-3`; the service log records `GET /js/api/ai.js?v=20260809-v140-core-connectivity-3 200`, and the served SHA exactly matches the release. A real server-managed LLM call returned five valid time-coded shots for each information-flow segment and passed the deployed parser.
- Data reconciliation: SQLite stayed at 48 tables with stable DB identity and `quick_check=ok`; total rows increased through normal production activity from 57,843 to 58,045 with no table decrease. Media also had no decrease: uploads 6,232→6,238, composed 986→991, canvas blobs 632→634, video projects/uploads/outputs unchanged at 56/168/1,694. Raw isolated history remains visible as missing 46 / pending 48, while isolated 46 / unisolated 0 / effective pending 0 keeps future drift fail closed; no reference was deleted and no media was fabricated.

## v140.5 - 2026-08-09（媒体隔离与用量恢复闭环，完整 RW）

- Source and scope: production runs functional SHA `390a563802fa62d2fa1278a25fe7ad266e1f294c` (`fix: recover durable video usage receipts`, parent `5f2ee647a6421bb782aa8079646f860c0a681eb8`) from `codex/v122-team-auth-home`, with release/cache identity `20260809-v140-media-isolation-1` and active release suffix `390a563`. Only audited code/static files and the public RW service override were deployed; production SQLite, accounts, media, authentication, private environment and Nginx were not replaced or modified.
- Linux closure: Ubuntu x86_64 / CPython 3.12.3 wheelhouses passed verify, offline `--no-deps`, exact-installed and `pip check`: main runtime 18 packages / manifest `a683258aec8c78864792d90184eed682709659dd6127f84ccfa04a6b2bb2a089`, main test 20 / `f064da114e892e9607ad076d728c23967ffdef4b560b0ba622c9edc1f22bfb79`, sidecar 37 / `2288f55ffcf180da34e7e7b02c119990dda042ad1de48d3ec479c6af96bbc1ac`. Locked main collected 773 with 772 passed and one approved v120 snapshot skip; sidecar passed 140/140, Node 124/124, compileall and tracked JavaScript syntax passed.
- Release verifier: Phase 0 `7c920495f0f67c630b0686891f16b5b90dea9571af8e42ba2e9ac54e35cc717c`; ESM 60 modules / 342 edges; canvas manifest `59b1d83c6235ad26c4b20c1e0182dec47205c53677a06035f95cde42667d6789`; runtime 58 files / 2,997,715 bytes / manifest `15ce077bfa61dba6e4bf54a59afc3804f6a4dced16fe467c7f5833cb085fe972`.
- Startup and RW gate: the main service now uses FastAPI 0.68 / Starlette 0.14 native startup/shutdown events. Journal evidence shows `Waiting for application startup`, then the complete production audit, then `Application startup complete`; readiness GETs remain observational. Main and sidecar are active/running with `ACG_READ_ONLY=0`, sidecar reports `ready=true / readOnly=false / writePolicy=normal`, and neither service has restarted unexpectedly.
- Media isolation: expand-only `140010` and the exact operator-reviewed plan were applied under fresh DB/backup/snapshot/media bindings. Raw audit remains `missingReferencedFiles=46 / pendingRows=48 / publicAvatarExemptions=2`, while `isolated=46 / unisolated=0 / effectivePending=0`. The 46 retained references are 38 succeeded canvas jobs, one published community canvas and seven server assets/uploads. No reference, owner, registry row or file was fabricated or deleted; isolated media returns 410 and future missing media remains fail closed.
- Durable usage recovery: sidecar audit recognizes 14 exact legacy v1 `operator-confirmed-unknown` immutable terminal receipts without changing history. A reviewed plan with SHA-256 `4821f2d57b1e8d46b3509cac29a44b12fd992701f6d2cef4b9d2a1f51b44d24a` imported only three sidecar-persisted completed operations whose central receipts were missing. Apply returned `inserted=3 / projected=3 / terminal=3 / unresolved=0 / outbox=0 / quickCheck=ok`; a fresh-binding replay returned `applied=false / reused=true / insertedRows=0`. No provider call or retry occurred.
- Protected points: the pre-apply SQLite v2 backup manifest is `9f9da21702593265787d1f38b8250ce5f1633c65938dc4cc651813329351d3a9`; the current 20-component snapshot manifest is `34fd1c951ff774a66712c93a11eff012af8d1e12b078b5cbcaeba99b3b9276aa` with media digest `bf84678f18a27114bc5423a529af11be55fb0a19914a222986e4c717a646bf42`; the post-apply backup manifest is `0779f88be2db761b9b7b1e9ec4cf7ef6bfdc60cd9e2b830eba47578719c46ba9`. Backup integrity, snapshot verify/restore-drill and SQLite `quick_check` all passed. Existing rollback releases and RO service checkpoints were retained.
- Data protection: no business table count decreased. Only the expected usage rows increased: `api_usage_events 3936→3939`, `model_usage_outbox 3829→3832`, `model_usage_receipts 3829→3832`, v2 entries `83→86` and v2 settlements `1→2`. Other business tables stayed unchanged. Media file counts stayed uploads 6,223, composed 986, canvas 632, video projects 56, video uploads 168 and video outputs 1,694.
- Final acceptance: repeated authenticated readiness is `ok=true / ready=true / writeReady=true / startupVerified=true / blockers=[]`, SQLite is `ok`, and sidecar usage is receipt 810 / raw pending 810 / terminal 810 / settled 40 / effective pending 0 / conflicts 0; raw is the historical evidence count, not unresolved work. Root, public entry, OpenAPI, health and community return 200; protected unauthenticated requests return 401; isolated media returns 410. A real browser rendered the home and community media, showed the retained-history isolation message and produced no application console error/warning. Post-release journals contain no traceback, exception, 5xx or startup failure.
- Rollback boundary: rollback may restore the prior audited code/service configuration only. It must not replace the current SQLite database, usage ledgers, accounts, media, authentication state or private environment, and it must not replay `140008/140009/140010`, tenant/resource/canvas/incident or historical usage settlements.

## v140.4 - 2026-08-09（受保护恢复工具上线，保持 RO）

- Source and scope: production runs functional SHA `eb4c9c599c25aaad3d361d0aa2d6697e870c5f0f` on `codex/v122-team-auth-home`, with release/cache identity `20260809-v140-production-recovery-1` and active release suffix `eb4c9c5`. Main and video sidecar are active/healthy, and systemd plus process cwd resolve to the same audited release. Only the reviewed code/static release and controlled expand-only settlement tools were used; production databases, accounts, media, authentication and private configuration were not replaced. `ACG_READ_ONLY=1` remains active.
- Linux closure: Ubuntu x86_64 / CPython 3.12.3 wheelhouses passed build/verify, offline `--no-deps`, exact-installed and `pip check`: main runtime 18 packages / manifest `b9db22222b6af456656c628fd2d416b64f9f05a049751a3b0dcc5eb184b4327c`, main test 20 / `a3ff598a8d2c3cece4ad74f1b069f5213dee3dc7888a9147aa97f11840ae7471`, sidecar 37 / `69943e2e8ee1936b6d819c44ef91395d543af7ab930b1e1cccc10dee12ed39e4`. Locked main collected 767 with 766 passed and one approved v120 snapshot skip; sidecar passed 140/140, Node 123/123 and compileall passed.
- Release verifier: Phase 0 `87fb5996ddd38bc6027b3a7d2b050b10017965a09b13d731c08e8fb7f8f4de85`; ESM 60 modules / 342 edges, graph `8a8b7888449f2d6ccdcf53164994d919ded4e6800cd5138146b60f1bac956aef`, closure `1db4271109d46345afda01a60a27a3610e5aae222c4683f7583e27de476126f2`; canvas manifest `59b1d83c6235ad26c4b20c1e0182dec47205c53677a06035f95cde42667d6789`; runtime 58 files / 2,901,522 bytes / manifest `a1782177ab8d2ac14beb1d25d791c5aa8df78ea58b9b10f8d4209c52749be61c`.
- Controlled settlement: schema `140008/140009` had already passed double-run and was not replayed. Tenant adoption applied 60 rows and replayed zero; resource settlement applied 166 rows, all preserving `historical source project absent`, and replay inserted zero; canvas recovery restored exactly 5 verified blobs / 10,924,695 bytes and replay restored zero. Incident adjudication inserted exactly 46 immutable receipts, replay inserted zero and explicitly left readiness unchanged.
- Usage v2: the exact reviewed set was 56 central unknown, 19 sidecar unknown, 7 submitted indeterminate and 1 succeeded. Apply returned `insertedRows=83`, `terminalRows=83`, `projectedRows=76`, `indeterminateRows=7`, `unresolved=0`, `outboxPending=0`, `quickCheck=ok`; replay returned `applied=false`, `insertedRows=0`, `reused=true`. No provider call or retry was performed.
- Final protected point: binding `v1404-post-usage-20260808T234813Z` binds SQLite backup manifest `bd1527e81e0e99d99499f021297866f5eb6124042860567a5837f36e4b3b9660`, DB logical digest `3106ebfa610a3cdf7e9e7d8d32ad9b0625027bb7cdfed248c3257c998733e026`, 20-component snapshot manifest `8426d3e038e6cea2618ac06945a35b6daf2634718f7b1cddf4b58e25f73c58d2` and media digest `b8946380118911ab6530be35444e19d38b0e0764b3a2e08bc0c03cfd595c30b9`. Verify, empty-target restore-drill and restored SQLite integrity all passed. Prior rollback releases/snapshots and all reports, hashes and backups were retained; only duplicate restore-drill copies reproducible from retained snapshots were removed.
- Data protection: compared with the pre-deployment protected binding, none of 46 table counts decreased and docs stayed at 14,786. Preserved counts include members 88, teams 1, team members 76, team suppliers 4, supplier bindings 52, team accounts 80 and community posts 60. Media inventory is uploads 6,223, composed 984, canvas blobs 632 (from 627 after the five verified restores), video projects 56, video uploads 168 and video outputs 1,694. New rows are limited to the controlled ledgers/scopes and five verified blobs; usage v2 ledger entries are 83.
- API acceptance: public root/index/health/community/download manifest return 200; unauthenticated protected config/account/asset/profile reads return 401; invalid login returns 401; an unauthenticated write probe returns 503 maintenance. The sidecar is ready while mutations are denied. Repeated protected `/api/ready` calls are identical and do not change the live gate. Of 138 media references across 60 community posts, 137 return 200 (117 PNG, 4 JPEG, 16 MP4) and one returns 404, exactly matching the sole missing published-community media.
- Closed-gate decision: full RW was not restored. Final readiness is `ok=true`, `ready=true`, `writeReady=false`; resource missing/orphan/invalid, usage unresolved/outbox/spool and registry conflicts are zero, but media remains `missingReferencedFiles=46`, `pendingRows=48`, `issues=[missingReferencedFiles]`. The 46 are 38 succeeded `customCanvasGenerationJobs` outputs, one published community canvas and seven server asset/uploads. Existing server files, historical snapshots/databases, filesystem recovery evidence and persisted provider URLs were exhausted; no reliable original exists beyond the five restored blobs. Adjudication receipts, placeholders, reference deletion, ownership fabrication or relaxed readiness must never be used to enable RW. Continue only after the user supplies exact originals/independently verifiable sources, or explicitly approves a reversible no-data-loss business isolation design.

## v140.3 - 2026-08-09（受保护切换，保持 RO）

- Source: production deploy SHA `7aeec8f65e02e7f5aa183ef6aa623c785813646a` on `codex/v122-team-auth-home`, cumulatively including functional commit `4708bdaeeb912cc4182c7c553ffd561f034065c0` and the snapshot-safety commits. The active sibling release is `20260809-v140-platform-stability-15-7aeec8f`; main, supplier UI and video workshop keep release/cache identity `20260808-v140-platform-stability-15`.
- Scope: built the new release from the exact Git archive and synchronized only audited code/static content, explicitly excluding main/server databases, JSON/runtime state, uploads, composed media, canvas blobs, model-usage spool, video-workshop projects/uploads/outputs, BGM, model cache, authentication, private environment and Nginx. A rejected unactivated construction that still contained tracked sidecar `.gitkeep` placeholders was moved intact to `/data/dumate-studio/rejected-releases/20260809-v140-platform-stability-15-7aeec8f-protected-placeholders`; it was never switched or deleted.
- Backup and recovery: while both writers were frozen, created SQLite v2 backup `/data/dumate-studio/migration-backups/pre-v1403-final-20260808T163547Z`, manifest SHA-256 `8db8e2e84fa58459b6b74cf316d6aa13611a54740d74bfedff521e1d1c4f4bdb`, backup SHA-256 `00dffa7beca95fb1ca1c396b9aef19500029161a47ead0941faa4ed317987bf1`. Created 20-component complete snapshot `/data/dumate-studio/snapshots/pre-v1403-final-7aeec8f-20260808T163547Z`, ID `f13c9d95e6a94e4299a032c50ebbfe62`, manifest SHA-256 `36a78674cb60a2c83db4a8cedb48cafc506a192954cdcc827ec1a99827b815ea`, media digest `e0ad0f8d1a33925c3f10f5c28bcc9fdf481609938abc9a7c1f99ac1b6014c9d0`. Independent verify, empty-directory restore-drill and restored SQLite integrity passed; existing rollbacks were retained without rotation.
- Linux validation: target Ubuntu / CPython 3.12 wheelhouses passed signature/hash verification, offline `--no-deps` install-check, exact-installed and `pip check`: main runtime 18 packages / manifest `3f8b942eb34b7ea757e9ad7ea9378e615152e4f07031c583d495f3dcdd27e77b`, main test 20 / `fd48f76360949430fd8bf217026544e8b63fa25814579316f1a1d0513033918a`, sidecar runtime 37 / `81d36bc2822f25dd4ec7df8589f6b0ca5e8ac722e3b43225cc7707383cab7262`. Secret-free gates passed main `744 + 1 approved skip`, sidecar `140/140`, Node `123/123`, and release Phase 0 `91e5f5457070c0da0cac4f1ecca28d17927fe3f30ff38126a3c500db2dac692b` with ESM `60/342`, canvas `63` files and runtime `56` files.
- Switch and health: exact systemd drop-ins point both services at the new release while the pre-existing private environment file continues to supply persistent paths and secrets. Both units are active with `NRestarts=0`, both process environments report `ACG_READ_ONLY=1`, main/root/custom-video/canvas/sidecar checks return 200, and protected readiness returns `ok=true`, `ready=true`, `writeReady=false`. Post-switch error-level and severe-pattern log counts are zero.
- Data protection: pre/post SQLite is byte-identical at SHA-256 `740d28eaddd98808722bd47a98eaf7ee6fdcf8290eb8760e6804a86bc81b88c9`, `quick_check=ok`, 42 tables and 56,957 rows. Key preserved counts include docs 14,786; members 88; team members 76; team accounts 80; supplier bindings 52; supplier activity 2,159; private media registry 9,364; resource scopes 14,620; model-usage receipts/outbox 3,829 each; canvas blobs 627; community posts 60. The post-switch media digest exactly matches the final snapshot; no protected count decreased and private configuration was not replaced.
- Closed-gate decision: RW was deliberately not restored. Resource preflight reports 166 unresolved and 15 invalid targets; media preflight reports 48 pending, 51 missing referenced files and 45 registry conflicts; usage reports 83 unresolved, with only 27 sidecar receipts present in the complete snapshot and 56 missing. Historical migrations are already marked successful, so no migration replay, data apply, bare SQL, ownership fabrication, provider retry or guessed usage was performed.
- Browser acceptance: a real public browser loaded the home shell, health/client manifest, first inspiration page and cursor-based “load more”; a community detail opened, and the video-workshop login boundary plus infinite-canvas static build were reachable. Missing community media returned real 404 responses, consistent with the media blocker. No production application credentials were supplied, and RO prevents write acceptance, so supplier parent/child, creator/admin mutations, paid generation, publishing/return-link, membership removal and account-disable flows remain unaccepted rather than being inferred from source tests.
- Rollback: direct compatible RO code/static rollback is `/data/dumate-studio/releases/20260808-v140-maintenance-read-fix-78dbbe3`; the pre-switch private environment copy is `/data/dumate-studio/config/backups/pre-v1403-switch-20260808T165133Z/runtime.env`, and the verified full snapshot above is the data-disaster recovery point. Ordinary rollback must switch code/static/service pointers only while preserving newer business data; raw v120 must never be pointed at the evolved database. No data snapshot, business media or existing rollback was removed.

## v140.1 - 2026-08-05

- Source: clean controlled commits `599c576` → `1b41200` → `0d690d6` on `codex/v122-team-auth-home`. Production main and video-workshop release identity is `20260805-v140-platform-stability-3`.
- Scope: created a new sibling release and overlaid only the audited 44 code/static changes from the final clean commit. SQLite, accounts, members, assets, deliveries, drafts, analytics, uploads, composed media, canvas projects/blobs, video-workshop projects/uploads/outputs/runtime, model cache, authentication and private environment files stayed on their existing persistent paths. No delete-style application sync, old migration replay, business-data restore, retry of failed tasks or local-state upload was used.
- Backup and switch: created and verified a fresh SQLite v2 backup, protected-data baseline and code/static rollback point before restoring RW. Both systemd services now resolve to the new release and private environment file; `/api/ready` reports `ready=true`, `writeReady=true`, SQLite `quick_check=ok`, and the sidecar reports ready with normal write policy.
- Linux validation: lock manifests and wheel files were independently reverified on CPython 3.12; fresh offline install-checks passed for the 18-package main runtime and 37-package sidecar. The locked main suite collected 720 tests with 719 passed and only the approved missing old-v120 snapshot case skipped; video-workshop passed 140/140 and Node passed 120/120. Release Phase 0, ESM, canvas and runtime hashes match the final commit.
- Data protection: pre/post SQLite remained 42 tables and 37,465 rows; members 79, private-media registry 6,679, model-usage receipts 219 and compose operations 5 were unchanged. Protected media counts were unchanged: uploads 4,545, composed 859, canvas blobs 481, video projects 34, video uploads 123 and video outputs 955. Private-media preflight closed with zero ambiguous, pending, missing-owner, missing-reference, registry-conflict and team-binding-conflict rows.
- Provider acceptance: server-managed LLM, TTS, image, Seedance, digital-human and analytics configurations report configured/reachable. Real minimum LLM, TTS, image and Seedance submit/poll calls passed. The same LLM/TTS/image/video config endpoints and minimum LLM/TTS/image calls also passed under an ordinary creator session, confirming that model access is server-side and not limited to an administrator browser or one computer.
- Team metric convergence: read-only production verification covered all 153 deliveries whose view/exposure triplets had been updated by an active supplier child. The updating child, its supplier parent and creator/admin projection returned the same authoritative values for every row; missing and mismatch counts were zero.
- Browser and logs: the public application loaded the target cache in a real browser at 1280px and 1024px with no horizontal overflow or white screen. Guest-only 401 responses for protected profile bootstrap were expected; no application exception was observed. Post-switch main and sidecar logs contained no new traceback, 5xx or timeout entry.
- Rollback and retention: direct code/static rollback is the prior sibling release `20260805-v140-platform-stability-2-1b41200`. After verifying the new release and retained rollback points, old code/static releases were rotated so exactly the newest five remain. Database/media snapshots, deployment evidence and all business/runtime data are outside this rotation and were not removed.

## v120 - 2026-07-28

- Source: controlled commit `769bed1` on `codex/v120-unified-workspace-shell`. Production main cache is `20260728-v120-shell-21`; video-workshop unified-shell cache is `20260728-v120-shell-7`; infinite-canvas build is `3kJ2GVghGaFvcIZrHvqcj`.
- Scope: deployed exactly `97` audited runtime code/static files from a clean detached target worktree. Tests, documents, local state, SQLite, accounts, members, assets, deliveries, drafts, analytics, uploads, composed media, canvas projects/blobs, video-workshop projects/uploads/outputs/runtime, model cache, authentication and private environment files were excluded. No delete-style synchronization, migration, reconcile, backfill, restore or business write was used.
- Validation: all `97` production hashes match the release manifest. Main service and video-workshop sidecar are active and healthy; SQLite `quick_check` is `ok`; the private-environment fingerprint is unchanged. The clean target passed main `383/383`, video-workshop `93/93`, Node tools `48/48`, JavaScript syntax, Python compilation and diff checks.
- Provider smoke: the server-managed language model is configured and its minimum test passed; image generation, TTS, Seedance, digital-human and analytics providers report configured and reachable, and the TTS minimum test passed. Video-workshop director, video, voice, media tools, quality check, transcription and the five-track shared BGM library all report ready. No provider credential was changed and no media-generation task was created.
- Browser smoke: an authenticated production administrator session loaded the unified overview, batch production, video workshop, infinite canvas, voice generation and personal-profile workspaces. Existing batch history and the current 12-account digital-human board remained readable; a historical video project retained its conversation, 11-scene plan, published output and download link. The selected canvas project loaded `16/16` images, project navigation remained present, and the voice workspace exposed shared custom/favorite/system voices and `201` existing voice assets. Pages had no horizontal overflow and no application-level console error; no generation, publication, deletion, retry or profile save was triggered.
- Data protection: no protected collection, row table or runtime directory decreased. Post-deployment counts include accounts `80`, members `67`, assets `4118`, productions `790`, jobs `980`, batches `99`, canvas drafts `61` and canvas blobs `344`. Uploads, composed media, canvas blobs, video-workshop runtime and private configuration were preserved.
- Recovery check: the first verification script queried the sidecar's obsolete `/health` path and triggered its designed code-only rollback. The rollback was fully verified against the prior cache, all target-file states and protected data. The deployment was then reapplied using `/api/health`; no database snapshot was restored and no concurrent production data was lost.
- Rollback and retention: created and verified pure code/static rollback point `v120-pre-20260728-200718` plus an independent consistent SQLite snapshot. Before removing the oldest code/static rollback set, its database snapshot was archived separately and passed SQLite integrity and hash equality checks. Exactly the newest five code/static rollback sets now remain; data snapshots and business/runtime files are never part of this rotation.

## v119 - 2026-07-27

- Source: controlled commit `fefd1b7`. Production main cache is `20260727-v119-4`; video-workshop and infinite-canvas static closures were intentionally unchanged.
- Scope: deployed exactly 12 audited main-platform runtime code/static files, including the fixed client manifest and two versioned installers, from a clean detached target worktree. Tests, documents, local state, SQLite, accounts, members, assets, deliveries, drafts, analytics, uploads, composed media, canvas projects/blobs, video-workshop runtime, model cache, authentication and private environment files were excluded. No delete-style synchronization, migration, reconcile, backfill, restore or business write was used.
- Validation: all 12 production hashes match the release manifest. Main service and video-workshop sidecar are active and healthy; SQLite `quick_check` is `ok`; recent main-service error-level journal count is zero. The clean target passed main 369/369 and video-workshop 92/92 tests, plus JavaScript, Python, Shell, diff, whitelist-route and installer-hash checks.
- Client distribution: manifest and both installer HEAD responses return the approved content type, attachment filename and byte length. Re-hashing each complete production HTTP download stream matched the approved SHA-256. The authenticated browser showed the client entry above settings, a working hover bridge, both system guides and a four-step modal without internal overflow; no download was started during acceptance.
- Browser smoke: production loaded `v119-4` with no horizontal overflow or application console error/warning. The publication donut used the same 186 returned-link records as the top publication metric; keyboard activation opened the Xiaohongshu account publication detail. The current batch board displayed real same-origin image covers for completed video deliveries; visible decoded covers were valid, and no generation, publication, deletion, cancellation or retry was triggered.
- Data protection: no protected collection decreased and accounts remained at 80. During the deployment window normal production activity added 4 analytics links, 1 asset, 5 supplier activity rows and 1 composed file; video-workshop runtime and model-cache files also increased. These writes were preserved. Upload and canvas-blob metrics did not decrease, and the private-environment fingerprint remained byte-identical.
- Rollback and retention: created and verified pure code/static rollback point `v119-pre-20260727-153633` plus a separate consistent SQLite snapshot. Five pure code/static rollback sets now remain, exactly at the retention limit, so no set was removed. Business/runtime snapshots are never part of this rotation; rollback must restore code/static files only and must not replace newer production data.

## v118 - 2026-07-27

- Source: controlled commit `37c5194`. Production main cache is `20260727-v118-7`; infinite-canvas build is `1lusSDhjMfp40zIG_fwT9`; video-workshop static files were unchanged.
- Scope: deployed exactly 63 runtime code/static files from a clean detached target worktree. Tests, documents, local state, SQLite, accounts, members, assets, deliveries, playback values, drafts, analytics, uploads, composed media, canvas projects/blobs, video-workshop runtime, model cache, authentication and private environment files were excluded. No delete-style synchronization, reconcile, migration, backfill, restore or business write was used.
- Validation: all 63 target hashes match the release manifest. Main service and video-workshop sidecar are active and healthy; SQLite integrity is `ok`; recent error-level service journals are empty. The clean target passed main 364/364 and video-workshop 92/92 tests, changed JavaScript syntax checks, Python compilation and diff checks.
- Browser smoke: an authenticated production administrator session refreshed to `v118-7`. The dashboard used returned-link publication counts, grouped total playback across 51 accounts, aligned platform distribution with the metric grid and had no horizontal overflow. Interaction metrics stayed on each row's right side with the open action at the far right. A real 12-production digital-human batch rendered 12 stable cover URLs; two polling observations produced identical row/media signatures. Infinite canvas loaded the target build with recent projects and thumbnails. Console reported zero errors and zero warnings. No task was generated, deleted, cancelled, retried or migrated.
- Data protection: no protected collection decreased. The consistent backup baseline had 879 jobs and post-deployment had 887 because normal production tasks continued during the window; these writes were preserved. Uploads, composed media, canvas blobs, video-workshop runtime, model cache and private-environment fingerprints were unchanged.
- Rollback and retention: created and verified pure code/static rollback point `v118-pre-20260727-123403` plus a separate consistent SQLite snapshot. Four pure code/static rollback sets remain, below the five-version retention limit, so no set was removed. Business/runtime snapshots are not part of this rotation.

## v117.23 - 2026-07-24

- Source: controlled commit `57d1275`. Production main cache is `20260724-v117-23`; infinite-canvas build is `KUHu0JKubL-tLCqpqdIyX`; video-workshop static files were unchanged.
- Scope: deployed exactly 34 code/static files from a clean detached target worktree. The archive roots were limited to application code and static resources; SQLite, accounts, members, assets, deliveries, drafts, analytics, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication state and private environment files were excluded. No delete-style synchronization, migration, reconcile or business write was used.
- Validation: all 34 production hashes match the deployment manifest. Main service and video-workshop sidecar are active and healthy; SQLite integrity is `ok`. The main suite passed 354/354, video-workshop passed 92/92, and infinite-canvas sizing regression passed 41/41, including the prior 20 repeated single-reference runs and 50 complex multi-reference planner rounds.
- Browser smoke: a production 3496×1022 historical project opened read-only with its exact-size output node intact. The size planner reports `3504×1168 → 3496×1022` as full-frame adaptation, with no crop or blurred-background instruction. Browser console reported zero errors and zero warnings. No model generation or project mutation was triggered.
- Data protection: every tracked business collection count matched the pre-deployment baseline, including accounts 80, assets 3721, productions 700, jobs 776, members 65 and custom projects 72. Protected upload, composed-media and canvas-blob file/byte metrics were unchanged, and the private-environment fingerprint remained byte-identical.
- Rollback and retention: created and verified rollback point `v117.23-pre-20260724-173927`, including an exact code/static archive and a consistent SQLite checkpoint. After verification, the oldest pure code/static rollback set was removed so exactly the newest five deployment rollback sets remain. Database, uploads, composed media, canvas blobs, video-workshop runtime, authentication and private configuration were not rotated or deleted.

## v117.21 - 2026-07-24

- Source: controlled commit `f483f8e`. Production main cache was refreshed and verified as `20260724-v117-21`; video-workshop static files were unchanged.
- Scope: deployed exactly 34 changed runtime code/static files from a clean detached target worktree. Tests, documents, local state, SQLite, business collections, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication data and private environment files were all excluded. No delete-style synchronization was used.
- Validation: target file hashes match the deployment manifest. Main service and video-workshop sidecar are active and both correct `/api/health` checks pass. SQLite integrity is `ok`; every tracked business collection count and every protected-directory file/byte metric matched its pre-deployment baseline. The existing authenticated browser session loaded the target cache and image-workshop route with no application console error.
- Supplier link handling: the deployed manifest includes the controlled clear-link UI and atomic server/store handling. This acceptance was source/static and read-only only; no supplier account, link, delivery, analytics or other business record was changed.
- Rollback and retention: created a fresh code/static rollback point and separate consistent SQLite snapshot. The first restart-check cycle safely restored the same code backup because the checker used the sidecar's obsolete health path; the correct `/api/health` route was then verified and the final manifest applied successfully. No production data or private configuration was touched. One verified code/static rollback set is currently present, below the five-version retention limit, so no rotation was required.

## v117.12 - 2026-07-23

- Source: `942ebec` hotfix from the controlled deployment branch. Production main cache was refreshed and verified as `20260723-v117-12`; the video-workshop cache remains `20260722-25`.
- Scope: deployed exactly three code/static files from a clean detached target worktree: the main entry and two main-platform modules. Tests and project records were excluded, and no delete-style synchronization was used.
- Validation: both services are active and both health checks pass. Production file hashes match the three-file archive; SQLite integrity is `ok`. A real authenticated administrator page shows a nonzero total playback figure and its read-only detail view includes historical supplier-entered playback records, rather than filtering them out for lack of analytics links.
- Data protection and rollback: created a fresh code/static rollback point and separate consistent SQLite snapshot. Private-environment fingerprint is unchanged. During the deployment window, one asset, one custom project and one upload arrived through concurrent normal business activity; no collection decreased and no protected data was overwritten. The delivery-sequence migration was not executed. There are 3 code/static rollback sets, below the five-version retention limit, so no rotation was needed.

## v117.11 - 2026-07-23

- Source: `5111913` from the controlled deployment branch. Production main cache was refreshed and verified as `20260723-v117-11`; the video-workshop cache remains `20260722-25`.
- Scope: deployed an exact 29-file code/static archive from a clean detached target worktree. No delete-style synchronization was used, and all protected database, business, runtime, authentication and private-environment paths were excluded.
- Validation: main service and video-workshop sidecar are active; both health checks pass. SQLite integrity is `ok`; protected runtime metrics and the private-environment fingerprint are unchanged. The asset collection advanced by 2 concurrent business writes during the deployment window, with no collection decrease or local-state replacement. An authenticated administrator browser session loaded the target cache, homepage and playback detail drawer without an application failure.
- Migration and rollback: created a fresh code/static rollback point and separate consistent SQLite snapshot. There are 2 code/static rollback sets, below the five-version retention limit, so no rotation was needed. The one-time delivery-sequence reconciliation was not executed: the only endpoint is administrator-authenticated and there is no safe front-end controlled action available, so authentication was not bypassed. Rollback must restore only code/static files, never newer business data.

## v117.7 - 2026-07-23

- Source: `defe1c2` from the controlled deployment branch. Production main cache was refreshed and verified as `20260723-v117-7`.
- Scope: deployed from a clean detached target worktree. The exact app code/static manifest contained 69 changed or added paths only; no delete-style directory synchronization was used. A nullable member-avatar schema column is added idempotently by service startup when absent.
- Validation: main service and video-workshop sidecar are active; both health checks pass. A real production supplier-admin page loaded the new cache, showed the daily returned-links assistant and copy-all entry, and had no application console error or warning. No supplier account, link, project, canvas or asset write was performed during acceptance.
- Data protection and rollback: created a fresh code/static rollback point and separate consistent SQLite snapshot. During the restart window the live `docs` collection advanced by two normal concurrent business writes; SQLite remained healthy, so rollback was intentionally not used. The deployment manifest excluded all protected business/runtime/configuration paths. Current code/static rollback count is within the five-version retention limit; no rotation was needed.

## v117.5 - 2026-07-23

- Source: `4789521` (`test: isolate timed director pipeline fixtures`), cumulative v117 deployment target. Production main cache is `20260723-v117-3`; video-workshop cache is `20260722-25`; the infinite-canvas static closure matches the target build.
- Scope: deployed from a clean detached target worktree after full no-private-environment validation. Exactly 123 code/static files were synchronized, and only 7 superseded tracked hashed static files were removed by explicit path. No delete-style directory synchronization was used.
- Validation: the main suite passed 324 tests and the video-workshop suite passed 90 tests, with Python, JavaScript, Shell and diff checks also passing. Main service and video-workshop sidecar are healthy. Browser smoke in an existing administrator session loaded the homepage, video-workshop historical-project list and infinite-canvas project library without white screen, application error or warning; all 10 visible canvas thumbnails loaded.
- Data protection and rollback: the deployment created a new code/static rollback backup and a separate data snapshot. Database collection baselines, protected runtime directory fingerprints and private-environment fingerprint were identical before and after deployment; SQLite integrity is `ok`. Code/static rollback retention was evaluated after validating the new backup: 3 recent backups exist, so no rotation was needed. Business data and data snapshots were not rotated.

## v104 - 2026-07-20

- Source: `08051bf`, including functional commit `b56b018`. Production main cache is `20260720-v104-1`; video-workshop cache is `20260720-18`.
- Scope: deployed an exact 28-file code/static archive from a clean detached target worktree. No file was added or deleted, and no delete-style synchronization was used. Production SQLite, accounts, members, assets, deliveries, uploads, composed media, canvas blobs, infinite-canvas drafts, video-workshop runtime, model cache, authentication state and private environment files were excluded.
- Backup and rollback: retained rollback point `v104-20260720-192346` with a pre-deployment code archive, consistent SQLite backup, business-count baseline and protected-runtime fingerprints. Rollback restores code/static files only and must not replace newer production business data.
- Validation: main and video-workshop services are active, both health endpoints pass, recent error-level service logs are empty, all 28 target hashes match and both production cache markers match v104. SQLite integrity is `ok`; no existing business table decreased; protected runtime metrics and private-environment fingerprints are unchanged.
- Browser smoke: an existing authenticated production session loaded the main application, drafts, video workshop and infinite canvas with zero console errors or warnings. The existing workshop delivery kept the same video node and source with ready state 4 during observation. Infinite-canvas project hash routes were intact, all 5 visible canvas images loaded, and all 15 visible draft images loaded. No generation, publishing, deletion, conflict resolution or other business write was triggered.
- Compatibility: the v103.4 same-origin language-model proxy self-heal remains present, the video-workshop cover stays constrained to 3:4, and infinite-canvas historical routes and thumbnails remain available. Provider secrets and production configuration were not changed.

## v103.4 - 2026-07-20

- Source: `32c6d1c` (`fix: self-heal shared language model proxy`). Production main cache is `20260720-v103-4`.
- Incident: late-loaded custom-creation modules could retain a different cached language-model client and falsely report that no language-model key was configured, despite the server-managed model being available.
- Scope: deployed an exact 27-file code/static archive only. The update adds one same-origin health probe before a late client reports a missing key, and unifies the module cache identity across the main application and custom creation entries.
- Validation: main health reports language-model configuration available; video-workshop health reports director, video and voice services ready. The deployed entry, custom-publish import and runtime late-module proxy smoke all serve the v103.4 behavior.
- Data protection: a code rollback archive and consistent SQLite backup were created before restart. Database table-count digest is unchanged after deployment; no runtime data, uploads, composed media, canvas blobs, video-workshop runtime or private environment file was synchronized or modified.

## v103.3 - 2026-07-20

- Source: `f28e610` (`fix: compact oversized image references`), following the v103.2 same-origin API correction. Production main cache is `20260720-v103-3`.
- Scope: deployed exactly 16 committed code/static files from a commit archive. No delete-style synchronization was used; SQLite, business collections, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication state and private environment files were preserved.
- Backup and rollback: retained a pre-deployment code and runtime snapshot with a table-count baseline. Rollback restores code/static resources only and must never replace newer production business data.
- Validation: the main service is active, health check passes, the static entry serves v103.3 and SQLite integrity is `ok`. All database table counts matched the pre-deployment baseline.
- Image reliability: public browser calls remain same-origin instead of falling back to a member's loopback service. Oversized PNG/JPEG references are now compacted before an upstream request, with a shared encoded payload budget below the upstream limit. This host has no Pillow in the service environment, so the verified FFmpeg fallback is active. No provider key or private environment value was changed, and no live image-generation task was created for validation.

## v102 - 2026-07-20

- Source: `ad4eebe`, cumulatively including the v97-v102 homepage dashboard chain from the v96 production baseline. Production main cache is `20260720-v102-1`.
- Scope: deployed from a clean detached target worktree after local Python, JavaScript and diff validation. The final exact-file synchronization changed 7 code/static files without delete-style synchronization. SQLite, business collections, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication state and private environment files were excluded.
- Backup and rollback: retained rollback point `v102-pre-20260720-133138`, containing a pre-deployment code archive, consistent SQLite backup and protected runtime baseline. Rollback restores code/static files only and must never replace newer business data.
- Validation: main service and video-workshop sidecar are active, both health endpoints are healthy, production static resources serve the target cache marker, and SQLite integrity is `ok`. Protected collection counts were unchanged: accounts 80, analytics links 76, assets 2864, batches 75, custom outputs 6, custom projects 5, jobs 692, metric snapshots 108, productions 543, products 12 and voice presets 6. Sessions advanced from 62 to 63 during deployment through concurrent normal activity. Upload, composed-media and canvas-blob file counts and byte totals are unchanged.
- Browser smoke: an existing authenticated production session loaded the dashboard and target resources. The platform donut rendered separate Xiaohongshu and video-channel sectors; keyboard opening the Xiaohongshu sector showed only that platform's two current-day account deliveries. No generation, publish, deletion, synchronization or other business write was triggered. Console noise was limited to a browser-extension message-channel warning, with no application-script error observed.

## v96 - 2026-07-19

- Source: `9b9ecf6`, including functional commit `6e865b2`. Production keeps main cache `20260718-v94-1` and video-workshop cache `20260719-17`; infinite canvas now serves build `kUskuLna5GU9XqKHhMh6P`.
- Scope: deployed from a clean detached target worktree after local main, video-workshop and targeted regression validation. The final exact-file sync updated 49 code/static files without delete-style synchronization. SQLite, accounts, members, assets, deliveries, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication state and private environment files were excluded.
- Backup and rollback: retained rollback point `v96-pre-20260719-224753`, including pre-deployment code archive, consistent SQLite backup, protected runtime manifest and baseline. Rollback restores code/static files only and must never replace newer business data.
- Validation: both production services are active and both health endpoints are healthy. Target hashes match for the v96 video-workshop service files and the new infinite-canvas build assets. SQLite integrity is `ok`; pre/post business counts are identical: accounts 80, members 23, assets 2846, productions 541, jobs 692, batches 74, analytics links 75, metric snapshots 108, sessions 62, voice presets 5, canvas drafts 2 and canvas blob rows 6.
- Browser smoke: an existing administrator session showed the video-workshop text input and enabled creation entry without a busy lock. An existing infinite-canvas project reopened in embedded mode and visibly loaded three historical image nodes. No media-generation, publishing, conflict-resolution or other write action was triggered during the smoke test.

## v95 - 2026-07-19

- Source: `09be746` (`docs: record v95 release commit`), including functional commit `9503008`. Production reports main cache `20260718-v94-1`, video workshop cache `20260719-17`, and infinite-canvas build `EjIfhNcQipNHVEUS6sYeu`.
- Scope: deployed from a clean detached target worktree after local tests, sensitive-pattern scanning, isolated staging and an exact target-file hash manifest. The final sync updated 55 code/static files and removed 5 obsolete hashed static files. Production SQLite, accounts, members, assets, deliveries, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication data and private environment files were excluded.
- Backup and rollback: retained rollback point `v95-pre-20260719-193633`, containing a pre-deployment code archive, consistent SQLite backup, private-configuration checkpoint, service metadata, business-count baseline and a 2858-entry protected-file manifest. Rollback restores code only and must never replace newer business data.
- Validation: the clean target passed the main suite 267/267 and video workshop suite 40/40, plus tracked JavaScript syntax, Python compilation, shell syntax and diff checks. Both production services are active; both health endpoints return 200; recent service logs contain zero severe application entries; all 55 deployed target files match the release manifest and all 5 obsolete files are absent.
- Data protection check: SQLite integrity is `ok`; accounts 80, members 23, assets 2846, productions 541, jobs 692, batches 74, analytics links 75, metric snapshots 108, sessions 62, voice presets 5, supplier bindings 64, supplier activity 146, active canvas drafts 1 and canvas blob rows 6 match the deployment baseline. None of the 2858 protected files changed or disappeared, and both private environment files are byte-identical to the rollback checkpoint.
- Browser smoke: the production administrator session loaded the main application and video workshop without a white screen or visible load failure. An existing infinite-canvas project reopened through `/XZ-Design/?embed=1#/project/...` after leaving and after a full platform refresh; the visible historical image nodes loaded successfully and the browser showed no 404 page.
- Residual browser state: the inspected browser carries an unresolved local/server concurrent-edit conflict. The server project index returns a stable private thumbnail and retains the complete image draft, but the home-card thumbnail temporarily falls back to a placeholder after a full platform reload until that project is opened. No conflict-resolution button was pressed during smoke testing, because choosing either copy would alter user state; this is not production data loss.

## v94 - 2026-07-19

- Source: `57641f6` (`docs: record v94 release commit`), including functional commit `77ce1de`. Production reports main cache `20260718-v94-1`, video workshop cache `20260718-16`, and infinite-canvas build `Cb3dOInBfODPy4nRJsfpy`.
- Scope: deployed a clean detached target export after full staging validation and a non-delete dry-run. Only code and static resources were synchronized. The production SQLite database, accounts, members, assets, productions, deliveries, drafts, analytics, uploads, composed media, canvas blobs, video-workshop runtime, model cache, authentication state and private environment files were excluded from synchronization.
- Backup and rollback: retained rollback point `v94-pre-20260719-120145`, containing the pre-deployment code snapshot, a consistent SQLite backup, private-environment and service checkpoints, collection baselines and a 2828-entry protected-file manifest. Rollback must restore code/service files only and must not replace newer production business data.
- Validation: the clean target passed the main suite 259/259, video workshop 23/23 and infinite-canvas migration fixture 4/4, together with tracked JavaScript syntax, Python compilation, shell syntax and diff checks. Both services are active, health checks pass, all three release identifiers match, SQLite integrity is `ok`, and recent service logs contain no error-level application entries.
- Schema: the v94 `custom_canvas_drafts` and `custom_canvas_blobs` tables were created through the release's idempotent initializer. They begin empty by design and are populated owner by owner when an affected member revisits the canvas with retained legacy browser data; no existing canvas summary or business collection was fabricated, migrated in bulk or deleted.
- Provider smoke: the production LLM configuration completed a real minimum request. The video-workshop director completed a real request and returned six scenes. Image, Seedance, digital-human and TTS configurations report configured and reachable; the sidecar reports its director, video and voice providers configured. No private provider value was printed, copied or replaced.
- Data protection check: all deployment baselines remained unchanged: accounts 80, members 23, assets 2844, productions 541, jobs 692, batches 74, analytics links 75, metric snapshots 108, sessions 63, voice presets 5, supplier bindings 64 and supplier activity 146. None of the 2828 protected files is missing, smaller or changed in size; private environment files are byte-identical to the rollback checkpoint.
- Browser smoke: the real public login page rendered in Chrome without a white screen. The controlled Chrome profile did not have a production login session, and credentials were deliberately not placed in automation logs, so recovery of a specific historical browser's images was not claimed as observed. The actual legacy-image recovery remains a first-visit check in the affected user's original browser; the server schema and 4/4 migration/revision/tombstone fixture are ready for that handoff.

## v93 - 2026-07-18

- Source: `ebc09c8` (`docs: record v93 release commit`), cumulatively including functional commit `5541b8d`. Production reports main cache `20260718-v93-2`, video workshop cache `20260718-16`, and infinite-canvas build `Or3fOhHL8VWpchYKGoLnX`.
- Scope: deployed code and static resources from a clean detached target worktree with explicit runtime exclusions and no delete-style synchronization. Production database, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, sub-application projects, model cache, private environment configuration, authentication cache and logs were preserved.
- Backup and rollback: retained rollback point `v93-pre-20260718-191924`, containing the pre-deployment code snapshot, a consistent SQLite backup and protected runtime checkpoints. Rollback must restore code/service files only and must not replace newer business data.
- Validation: the clean package and isolated server staging passed the main application suite 217/217 and video workshop suite 21/21, plus JavaScript syntax, Python compilation, shell syntax and diff checks. Both services are active; health checks return 200; all three static release identifiers match; SQLite integrity is `ok`; and recent main/sidecar logs contain no severe application errors.
- Data protection check: deployment pre/post baselines were identical: accounts 80, members 23, assets 2839, productions 539, jobs 689, batches 74, analytics links 66, metric snapshots 108, sessions 63, voice presets 5, supplier bindings 64 and supplier activity 126. Upload, composed-media, model-cache and video-workshop runtime file counts and byte totals did not decrease, and the private environment fingerprint did not change.
- Browser and provider smoke: administrator login, overview, custom creation, video workshop, infinite canvas, voice and delivery views rendered without console errors. The video workshop loaded its existing project and playable final media; existing private LLM, video, voice and image configurations remained in use and were not replaced.
- Post-deployment diagnosis: the user confirmed video-workshop final media is available. A real infinite-canvas browser retained project summaries but opened an affected historical project with an empty canvas. Code review traced the regression to the v93 localStorage-to-IndexedDB detail migration, which lacks per-project read-back verification and a durable legacy backup. The canvas server currently stores only published summaries, so this is a browser-detail persistence regression rather than production database deletion.
- Performance diagnosis: the host is lightly loaded, health latency is low, media files exist and Range requests return 206. The remaining slowdown is front-end bootstrap work: remote login blocks on an approximately 7 MB full `/api/state` snapshot and sequential IndexedDB replacement, while the draft view eagerly creates all visible cover requests. The code task has been asked to add recoverable migration, lightweight/incremental bootstrap, background cache writes, lazy/virtualized draft rendering and stalled-media recovery before the next deployment.

## v92.2 - 2026-07-18

- Source: `09c01fc` (`docs: record v92.2 release and deployment safeguards`), cumulatively including functional commit `b03439a`. Production reports main cache `20260718-v92-3`, video workshop cache `20260717-15`, and the expected infinite-canvas build.
- Scope: deployed exactly the 41 changed application code/static files from a clean target worktree after a file-only dry-run. No delete-style synchronization was used. Production database/runtime state, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, sub-application projects, model cache, private environment configuration, authentication cache and logs were excluded.
- Backup and rollback: retained rollback point `v922-pre-20260718-134500`, containing the pre-deployment code snapshot, a consistent SQLite backup, protected-directory manifests, service definitions, private-configuration checkpoint and collection baseline. Rollback must restore code/service files only and must not replace newer business data.
- Validation: the clean worktree and isolated server staging passed the main application suite 216/216 and video workshop suite 20/20, together with tracked JavaScript syntax, Python compilation, shell syntax and diff checks. Both services are active, both health endpoints return 200, the deployed marker matches the target commit, the post-sync dry-run reports zero pending code files, and recent service logs contain no error-level entries.
- Browser smoke: the administrator session loaded the main application, video workshop, infinite canvas, batch workspace and delivery list without white screens or console errors. Video publishing exposes multi-reference cover selection and final-cover zoom. A disposable empty canvas project was removed after the read-only smoke. All 7 inspected server-managed batch images carried stable `asset_rev` values, and the delivery list displayed global publication sequences.
- Cache policy: main/video-workshop HTML and unversioned assets return `no-store`; resources carrying the release version parameter and hashed infinite-canvas assets return one-year `immutable`. The platform proxy, rather than the loopback-only sidecar port, is the authoritative video-workshop cache boundary.
- Supplier projection: read-only production checks covered 267 child-visible deliveries across 3 assigned child accounts. Creator, supplier-parent and supplier-child sequence mismatches were 0; current-link mismatches were 0; unassigned deliveries exposed to children were 0. Forty-five returned-link records remained consistent after refresh projection. No production delivery was modified for this check.
- Data protection check: pre/post baselines were identical: accounts 80, members 23, assets 2837, productions 539, jobs 689, batches 74, analytics links 66, metric snapshots 108, sessions 63, voice presets 5, supplier bindings 64, supplier activity 122, uploads 2575 files, composed media 174 files, model cache 17 files and video-workshop runtime 47 files. SQLite integrity remained `ok`, protected-directory byte totals were unchanged, and the private environment fingerprint remained unchanged.

## v92 - 2026-07-18

- Source: `d594ba7` (`docs: record v92 release and deployment safeguards`), cumulatively including functional commit `8a33494`. The online main entry reports `20260717-v92-1`; the video workshop reports `20260717-15`; infinite canvas reports build `s4XuKSY12Bmly5rxjq45o`.
- Scope: deployed a clean Git archive containing code and static resources only. The production database, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, sub-application projects, model cache, private environment configuration, authentication cache and logs were excluded. The pre-deployment rollback point is `v92-pre-20260718-002112`.
- Validation: the main application passed 201/201 tests and video workshop passed 16/16 in both the clean package and isolated server staging checks. Tracked JavaScript syntax, Python compilation, shell syntax and diff checks passed. Both production services are active, the main health endpoint returns 200, and the final deployed marker matches the target commit.
- Video workshop regression: production was verified over ordinary HTTP with `crypto.randomUUID` unavailable. Image drop and real text submission both responded normally; a one-time simulated chat failure displayed an error and released the busy state. Image, audio and video attachments all entered their expected preview slots, and unsent attachments were cleared without creating a project.
- Infinite canvas regression: a real reference image produced exactly two independent image results for a two-style request. The successful response confirmed that the reference reached the image provider rather than being silently discarded.
- Provider smoke: the existing private MiniMax-M3 language model, MiniMax TTS, image provider, Seedance video provider, OmniHuman and JustOneAPI configurations all reported configured and reachable. Real minimum LLM and TTS tests passed; the sidecar reported director, Seedance, TTS, media tools, quality checks, transcription and BGM ready. No private configuration was replaced.
- UI smoke: administrator login, overview, single creation, batch production, delivery list and voice generation rendered without white screens or console errors. Delivery download/publication filters were visible and the layout had no horizontal overflow. Supplier role isolation remains covered by the 201-test server suite; no production supplier credential was used or altered during this deployment.
- Data protection check: accounts 80, members 23, analytics links 66, batches 74, jobs 692, metric snapshots 108, productions 540, sessions 63, voice presets 5, supplier bindings 64, supplier activity 121, uploads 2573 files and deleted-doc count 151 were unchanged; SQLite integrity remained `ok`. One asset and two composed files appeared during validation from concurrent production activity and contained no deployment-test marker. All disposable workshop/canvas test state, runtime media and the single test tombstone were removed precisely.

## v91.1 - 2026-07-17

- Source: `0312f48` (`docs: record v91.1 release commit`), cumulatively including v89-v91.1 and functional commit `3aafbc9`. The online main entry reports `20260717-v91-2`; the video workshop entry reports `20260717-13`.
- Scope: deployed 260 tracked code/static files from a clean target export and removed exactly 4 obsolete tracked source modules. No delete-style directory synchronization was used. Production database/runtime state, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, sub-application projects, private environment configuration, authentication cache and logs were excluded.
- Backup and rollback: retained rollback point `v911-pre-20260717-022439`, containing the pre-deployment code snapshot, a consistent SQLite backup, upload/composed checkpoints, private configuration backup and collection baseline. Rollback restores code/service configuration only and must not replace current production business data.
- Result: clean export validation and the final production-code rerun both passed 183/183 tests, together with all tracked JavaScript syntax checks, Python compilation, shell syntax and diff checks. The first production rerun exposed only a repository-layout assumption in 5 Node-backed tests; a temporary parent-level application mapping reproduced the checkout layout, all 183 tests passed, and the mapping was removed immediately. The main service and loopback-only video workshop sidecar are active; both health checks pass, the deployed marker matches the target commit, and recent service logs contain no traceback or application error.
- Provider smoke: main MiniMax-M3 and MiniMax TTS real minimum calls passed. Image, Seedance, OmniHuman and JustOneAPI reported configured and reachable. The sidecar reused the same private LLM, Seedance and TTS settings without copying or changing any key.
- Reference-media smoke: infinite canvas generated a real image with one submitted reference and returned `usedRefs=1 / skippedRefs=0`. Video workshop accepted one image, one narration audio and one video; it transcribed the uploaded narration, assigned the image as Seedance reference, assigned the video as editing material, generated one Seedance scene, inserted the video material into the timeline, and produced a valid H.264 + AAC final. The main proxy served the final MP4 with `206 Partial Content`; an external browser also fetched the reference media and final output successfully.
- Server dependency: the host could not reach the default speech-recognition model source. The sidecar now uses a reachable mirror with the incompatible Xet path disabled, and the `small` transcription model is prewarmed in the persistent model cache. A real uploaded narration was transcribed successfully after this change.
- Role smoke: administrator, editor, supplier-parent and supplier-child tokens all loaded their authorized state projections. Administrator/editor access to custom creation succeeded; supplier roles were correctly denied access to private creation sub-applications. Infinite canvas and video workshop static entries loaded successfully.
- Data protection check: accounts 80, members 12, analytics links 45, jobs 682, batches 65, metric snapshots 108, sessions 57, voice presets 5, supplier bindings 52 and composed media 153 were preserved. During validation, active users added 7 assets and 6 uploads and deleted 2 productions; these valid concurrent writes were retained. The private environment fingerprint remained unchanged. Disposable reference tests were removed from production project/runtime collections after evidence was copied to the staging record.

## v88 - 2026-07-16

- Source: `f44d770` (`v88 align batch references and copy regeneration`), cumulatively including v86-v88. The online entry reports `20260716-v88-1`.
- Scope: deployed exactly 34 modified tracked application code and static files from a clean worktree after checksum validation, complete staging tests and a file-only dry-run. No file deletion or directory metadata synchronization was used. Production database/runtime state, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, environment configuration, authentication cache and logs were excluded.
- Backup and rollback: retained rollback point `v88-pre-20260716-164948`, containing the pre-deployment code snapshot, a consistent SQLite backup, upload/composed checkpoints, private environment backup and collection baseline. Rollback restores code/static resources only and must not replace current production business data.
- Result: clean local, server staging and production environments all passed the complete test suite 43/43, all JavaScript syntax checks and Python compilation passed, the private environment fingerprint stayed unchanged, service restart and health checks passed, and recent service logs contained no error or traceback.
- v87 browser smoke: administrator login loaded 80 accounts with no console error or warning. The BGM library showed two legacy account-bound assets as a flat shared library without an account filter; the editing-material library also used the shared view. No production asset was created, renamed, downloaded or deleted during validation.
- v88 browser smoke: the batch workspace loaded the production task board, 50 image accounts, unified reference and per-account custom reference areas. Existing sessions and drafts remained readable. Explicit-reference/type-switch/title-regeneration behavior is covered by the production 43/43 regression suite; no live generation or task mutation was used for smoke testing.
- Provider smoke: LLM, image, TTS, Seedance and digital-human configuration endpoints all reported configured and reachable. No private provider value was changed.
- Data protection check: no protected collection or file count decreased. Accounts 80, members 12, analytics links 45, productions 465, jobs 679, batches 64, metric snapshots 108, sessions 56, voice presets 5, supplier bindings 52 and composed media 147 were preserved. Active users added one asset and one upload while validation was running; these valid production writes were retained.

## v85 - 2026-07-16

- Source: `5f7a9ec` (`v85 stabilize audio mix and subtitle recognition`). The online main JavaScript and stylesheet entries report `20260716-v85-1`.
- Scope: deployed exactly 9 changed application code and static files from a clean commit archive after a dry-run. No delete-style synchronization was used. The production database, runtime state, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, environment configuration, authentication cache and logs were excluded.
- Backup and rollback: retained rollback point `v85-pre-20260716-120840`, containing the pre-deployment code snapshot, a consistent SQLite backup and upload/composed checkpoints. Rollback must restore code/static resources only and must not replace current production business data.
- Result: the production virtual environment passed the complete test suite 35/35, all JavaScript syntax checks and Python compilation passed, the private environment fingerprint stayed unchanged, service restart and health checks passed, and post-restart service logs contained no error or traceback.
- Audio/subtitle validation: a disposable real FFmpeg test preserved the digital-human clip voice, mixed a looping BGM, skipped duplicate external narration and produced a valid AAC audio stream. Information-flow subtitle quality guards, hallucination cleanup, recognition deduplication and manual-track protection passed the production test suite.
- Browser smoke: administrator login loaded 80 accounts with no console error or warning. Account performance opened a per-content detail with title, platform, playback, likes, favorites, comments, shares and the publication link. The data assistant returned a plain-text answer without Markdown formatting.
- Provider smoke: LLM, image, video/digital-human and TTS configuration endpoints all reported configured and reachable. No private provider value was changed.
- Data protection check: no protected collection or file count decreased. Accounts 80, members 12, assets 2524, productions 452, jobs 676, batches 62, metric snapshots 108, sessions 55, voice presets 4, supplier bindings 52, uploads 2313 and composed media 142 were preserved. While validation was running, active users added 2 analytics links and 4 supplier download activities; these valid concurrent production writes were retained instead of being overwritten from the backup.

## v84 - 2026-07-16

- Source: `7756a08` (`v84 unify dashboard and editing workflows`). The online entry reports `20260715-v84-5`.
- Scope: deployed exactly 29 changed application code and static files from a clean commit after a dry-run. No delete-style synchronization was used. The production database, runtime state, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, environment configuration, authentication cache and logs were excluded.
- Backup and rollback: retained rollback point `v84-pre-20260716-011950`, containing the pre-deployment code snapshot, a consistent SQLite backup and runtime media checkpoints. Rollback must restore code/static resources only and must not replace current production business data.
- Result: the production virtual environment passed the complete test suite 31/31, all JavaScript syntax checks and Python compilation passed, the private environment fingerprint stayed unchanged, service restart succeeded, and the health endpoint remained available.
- Server dependency: the host initially had no CJK font file. Standard Noto CJK fonts were installed and the application font selector then returned a valid family and directory; a real one-second Chinese SRT burn-in with FFmpeg produced a valid video.
- Provider config smoke: LLM, image, video, digital-human and TTS configuration endpoints all reported configured and reachable. No private provider value was changed.
- Data protection check: pre/post counts were identical: accounts 80, members 12, assets 2523, productions 453, jobs 676, batches 62, analytics links 23, metric snapshots 108, sessions 55, voice presets 4, supplier bindings 52, supplier activity 28, uploads 2312 and composed media 140.
- Browser smoke: administrator login loaded the composite dashboard with 80 accounts and no console errors. The merged assets/drafts route opened on the draft panel and showed 53 existing drafts; no archive, delete or generation action was performed.
- Validation note: a first temporary test directory contained only the 29-file delta, so unchanged imports were absent and the test process stopped before production synchronization. Rebuilding the temporary directory from the complete current application plus the delta passed 31/31; production was not changed by the incomplete-stage attempt.

## v83 - 2026-07-15

- Source: `9c9f594` (`v83 stabilize creative and editing workflows`). The online entry reports `20260715-v83-2`.
- Scope: deployed exactly 28 changed application code and static files from a clean commit archive after an rsync dry-run. No delete-style synchronization was used. The production database, runtime JSON/state, uploads, composed media, accounts, members, assets, deliveries, drafts, analytics, environment configuration, authentication cache and logs were excluded.
- Backup and rollback: retained rollback point `v83-pre-20260715-174905`, containing the pre-deployment code/config snapshot, a consistent SQLite backup and runtime media checkpoints. Rollback must restore code/static resources only and must not replace current production business data.
- Result: the server virtual environment passed the complete test suite 23/23, all deployed JavaScript syntax checks and Python compilation passed, the environment fingerprint stayed unchanged, service restart succeeded, and the health endpoint remained available.
- Data protection check: pre/post counts were identical: accounts 80, members 12, assets 2488, productions 452, jobs 676, analytics links 23, metric snapshots 108, sessions 55, batches 61, voice presets 4, supplier bindings 52, supplier activity 28, uploads 2296 and composed media 138.
- Browser smoke: administrator login loaded the production state and reported 80 accounts with no console errors. The creator settings member list omitted supplier-child accounts, the restored supplier parent remained present, and analytics displayed separate `account / published title` columns with the account filter available.
- Incident log: the first post-sync test command used the system Python and stopped before service restart because project dependencies were unavailable there. The validation was rerun with the project virtual environment and passed 23/23; no service outage or business-data write occurred, so no additional product incident was added to `Problem Document.md`.

## v82.3 - 2026-07-15

- Source: `1a82804` (`v82.3 fix shared remote state and link replacement`). The online entry reports `20260715-v82-5`.
- Scope: deployed tracked code and static resources from a clean target archive with explicit runtime exclusions and without delete-style synchronization. The production database, JSON/runtime state, uploads, composed media, accounts, members, assets, deliveries, remarks, drafts, analytics, environment configuration and authentication cache were preserved.
- Backup and rollback: retained rollback point `v82.3-pre-20260715-133201`, containing the pre-deployment code/config snapshot, a consistent SQLite backup and runtime media checkpoints. Rollback must restore code/static resources only and must not replace current production business data.
- Result: the complete server test suite passed 16/16, tracked JavaScript syntax checks and Python compilation passed, service restart succeeded, and the health endpoint remained available.
- Data protection check: pre/post counts were identical: accounts 80, members 10, assets 2377, productions 426, jobs 670, analytics links 22, metric snapshots 108, sessions 52, uploads 2188 and composed media 109.
- Role smoke: supplier-parent and assigned supplier-child logins both issued `GET /api/state`; the parent saw both isolated deliveries while the child saw only the assigned delivery. Administrator link forgery and child updates to an unassigned delivery were denied. Supplier-only download-state semantics remained intact.
- Link replacement smoke: the modify dialog selected the full current URL, multi-URL input resolved to the last newly pasted URL, and the new URL became the single current link in the supplier row, creator snapshot and analytics link. The page updated locally without horizontal overflow, console errors or warnings.
- Incident log: the ESM singleton and prefilled-link risks were already recorded in `Problem Document.md`; no additional production incident was introduced by this deployment.

## v77 - 2026-07-14

- Source: `90295f1` (`v77 polish voice preview actions and transitions`), including the cumulative v73-v77 code and static assets. The online entry reports `20260714-v77-1`.
- Scope: deployed tracked code and static resources from a clean worktree. The production environment files, database/JSON state, uploads, composed media, accounts, members, assets, delivery records, drafts, analytics, voice data, authentication cache and logs were excluded from the sync.
- Backup and rollback: retained rollback point `pre-v77-20260714-110406`, containing the pre-deployment code snapshot, a consistent SQLite backup and runtime file checkpoints. Rollback restores code/static resources only and must not replace current production business data.
- Result: service restart and health check passed. Python compilation and tracked JavaScript syntax checks passed before restart; the deployed private environment fingerprint remained unchanged.
- Data protection check: accounts 80, members 8, analytics links 21, metric snapshots 108, sessions 51 and voice presets 4 were unchanged. No key collection decreased. Assets, jobs, productions and uploads increased slightly while members were actively using the platform during validation; these were live production writes, not local-state replacement.
- Model smoke: LLM and MiniMax TTS minimum calls passed. Image generation passed both routes with real outputs: no reference image used text-to-image and a valid reference image used image-to-image. Video, digital-human and analytics providers reported configured and reachable; no private provider configuration was changed.
- Role smoke: administrator completed a real browser smoke test. Supplier-parent visibility was validated against a read-only production database copy; because production currently has no supplier-child member, the child binding and visibility path was exercised only in that disposable copy. Both supplier projections reported zero private-asset leakage and created no production test accounts or state changes.
- UI smoke: cold start, login, homepage, batch board and analytics loaded without white screen. Voice synthesis produced a playable preview at default speed `1.2`; download/delete were present, the two archive actions aligned on one row, and synthesis/design/management panel switching produced no console errors.
- Incident log: no new production incident was found, so `Problem Document.md` was not changed.

## v72 - 2026-07-12

- Source: `1eb8f3f` (`v72 finalize monochrome UI and task navigation`).
- Scope: deployed code and static resources only. Runtime data, uploads, composed media, environment configuration, authentication cache and logs were excluded from the sync.
- Backup and rollback: a pre-deployment runtime backup and the prior stable release are retained. Rollback restores code and static resources only; it must not restore or replace production business data.
- Result: health check passed and the online static entry reports `20260712-v72`.
- Data protection check: production accounts remain at 80. Existing assets, productions, delivery records, drafts and analytics remain available after deployment.
- Role smoke: administrator, supplier parent and supplier child login/authorization paths passed. Temporary supplier test accounts were removed after verification.
- UI smoke: homepage task details route into the relevant workspace; batch creation exposes per-account reference image controls; supplier navigation and delivery view load; voice lab exposes MiniMax voices, preview/favorite controls and default speed `1.2`.

## Operating Rules

- Deploy only tracked code and static assets from the approved release commit.
- Never sync local runtime state over production data. Preserve the server database, JSON state, uploads, composed media, environment files, credentials, authentication cache and logs.
- Record a deployment result in both this file and the corresponding version section. Log only real server incidents in `Problem Document.md`.
- Keep this log redacted: do not add server addresses, passwords, API keys, tokens or account credentials.
