> ⚠️ **本文档对应 v4 旧版**（已整体备份至 `_backup_v4/`）。当前版本为 **v5 全量重构**，请阅读 [启动与架构说明-v5.md](启动与架构说明-v5.md)。启动方式：双击 `start.command` 或 `node tools/serve.mjs 4188`（v5 用 ES Modules，不能 file:// 直接打开）。

# Dumate Content Factory 本地预览入口（v3）

## 启动方式

在当前文件夹执行：

```bash
python3 -m http.server 4188
```

然后浏览器打开 `http://localhost:4188`，或直接双击同目录下的 `index.html`。

## v3 这一版做了什么

整套界面与交互重做。视觉上「浅色为主 + 视频生成台深色」，全流程 6 个页面打通：
**账号首页 → 创建账号（含图片资产）→ 创作空间 → 脚本生成 → 提示词生成 → 即梦式视频生成台 → 粗剪导出 / 资产 / 设置。**

核心链路严格按你描述的流程实现：

1. **账号分组 + 定位**：左栏按图文组 / 视频组 / 混合组分类；每个账号带定位、平台、模式、语气。
2. **创建账号即绑定图片资产**：创建对话框里有「账号图片资产」区，上传的图后续在生成台可直接 @ 调用。
3. **脚本生成**：AI 依「账号定位」生成口播稿，并自动按 15s 拆段（右栏显示分段结构）。
4. **提示词生成**（独立一步）：每段拆成视频提示词；超过 15s 自动拆两段，套用数字人结构——
   **前 15s 数字人出镜 + 产品演示，后 15s 产品演示延续 + 数字人出镜**；含 UI 镜头的段会提示绑定截图参考。
5. **即梦式视频生成台**（深色）：左侧片段/历史，中间生成结果对话流，底部悬浮输入框；
   支持上传图片做全能参考、拖拽 / 粘贴图片、@ 资产库、UI 锁定、比例/时长切换，生成版本可「加入粗剪」。
6. **粗剪导出**：时间线拖拽排序，一键生成规范导出命名。

脚本内容围绕「百度搭子 Dumate」功能特点做延展教学。

## 新增（v3.1）

**1. 即梦式内联 @ 资产**
视频生成台的输入框已改成富文本：@ 资产或上传/拖拽/粘贴图片时，会以「小缩略图 + 名字」的芯片形式直接出现在文字中间（点 × 可删），和即梦工作台一致。发送给模型时芯片自动序列化成 `@资产名`。

**2. 已接入语言模型（DeepSeek）生成提示词**
脚本生成、提示词生成已接 DeepSeek（OpenAI 兼容协议），默认 key 已内置。
提示词严格按你给的「前15秒 / 后15秒」两段式框架输出（数字人开场 + 产品界面引入 + 任务拆解 / 执行结果 + 数字人收束 + 负面提示词），每 30 秒一个场景。

- 模型配置在 `app.js` 顶部 `CONFIG`（endpoint / model / apiKey），也可在浏览器控制台改：`DumateConfig.model = "deepseek-reasoner"`。
- 在「设置 · API 配置」里把「服务类型」选**脚本生成 API**保存新 Key，会实时覆盖 `CONFIG.apiKey`；Provider 填成 `http://localhost:8787/chat` 可切到本地代理。
- 调用失败（网络 / CORS / Key 错误）会自动回退本地模板，并在提示里说明原因，页面不会卡住。

**如果浏览器报 CORS 错误**（直连被拦），在本文件夹另开一个终端运行：

```bash
python3 proxy.py
```

然后在浏览器控制台执行 `DumateConfig.endpoint = "http://localhost:8787/chat"`，再重新生成即可。代理只是把请求转发到 DeepSeek 并补上跨域头。

> 注意：当前 key 写在前端代码里，仅适合内部原型；对外发布前请改为后端调用，避免 key 暴露。

## 新增（v3.2）

- **脚本改成结构化分镜表**：脚本不再是大段文字，而是一张可编辑表格。视频模式列为「时间 / 核心思想 / 画面分镜 / 口播」；图文模式自动去掉口播，列为「核心思想 / 画面分镜 / 图上文案」。单元格点开即改，可加/删镜头。
- **脚本优化按钮**：表格下方填"优化方向"（如"开头更有钩子/减少广告腔"），点「优化脚本」让模型按方向重写整张表。
- **下一步据脚本生成**：视频提示词、图片提示词都改成读取这张分镜表的内容来生成。
- **修复**：视频生成台切换「前15s / 后15s」时，输入框现在会正确载入各自的提示词（之前两段显示同一段）；切回会保留你的编辑。

## 一期（v4.0，分期交付中）

- **新 logo**：横向胶片框 + 播放键 + 闪光，蓝→紫渐变（矢量复刻你给的图）。
- **返回上一步**：顶栏左侧返回按钮，按导航历史逐步回退。
- **脚本简化**：去掉目标时长 / 内容模式选项卡，只留「创作主题」+「方向」按钮（宝妈 / 职场 / 创作者 / 教培，不选 = 随机）。图文模式额外多一个「总风格描述」。
- **更具体的提示词**：视频提示词强制依据分镜表逐镜头展开，画面描写要求写到景别 / 动作 / 界面文字 / 运镜 / 动效，不再笼统。

### 二期（v4.1）已完成：分镜图工作台

视频流程在脚本和提示词之间新增「分镜图」步骤：
- 工作流：脚本生成 → **分镜图** → 提示词生成 → 视频生成 → 粗剪导出。
- 顶部可设「统一参考图」（从资产选或上传，如 logo / 角色版），所有分镜图共用。
- 「按脚本生成图片提示词」：脚本每个镜头对应一张分镜图的画面提示词，卡片可编辑。
- 每张卡片可「生成此图 / 上传 / 重生成」；「全部生成并入库」会把分镜图加入资产库（带「分镜图」标签），视频生成台即可 @ 调用。
- 可「跳过此步」直接进提示词。
- 小改：视频/图文脚本页右侧栏都已去掉；视频提示词不再含 emoji（指令 + 输出兜底过滤）。

### v4.2 增量

- 创建账号：内容形式改为「图文 / 数字人 / 混剪」，新增「绑定账号名称」，弹窗内实时显示素材命名规则。
- 素材自动命名：导出时按 `平台码-账号名-内容形式-序号`（如 `XHS-职场效率酱-数字人-001`）自动生成，并打标签（成片/内容形式/带字幕）入素材库，序号递增。
- 分镜图改横向卡片：左=图片提示词（可编辑）、中=分镜图、右=操作；统一参考图支持拖拽，且会真正传入生成（_mapRefs 映射）。
- 产品名净化：除 Dumate / 百度搭子外，"百度App""微信""抖音"等统一改写为「产品 logo / 产品界面」（指令 + 输出兜底）。
- 粗剪加字幕：可「生成字幕 SRT」（按脚本口播自动生成、可下载 .srt）+「烧录字幕」开关。
- 视频生成台新增「进入云剪辑」按钮，直达粗剪。

### refs → 模型参数映射（已写好）

`DumateAPI._mapRefs(refs)` 把 @ / 拖入的图按角色拆成 `{ all, character, firstFrame }`；
`generateVideo` / `generateImage` 里已写好真实调用模板：填好 `CONFIG.video` / `CONFIG.image` 的 endpoint+apiKey+model 后，会把 all→`reference_images`、firstFrame→`first_frame_image`、character→`character_image` 发给模型（字段名按你的视频/图片 API 文档微调即可）。未配置时走占位。

### v4.3 精简 + 三期供应商端（已完成）

- 创建账号：删掉「绑定账号名称」「内容语气」；内容形式只分「视频 / 图文」；账号名即用于命名。
- 清理无用按钮：去掉「进入账号」「导出配置」；左栏账号去掉小图标、改小圆点更紧凑。
- 首页极简：去掉最近创作/统计/大卡片框；改为标题 + 两大入口（视频创作 / 图文创作）+ 简洁账号列表，点入口筛选、点账号进入。
- 创作空间：右侧只保留「账号资产库」（可拖拽/上传/@ 调用）；主区是显眼的「创作链路」大卡片（带图标、描述、动效），其他链路一眼可见；底部「继续任务」。
- 供应商端：左栏「供应商端」入口切换；表格式成片库（素材名/账号/平台/形式/标签/状态/操作），支持全部/视频/图文/标签筛选、勾选批量下载，下载后状态变「已下载」。
- 成片命名联动：导出按 `平台码-账号名-内容形式-序号`（如 `SPH-AI办公君-视频-001`）自动入库 + 打标签，供应商端即可见。

### 已知后续

- 真实视频/图片 API（映射模板已就位，填 CONFIG.video/CONFIG.image 即可）。

### 三期（已完成，见上）

- 账号层级树（图文/视频 → 细分风格/数字人 → 绑定小红书账号名）+ 供应商端（同一应用切换，表格式素材库、打标签、按标签自动命名、批量下载、已下载状态）。

### 待接真实 API

- 视频：即梦/Seedance/千帆。@ 的参考图已按角色收集进 refs，接入时把 refs 映射到模型的参考图字段并上传即可。
- 图片/分镜图：图片模型返回 imageUrl 后，卡片与资产会自动显示真实图（现为渐变占位）。

## 真实 API 接入点

所有模型调用收敛在 `app.js` 末尾的 `window.DumateAPI`：

```text
DumateAPI.generateScript()   // 脚本生成（语言模型）
DumateAPI.generatePrompts()  // 提示词拆分
DumateAPI.generateVideo()    // 视频生成（即梦 / Seedance / 千帆）
DumateAPI.generateImage()    // 图片 / 角色版生成
```

当前返回示例数据。接入时在「设置 · API 配置」保存对应 Key，再把每个方法体替换成真实 `fetch`，**保持返回结构不变即可，UI 层无需改动**。

## 备注

- 旧版（v2.1）文件已备份在 `_backup_v2.1/`。
- 当前为前端静态原型，数据在内存中，刷新会重置。需要持久化时可接本地服务 / Electron / Tauri。
