# Problem Document

更新时间：2026-07-24

本文档是星阵项目的长期避坑日志。遇到明确报错、白屏、交互错位、数据覆盖风险、权限串数据、服务器与本地差异或部署失败时必须更新；普通功能流水账写入 `version.md`。

## 2026-07-24 v117.22 修复：尺寸标签不能进入图片语义，超宽图不能用模糊背景补边

- 现象：无限画布选择 3496×1022 等超宽尺寸后，只要求修改副标题的参考图编辑却生成了 16:9 中心画面，底部和两侧出现明显模糊扩边；用户未要求变化的构图被尺寸系统干扰。
- 原因：新尺寸适配同时做了三件不该耦合的事：把“最终交付像素”追加到提示词、把超宽目标映射为最接近的固定 16:9 比例、再用高斯模糊背景填满目标画布。UI 的尺寸标识因此从输出契约越权成了创作指令，后处理又把错误固化进成图。
- 修复：用户提示词与尺寸彻底分离。单图编辑逐字透传提示词；目标像素通过 MaaS `size` 字段传输。超出上游 3:1 约束时，完整原图只在请求内部做可逆母版适配，返回结果完整恢复到用户像素；移除模糊扩边、居中裁切和尺寸文字注入。多参考图仅补输入角色说明，不改写用户内容。
- 收口：后端修复后，尺寸规划弹层和“适配新尺寸”快捷操作仍残留“裁切”字样。虽然它们不再触发后端裁切，快捷操作中的旧文案仍可能把裁切误当成用户创作意图。v117.23 已把两处统一改为“完整画面适配、不裁切、不补模糊背景”，并用静态回归禁止旧语义回流。
- 服务器差异：生产主服务原先没有安装 Pillow，首次部署验收因此在精确尺寸回归中被安全拦截并自动回滚。Pillow 现已写入主服务依赖；部署必须先安装依赖并复测 PNG/JPEG 编解码，不能因为开发机已安装就默认服务器也具备。
- 规避：比例 / 像素是传输与输出契约，不是视觉风格。任何尺寸功能必须分别验证“提示词原样、结构字段正确、完整边缘保留、最终像素准确”四件事；不得用模糊填充或裁切掩盖上游比例限制。至少保留超宽单图重复测试、PNG/JPEG 输入和多参考图目标拆分回归。

## 2026-07-24 v117.21 修复：MiniMax-M3 兼容答复不能被误判为未看图

- 现象：用户上传统一参考图后，界面提示“统一参考图未完成内容识别”。这并不必然表示图片读取失败；MiniMax-M3 兼容链路会移除不支持的 JSON mode，模型有时返回 `summary`、`required_terms` 或中文摘要文本，旧代码只读取 `brief` / `requiredTerms`，于是将有效答复视为空。
- 原因：保护泛化文案的失败阈值是正确的，但解析器过度假设上游严格遵守 camelCase JSON 字段。错误提示又没有区分“无法读取图片、模型未返回摘要、模型漏主题词”三种情况，用户无法判断下一步。
- 修复：支持 `summary`、`contentBrief`、`required_terms`、`terms` 及明确的“摘要 / 主题词”文本。有效摘要缺主题词时，只从用户上传且非通用的附件名补充主题约束，正文仍需通过主题词校验；没有摘要则保持停止并输出可诊断原因。另修正中文连写 Logo 名称的识别。
- 规避：对兼容模型不能把 JSON mode 当作已成立的协议。每个模型输出都要有字段别名、可控文本格式和“有摘要 / 无摘要”的回归；保护性拦截应说明精确失败边界，不能只给笼统的未完成提示。

## 2026-07-24 v117.20 修复：参考图不能因图卡较少而被静默遗漏，误传链接必须可撤回

- 现象：用户只选择生成两张图片但上传五张统一参考图时，旧的“一图一主素材”修复只覆盖到前两张或被 Logo 占满，其余上传素材没有进入任何实际图片请求；供应商误回传发布链接后，前端和服务端都把 URL 视为必填，无法恢复为未回传状态。
- 原因：统一参考覆盖规则没有把“附件数量大于图卡数量”视为同一张图可承载多个有区别职责的正常情形；回传接口没有“清除”命令，空字符串只能走 URL 格式校验失败。旧资产回推保护还只会复制服务端存在的发布字段，无法表达“服务器已刻意删除该 URL”。
- 修复：参考规划按附件而非图卡选择主归属，多个附件落在同一图时为每个附件分别写出位置与用途；前端仍在提交前验证全覆盖。回传接口新增受原权限边界保护的清除操作：资产保留下载 / 交付和历史播放快照，活动分析链接标为 `superseded`；用 `publishedClearedAt` 连同较新的发布时间戳保护清除态，旧浏览器的错误链接不会复活。
- 规避：不能用“图卡数少于附件数”作为忽略用户素材的理由。生成计划必须对每张统一参考给出可检查归属；任何可逆业务回传字段都需同时设计“设置、修改、清除、旧快照竞争”四条路径，清除不等于删除历史数据。

## 2026-07-24 v117.19 修复：统一参考图不能只被 Logo 挤占或在正文阶段失去主题

- 现象：用户上传 Logo、产品套件、流程截图和主界面四张统一参考图后，视觉规划可能让前三张图只使用 Logo，最后一张才使用套件，流程和主界面完全没有随任何图片提交；标题式正文仍可能退化为泛化“桌面智能体 / 效率工具”种草文，逐图提示词也没有写出遗漏素材应落在哪里。
- 原因：旧服务端只拦截“每张图全量使用所有附件”的广播，没有把“每个统一参考必须至少有一张主图”设为结果不变量；前置视觉摘要还允许模型忽略它判断为无关的附件，正文模型没有可验证的主题词约束。此前 10 轮回归使用的是理想化模型响应，因此没有把该反例覆盖进去。
- 修复：视觉摘要要求先归纳全部统一参考图的共同产品 / 功能 / 流程主题，并返回核心主题词；正文必须自然覆盖这些主题词，否则停止而非输出泛化文。逐图规划在服务端为每个共享附件选择唯一主图归属并保留明确的版面落位说明，客户端再次检查覆盖完整性；旧参考计划签名作废后按新策略重算。
- 规避：参考图规划测试不能只验证“模型返回了可接受的答案”。必须持续注入遗漏附件、单一 Logo 重复、局部复用、all-to-all 广播和泛化标题等反例，并断言“所有用户统一参考均被使用、每个图只携带自己的子集、正文覆盖视觉确认主题”三项同时成立；没有安全登录态时，应明确标注为契约回归，不能宣称已经完成真实模型语义验收。

## 2026-07-24 v117.17 修复：供应商模型异常不能被本地统计伪装成智能回答

- 现象：供应商数据助手的接口已配置 MiniMax-M3，但模型调用、登录态或服务端请求失败后，浏览器会捕获错误并改用 `supplierDataAnswer()`。因此用户输入“你好”仍得到“当前共有 N 条交付内容”，无法区分真实模型回答与规则兜底。
- 原因：服务端将模型异常吞掉并返回一个事实回退；前端又把接口失败、离线模式和空响应统一降级为本地字符串规则。两层静默降级叠加后，模型实际是否被调用在界面上完全不可见。
- 修复：除权威 URL 清单的逐字保留外，所有供应商聊天轮次都请求 MiniMax-M3。链接问题也让模型先完成对话，再由服务端追加可复制的原始链接。模型不可用时服务端返回受控 5xx，前端显示明确失败提示，绝不再回答本地统计。问候提示也明确要求模型自然回应而非输出数据概览。
- 规避：任何承诺“模型问答”的入口都不能使用无标记规则答案冒充成功。降级必须在 UI 上可辨识；涉及准确数字和 URL 时，可将权威字段作为模型上下文或服务端附加内容，但不应跳过整轮模型对话。供应商模型调用必须继续使用服务端按角色裁剪的数据快照，不能将全局浏览器 state 或私密配置交给模型。

## 2026-07-24 v117.16 修复：参考图规划不能在真正提交前被全量回填覆盖

- 现象：图文工坊即使已经得到“每张图只用一张附件”的前置计划，首次批量生成时仍可能显示每张“参考图实际使用 4/4”；非 Logo 截图没有作为某一页的完整主体出现，提示词和用户素材脱节。
- 原因：均衡回退虽然计算了逐图附件子集，但 `applyBatchImageReferencePlan()` 只信任视觉模型结果；无视觉模型或缺卡时又把 `refGroups.all` 回填给每张图。视觉规划异常返回全量附件时，也没有在服务端阻断 all-to-all 广播。
- 修复：执行阶段统一消费逐图计划（包括均衡回退），不再回填全量参考图；视觉规划显式要求非 Logo 素材作为单页完整主体，并为已选附件补充名称、附件编号、版面位置和职责。若模型仍把同一批所有附件分给所有图，服务端按附件分散保留确定性主图卡，避免无关参考污染。
- 规避：参考图“规划数组”和真正提交给图像模型的附件数组必须同源。任何降级路径都要断言每张图只收到它自己的子集；不能因为视觉模型不可用就退化为全量广播，更不能把附件的长篇视觉描述重复塞进提示词。

## 2026-07-24 v117.16 修复：数据助手既要用模型解释，也不能让模型改写链接与事实

- 现象：供应商提问“昨天交付多少条”时可以得到准确计数，却没有进入 MiniMax-M3；而把原始回传链接交给模型又可能导致 URL 被删减、换行或改写，影响复制。
- 原因：早期保护策略把所有可确定事实直接短路，模型只处理开放式问题；同一个快照没有区分“需要模型解释的统计数据”和“必须逐字保留的原始链接”。
- 修复：原始回传链接继续由服务端权威列表直接返回；其他问题一律向 MiniMax-M3 提供当前供应商授权快照。日期 / 数量 / 播放量等同时附带 `authoritativeAnswer`，要求模型先完整输出服务端结论，只能追加基于快照的解释；模型异常仍安全回退到权威结论。
- 规避：模型接入不能用“直接短路”掩盖，也不能牺牲精确可复制数据。业务问答应把数值计算、权限裁剪、模型解释和原始字段保留拆成独立边界，并用模拟调用回归同时覆盖两种路径。

## 2026-07-24 v117.15 修复：供应商问答不能把“昨天”退化为总累计

- 现象：供应商输入“昨天交付多少条”后，右侧助手回答了“当前共有 365 条交付内容”，没有按昨天统计；用户无法判断它是否真的接入 MiniMax-M3。
- 原因：供应商助手原本是浏览器里的规则函数，只识别“今天 / 今日”，没有服务端日期解析、权限数据快照或模型调用。未匹配的提问必然退化为累计总数，且前端无法保证供应商子账号只把自己可见数据交给模型。
- 修复：新增受 `require_member` 保护的供应商只读问答接口。它先通过 `store.state_for` 取得当前供应商角色本来可见的账号 / 已交付资产，再按中国时区直接计算今天、昨天、前天或指定日期的交付、回传和播放量。开放式问题才将裁剪快照交给部署的 MiniMax-M3；调用异常时安全退化为权威概览，精确日期问题不依赖模型。
- 规避：任何带有日期、数量、权限边界的业务问答都必须先由服务端权威数据计算；LLM 只用于基于同一授权快照的解释、归纳和开放式分析。不能用前端字符串规则冒充模型接入，也不能把全局 state 发送给供应商角色。

## 2026-07-24 v117.15 修复：混合资产快照不能因一条他人变更吞掉自己的交付

- 现象：创作者的浏览器快照同时包含他人交付和自己新生成交付时，只要他人记录在服务器已变化，原有权限检查会整体拒绝该批保存，表面上看像自己的交付“也一起消失”。
- 原因：`upsert_member_assets` 把混合快照中任何一条非本人变化都当作整批越权，导致同一请求里允许保存的本人新增行也被放弃。
- 修复：精确移植 `f16bd44`：保持他人资产不可改，跳过陈旧的他人变化并计为 `denied`，继续持久化本人的允许行；当请求只包含越权变更时仍返回权限错误。接口也返回实际 `written / denied`，不再假装整批写入成功。
- 规避：客户端可能携带陈旧的混合快照，权限处理必须按行裁决并区分“拒绝越权行”和“拒绝整批请求”。部署时必须合并此修复，严禁用旧版 `store.py` 覆盖服务器。

## 2026-07-24 v117.14 修复：参考图不能在每张图的提示词中全量重复

- 现象：图文工坊上传多张统一参考图、正文又由标题独立生成时，四张图都可能在提示词中列出全部附件名称；正文与用户上传的真实素材没有内容关联，图片模型同时收到过多、互相无关的参考信息。
- 原因：图卡提示词链路把全局 `styleRefName` 继续注入每一张模型请求，同时标题写正文发生在参考图规划之后或完全没有视觉上下文；没有视觉模型时的回退还会把全部统一参考图附到全部图卡。
- 修复：把流程明确拆为“统一参考图视觉摘要 → MiniMax-M3 完整正文 → 标题 / 正文 / 图卡的逐图附件编排 → 单图提交选中的真实附件”。逐图规划存在时不再注入全局参考图名称；视觉不可用的回退按图卡均衡分配统一参考图。单图定制参考继续硬隔离在其图卡内。
- 规避：参考图的视觉理解、正文主题、附件路由和图像生成必须是四个独立阶段。真实附件已经随生成请求上传时，提示词只能说明“在哪用、怎么用、保留什么”，不能再把所有图片内容逐项复述或把所有附件广播到每张图。

## 2026-07-24 v117.14 调整：Logo 不等于强制角标

- 现象：视频工坊把文件名带有 Logo 的素材直接缩小并固定到角落；产品口播提到品牌时，关键镜头反而没有引用用户提供的真实标识，可能出现模型重绘的错误 Logo。
- 原因：素材归一化和最终剪辑回退把 `is_logo` 当成固定 overlay 指令，绕开了导演对口播语义、镜头中心主体和用户明确要求的判断。
- 修复：仅当用户明确说角标 / 水印 / 角落时，Logo 才采用小尺寸角落叠加。其余 Logo 与普通视觉素材一样由导演决定 cutaway、中心展示或画中画；在品牌被口播明确提及时，导演提示要求优先在该镜头使用用户提供的真实 Logo reference / both。
- 规避：文件类型只是一条弱线索，不能覆盖用户意图和导演语义。素材落位必须先满足口播内容与镜头叙事，再考虑角标习惯；只有显式位置请求才可以作为硬约束。

## 2026-07-23 v117.10 修复：总播放量不能依赖接口链接集合

- 现象：创作端总播放量详情上线后，已有供应商手动填写的播放量在未建立 `analyticsLinks` 时被排除，总卡片可能显示为 0。
- 原因：详情功能错误地把接口同步链接当成播放量账本；历史人工回填数据只保存在交付素材的 `viewCount` / `viewsUpdatedAt`。
- 修复：总卡片和详情统一遍历全部已交付素材，以供应商保存的 `viewCount` 汇总；有回传链接时仅补充“查看链接”入口，缺失链接的历史填写也保留在明细中。
- 规避：业务 KPI 的统计源必须是其权威写入字段，外部同步表只能作为补充信息。新增明细视图前必须验证“只有人工填写、没有接口链接”的历史数据仍参与合计。

## 2026-07-23 v117.5 修复：完整视频工坊回归不能依赖本机私密环境或遗漏时序导演

- 现象：在无私密环境的干净工作区中，完整视频工坊回归有三个用例失败：时长修订导演缺少 LLM Key；QA 失败和通知失败事务测试缺少时序导演 mock。
- 原因：v115 引入真实口播后的时序视觉导演，但旧测试 fixture 没有把它视为外部服务边界；其中一处还无意依赖开发机已有的 `providers.settings.llm_api_key`。
- 修复：语义测试显式注入测试 Key 和模型设置；事务公共 fixture 统一 mock `lock_timed_visual_plan` 并返回当前契约的四个字段。专用双镜头 fixture 继续覆盖公共 mock。
- 规避：完整回归必须在无私密环境下通过。新外部依赖加入流水线时，所有覆盖该路径的 fixture 必须提供同构返回；测试设置不得从开发机环境继承凭据。

## 2026-07-23 v117.4 修复：时序流水线回归必须 mock 真实导演边界

- 现象：部署前完整回归中，“先测口播再提交镜头时长”用例的 Seedance 提交记录为空。
- 原因：流水线新增真实口播时长后的 `lock_timed_visual_plan` 阶段；用例只 mock 了 TTS、测时长、Seedance 和合成，测试环境的独立 provider 配置使导演调用在提交镜头前失败。
- 修复：用例 mock 时序导演，并返回 `scenes`、`minimum_units`、`public_summary`、`asset_placements`；同时断言它收到实测的 10 秒口播并在 Seedance 前执行。
- 规避：流水线增加新的外部服务边界时，端到端回归必须补齐该边界的真实返回结构。不能只看后续任务数组为空而把测试桩失败误判为业务排程错误。

## 2026-07-23 v117.3 调整：模式切换不能让未变化的左侧音色库参与动效

- 现象：音色合成、设计和管理只切换右侧工具内容，但左侧音色库也随之淡出 / 淡入，造成无意义的闪动感。
- 修复：模式切换保留左侧音色库和中间编辑器的既有 DOM，只替换右侧功能面板；动效选择器也仅作用于右侧面板。
- 规避：局部模式切换要按实际内容边界划分刷新范围。未变化的导航、列表和输入区既不能重建，也不能附带过场动画。

## 2026-07-23 v117.2 修复：账号停用 / 恢复不能改变既有编号

- 现象：供应商停用或恢复账号后，该账号及其后的账号编号会变化。
- 原因：状态更新使用 SQLite `INSERT OR REPLACE`，会产生新的物理 `rowid`；供应商快照又按 `rowid` 投影编号，导致更新过的账号被排到最后。
- 修复：账号首次被供应商管理时固化当前编号，新建账号使用下一个编号；快照投影优先读取已固化编号，旧账号按未占用的原始位置补位。
- 规避：任何面向用户展示的业务编号都不能依赖 SQLite `rowid`、更新时间或数组过滤后的顺序。可逆状态更新必须保留可见编号，并用“停用—恢复前后全量编号一致”回归保护。

## 2026-07-23 v117.1 调整：账号卡片的空状态提示不能挤占操作区

- 现象：未填写主页链接的供应商账号卡片显示“主页链接请在编辑账号中填写”，使编辑、停用 / 恢复与“分配给”下拉被分成多行，卡片高度不必要地增加。
- 修复：空状态不再输出说明文字；将两个图标操作和分配下拉收进同一行。主页链接存在时仍显示可打开的链接，编辑入口继续合并在账号编辑对话框中。
- 规避：卡片的非必要空提示不能抢占主操作区。需要说明的字段应放入编辑对话框或帮助文案，账号批量操作必须优先保持同行、紧凑和稳定。

## 2026-07-23 v117 修复：多参考图“图 N 调整为图 M”必须区分目标与风格供体

- 现象：无限画布已能识别指定图和多图编辑，但“将图 2 的背景调整为图 1 的浅蓝色”会把图 1 也排入编辑目标，造成额外任务，极端时又表现为无关生成。
- 原因：供体判定只覆盖“调成 / 换成”，没有覆盖自然语言中更常见的“调整为 / 改为 / 替换为”；解析器看到图 1 前的“调整为”时仍把它当成待处理图。
- 修复：将上述连接词加入风格供体判定。补充 10 轮确定性规划测试，覆盖单图指定、多图并发、图号供体与全部参考图；测试只执行任务规划，不请求模型或写画布。
- 规避：有编号附件的编辑请求必须把“动作对象”与“样式来源”分别解析。新措辞加入时先扩展确定性供体词表和测试样例，不能依赖模型在多图输入中猜测用户目标。

## 2026-07-23 v117 修复：音色模式切换不能重建正在输入的口播编辑器

- 现象：点击音色合成、音色设计或音色管理时，整块语音工作台重新渲染，中间长文本输入板会闪烁，并可能丢失焦点或中文输入法组合状态。
- 原因：模式状态变化直接替换工作台根节点，虽然文本已写入本机状态，却没有保留正在编辑的 textarea DOM 节点。
- 修复：切换前保留中央编辑器节点，重绘后只替换左右功能面板；左、右面板独立执行淡出 / 淡入，中央编辑器保持原节点、文本和焦点语义。
- 规避：多栏工作台的局部模式切换应按面板边界更新。任何包含正在输入文本、上传队列或播放状态的中栏都不能作为整页重绘副作用被销毁。

## 2026-07-23 v117 调整：设置看板的明细表不应靠横向滚动承载核心数据

- 现象：管理员接口用量明细在弹层内以宽表呈现，需要左右拖动；成员和产品也按单行堆叠，设置页无法快速扫描。
- 原因：明细表以固定最小宽度避免列压缩，设置页沿用列表布局，没有把“聚合看板”和“单账号深入查看”拆开。
- 修复：设置页改为紧凑成员卡片、置顶申请看板和默认折叠产品库；主表与弹层均使用响应式列，账号级明细单独打开并在窄屏隐藏次要列，而非产生横向滚动。
- 规避：看板首页先服务扫描，复杂原始记录再按实体展开。表格需要响应式降级策略；禁止用横向滚动作为关键账号、token 和时间数据的唯一阅读方式。

## 2026-07-23 v116 修复：多参考图编辑不能走通用多图生图

- 现象：无限画布同时选中多张参考图后，用户说“这两个都换成某风格”或“图 2 修改风格”，聊天仍走通用生成路径。模型把多张图混合重绘，既没有创建逐图任务，也可能返回与原图无关的新图。
- 原因：聊天入口只区分“有无参考图”，把全部参考图一次交给生成接口；它没有识别图号 / 全量编辑语义，也没有区分“待编辑原图”和“风格参照图”。
- 修复：新增定向编辑规划：明确图号时仅处理该图；出现“都、全部、每张、同时”等多图编辑意图时为每张目标建立独立并发任务。每个请求第一张固定为待编辑原图，其他选图只作为风格参照；服务端 transform 接口保持此顺序并禁止无关新图、拼图与多图合成。
- 规避：多参考图不等于多图融合。聊天层必须先判断目标集合，再按目标图拆分请求；服务端必须用目标图优先的输入顺序和可核验引用数量保护编辑语义。回归应覆盖“图 2 改风格”“图 2 参考图 1 的浅蓝底色”和“两个参考图都改”的单图 / 多图并发场景。

## 2026-07-23 v116 调整：供应商工具与选中状态不能继续占用内容流布局

- 现象：供应商全部账号的搜索、新建和筛选仍占用页面内容区；设置页的批量建立子账号按钮单独撑开一行。账号列表每项纵向占用过高。发布清单选中素材后，“已选 N 条”作为普通筛选项参与 flex 换行，挤压批量下载按钮并把表格下推。
- 原因：供应商虽然已复用左侧导航，但工具入口仍分散在各个页面 renderer；选中数量被当作普通过滤控件，而不是不应影响列表高度的状态提示。
- 修复：将账号页与设置页的角色专属动作统一挂入顶部操作栏，内容区只保留数据本身；设置页账号列表改为响应式紧凑横向卡片网格。已选数改为桌面端右上固定角标、小屏回退到正常文档流，保持键盘和屏幕阅读器可达。
- 规避：顶部全局动作、页面筛选与“当前选中数”必须区分布局职责。只反映状态的短标签不能成为会触发主工具栏折行的 flex item；跨供应商页面重复出现的动作应由外壳统一托管，而不是每页各留一条空白工具行。

## 2026-07-23 v116 修复：供应商首页两列卡片不能各自按视口高度结束

- 现象：右侧数据助手虽然顶边已对齐，但以固定视口高度结束；左侧最近操作卡随内容更高，两个卡片底边仍然错开。
- 原因：网格本身允许两列拉伸，助手却额外设置了独立 `height` 并使用 `align-self: start`，主动退出了同一行的拉伸规则。
- 修复：保留助手粘性定位与最小可用高度，移除独立固定高度并改为 `align-self: stretch`；左列因最近操作而增长时，助手同样延伸，消息区仍弹性占据中段，输入框保持底部。
- 规避：同一桌面网格里要求底边齐平的卡片不能一边使用内容高度、一边使用单独视口高度。需要独立输入可用性时用 `min-height` 和内部网格控制，而不是让外卡脱离行高。

## 2026-07-23 v116 调整：Token 看板必须能追溯到功能、模型和单次调用

- 现象：管理员只能看到按创作者汇总的 token，无法判断消耗来自哪个功能或模型，也不能核对最近一次真实调用。
- 原因：SQLite 已保存功能名、模型、输入 / 输出 token 和调用时间，但管理员 API 只返回成员维度汇总，前端没有展开入口。
- 修复：新增管理员只读明细接口和弹层，提供“功能 + 模型”汇总及最近 120 笔调用记录。继续只采集上游 usage，不估算未返回 usage 的图像、视频、语音或其他计费。
- 规避：成本看板至少要同时提供“聚合视图”和“可追溯明细”，并清楚标注统计边界；详情接口仍必须沿用管理员鉴权，不能把成员调用记录暴露给普通创作者或供应商。

## 2026-07-23 v116 修复：健康接口正常不等于本机 Python 服务已加载新增路由

- 现象：管理员页面点击“刷新”读取语言模型用量时返回 HTTP 404，尽管工作区 `server/main.py` 已定义 `/api/admin/llm-usage`，入口静态资源也已经更新。
- 原因：8787 仍由历史 Uvicorn 进程提供服务。静态文件从工作目录实时读取，所以页面能加载新 JS；Python 路由则只在进程启动时注册，导致“健康接口 200、特定新 API 404”的混合版本状态。
- 修复：通过项目标准 `start.command` 精确重启本机主服务与同实例 sidecar；重启后未登录请求返回预期 401，已登录管理员页面实际取得用量汇总。没有修改数据库、账号或环境变量。
- 规避：新增后端接口验收必须同时核对源码路由、实际请求状态和当前服务实例，不能用 `/api/health`、静态缓存标识或浏览器已刷新推断 Python 已热更新。发现“新静态已生效而新 API 404”先按受控启动器重启，不要重复修改已存在的路由。

## 2026-07-23 v116 修复：非等比 SVG 趋势图不能直接绘制圆形交互节点

- 现象：供应商首页的折线趋势在宽卡片中正常铺满，但节点被拉成椭圆；用户悬停时也无法直接知道该日期对应的交付数量。
- 原因：折线路径使用 `preserveAspectRatio="none"` 适配宽高比，SVG 内 `circle` 同时按横纵比例缩放，视觉上不再是正圆。
- 修复：保留自适应 SVG 路径和面积填充，节点移为相对图层定位的固定像素圆点；每个节点携带日期和数量提示，悬停或聚焦时显示“X 条交付”。
- 规避：需要铺满任意比例容器的 SVG 可以使用非等比路径，但圆点、头像、命中区和 tooltip 不应作为同一 SVG 坐标内的几何圆形；改用 HTML 叠层或按实际宽高比计算椭圆半径。

## 2026-07-23 v116 修复：口播为主时间轴时，超阈值编码偏差应先自动重校而非直接终止

- 现象：基础容差已允许几秒内音画偏差，但超过容差的 MP4 编码差异仍会让视频工坊直接停在失败状态，需要用户再次输入“继续”才能恢复合成。
- 原因：变速版和最终成片只进行误差断言；即使口播音频完整、视频轨可重新编码，也没有先尝试把画面轨压回口播实测时长。
- 修复：保留口播音频不变，检测到超阈值后自动按“视频实测时长 / 口播实测时长”重定时画面轨，重新探测并再次校验；成功时在项目事件中明确记录自动重校，再继续交付质检。修复只作用于本次临时成片，不增加静帧、静音或循环画面。
- 规避：音画异常应区分“可用媒体的编码时间漂移”和“媒体轨缺失 / FFmpeg 失败”。前者先自动修复并留事件证据，后者才保留可恢复错误；不能把任意失败静默当成功，也不能用静帧或静音伪造时长一致。

## 2026-07-23 v116 修复：供应商首页局部轮播不能重绘整个首页，问答不能只保存在 DOM

- 现象：最近操作轮播时，供应商首页的统计、图表和右侧问答都会瞬间闪动；用户刷新或因筛选触发首页重绘后，已发出的数据问答记录消失。右侧卡片还比左侧“已发布”指标下沉一截。
- 原因：定时器直接调用 `renderSupplierOverview()`，把整页 `innerHTML` 换掉；问答消息只追加到当前 DOM，没有以供应商身份保存。粘性卡片的 `top` 偏移又与左栏起点不一致。
- 修复：轮播每 3 秒仅替换最近操作的三行，并执行局部翻转退场 / 入场，页面其余节点保持原位；问答历史按当前登录供应商成员 ID 作为本机存储键，保留最近 40 条并在渲染时恢复；助手卡片贴齐网格顶边。无存储权限时只降级为当前页会话，不影响问答。
- 规避：定时 UI 不得为内容轮播调用页面级 renderer。所有需要跨刷新保留的会话类内容都应有明确的、按成员隔离的持久层；若只需要本机体验，local storage 可作为缓存，但不得将其误称为服务端审计记录。

## 2026-07-23 v116 修复：供应商账号编辑不能成为角色版和声线的权限绕行入口

- 现象：供应商管理员的账号编辑器仍可见并修改数字人角色版、固定口播声线与参考音频；即使隐藏前端控件，专用账号接口此前也会接受这些字段。
- 原因：供应商与管理员共用账号编辑器，只有风格和图文提示词做了前端条件分支；服务端供应商账号白名单仍包含角色版和声线字段。
- 修复：供应商界面移除上述编辑入口，提交时保留现有配置；服务端白名单删除 `charBoardAssetId`、声线 ID / 名称及参考音频字段。定向回归以恶意替换 payload 验证创作者既有角色版与声线配置保持不变。
- 规避：权限收紧必须同时移除 UI 入口和服务端写入能力。前端“不可编辑”只属于体验层，所有账号配置字段都应按角色做显式服务端白名单。

## 2026-07-23 v116 修复：供应商专用账号接口不能继续成为通用资产上传入口

- 现象：供应商账号编辑器的角色版和声线入口已收紧后，仍保留“账号图片资产”上传区；专用账号请求也会接收其中的新资产，容易把供应商账号维护与创作侧通用资产库混在一起。
- 原因：账号头像、角色版、声线参考与通用账号图片原先共用同一个 `assets` 请求数组，前端只靠界面分区区分用途，服务端没有按标签二次限制。
- 修复：供应商创建和编辑对话框移除通用图片资产区，仅保留账号头像；服务端只接受显式带“头像”标签、且路径归属当前供应商的随账号资产。回归同时验证普通“账号资产”被拒绝、头像仍可通过。
- 规避：复用数组字段不代表可复用权限。涉及账号维护的附件必须按用途做服务端白名单，尤其不能让“隐藏上传按钮”成为唯一保护。

## 2026-07-23 v116 调整：供应商导航应复用创作端左轨，不应为相同路由维护独立底部 Dock

- 现象：供应商首页右侧助手需要贴齐内容区右缘，但底部悬浮 Dock 迫使供应商页面使用单列壳与底部安全区，视觉层级和创作端不一致。
- 原因：供应商此前为移动端式底部 Dock 覆盖了应用外壳的 64px 左轨网格；虽复用相同导航按钮，布局却分成两套，横向内容无法利用完整主列。
- 修复：供应商恢复使用创作端同一左侧竖向导航与角色可见性规则，移除底部 Dock 及其移动端覆盖；供应商页面解除居中最大宽度，让数据助手跟随主内容区右缘。
- 规避：当角色只是可见路由不同、而非信息架构不同，应优先复用同一导航容器和布局网格。不要为同一组路由维护第二份固定导航，否则会持续引入安全区、对齐和响应式分叉问题。

## 2026-07-23 v116 修复：粘性问答卡不能依赖左侧长内容决定输入框位置

- 现象：供应商首页右侧数据助手的初始回答可见，但问题输入框被拉到左侧长内容的页面底部，首屏看起来像没有对话入口；最近操作列表也在底部导航前显得突然结束。
- 原因：问答卡同时使用 `height: 100%` 和较大的最小高度，网格拉伸后其输入区随左列高度远离可见视口；最近操作只有底部边线，没有为列表结尾提供视觉过渡。
- 修复：问答卡改为按视口限定高度并从网格起点粘性定位，输入和快捷问题保留在卡片可视区域；最近操作补齐完整卡片边界、底部渐隐和自动轮播。
- 规避：左右分栏中任一侧包含持续输入时，应以该侧自身可用视口高度布局，不能让另一列内容高度决定输入组件的位置；固定底部导航存在时还要给滚动内容与列表尾部预留安全间距。

## 2026-07-23 v115 修复：历史失败不能被后续问答覆盖，供应商停用不能等待无关全量持久化

- 现象：视频工坊曾因变速成片音画时长存在极小差异而停住，用户随后输入“没关系继续合成”或“继续”，界面只追加“会继续推进”的文字而不创建合成任务；供应商账号点击确认停用后长时间没有按钮反馈，用户无法判断请求是否生效。
- 原因：视频项目在后续导演问答后被重新写成 `conversation / brief`，当前 `project.error` 为空，旧恢复入口只接受 `status=failed`，因此丢失了消息历史中的可执行失败阶段。供应商停用则在权威接口成功后继续等待整个客户端状态的全量持久化，按钮只有禁用态而没有进度文案。
- 修复：恢复逻辑会从最新助手错误消息识别仍可执行的失败阶段；“继续 / 继续合成 / 没关系继续”等明确意图会直接调度后续字幕、合成、检查与交付。音画同步以真实口播为主轴，几秒内偏差自适应重定时，明显错位仍拦截。供应商停用 / 恢复按钮立即显示进度，成功后采用服务端权威账号结果更新局部状态，不等待无关集合全量保存。
- 规避：可恢复任务不能只依赖易被会话状态覆盖的顶层 `status/error`，必须保留并读取最近可执行失败证据；“继续”验收要断言真正创建后台任务，不能只看回复文本。业务按钮必须先展示忙状态，局部服务端写入不应被无关全量缓存写入阻塞。

## 2026-07-22 v112 修复：画布视口不能决定新结果位置，异步多下载不能当作批量导出

- 现象：用户平移或缩放画布后再次创建任务，新图片会跟随当前视口落到不同位置，看起来像随机散落；放大图片后按左右键虽然选中项变化，预览画面却可能不切换；批量选中两张图片后点击批量导出，浏览器只保存第一张。
- 原因：生成占位以当前视口中心作为碰撞查找起点，而不是现有内容边界；画布与放大层各自监听同一组方向键并更新不同状态；批量导出在异步渲染完成后循环触发多个 `<a download>`，第二次开始已脱离原始用户手势，浏览器会按多文件下载策略拦截。
- 修复：生成结果统一从当前内容边界右侧寻找空位，视口只负责观察、不再参与结果布局；放大层开启时画布快捷键退出，由放大层单独切换内部图片并同步选择，补充方向过渡；多选导出先并行渲染所有图片，再打包为一个无压缩 ZIP，由一次用户下载动作完成。
- 规避：无限画布要区分“内容坐标”和“观察视口”，持久布局不能依赖瞬时视口；模态层必须明确拥有键盘事件；浏览器批量导出应优先单文件容器，不能假设多个异步下载都会获准。回归需同时覆盖源码契约、生产构建和减少动态效果模式。

## 2026-07-22 v111 修复：媒体任务失败恢复不能假定视频单元已经存在

- 现象：同一批次同时启动两条视频，一条正常生成，另一条失败；失败卡点击“重生视频”后提示“没有可重新生成的视频单元”，原始错误不再可见。部分视频工坊项目又会把用户明确指定的“图1参考、其他图片做剪辑素材”全部归为参考，导致素材没有进入最终剪辑。
- 原因：失败账号在语言模型生成文案 / 口播 / 分镜阶段就已停止，尚未创建任何 Seedance job；旧重生入口只会重派已有单元，因此用二次错误覆盖了真正的上游失败。附件用途则完全信任一次导演结构化结果，缺少对用户明确编号指令的确定性校验。
- 修复：没有可用视频单元时，重生入口重新执行该账号的脚本与分镜，再确认 jobs 已创建；已有单元仍按原路径只重生视频。附件用途增加窄范围校验：只纠正明确编号或“其余图片做剪辑素材”的角色误判，导演选择的场景、展示方式、位置、时长和口播锚点保持不变。Seedance 提交边界统一增加无文字负面要求；版权 / 安全拒绝只原位有界改写失败镜头。
- 规避：任务恢复必须从已持久化阶段判断下一步，不能假设失败一定发生在媒体生成后；重试不能覆盖原始失败原因。模型负责开放式判断时，用户明确指定的附件身份仍要做确定性校验，但校验只能约束身份，不能吞掉导演的剪辑决策。

## 2026-07-22 v110 修复：浏览器请求不能拥有导演与生产任务生命周期

- 现象：同一个附件和标题，不切走视频工坊时可能正常进入生产，切换页面或会话后更容易停在导演阶段；失败提示为“MiniMax-M3 导演自检请求失败：没有返回可用消息或工具参数”。新建会话也要等首条导演请求结束或下一轮历史轮询后才出现在左侧。
- 原因：旧 `/api/chat` 在 HTTP 请求内同步等待 M3 首次决策，只有拿到有效 plan 后才把媒体 pipeline 放入后台；浏览器切页、iframe 卸载和前端 busy 状态因此与导演连接耦合。M3 完整工具结果又沿用 4096 token 旧预算，空消息只恢复 1 次，复杂附件与长计划更容易得到空 / 截断工具响应。新会话只有首条消息隐式创建项目，左栏依赖 4 秒轮询。
- 修复：消息与附件落盘后立即登记一个覆盖“导演 → pipeline”的服务端 task，并返回受理状态；task 由项目注册表持有，只有显式停止或服务进程停止才取消。空响应采用最多 3 次有界恢复，导演完整结果最少 12000 token；永久错误不重试。新增空会话创建接口和主平台 owner 映射，前端立即上屏并用请求代次阻止旧历史覆盖。
- 规避：异步媒体产品不能把浏览器 fetch 当任务所有者；回归必须在导演尚未返回时结束 / 丢弃前端请求，再确认 pipeline 仍创建并推进。模型稳定性要区分瞬态空结果和永久配置错误，复杂结构化输出的 token 预算必须与 schema 规模匹配。新建实体应先持久化再乐观显示，不能依赖长请求结束后的轮询副作用。

## 2026-07-22 v109 修复：批量确认准备逻辑必须有显式依赖并处于可见异常边界

- 现象：批量信息流或数字人账号已选择、标题与文案已填写，点击“确认执行”只出现按钮焦点框，没有任务、进度卡或错误提示；再次点击仍无响应。
- 原因：确认校验调用 `groupOf()` 判断账号类型，但 `view.js` 没有导入该函数，浏览器同步抛出 `ReferenceError`。这段归一化与校验又位于 `try/catch` 之前，因此错误既没有 toast，也没有进入按钮解锁逻辑。只检查函数实现或下游任务状态无法发现，因为请求根本没有到达批次编排器。
- 修复：显式从账号领域模块导入 `groupOf`；把账号类型归一化、引用清理、文案 / 角色版校验、忙状态和批次创建纳入同一异常边界。启动前让浏览器先绘制忙状态，成功创建 production 与进度卡后再重绘整张计划卡；任何同步或异步错误都恢复为 pending 并显示可读原因。
- 规避：事件处理器中所有可抛错的准备步骤都必须在异常边界内，且不能依赖未显式导入的全局符号。回归要从真实按钮点击开始，至少断言批次、production、进度消息和按钮恢复，而不能只单测 `startBatch()`。

## 2026-07-22 v109 修复：导演节奏不能由重复数值提示替代剪辑判断

- 现象：虽然提示词声称节奏由导演决定，实际多数画面仍接近固定 3–4 秒切换；Seedance 单个 4–15 秒片段内部可以多次切镜，却没有充分利用，长镜头与瞬时快切的差异不足。
- 原因：导演系统提示和下游渲染提示同时注入“约 2–4 秒推进一次”的参考句。相同数值在两层重复出现后成为强先验，模型倾向等间隔安排动作，掩盖了口播列举、反差、证据、情绪与转折本应产生的不同剪辑节奏。
- 修复：保留 4–15 秒作为 Seedance 技术生成边界，删除两层固定数值参考；借鉴 OpenMontage 的分层方法，把场景叙事职责、镜头意图和内部剪辑决定分开建模。每个内部节拍记录口播锚点、相对节奏、信息增益与切换原因，再作为时间结构交给 Seedance；没有新增信息时允许长镜头，不机械补切。
- 规避：节奏优化只能描述判断依据和剪辑意图，不能写“一句几个镜头”或固定秒数。回归需要同时使用高密度列举与克制故事两类输入，确认前者出现快慢变化、后者保留停留；还要断言接口技术时长没有被误当成成片镜头时长。

## 2026-07-22 v108 修复：并发保护必须覆盖上游任务生命周期，批量视频必须等到单一成片

- 现象：浏览器分别计算普通视频和数字人并发，混合或多人同时创作时总任务数可能超过上游 10；批量信息流的两段视频、数字人的多段视频全部成功后，状态机又会立即进入审核，供应商交付存在拿到源片段而非单一成片的风险。
- 原因：浏览器并发只约束当前标签页，服务端旧信号量只包住提交 HTTP 请求，任务一返回 ID 就释放，无法统计仍在 Seedance / 数字人侧运行的活跃任务。批量状态机只调用 `autoAssemble` 建时间轴，没有把 `/api/video/compose` 成功作为进入审核的前置条件。
- 修复：浏览器所有视频共享 10 槽，并把普通信息流限制为 8 个并行片段；服务端使用跨成员 FIFO 租约，从提交前持有到轮询终态或取消，等待请求取消时显式跳票。批量片段完成后必须合成一个 MP4，成功才进审核，失败保留明确错误并停止冒充交付；数字人字幕在该合成步骤统一烧录。
- 规避：并发上限必须按“仍在上游运行的任务”计数，不能按提交请求数或单一浏览器标签计数；批量视频回归必须同时断言最大活跃数、排队顺序、取消不堵塞、最终只有一个交付 URL、数字人字幕与信息流默认空字幕，不能只看所有片段状态为 succeeded。

## 2026-07-22 v108 修复：团队数据可见与私有创作数据必须分层

- 现象：创作者首页的累计 / 近 7 日交付和发布清单只包含本人记录，无法获得团队完整数据；如果粗暴放开全部 assets，又会把其他成员的草稿、参考图和私有项目一并泄露。
- 原因：服务端把“交付记录可协作”和“创作中素材私有”都放在同一个 owner 过滤分支中，前端也没有发布人默认筛选来承接团队列表。
- 修复：编辑角色可见所有 `delivered` 记录及每条交付明确引用的封面 / 包内媒体，其他成员的未交付资产、productions、jobs、sessions、batches 和定制项目继续隔离。发布清单进入时默认当前成员，并提供发布人筛选；首页发布数量只按供应商回传链接计数。
- 规避：团队协作权限要按业务对象和引用关系开放，不能把整个 assets 集合设为共享。回归必须同时包含“看得到团队交付及其预览”和“看不到他人私有草稿”两组相反断言。

## 2026-07-22 v107 修复：导演不能把可自主决策的偏好变成必填表单

- 现象：用户已给出完整主题、口播或成片目标，视频工坊仍追问“更喜欢哪种气质 / 结尾 / 镜头语言”；“继续”可能又生成一段完整计划文字，但没有创建 pipeline task，形成鬼打墙。
- 原因：导演工具同时提供追问和生产，旧描述只说“信息不足”，模型容易把创意上的可选决策判为缺失信息。强制指定工具时，部分 OpenAI 兼容网关还会把调用输出为普通 XML 文本，前端遂将它误显示为新问题。
- 修复：只有缺失主题、主体或核心目标才允许 `ask_user`；受众细分、风格、镜头、结尾和可选附件由导演自主决策。已有可执行简报却仍追问时，只提供生产工具做一次自纠；网关仍返回非标准文本时再做一次有上限的格式纠正，失败直接报错，不再冒充用户问题。
- 规避：产品应当验证“是否真正创建生产任务”，不能仅看 `/api/chat` 返回 200 或助手文本看起来完整。自纠只能纠正行动与协议格式，不得同时写入固定镜头数、固定节奏、固定时长或强制语速。

## 2026-07-22 v107 修复：批量视频参考图必须按封面与画面分开建模

- 现象：批量真人视频点击“确认执行”时缺少立即反馈；同一组“统一参考 / 本号定制参考”又被同时当作封面参考和视频画面参考，数字人角色版也没有成为每个账号封面的独立身份参考。
- 原因：批量编排器只有一个通用 `video refs` 集合，无法表达“真人任务图只做封面”与“信息流任务图同时给视频和封面”的产品差异；确认按钮的忙状态又在可同步抛错的准备代码之后才可见。
- 修复：拆分 `batchCoverRefIds` 与 `batchSceneRefIds`。真人 / 数字人的任务参考只合并到封面，数字人封面再加入本账号 `charBoardAssetId`；信息流则保留对应账号的视频 + 封面共用。确认按钮先进入忙状态，全部准备与启动逻辑被同一 `try/finally` 覆盖。
- 规避：参考图不能按“一组图全链路通用”建模，必须按产物与用途分组，并用多账号不同角色版的行为级测试防止串号。交互按钮的忙状态必须在任何可抛错代码之前建立。

## 2026-07-22 v106 修复：任务轮询和心跳不能争抢同一个状态文本节点

- 现象：视频工坊运行时，底部“正在生成镜头”右侧说明和已等待秒数不断整行闪跳；即使成片节点没有重载，状态文字仍会在短句和长句之间反复变化。
- 原因：项目每 1.3 秒轮询一次，`renderEvents()` 会把同一个 `production-live-text` 重置为最新事件标题；另一个 3 秒心跳又把该节点改写为“事件标题 + 轮换说明 + 秒数”。两个更新源交替覆盖整行并改变文字宽度，形成持续重排。
- 修复：任务标题、滚动说明和秒数拆为三个节点。真实事件只更新标题；心跳只原位更新等宽秒数，并每 6 秒对说明做一次纵向滚轮过渡。滚动窗口保持固定宽度，兼容 `prefers-reduced-motion`，不改变服务轮询或生产状态机。
- 规避：轮询 UI 的同一可视字段只能有一个状态所有者。高频数值、低频阶段说明和真实业务事件必须拆分更新；动画只能作用于固定尺寸的子节点，不得通过反复替换整段 DOM 或整行文本制造进度感。

## 2026-07-21 v106 修复：新项目首次生成不能读取上一轮共享尺寸

- 现象：无限画布首页选择“小红书 3:4”等尺寸后，第一次创建并自动生成仍可能使用旧尺寸；退出项目再进入并生成则恢复正常。
- 原因：首页已把选择正确保存进新项目 `targetSize`，但首次进入时“读取首页暂存提示词并生成”和 `enterProject()` 把项目尺寸写入共享 `composerSize` 是两个独立 effect。自动生成先执行时会优先读取上一轮共享尺寸，形成只发生在首次请求的竞态。
- 修复：首页交接的首次生成显式传入当前项目 `targetSize`；后续画布内生成不传该参数，继续读取用户实时选择的 `composerSize`。未修改模型参数、提示词、参考图或历史项目数据。
- 规避：跨页面创建后立即执行的首个任务，必须把本次事务的关键参数随交接载荷显式传递，不能依赖另一个 effect 稍后同步全局状态。回归必须覆盖“选择非默认尺寸后首次自动生成”，不能只验证项目字段或第二次进入。

## 2026-07-21 v106 修复：子应用新接口必须同步通过主平台精确代理

- 现象：视频工坊已有变速引擎和 `speed-version` 接口，但从主平台点击“生成变速版”却返回“该视频工坊接口未开放给主平台”。
- 原因：主平台同源代理只白名单了项目读取、`retry` 和 `cancel`；子应用单测能直接命中 sidecar，但真实嵌入链路会在主服务先被拒绝。
- 修复：主平台仅新增 `projects/{id}/speed-version` 精确路径，先校验当前成员对项目的 owner 绑定，再限定为 `POST` 后转发；不开放任意 sidecar 路径，变速结果也不重新同步或改写项目归属。
- 规避：新增子应用 API 时必须同时验证“直连 sidecar + 主平台同源代理 + owner 越权 + HTTP 方法”。只看子应用接口存在或单测通过，不代表主平台中真实可用。

## 2026-07-21 v106 修复：长口播不能靠重复单镜头或静止尾帧补时长

- 现象：两分钟左右的视频会在多个口播段重复使用同一段 Seedance 画面；过去为消除静止帧加入的目标时长、固定语速或硬镜头规则又会削弱导演理解力，甚至形成“只规划、不执行”的循环。
- 原因：导演语义镜头、TTS 真实时长、Seedance 单次 15 秒技术上限和最终播放速度属于四个层级。若把技术上限反向写成导演硬约束，导演智能会下降；若一个语义镜头只生成一个短文件再覆盖更长口播，合成只能重复、拉伸或补静止帧。
- 修复：导演只接收非强制的节奏建议，TTS 保持自然语速；口播完成后按实测音频窗口在后端拆分最多 15 秒的独立技术视觉节拍，每个节拍单独生成并按最多 10 个并发排队。正常合成保留原源，再用 FFmpeg 同步调整视频 PTS 和音频 tempo 派生默认 1.2x 及用户选择版本。
- 规避：回归必须同时断言长窗口产生不同输出文件、超过并发上限的任务排队、成片变速不触发导演 / 视频 / 语音模型、原始成片不被覆盖。任何节奏优化都只能给导演软目标，禁止写入固定镜头公式或让接口能力决定创意结构。

## 2026-07-21 v106 修复：模型像素网格适配不能误报为内容裁剪

- 现象：无限画布选择 B 站封面等非 8 像素整除尺寸时，界面提示“生成母版 1152×720 → 裁切为 1146×717”，用户会误以为画面主体必然被裁掉。
- 原因：图片模型要求宽高落在 8 像素网格，目标 1146×717 与合规母版只相差 6×3 像素；旧说明把这种输出对齐与超宽比例、超大尺寸的真实裁切共用同一个 `crop` 文案。
- 修复：尺寸规划显式区分网格对齐与真实裁切。只有 8 像素网格差异时显示“自动适配 / 精确适配为目标尺寸”，说明保持主体构图；超宽比例或超大母版仍保留真实裁切 / 超分警告。
- 规避：技术尺寸归一化、构图裁切和超分必须使用不同风险语义。测试应覆盖非 8 像素常用规格，不能只检查模型提交尺寸合法。

## 2026-07-21 v105 修复：数字人口播文本变化后不能按镜头位置复用旧媒体

- 现象：口播中的“第一步”被读成“前排步”；用户修改自动口播后再次生成，成片仍播放修改前的内容；片段边界处很短的最后一句字幕在时间轴上看起来与下一条重叠。
- 原因：通用合规清洗包含过宽的“第一 → 前排”替换，误伤正常序数词；数字人分段合并在找不到相同口播时又按镜头位置匹配，并无条件复用旧音频、旧视频与任务；时间轴给所有字幕强制 24px 最小宽度，使实际不重叠的短提示块覆盖到相邻位置。
- 修复：只保留“第一名”等明确风险短语的定向清洗；镜头位置只允许延续角色参考，音频、视频与任务必须在口播逐字一致时才复用；生成前先同步编辑器，短字幕块按真实时长缩窄并使用紧凑样式。
- 规避：可变文本驱动的媒体缓存必须把规范化文本或内容签名作为身份，不能仅靠数组下标、镜头号或 DOM 位置。内容安全替换必须使用完整词义边界，回归至少包含“第一步 / 第一次 / 第一名”与改稿后音频身份变化。

## 2026-07-21 v105 修复：信息流自动字幕和源片段不能冒充最终交付

- 现象：信息流虽然产品规则为默认无字幕，部分任务进入剪辑台后仍出现旧自动字幕；供应商下载视频压缩包时得到前后两段源视频，而不是一条合成成片。
- 原因：信息流自动拼时间轴仍从口播行直接铺字幕，旧提示词字幕来源也会在重新进入时被当成受保护轨；剪辑台只有在额外的“视频任务完整”判断为真时才合成，而审核发布没有再次校验 `finalVideoUrl`，下载层又把 `clipUrls` 当作成片回退。
- 修复：信息流自动拼接只建立视频时间轴，默认清空非人工字幕；主动点击“匹配字幕”产生独立 manual 标记并可持续保留。剪辑台对任何变化后的完整时间轴执行合成，普通视频没有单一成片 URL 时禁止发布，下载层不再把多个源片段写成最终视频文件。
- 规避：原始镜头、剪辑时间轴、合成成片和发布交付必须是四个不同状态。发布和下载都必须校验单一成片身份；字幕的“默认关闭”和“用户主动建立”也必须用持久来源标记区分，不能仅以数组是否非空判断。

## 2026-07-20 v104 修复：任务轮询不能用项目更新时间驱动媒体重载或整页重绘

- 现象：视频工坊生成封面或运行任务时，已有成片反复白屏 / 重载；主平台数字人、信息流和普通视频在任务运行中也会周期性闪烁，音频、视频、封面及输入节点被反复重建。
- 原因：视频工坊把每次保存都会变化的 `project.updatedAt` 放进成片媒体签名，轮询因此持续调用 `video.load()`；主平台工坊订阅 `job:update` 后不区分进度变化与结构变化，每次轮询都执行整页 `innerHTML` 重绘。
- 修复：视频工坊媒体签名只比较成片 ID、URL、画幅、实测时长和成片自身更新时间；主平台首次进入生成中及最终成功 / 失败保留完整刷新，中间轮询只原位更新进度文字和进度条。
- 规避：媒体刷新信号必须来自媒体自身版本，不得复用项目级更新时间。轮询 UI 必须区分“进度更新”和“DOM 结构变化”；回归需要断言无变化轮询不调用媒体重载、不替换已有媒体节点，同时真实成片替换仍会刷新一次。

## 2026-07-19 v96 修复：真实口播时长只能指导后端时间线，不能反向限制导演智能

- 现象：视频工坊在修改既有成片时反复输出“信息够了，我会……”但不建立制作任务；点击“继续、什么、说啊”又重复规划。进入制作后，单个口播窗口超过 Seedance 15 秒还会硬失败，用户看到“恢复任务”却始终无法产出。
- 原因：目标时长、口播字数、固定 `1.2x` 和二次口播校准被写进导演层并在返回后再次强制修改计划；本地修订解析器又把泛化的“补镜头”和“3 个镜头”误当成旧成片局部修改。下游同时把模型单次 15 秒上限当作导演镜头上限，导致创作判断、接口技术边界和合成时间线三层互相干扰。
- 修复：恢复上一稳定版主导演协议，删除所有本轮新增的秒数、字数、语速和强制修复约束；TTS 使用自然语速，生成后以实测音频时长建立后端时间线。接口提交只做技术限长，合成使用连续运动重定时而非静止尾帧；上下文跟进复用用户自己的最近有效原话，不给模型补写新的命令。局部修订解析改为显式动作与镜头编号优先，新创作、咨询和保留定格不再被劫持。
- 规避：任何视频时长优化都必须把“导演创意决策、音频真实时长、模型接口上限、合成时间线”分层。禁止为了满足目标秒数改写口播、强制语速、强制工具或让模型重复规划；生产回归必须同时断言已经创建 pipeline task，而不能只看 `/api/chat` 返回 200 或页面出现规划文字。

## 2026-07-19 v95 部署避坑：Git 重命名不能按普通增量文件解析

- 现象：v95 第一份增量包的普通代码已是新版，但无限画布新 build ID 目录缺少两个由旧构建目录重命名而来的 manifest，旧哈希 chunk 也没有按预期移除。服务健康但子应用静态闭包不完整，如果只看主页探活会误判为已部署成功。
- 原因：打包器把 Git `R100` 记录当作普通路径处理，没有同时执行“携带新路径 + 移除旧路径”；删除清单还保留了仓库上层前缀，与服务运行目录的相对路径不一致。首次健康检查又使用了 sidecar 的错误路径，安全逻辑因此正确执行了一次假阴性回滚。
- 修复：改为解析 `git diff --name-status -z -M`；`M/A` 携带目标路径，`D` 移除原路径，`R` 必须同时携带新路径并移除旧路径。同步后按目标提交生成的 SHA-256 manifest 逐文件核对，并要求旧文件残留数为 0；sidecar 只使用已验证的真实健康接口。
- 规避：子应用发布不能只比较 HTML 标识；还必须验证 build manifest、新哈希 chunk、动态路由 chunk 均返回 200，以及旧哈希文件已精确清理。回滚包只恢复代码 / 静态文件，数据库与运行目录永不随代码回滚。

## 2026-07-19 v95 修复：静态导出页面不能把运行时项目 ID 当成真实文件路径

- 现象：无限画布首页可以创建项目，但退出后再次点击项目卡会在 iframe 中出现浏览器通用错误页；刷新详情或在 hydration 尚未完成时点击更容易复现。
- 原因：静态构建只提供 `/XZ-Design/` 入口，不会为每个运行时项目 ID 生成 `/project/{id}` HTML。项目卡虽然在 React 点击事件里写入 hash，但其原生 `href` 丢失了 `/XZ-Design/` 基路径与 `embed=1`；只要事件尚未绑定、浏览器执行链接兜底或发生恢复竞态，就会请求服务器必然 404 的真实路径。
- 修复：静态 / 嵌入模式的 `projectHref()` 与 `homeHref()` 直接生成可独立打开、刷新和复制的 `/XZ-Design/?embed=1#/...` 地址；程序化导航继续复用同一 hash 契约，不再依赖 `preventDefault()` 把错误链接临时救回来。
- 规避：静态 SPA 的每一个可点击链接都必须在 JavaScript 未运行时仍指向真实存在的入口。路由测试不能只断言点击回调，还要覆盖原生 `href`、静态导出、退出重开、详情刷新、后退 / 前进和 iframe 内无错误页。

## 2026-07-19 v95 修复：轻量项目摘要同步不能丢掉服务器新推导的缩略图

- 现象：无限画布项目和图片节点都存在，项目也能正常打开，但首页在刷新后把原本缩略图显示成黑块、白块或只有尺寸的占位；再次进入项目并不会丢图。
- 原因：服务端已经能从 owner-scoped 完整草稿中的稳定 blob URL 推导缩略图，但前端遇到本地 clean IndexedDB 项目时只更新修订号，没有把服务器返回的 `thumbnailUrl` 等轻量摘要字段合并回来。于是列表继续使用不含图片的旧本地摘要，表现得像图片消失。
- 修复：clean-IDB 分支也合并经过净化的服务器项目摘要，完整 `items / messages / viewport` 仍由本地缓存和服务器草稿协议管理；服务端缩略图只引用当前 owner 的稳定 blob URL，按生成图、参考图顺序选择，不把 data URL 写入列表响应。图片删除后重新推导，纯文本项目保留占位。
- 规避：同步协议必须区分“完整详情权威状态”和“服务端可重新推导的展示元数据”。本地无脏修改不等于可以忽略服务器新增的缩略图、状态或版本字段；回归应使用真实图片 blob，覆盖连续刷新、项目内返回、图片删除回退与纯文本项目。

## 2026-07-19 v94 修复：模型稳定性不能依赖无差别重试或本地模板

- 现象：临时网络波动、提供方繁忙、空响应或损坏 JSON 会让模型功能偶发失败；如果每一层都自行重试，同一次点击又可能产生多次重复请求。更危险的是，用本地模板吞掉错误会让界面显示“已生成”，却不是当前标题对应的真实模型结果。
- 原因：主浏览器、服务端代理、视频导演和无限画布过去各有不同的异常判断；部分路径对所有错误重试，部分路径完全不重试，业务 JSON 校验又可能与传输层重复重试。鉴权、额度和配置错误被重试只会增加等待与费用，模板兜底则掩盖真实故障。
- 修复：四条模型链路统一为最多一次短延迟瞬态恢复，只覆盖网络故障、`408 / 425 / 429 / 5xx`、提供方临时繁忙、空响应和损坏业务 JSON；`401 / 403`、配置、余额 / 额度、明确超时直接返回。传输层与语义层分工，严格视频发布只在真实模型输出违反标题 / 正文契约时再请求一次，提示词、温度和模型参数保持不变，最终失败保留用户输入并显示可读错误。
- 规避：稳定性测试必须同时统计请求次数、错误分类、模型来源和输入保留。生产“AI 生成”只允许 `source=llm` 进入成功态；重试预算只能由一个明确层级拥有，不能把慢、贵、重复或模板伪装当成稳定。

## 2026-07-19 v94 修复：浏览器迁移不能成为画布图片的唯一副本

- 现象：v93 把完整画布从 `localStorage` 拆入 IndexedDB 后，现实浏览器出现“项目摘要仍在、进入后图片节点为空”。如果空详情继续参与保存，还可能把唯一剩余的非空副本覆盖掉。
- 原因：旧迁移只有“写事务结束”这一道门，没有不可变 legacy 备份、逐项目 read-back、关键图片 URL 校验、namespace 变化恢复和服务器完整草稿副本。浏览器存储升级、配额、清理或 origin 变化中的任一失败，都可能让摘要与详情失去原子性。
- 修复：迁移改为“原样备份 → 写 IDB → 逐项目读回核对 → 持久化迁移清单 → 收敛摘要”的两阶段协议；任一步失败都保留旧完整 payload。摘要有而详情空时自动搜索 legacy key、历史 namespace 与备份，无法恢复则阻止空画布渲染 / 上传并显示重试和诊断。完整草稿同时写入 owner-scoped 服务器，浏览器只作缓存；修订合并、墓碑、单飞保存和迟到响应保护共同阻止空覆盖与删除后复活。
- 文件边界：data URL 图片转换为成员私有 `server/canvas_blobs/`，读取只通过短期 HttpOnly 会话和稳定同源 URL。该目录是生产业务数据，部署、回滚和代码同步必须像 SQLite、uploads、composed、视频 runtime 一样保护；事务失败只能删除本次新建且没有已提交数据库引用的文件，不能删除预先存在的可恢复孤儿。
- 规避：任何浏览器大状态迁移都必须准备真实旧版本多项目夹具，覆盖 Base64、配额失败、读回失败、namespace 改变、刷新 / 重登、A / B 浏览器、删除墓碑和空快照。服务器完整副本确认可用前，永远不能先删除浏览器旧副本。

## 2026-07-19 v94 修复：模型失败不能用本地模板伪装为“已生成”

- 现象：视频工坊发布阶段点击“根据标题生成文案”，语言模型配置异常或返回内容不合格时仍出现一段通用文案，用户无法分辨它并非模型根据当前标题生成。
- 原因：通用 `generateCopy` 为旧离线 / 演示场景保留了语义兜底，定制发布未声明严格模式，错误被 `_mockCopy` 吞掉并伪装为成功。
- 修复：视频工坊发布显式使用 `requireLlm: true`；请求失败、JSON 不完整、标题或正文与主题不匹配都会抛出可读错误，恢复并保留用户原文案。只有 `AI.lastSource=llm` 才视为生成成功；真实提供方验收是发布门槛。
- 规避：凡按钮文案声明“AI 生成 / 根据标题生成”，必须区分 `llm / fallback / error` 来源。生产功能不能把 fallback 当模型成功，回归同时覆盖真实请求、无效模型响应、网络失败与输入保留。

## 2026-07-19 v94 修复：登录首屏不能被全量缓存扫描和落库阻塞

- 现象：服务器资源空闲、健康接口毫秒级，但管理员登录、草稿箱和定制子应用历史恢复仍偶发等待很久；旧实现还可能在延迟集合未到达时显示假的空列表。
- 原因：登录前先扫描本地全部 IndexedDB，验证后又下载约 7 MB 全量状态并逐集合同步落库；供应商首包也包含全部资产。网络、解析和缓存写入被串联到首屏关键路径。
- 修复：启动只读取身份与本地通知；远端 bootstrap 只取身份、账号、产品、音色和必要配置，重集合后台分组水合，网络请求与 IndexedDB 写入解耦。供应商资产也延迟加载；未水合页面显示可重试状态，成员切换时用 generation / member / token 拦截旧响应和旧缓存写入。草稿分批渲染并延迟解码封面。
- 规避：性能优化不能牺牲正确性。登录门只等待身份、权限与首屏必需状态；任何延迟集合在完成前必须显示“同步中 / 可重试”，不得把未加载等价为业务空数据。

## 2026-07-18 v93 上线事故：画布项目摘要与完整节点分拆时必须可校验、可回滚

- 现象：升级 v93 后，无限画布的历史项目卡仍然存在，但进入项目后显示空画布，原有图片节点不见。视频工坊的项目与成片经后续复核正常，不属于同一类数据丢失。
- 原因：v93 在浏览器内把项目摘要留在 `localStorage`，将 `items / messages / viewport` 迁到按成员分仓的 IndexedDB。迁移只等待写事务结束，没有逐项目读回、数量 / 关键 URL 校验、不可变 legacy 备份键或 namespace 变化恢复。摘要先成功收敛之后，详情读取失败就会呈现“有项目壳、无图片”。未发布画布详情当前又是浏览器 owner-scoped 状态，服务器只有已发布摘要，无法从业务数据库直接还原完整节点。
- 处置：生产数据库、上传、成片和子应用运行目录均不修改。代码修复必须先搜索旧 key / 旧 namespace / legacy 备份，写入 IndexedDB 后逐项目读回校验，全部通过后才可截短 localStorage；摘要存在但详情为空时要显式报恢复失败，不能把空画布再写回覆盖。
- 规避：浏览器大状态迁移不能只验证“新库已创建”。必须覆盖多项目、Base64 / 服务器 URL、配额不足、事务中断、读回失败、成员 namespace 变化、刷新 / 重登和空状态防覆盖。长期应将完整 owner-scoped 项目存到服务器，IndexedDB 只做可丢弃缓存。

## 2026-07-18 v93 上线性能回归：登录门不应等待全量工作区落库

- 现象：子应用合并后用户明显感到登录时间变长，草稿页打开和历史媒体首次恢复也比旧版慢。服务器负载、内存、磁盘和健康延迟正常，媒体文件存在并能返回 `206 Partial Content`，排除了公网带宽或服务器资源不足。
- 原因：远端登录在放行界面前必须等待完整 `/api/state`；当前管理员快照约 7 MB，含数千资产、数百任务和生产单。客户端解析后又按集合顺序执行 IndexedDB `replaceAll`，把网络、JSON 解析和全量本地落库全部放在登录关键路径。草稿列表还一次渲染全部行并立即请求所有封面，没有分页 / 虚拟化和图片懒加载。
- 处置：保持服务器及线上数据不动，将问题回传代码任务。修复应提供轻量 bootstrap 或 `collections / since / cursor`，先返回身份、账号、产品与首屏必需状态，其他集合按页面 / 增量后台加载；IndexedDB 写入分片且不阻塞首屏。草稿按日期默认折叠、分页或虚拟化，封面使用 `loading=lazy` / 异步解码，视频仅在展开或进入详情时创建。
- 规避：登录成功标准应是“身份、权限与首屏必需状态可用”，不是“全部历史数据已重写到浏览器”。性能验收要记录鉴权、快照 TTFB / 下载 / 解析、IndexedDB 和 first render 分段时间，并用真实大数据量冷启动回归，不能只看健康接口。

## 2026-07-18 v93 新增避坑：已知口播不应再让 ASR 决定字幕内容和时间轴

- 现象：数字人已有逐段生成的清晰 MP3 和确定口播原文，Whisper 仍会误识、漏识或整段偏移；信息流依赖 ASR 又会因为前后片段声场、模型可用性或口播写法变化而时好时坏。多套 Whisper、VAD、提示词白名单和均分兜底同时存在时，还会出现旧异步结果覆盖新时间轴。
- 原因：数字人的内容真源本来就是分段口播文本，但旧链路又让通用 ASR 重新猜测内容；信息流提示词已包含时间段与真实说话描述，旧链路却把它仅作为 ASR 的辅助白名单，导致上下游两个系统互相修正但都不是唯一真源。
- 修复：主平台删除 Whisper / VAD 自动字幕接口、前端调用、安装脚本和健康检查。数字人以分段口播原文为内容真源，以每段实测音频 / 视频时长为片段边界，再按标点和字数分配。信息流只读取原提示词时间段中“真实说话主体 + 冒号 / 说话动词 + 口播”，严格排除 UI、道具文字、元数据、导演说明与负面要求。人工轨永不被自动流程覆盖，来源不可靠就返回空轨。
- 规避：当内容和时间边界都已知时，不要再引入一个通用识别模型作为真源。字幕测试必须同时覆盖完整内容、单调时间、片段边界、时间轴变化失效、人工轨保护和猜测字幕为零。视频工坊用户主动上传的任意外部音频是另一类需求，其隔离 `faster-whisper` 不得回流主字幕链。

## 2026-07-18 v93 新增避坑：同一会话的发布状态必须绑定成片身份

- 现象：视频工坊同一个对话可能多次修改并生成多个成片。如果发布状态只保存在项目 / 对话层，发布任意一个后其他成片也会显示“已发布”，撤回其中一个又可能清空整个对话的标记。
- 原因：旧 `publishedDeliveryId / publishedCount` 是项目汇总字段，可以回答“这个对话发布过几次”，但不能回答“哪一个成片对应哪一条发布记录”。
- 修复：每次生成创建独立 `deliveryId` 和 `outputId`，发布弹窗把 `sourceDeliveryId / sourceOutputId` 传入服务端事务，项目以 `publishedVideoOutputs[outputId]` 记录对应交付、时间与状态。当前成片、历史列表、已 / 未发布筛选和撤回全部逐 `outputId` 查询；撤回时从尚存交付重建映射，不动其他成片。
- 规避：项目级发布计数只能用于汇总，不能驱动单个产物的 UI。凡是一个项目可产生多个可发布成果，必须对“发布 A、发布 B、撤回 B，A 仍已发布”做原子回归。

## 2026-07-18 v93 新增避坑：子应用慢加载应先减少数据和解析量，不能先拆掉隔离边界

- 现象：服务器同一个子应用有时秒开、有时长时间等待。直接把 iframe 改为主平台原生 DOM，或把 sidecar 合并进主服务，可能因样式、事件、本地存储、Cookie 和长任务生命周期冲突引入更大不稳定。
- 原因：视频工坊慢点主要是历史项目全量解析和下载；无限画布曾把含 Base64 图片的全项目、消息和节点同步存入 `localStorage`。这些耗时与是否 iframe 无直接因果关系，前端原生挂载也不会自动解决后端全量读取。
- 修复：先增加各加载阶段耗时记录、视频成员项目索引、服务端分页摘要缓存和 `Server-Timing`；无限画布只在 `localStorage` 保存项目摘要，完整项目迁入当前成员 IndexedDB 并按需加载，浏览器不支持时保留旧存储回退。本轮保留 iframe、`postMessage`、owner 映射与独立 sidecar，不改账号隔离和部署生命周期。
- 规避：子应用架构重构必须先用耗时证据定位网络、静态资源、状态恢复、项目摘要和完整项目的各自成本。只有低风险减载、成员隔离、版本升级和回退开关全部验证后，才能灰度尝试原生挂载；后端仍应在最后阶段评估是否合并。

## 2026-07-18 v92.2 新增避坑：角色可见子集不能用于重新计算交付序号

- 现象：同一条素材在创作端、供应商管理员和供应商子账号视角显示不同序号；子账号没有被分配到部分内容账号后，它看到的列表又从自己的第一条重新编号。回传链接成功后，当前行仍可能继续显示“未下载 / 回传链接”，刷新后才有机会变化。
- 原因：前端按照当前角色可见的列表重新生成顺序号，把“当前页面排名”误当成素材身份；子账号的权限过滤天然会制造缺口。回传接口完成后又只依赖后续全量同步，没有把服务端返回的权威资产合并回当前同一对象，晚到的旧页面整集合快照还可能携带旧发布字段。
- 修复：新交付统一使用服务端持久化 `pubSeq`；没有该字段的旧交付先从全量交付集合生成只读 `projectedSeq`，再按角色过滤，因此允许子账号看到不连续序号但同一素材始终一致。回传接口返回完整权威资产并原位合并，立即渲染“已回传 ✓ / 改链接”；服务端以 `publishedUpdatedAt` 保护链接和状态，拒绝晚到旧快照倒退回传结果，投影字段在写入时强制剥离。
- 规避：列表序号、发布编号和外部沟通编号必须来自全局持久身份，不能按角色、筛选或分页重新编号。任何关键写操作完成后都要使用服务端返回对象更新当前状态，并同时测试即时界面、刷新 / 重登、并发旧快照和越权访问。

## 2026-07-18 v92.2 新增避坑：账号验证成功不等于工作区同步成功

- 现象：服务器较慢或 `/api/state` 临时失败时，登录按钮长时间没有明确阶段反馈；更危险的是，已经取得 token 后如果状态同步失败，页面可能留下半登录身份或错误进入空工作区。
- 原因：旧流程把远端登录与工作区拉取当成一次模糊等待，`pullRemote()` 返回 `false` 没有被视为阻断错误；token、角色与当前成员 ID 在同步完成前已部分写入，自动续登也没有统一清理失败状态。
- 修复：登录拆分为“正在验证账号 / 正在同步工作区”两阶段并锁定重复提交；`enterRemote()` 必须确认 `pullRemote() === true` 才进入应用。同步失败会清除 token、角色、当前成员和持久化 meta，保持登录门可见、展示可读错误并恢复全部控件；自动续登使用相同失败清理。
- 规避：鉴权、授权数据加载与界面放行必须是三道独立门。任何远端登录都要覆盖“验证成功但状态拉取失败”测试，不能以已有 token 或健康接口成功推断工作区已经可用。

## 2026-07-18 v92.2 新增避坑：覆盖同名资产文件后必须同步改变浏览器可见 URL

- 现象：批量微调确实再次调用图片模型并覆盖服务器文件，但页面仍可能在一小时内显示旧图，用户会误判为“重新生成没有执行”。刷新、切换页面甚至重新打开抽屉也可能继续命中同一缓存 URL。
- 原因：资产 ID 与服务器文件 URL 保持不变，而浏览器和中间缓存只按 URL 判断资源是否变化；仅更新 IndexedDB 对象或文件内容不会使已有缓存失效。
- 修复：替换资产 Blob 时同步更新内容哈希和单调递增 `blobUpdatedAt / updatedAt`；平台 `/api/files/` URL 按该修订生成稳定 `asset_rev` 查询参数。没有再次修改时 URL 不抖动，真正重生成后 URL 必变；外部、data 和 Blob URL 不附加参数，资产 ID、服务器文件名与删除语义保持原样。
- 规避：可变文件与不可变静态资源必须使用不同缓存策略。覆盖同名业务文件时要让 URL 携带内容或修订身份，并测试现有 query / hash、刷新后稳定性、连续同毫秒更新和外部 URL 不受影响。

## 2026-07-18 v92.2 新增避坑：全站禁用静态缓存和每次全量解析项目都会制造偶发慢加载

- 现象：同一服务器上视频工坊、无限画布和历史项目有时秒开、有时等待很久；服务健康正常，功能也没有固定报错。
- 原因：主服务曾给所有静态文件统一设置 `no-store`，导致已经带版本号或内容哈希的 JS、CSS、字体仍被重复下载；视频工坊每次读取历史列表还会逐个解析完整项目 JSON。简单预热 iframe 虽可缩短首开，却会改变登录 Cookie、`postMessage` 和 IndexedDB 的初始化时序。
- 修复：只对明确带 `?v=`、内容哈希或 Next 哈希路径的资源使用一年 `immutable`；HTML、未版本化文件、API、缺失响应继续禁止缓存。视频项目摘要按文件 `mtime_ns + size` 缓存，保存、外部修改和删除都精确失效；不实施 iframe 预热。
- 规避：静态缓存必须以“URL 是否不可变”为边界，不能一刀切。性能优化要优先减少重复传输和重复解析，并验证退出登录、成员切换、项目隔离、版本升级与错误响应；不要用可能提前启动业务运行时的预热换取表面速度。

## 2026-07-17 v92 新增避坑：普通 HTTP 页面不能假设 `crypto.randomUUID` 一定存在

- 现象：生产视频工坊首页可以加载，拖入图片没有预览，输入文字后点击发送或按 Enter 也没有任何反馈；服务端健康、模型配置和静态文件版本均正常，但操作后没有聊天请求到达后端。
- 原因：生产从普通 HTTP 地址访问，不属于 secure context，浏览器没有 `crypto.randomUUID()`。附件 ID 与 pending 消息 ID 直接调用该 API会同步抛错；发送函数又在异常保护之外先设置 `busy=true`，因此界面永久锁住且没有 toast。
- 修复：统一客户端 ID 生成器，优先使用可用的 `crypto.randomUUID`，否则使用 `crypto.getRandomValues` 生成 UUID，再以时间戳、递增序号和随机串做极端兜底。发送函数从设置 busy 后立即进入 `try/finally`，附件清理、预览渲染和 pending 建立的任何同步异常都会显示错误、恢复输入/附件并解锁；拖拽、选择、粘贴和 change 事件也统一捕获异常。
- 规避：浏览器端不得把 secure-context-only API 当作无条件基础能力。凡在网络请求前创建 ID、读取文件或更新 UI，都要在不支持该 API的真实 HTTP 环境测试；busy 必须只有一个 `finally` 负责释放，不能从 fetch 之后才开始异常保护。

## 2026-07-17 v92 新增避坑：字幕解析只能读取高质量视频提示词，不能清洗或重写它

- 现象：为方便字幕提取增加双引号、固定口播格式或硬性 A/B 规则后，原本高质量的信息流提示词出现结构变窄、自然台词减少、用词被替换、箭头被删除或句子被截断；只按冒号后文本抓字幕又会把 `台词风格：短促自然`、`口播要点：先检查字段` 和 UI 文案误当口播。
- 原因：视频创意生成、文本清洗和字幕定位三个层次混在一起。下游解析需求反向改变上游 prompt schema，通用小红书清洗器又被用于视频导演文本；简单冒号正则没有理解主体、说话动词和界面上下文。
- 修复：恢复原信息流六类创意引擎、系统提示、随机度与重试策略，删除所有字幕专用生成约束和破坏性后处理。字幕改为独立只读解析器：引号和明确口播字段优先；冒号候选必须有真实角色主体与说话证据；自然无冒号写法要求“人物 + 说/问/喊/回答”等严格结构；元数据、导演说明、UI 显示和 B 面软件界面全部拦截。
- 规避：提示词质量与字幕召回必须分开验收。每次字幕修复都要同时跑创意提示词质量样本、逐字不改写断言、A/B 角色分工、真实台词召回和 UI/元数据误判对抗样例；宁可漏掉不可靠字幕，也不能污染提示词或把界面文字烧进成片。

## 2026-07-17 v92 新增避坑：开发机存在本地 `.venv` 不等于部署包泄漏运行环境

- 现象：全部功能测试通过，但部署卫生测试因为 `apps/video-workshop/.venv` 在开发机存在而失败；直接删除它虽然能让测试变绿，却会破坏本地 sidecar 启动环境，并且下一次启动还会重新创建。
- 原因：测试把“工作树物理不存在”误当成“部署包不会包含”。实际上本地与非 Docker 生产启动器都需要独立虚拟环境，真正的安全边界是 Git 忽略、Docker 构建上下文排除和 rsync 文件清单排除。
- 修复：保留受保护的本地 `.venv`，测试改为断言子应用 `.gitignore`、根 `.dockerignore` 与非 Docker 部署指南均排除该目录；仍继续物理禁止 `.env.local`、Playwright 缓存、上游 `.git` 和业务运行数据进入可部署副本。
- 规避：部署卫生测试应验证实际构建/同步边界，而不是要求开发机删除正常运行依赖。部署时仍不得 force-add 虚拟环境，也不得使用 `--delete-excluded` 删除服务器独立运行环境。

## 2026-07-17 v91.1 新增避坑：语音识别依赖已安装不等于首次模型下载可用

- 现象：视频工坊健康接口显示口播转写组件已安装，但第一次上传口播音频后仍提示网络不可达；图片和视频附件已经保存并完成媒体探测，导演却停在等待可用口播。
- 原因：生产机无法直连默认模型仓库。可达镜像能够返回模型元数据，但新版下载器默认继续使用 Xet / CAS 文件重建路径，该路径返回鉴权错误；持久模型缓存又是首次创建，因此没有本地快照可回退。
- 修复：视频工坊 sidecar 单独配置可达模型镜像并禁用 Xet 下载路径，将 faster-whisper `small` 模型预热到持久模型缓存后重启服务。随后同一上传音频完成真实中文转写，参考图、口播音频和视频素材也完成了一次端到端成片。
- 规避：转写健康检查不能只验证 Python 包可导入，还必须验证“模型缓存存在或模型源可下载”以及一次真实短音频转写。模型缓存应放在部署同步之外的持久目录；切换代码版本不能清空它，也不能把大模型文件提交进仓库。

## 2026-07-17 v91.1 新增避坑：严格字幕校验前必须完整提取现有提示词中的自然口播写法

- 现象：同一条 30 秒信息流视频中，后 15 秒字幕准确，前 15 秒完全没有字幕；直接检查音轨和 Whisper 输出后确认前段有清晰连续人声。
- 原因：后段使用 `旁白：` 等明确标签，旧解析器能生成字幕白名单；前段使用“嘴角又急又无奈地说：‘……’ / 喘了一口气说：‘……’ / 闷声说：‘……’”等自然描述，解析器没有提取任何口播提示。服务端严格模式随后正确拒绝了没有白名单的 ASR 文本，因此表现为“识别不到”，而不是音频静音或 Whisper 没有运行。
- 修复：只扩展字幕后处理解析器，识别紧邻引号的明确说话动词和 `旁：` 缩写；普通标签只接受引号内文字，只有 `口播原话：` 可接受无引号文本。继续拦截界面显示文字、导演说明、字幕规则、声音设计、负面约束和任意普通引号；不修改视频提示词生成规则，不恢复全文均分字幕。
- 规避：排查“某一段无字幕”时必须分别核对真实音轨、ASR token、前端提交的 `text / hints` 和严格校验结果。Whisper 有输出但 `hints=[]` 时，应修复现有口播来源解析，不能降低音频证据阈值或要求上游视频提示词迁就字幕协议。

## 2026-07-17 v91.1 新增避坑：子应用 HTTP 客户端参数必须兼容实际运行环境，不能只依赖锁定依赖

- 现象：视频工坊界面可打开、健康状态显示模型已配置，但发送普通对话立即出现“导演请求失败”；请求甚至没有稳定进入上游语言模型阶段。
- 原因：当前本地共享环境仍可能加载旧版 `httpx 0.19.0`，而新代码直接传入只在较新版本支持的 `follow_redirects` / `proxy` 参数。标准 sidecar 虽然有独立依赖环境，手工启动、历史进程或路径漂移仍可能绕过它，形成“页面正常、请求一发就失败”的本地差异。
- 修复：创建客户端和流式请求时检查当前 `httpx` 的真实函数签名，新版使用 `proxy / follow_redirects`，旧版使用 `proxies / allow_redirects`；标准本地、Docker 和服务器部署继续保留独立 sidecar 环境与依赖范围。重启本地 sidecar 后，旧版环境也能正常构造导演请求客户端。
- 规避：依赖锁定和运行时兼容是两层保护。涉及 sidecar、手工启动或多虚拟环境的项目，健康检查除配置外还应覆盖最小客户端构造；报“模型请求失败”时先确认异常发生在本地参数、网络连接、上游 HTTP 还是响应解析阶段，不能直接判断语言 API 已挂。

## 2026-07-16 v91 新增避坑：修字幕不能反向要求视频提示词迁就字幕解析器

- 现象：为提高信息流字幕兜底命中率，如果在视频提示词生成规则里强制每个时间段写口播原话或“无口播”，会改变既有创意提示词结构，并把字幕实现细节泄漏到上游视频生成，存在模型输出变形和历史链路冲突风险。
- 原因：字幕时间定位和视频创意提示属于不同层。解析器命中率不足时直接修改提示词 schema，看似能得到更多时间标记，实际会让视频模型承担字幕协议，还可能重新引入规定文本成为字幕的旧问题。
- 修复：撤销所有为字幕新增的提示词格式约束，并增加负向回归断言。字幕层先读取真实音轨；信息流识别失败时只解析现有提示词本来就有的明确口播 / 台词 / 对白 / 旁白字段和精确时间段，不读取普通引号、导演说明、画面要求或负面约束；没有可靠候选就空轨。数字人始终以原始口播音频为真源，不使用提示词兜底。
- 规避：字幕质量问题只能在音频真源、ASR 时间锚点、显式字段解析、轨道失效和人工编辑保护中解决。除非产品需求明确改变视频提示词本身，否则不得为下游解析方便而增加上游格式协议。

## 2026-07-16 v91 新增避坑：账号种子只能初始化真正空库，不能持续同步管理员配置

- 现象：管理员删除、补充或修改账号后，重新登录可能被本地种子补回、覆盖甚至清理；内置选题库还可能在用户没有明确标题时替用户选择主题，使账号定位、默认题库和当前模型提示相互打架。
- 原因：旧初始化同时承担首次建库、版本补齐和远端清理，无法区分“还没初始化”与“管理员已经接管”；批量链路又保留默认主题、随机题目和占位标题兜底。
- 修复：账号种子仅在数据库没有任何账号且没有初始化标记时一次写入；已有账号、额外账号、管理员删除状态和编辑字段全部保持原样。删除重复种子文件和从未读取的 `position` 字段，移除默认 / 随机选题；标题为空时在 Agent 与 AI 两层直接拦截，`xhsTrendLibrary` 只在用户给出标题后提供结构参考。
- 规避：可编辑业务配置必须明确区分“首次默认值”和“持续权威值”。种子不得兼任线上迁移或同步器；需要迁移时必须另建可审计、可回滚且不覆盖人工字段的显式流程。

## 2026-07-16 v91 新增避坑：异步单图结果必须绑定图片槽位身份，不能只绑定当前界面

- 现象：用户点击某张图的“生成此图”后切到下一张继续生成，前一个请求晚返回时可能写入当前选中的槽位，造成两张图互相覆盖，看起来像重新生成影响了其他图片。
- 原因：旧回调依赖可变的当前索引和当前选中状态，没有在请求开始时冻结生产单、槽位 ID 和请求修订号。
- 修复：提交时记录目标生产单与稳定槽位身份，结果返回前再次校验槽位仍存在且请求仍是该槽位的当前修订；不匹配的旧结果不写入任何其他图片，并清理本次产生的孤立临时资产。
- 规避：所有并发生成、上传和轮询结果都必须绑定稳定业务 ID 与修订号；列表索引、当前选中项和 DOM 节点只能用于显示，不能作为异步写回依据。

## 2026-07-16 v91 新增避坑：定制子应用代理和通用集合接口不能成为权限绕行入口

- 现象：即使专用项目 API 已按 owner 隔离，若通用集合仍允许浏览器写入定制项目 / 交付，或媒体代理接受任意 URL，攻击者仍可能伪造发布状态、修改他人数据，或让服务器请求回环、内网和云元数据地址。
- 原因：子应用接入时容易只关注 iframe 与专用路由，忽略旧通用状态接口和代理的跨层能力；只校验 URL 字符串或首个响应也无法阻止 DNS / 重定向后的内网落点和超大响应。
- 修复：定制项目与交付禁止通过通用集合写入，所有高成本创作接口要求创作成员或管理员身份；代理对协议、主机解析、IP 范围、每次重定向、媒体类型、响应大小和超时逐层校验，拒绝回环、私网、链路本地和非预期内容。
- 规避：新增子应用时同时审计专用 API、通用 CRUD、文件服务、代理、重定向和后台任务入口；权限测试必须包含直接调用接口、跨 owner、匿名、高成本请求和 SSRF 变体，不能只验证 UI 上看不到按钮。

## 2026-07-16 v90 新增避坑：子应用隔离必须由登录身份和服务端 owner 校验决定，不能信任 URL 或浏览器分仓

- 现象：视频工坊、无限画布和语音生成接入同一“定制创作”入口后，如果只改 localStorage 键名、URL 查询参数或前端列表过滤，同一浏览器切换成员仍可能看到上一人的草稿、语音运行态或私有素材；把定制音色改成全员可选后，全量快照同步还会在两人并发维护时触发 403，导致本人新音色刷新后丢失。旧 TTS 生成与音色设计接口没有登录校验，匿名请求也能消耗服务器额度。
- 原因：浏览器分仓只解决正常 UI 切换，不是权限凭据；子应用自己的项目 ID、输出文件和状态没有天然继承主平台成员身份。共享定制音色又同时包含“全员可读”和“仅创建者或管理员可写”两种权限，不能继续沿用普通整集合写穿透。
- 修复：视频工坊由主平台 Bearer 登录态签发短期同源会话，项目列表、详情、附件和成片均按服务端 owner 映射校验；无限画布移除可伪造的 `?owner=`，由服务端为当前成员派生本地 namespace，真正需要跨设备或发布的项目与输出继续写入 owner-scoped API。语音页面、收藏、临时音频、参考音频和生成资产按成员隔离；定制音色全员可见，但客户端只提交当前成员可维护的单条变化，服务端拒绝非 owner 写删。TTS 配置、测试、声线查询、音色设计和生成全部要求管理员或创作成员登录。
- 规避：任何子应用接入都要同时检查“界面分仓、API owner、文件读取、写入、删除和发布”六层，不能把 query、iframe 名称或 localStorage 当作服务端授权。浏览器同机 DevTools 仍可读取本机 localStorage，这是本地缓存的固有边界；它不能因此获得其他成员的服务端项目、输出或发布资产。若未来要求抵御同机浏览器操作者，需要把画布完整草稿迁移为服务端持久化或独立受控存储。

## 2026-07-16 v90 新增避坑：定制创作发布与回撤必须使用服务器权威事务，前端限制不能作为数据保护

- 现象：两个创作者并发发布时，客户端计算的账号序号可能过期，出现交付 `exportSeq` 已是新值但文件名仍带旧序号；如果只在按钮上拦截，调用回撤 API 仍可能删除供应商已经下载或已经回传发布链接的交付。
- 原因：旧桥接先在浏览器修改账号计数和交付快照，再异步写多个集合；服务端虽然原子分配序号，但曾沿用客户端提交的名称。回撤规则也只存在于前端 `canDeleteDelivery`，专用服务端事务没有再次校验供应商下载和发布状态。
- 修复：发布在 SQLite `BEGIN IMMEDIATE` 事务中重新读取账号、分配全局发布序号和账号导出序号、按服务器序号重算权威交付名称，并一次写入账号、资产、交付和定制项目；同一 deliveryId 重试保持幂等。回撤同样在事务内校验 owner、项目、交付以及 `supplierDownloadedAt / publishedUrl / status`，已下载或已发布时返回冲突，不修改计数、分析数据或共享资产；允许回撤时才清理交付和关联并恢复草稿或上一版本。
- 规避：计数、编号、交付名称、已下载和已发布状态都必须以服务端事务内现读数据为准。前端隐藏按钮只负责交互提示，任何删除、撤回或覆盖 API 都要重复执行同一业务校验，并用并发与直接调用接口测试验证。

## 2026-07-16 v90 新增避坑：复制子应用不能复制本机密钥和运行数据，主服务与 sidecar 必须一起启动但保持端口隔离

- 现象：视频工坊和图片平台在原目录中可以运行，复制进主项目后却可能因为 `.env.local`、虚拟环境、模型缓存、项目目录和上传目录没有一起处理而在服务器失效；反过来如果整目录打包，又会把本机密钥、历史草稿、成片或大模型缓存带入部署。只启动 8787 时，视频工坊界面能打开但内部 API 返回 503。
- 原因：静态无限画布和带 Python/FFmpeg/模型依赖的视频工坊生命周期不同；旧本地启动器、Docker 和服务器脚本只管理主 FastAPI。视频工坊原有本地 BGM 目录也不是主平台共享 BGM 库，容易造成“资产页有音乐、工坊选择不到”的数据真源分裂。
- 修复：原项目保持不动，在 `apps/` 保存可维护副本、在 `vendor/` 保存无限画布部署产物；环境文件只保留空白示例，真实 Key 由根目录环境或服务器注入。8787 继续作为唯一对外入口，8765 sidecar 只绑定回环地址；Finder 启动器、服务器脚本和 Docker 同时管理两项服务并把项目、上传、成片与模型缓存放入排除 Git 的持久目录。视频工坊默认只读主平台 `DATA_DB / UPLOAD_DIR` 中的共享 BGM，拒绝口播、TTS、数字人声线、外部 URL、路径穿越和越界软链接；空曲库时正常输出无 BGM 成片。
- 规避：部署子应用前必须分别核对代码、静态产物、系统依赖、环境变量、持久目录、许可证和健康检查；同步时只能更新代码与静态资源，不能覆盖数据库、上传、成片、模型缓存或服务器私密环境。sidecar 不得单独暴露公网，OpenMontage 的 AGPL-3.0 网络使用义务也必须在正式上线前确认。

## 2026-07-16 v90 新增避坑：视频长任务不能依赖单次下载请求和进程内运行态

- 现象：真实视频项目在多个 Seedance 镜头并行生成时发生 `ReadTimeout`，已经完成的镜头和口播存在，但任务整体停在失败或运行中；用户点击恢复后只指定镜头 2，镜头 4 同样缺失却没有继续。服务进程重启后，持久化状态仍可能写着 running，但实际协程已经不存在。
- 原因：旧下载把远端视频一次性读入最终文件，长响应超时会留下不完整文件；运行态只记录在当前 Python 进程的异步任务中，重启后没有孤儿状态修复。重试逻辑又把“从某个镜头恢复”误解为“只允许生成这个镜头”，没有继续扫描其他缺失文件。
- 修复：远端视频分块写入 `.part`，对超时和临时网关失败做有界重试与退避，完整下载后原子替换最终文件；启动和项目读取时识别没有真实活跃任务的 running 状态并转为可恢复。恢复时保留口播和全部已完成镜头，从用户指定或第一个缺失镜头开始，同时继续其余缺失镜头；同一项目只允许一个活跃制作任务，避免重复排队。
- 规避：分钟级媒体任务必须同时拥有持久状态、进程级活跃映射、原子下载、文件完整性、重启恢复和幂等重试；“已有文件”只能在通过有效媒体探测后复用。恢复入口应扫描整个产物集合，不能把一个起始镜头号当作唯一允许执行的镜头。

## 2026-07-16 v90 新增避坑：导演镜头总和不等于成片时长，必须以真实口播音频做最终约束

- 现象：真实项目的导演计划写了 40 秒，六个镜头时长合计也是 40 秒，但口播只有 108 个有效中文字，生成 MP3 实测 24.696 秒；合成按真实口播主时间线工作，因此最终成片只有 24.7 秒。镜头表面满足时长会掩盖口播不足。
- 原因：旧导演提示只要求填写 `duration_sec` 和镜头时长，没有约束主题创作的口播字量；TTS 固定语速，合成前也没有把用户明确时长与实际音频进行比较。用镜头时长、静帧或静音补齐会破坏连续口播和用户对节奏的预期。
- 修复：从用户消息提取明确总时长并排除 `0-2s / 3-6s` 等分镜时间码；主题创作按自然语速给出口播有效字量范围，明显不合格时只允许一次结构化校准。TTS 只在 `0.85-1.15` 内小幅调速，生成后用 ffprobe 实测口播并执行 ±10% 容差，不合格就停止合成。用户上传口播仍只做格式标准化，继续作为唯一主时间轴，不改写、不拉伸。
- 规避：目标时长验收必须比较“用户明确时长、口播文本容量、TTS 返回时长、媒体实测时长、最终成片时长”五层；镜头时长和转场只能服从音频时间线。不能用长静音、静帧、循环画面或异常语速把不合格口播伪装成目标时长。

## 2026-07-16 v90 新增避坑：嵌入页隐藏条件和样式缓存必须在首屏 CSS 前同时生效

- 现象：主平台已经增加自己的视频工坊顶部切换栏，子应用原来的“视频导演台 / 项目标题 / 服务状态”栏仍偶发显示，形成两层顶部；只在主服务返回 HTML 时注入嵌入标记，旧主进程或新旧缓存交错时还会出现首屏闪一下再消失。
- 原因：隐藏样式依赖 `data-platform-embedded`，但该属性曾只由当前主服务动态注入；已经运行的旧主进程、直接打开的子应用 HTML或浏览器缓存可能没有该属性。修改了 `views.css / ui-motion.css` 却保留旧查询版本时，服务器也会继续命中覆盖文字的历史规则。
- 修复：iframe URL 明确携带 `embed=1`，视频子应用在 `<head>` 最早阶段、加载 CSS 之前同步设置嵌入属性；主服务继续保留服务端注入作为第二层保障。嵌入样式直接折叠 header 所在网格行，避免只做透明隐藏；主平台本版修改过的所有样式统一提升为 `20260716-v90-1`。
- 规避：嵌入模式的首屏结构条件必须由静态 HTML 或加载 CSS 前的同步脚本建立，不能依赖异步消息或单一运行进程。任何解决布局冲突的 CSS 修改都要同步更新入口缓存标识，并用计算样式验证元素是 `display:none` 且网格不再预留高度。

## 2026-07-16 v89 新增避坑：数字人字幕必须直接分析原始口播音频，不能让成片音轨和视频时长覆盖真源

- 现象：数字人已经有清晰 MP3 口播，智能字幕仍会错位；信息流偶发出现口播没有说过的导演说明或规定文字。重新生成音频、调整片段、裁切或排序后，旧字幕还有机会晚到并覆盖新时间轴。
- 原因：旧前端只把数字人成片 URL 交给字幕接口，没有提交分段 `audioAssetId`；视频 metadata 又会覆盖口播时长。服务端把整段已知文案作为 Whisper initial prompt，低匹配时还可能用 VAD 把全文平均铺入音轨。本机 whisper.cpp 的 Metal 推理实际会崩溃，旧返回却可能因二进制存在而继续标成 Whisper，掩盖了已经降级的事实。字幕只绑定片段序号，没有稳定 `clipId` 和异步修订号。
- 修复：数字人优先把原始 `audioUrl / audioDataUrl` 交给字幕接口，视频只作为信息流声轨来源；视频时长与口播时长分开保存。Whisper 不再接收整段文案提示，已知台词只用于识别后的 token 锚点校正；独立音频或严格信息流没有真实锚点时返回空轨，不再均摊全文。macOS 默认使用 CPU 推理，其他平台 GPU 失败只重试一次 CPU；接口返回真实 `engine / inputSource / clipSources`。字幕绑定稳定 `clipId`，裁切、排序、拆分、重新生成音频会递增修订号并丢弃旧异步结果；多条台词在同一字幕轨上强制不重叠。
- 规避：媒体链路必须分别保存原始口播、视频原声和 BGM 的真源及持续时间；“可执行文件存在”不能当作识别成功。任何自动字幕都要同时验证输入源、真实识别引擎、token 锚点、片段身份和请求修订号，低质量时宁可空轨也不能伪造完整字幕。

## 2026-07-16 v89 新增避坑：字幕时间轴和最终合成必须使用同一裁切与转场语义

- 现象：剪辑台调整片段入点或时长后，字幕看起来已经移动，但最终成片仍按未裁切素材拼接；数字人片段之间的叠化还会让下一段口播提前进入，产生“字幕晚于声音”的错觉。
- 原因：前端时间轴记录了 `trimIn / dur`，旧合成预处理没有真正执行裁切；数字人预览和合成沿用了 0.35 秒交叉叠化，口播音轨也被一起交叉。
- 修复：服务端预处理每段视频时实际执行 `trimIn` 和 `dur`；字幕分析使用同样的裁切范围。数字人口播片段改为零转场硬切，避免相邻人声重叠；人工字幕继续保留，自动派生轨在时间轴结构变化时失效并重新识别。
- 规避：字幕、预览和最终合成必须共享同一套片段入点、时长、排序和转场参数；有连续口播的片段不能默认使用会混叠声音的视觉转场。

## 2026-07-16 v89 新增避坑：服务器托管语言模型不能被浏览器旧配置覆盖，也不能把失败伪装成本地模板成功

- 现象：服务器图文工作台输入标题后点击生成，正文没有由语言模型重写，页面却直接出现本地模板提示词；同一功能在本地和服务器表现不一致。
- 原因：登录后的旧浏览器语言模型 key/provider 会覆盖服务端下发的同源 `/api/chat/completions` 配置；正文来源没有明确记录，模板正文可能被误当成用户手写内容。提示词请求失败时又会静默回落本地模板，掩盖真实接口错误。
- 修复：服务端托管模式锁定同源语言模型配置，忽略浏览器陈旧覆盖；标题变化或正文为空时先调用语言模型生成正文并记录来源，成功后才把标题和正文交给图卡提示词模型。该链路设置 `requireLlm`，任一步失败都明确报错，不再显示成模型成功。多图提示词按正文结构编排，封面保留完整标题，其余图片分配互补信息并限制数量和长度。
- 规避：串联模型流程要保存每一步的来源和依赖输入，并测试真实调用顺序；服务器托管配置必须高于客户端缓存。需要模型完成的功能不能以静默模板兜底伪造成功。

## 2026-07-16 v89 新增避坑：参考图是否真正进入上游请求必须由服务端回执证明

- 现象：任务板显示已经选择参考图，但部分输出图没有明显参考；仅靠结果视觉无法判断是模型自由发挥、文件读取失败，还是请求根本没有携带参考图。
- 原因：前端只记录“计划使用”的资产 ID，图片服务没有返回实际成功读取和发送的参考数量；显式参考文件失效时，部分入口还会静默降级成纯文生图。
- 修复：图片请求携带 `intendedRefAssetIds`，服务端返回 `usedRefs / skippedRefs / mode`；显式选择参考图但实际使用为零时直接失败。图文、批量图片、视频封面和信息流分镜都保存并展示引用回执，数字人封面不再失败后自动移除参考图重试。
- 规避：生成参数的“意图”和“实际执行”必须分开记录；参考图类功能至少验证界面可见、服务端已读取、上游模式正确和逐产物回执四层，不能只看生成结果像不像。

## 2026-07-16 v88 新增避坑：生成使用的参考图必须与任务板展示保持同一真源

- 现象：批量图文创建任务时参考图区域看起来为空，但生成结果实际参考了账号以前的图片；只有进入微调抽屉后，旧参考图才作为“已使用参考”出现。批量视频也存在自动补入账号历史产品图、Logo、界面图的同类风险；从图文切到视频时，旧类型字段还可能留在任务数据里但界面不显示。
- 原因：任务板只展示 `sharedRefAssetIds / coverRefAssetIds / accountRefAssetIds`，生成器却额外合并 `imageStyleAssetId`、按名称和标签推断的账号默认参考图及旧 `sharedRefAssetId`。界面展示和真实 provider 入参不是同一个数据源，类型切换也没有清理不兼容字段。
- 修复：批量图文和批量视频都只读取任务板明确选择的统一与账号定制参考图；新成图按图片槽位保存实际引用快照，微调和重生成沿用快照。内容类型切换立即清空另一类型的统一参考和账号定制参考；批量视频禁止回落账号默认素材，数字人角色图作为明确账号身份配置继续保留。
- 规避：任何生成参数都必须满足“界面可见、确认前可删、生成后可追溯”三项。新增参考素材来源时不能只改 provider 入参，必须同时补任务板展示、类型切换清理、逐产物快照和重生成测试；历史已完成产物没有快照时不能用当前选择伪造旧生成依据。

## 2026-07-16 v87 新增避坑：共享素材不能继续依赖账号归属决定可见性和删除范围

- 现象：BGM 与剪辑素材实际应供所有剪辑台共用，但旧资产仍按账号分组和筛选；其他账号的剪辑台选不到这些 BGM。更危险的是，删除原账号时可能连同仍被全平台使用的 BGM 或剪辑素材一起删除。
- 原因：旧逻辑把 `accountId` 同时当作素材展示归属、权限边界和级联删除依据，没有区分“账号私有资产”与“平台共享剪辑资源”；历史上传记录即使内容已经属于 BGM / 剪辑素材，仍保留旧账号绑定。
- 修复：按素材类型、名称与标签识别共享 BGM 和剪辑素材，前后端都让其跨账号可见；共享库不再提供账号筛选或分配入口。删除账号时这类历史素材只清空旧账号绑定并保留资产与文件，其他私有素材继续按原规则处理。
- 规避：资产权限和删除策略必须由业务角色决定，不能只依赖 `accountId`。新增共享资产类型时至少验证“跨账号可见、普通私有资产不可见、剪辑器可选择、删除原账号后共享资产仍存在”四条链路。

## 2026-07-16 v86 新增避坑：字幕纠错只能替换内容，不能丢失词级时间或输出未验证提示词

- 现象：数字人与信息流字幕虽然经过 Whisper 识别，仍会整句提前或滞后；信息流还可能出现口播中没有的提示词说明、规定文本或负面约束。
- 原因：whisper.cpp 已启用按词拆分，但服务端只读取普通 JSON 的段级起止时间，没有读取 token 时间戳；前端随后又把识别结果取整到 0.1 秒并按游标重新排列。信息流把脚本句和提示词候选文字合并后用于兜底，即使音轨没有说出也可能被均分到整段视频。
- 修复：启用 whisper.cpp 完整 JSON，读取 token 起止并展开为中文字符时间锚点；已知口播作为唯一字幕内容白名单，ASR 只负责确认和定位。信息流再用提示词时间线限定搜索窗口，并采用更高匹配阈值；未确认的口播不输出字幕。精确轨保留 0.01 秒时间且不再被旧归一化逻辑重排。
- 规避：`split-on-word` 参数不代表调用方已经获得词级时间，必须检查真实输出结构和解析代码。任何提示词辅助字幕都必须遵循“内容白名单 + 音频证据 + 时间窗口”三重约束；低质量时宁可没有自动字幕，也不能把导演说明伪装成口播。

## 2026-07-16 v86 新增避坑：取消账号分类后必须同时删除模型示例、路由和筛选链路

- 现象：界面已经不再使用旧账号分类，但 `prompts.js` 仍向模型举 `宝妈 / 宝爸` 等目标人群例子，Agent 仍按 `职场效率 / 家庭管理` 等标签识别指令、筛选账号并把标签传入模型，导致账号真实风格与旧分类同时约束输出。
- 原因：旧分类不只存在于账号数据字段，还分散在随机方向池、Markdown 账号解析、批量建议、意图路由、账号匹配、任务变体和状态示例中；只隐藏 UI 或清空一个常量无法消除模型干扰。
- 修复：删除活动代码中的分类常量、目标人群示例、标签路由、标签筛号、标签展示和模型字段；创作变体改为使用账号真实 `styleProfile`。内置种子和旧迁移逻辑也不再创建或携带 `qtags`；不批量改写历史账号记录，旧数据即使仍含该字段也不再被业务代码消费。
- 规避：取消业务分类时应扫描“生成提示、兜底池、解析 schema、路由参数、筛选条件、界面展示、种子同步”完整链路；历史数据兼容和活动逻辑清除要分开处理，避免为清字段而覆盖线上账号。

## 2026-07-16 v85 新增避坑：最终成片缓存必须包含全部音轨参数并保留片段原声

- 现象：数字人剪辑加入 BGM 后，审核成片只剩 BGM、原口播消失；回到剪辑台调整 BGM 再审核，仍看到最初合成的旧版本。信息流重新启用人声识别时还存在低质量字符进入字幕轨和页面重复识别的风险。
- 原因：服务端有 BGM 时只映射外部音频输入，丢弃数字人视频片段自带的口播音轨；前端成片缓存只比较字幕和时间轴，没有比较 BGM、两路音量及数字人保留原声语义。服务端输出名按秒和标题生成，短时间连续合成可能得到同名 URL。字幕识别也缺少统一的质量闸门和进行中任务复用。
- 修复：数字人以合成后视频的原声作为主口播，与循环 BGM 使用 `amix` 混合；混音配置加入缓存签名，任意音轨或音量改变都清空旧成片；输出文件名改用纳秒时间和随机后缀。字幕识别增加乱码、控制标记和幻觉句过滤，同一素材组合共享一个进行中任务，低质量识别回退到稳定估时轨，人工轨永不被自动流程覆盖。
- 规避：媒体缓存键必须覆盖所有会改变最终字节的输入；真实口播位于视频片段时不能假定独立音频资产是唯一声音源。音视频修复至少用可区分频率的实际 FFmpeg 合成验证“应保留、应混入、应排除”三类音轨，并测试连续两次合成生成不同 URL。

## 2026-07-16 v84 新增避坑：代码选择了中文字体不等于服务器已经安装字体

- 现象：v84 字幕合成代码能够选择 Noto CJK 字体名，但生产服务器最初没有任何中文字体文件，字体检测返回名称却没有有效目录，继续合成仍可能出现方框字。
- 原因：本地系统自带中文字体，而最小化服务器镜像没有安装 CJK 字体包；只做代码测试无法覆盖服务器字体资产差异。
- 修复：生产服务器补装标准 Noto CJK 字体并刷新字体缓存，随后用与应用相同的 FFmpeg `subtitles` 过滤器真实烧录中文 SRT，生成有效视频文件。
- 规避：涉及字体、编码器或系统二进制的版本，部署验收必须同时检查“代码选择结果、真实文件存在、最小实际输出”三层，不能只看函数返回的字体名。

## 2026-07-15 v84 新增避坑：登录后的账号种子补全不能覆盖人工编辑风格

- 现象：管理员在服务器修改账号创作风格后短时间内显示正常，但再次登录或状态同步后又恢复成原来的预设风格，看起来像服务器没有保存。
- 原因：登录后的账号画像补全会再次把内置账号种子中的 `styleProfile` 和 `imagePromptTemplate` 写回账号；旧数据没有“该字段已由管理员维护”的标记，因此远端成功保存也会被客户端下一轮补全覆盖。
- 修复：账号创建或编辑时写入 `styleEditedAt`；账号画像补全仍可更新模式、标签和未设置声线，但检测到人工风格时间后跳过两个风格字段。旧账号在第一次人工保存后自动进入保护状态。
- 规避：初始化种子只能补空值或未被人工接管的字段；任何可在后台编辑的配置都应有明确的来源或修改时间，不能在每次登录时无条件重放默认值。

## 2026-07-15 v84 新增避坑：导出后清理服务器文件不能使用不等待结果的请求

- 现象：资产页提示已经导出并清空，但服务器文件删除请求若失败，页面记录可能先消失，真实文件仍占用服务器空间。
- 原因：旧删除流程先移除本地资产与引用，再以不等待结果的方式发送服务器删除请求；请求失败不会阻止成功提示，也无法继续定位遗漏文件。
- 修复：服务器文件删除必须先返回成功或文件已不存在，之后才清理本地引用、二进制与远端文档；其他错误立即中止并保留未完成资产。ZIP 内没有取得任何真实文件时禁止开始清理。
- 规避：所有“导出并删除”都必须遵循“完整读取文件 → 生成归档 → 用户确认下载 → 等待服务器删除成功 → 删除索引”的顺序，不能把异步删除当作无关紧要的收尾。

## 2026-07-15 v84 新增避坑：字幕来源与页面刷新必须拥有明确的覆盖优先级

- 现象：数字人已识别字幕会在媒体时长更新时重新生成，页面持续跳动；人工改好的字幕也可能被后续识别或审核合成覆盖。
- 原因：刷新函数只判断是否存在字幕，没有把人声分析轨和人工轨视为受保护状态；素材视频与数字人还共用同一种识别入口。
- 修复：人工轨优先级最高，数字人人声分析轨在同一素材不变时保持稳定；素材视频改为脚本加片段时长估算。只有用户明确点击重新识别时才允许重建数字人自动轨。
- 规避：字幕状态必须保存来源和素材签名；元数据到达、页面渲染、路由切换和进入审核都只能归一化受保护轨道，不能隐式重做识别。

## 2026-07-15 v84 新增避坑：上游并发限制要在应用层排队而不是直接失败

- 现象：批量图片、参考图编辑或视频任务达到上游并发上限时直接报错，用户需要手动反复点击，且不同生成入口行为不一致。
- 原因：只有部分 JSON 图片请求具备简单重试，multipart 图片编辑和视频任务直接提交；本机同时触发多个批次时没有统一并发闸门。
- 修复：图片 JSON、图片 multipart 与视频提交分别进入有界信号量，并对并发、限流和暂时性网关响应等待后重试；真实业务失败仍保留明确错误，不无限吞掉异常。
- 规避：新增任何生成入口时必须同时覆盖正常提交、并发忙碌、暂时性失败和最终失败四种测试，不能只验证单任务成功。

## 2026-07-15 v83 新增避坑：生产更容易命中兜底时会暴露源码里的固定语义模板

- 现象：本地图文提示词与正文强相关，生产却反复出现同一句“内容重点”，看起来像部署了旧版本或缓存未更新。
- 原因：当前源码仍保留按“文件 / 资料 / PDF”等关键词映射固定办公案例的兜底，并由旧的“发布文案主题 / 内容重点 / 图片具体内容”外壳包装。模型正常返回时本地不明显，生产一旦走不同返回或兜底路径就稳定暴露固定句，因此并非只有静态缓存问题。
- 修复：删除关键词到固定案例的语义映射；模型和兜底都只使用最终标题、正文及逐张内容分配，输出前再清洗规划话术；账号风格压缩为一句视觉描述，不能补写内容事实。
- 规避：本地与生产差异排查要同时比较入口缓存、实际资源内容和异常/兜底路径；生成链路不得用硬编码业务案例填补语义缺口，专项测试必须显式扫描固定句和内部模板。

## 2026-07-15 v83 新增避坑：字幕文字编辑必须改变轨道来源状态

- 现象：信息流从视频声轨识别出的字幕含未说内容且时间不准；用户手动改好字幕后，媒体时长回填或进入审核合成又恢复为自动字幕。
- 原因：无独立口播音频的信息流仍复用了人声识别流程；字幕文本输入只保存文字，没有把 `subTimingSource` 改为手动，后到达的视频 metadata 会强制重建字幕轨。
- 修复：信息流改用脚本内容和片段真实时长估算字幕，并清除声音、台词、角色、镜头等提示词字段；任何文字、时间、拖动或删除操作都标记为手动轨，之后只允许归一化边界，不再自动覆盖。
- 规避：自动字幕必须区分独立音频识别、视频估时和人工编辑三种来源；异步 metadata 与页面重渲染不能拥有覆盖人工结果的权限。

## 2026-07-15 v83 新增避坑：异步媒体任务不能在页面渲染尾部自动重复启动

- 现象：进入剪辑台后页面持续跳动，字幕像在反复识别；点击下一步后长时间停在“合成成片”，失败后又可能由下一次渲染再次自动发起。
- 原因：剪辑页每次渲染尾部都会排队音频时间识别和自动合成，合成 `finally` 又整页重绘；异常或中断后持久化的进行中标记也没有进程级真实状态校验。
- 修复：同一片段组合的字幕识别只自动尝试一次，手动按钮才可强制重试；合成只由用户的下一步触发，不在渲染阶段自动执行，并增加超时、中断态恢复和按钮复位。
- 规避：长时间媒体请求必须有单一显式触发源、进程内活跃集合、请求签名去重与有界超时；渲染函数不得无条件发起长时间写操作。

## 2026-07-15 v83 新增避坑：渲染函数不得为尚未开始的生成链路创建业务占位数据

- 现象：数字人刚进入工作台就出现四个分段占位，一键生成后也沿用了 11/11/16/14 秒的碎分段，而不是尽可能接近 30 秒。
- 原因：用于显示分段的函数在每次渲染时直接调用分段规划并写回生产单，且规划时沿用逐句估时，相邻句没有以语义断句重新合并。
- 修复：显示函数改为只读，初始只显示说明空态；真正点击生成时才按句号、问号、感叹号等语义边界切分，再用目标 27 秒、上限 30 秒的规划器尽量减少分段；旧碎分段在尚未生成视频时可安全重排。
- 规避：页面渲染必须保持幂等且尽可能无副作用；占位 UI 不应转化为持久化业务对象，生成策略变更要有旧数据安全重排测试。

## 2026-07-15 v82.3 新增避坑：同一 ESM 文件带不同 query 会产生独立运行实例

- 现象：供应商真实浏览器登录成功，但页面没有发起 `/api/state`，发布清单为空；健康、登录及供应商辅助接口本身都正常。
- 原因：入口以带缓存 query 的 URL 导入 `remote.js`，`store.js` 却以无 query URL 导入同一文件。浏览器把两者视为不同 ESM，入口初始化的是一个实例，`pullRemote()` 读取另一个实例中仍为 `false` 的 `_on`，因此提前返回。
- 修复：所有直接引用共享客户端的模块统一使用无 query 的规范 `remote.js` URL，静态文件继续由服务端 `no-store` 响应并通过入口缓存标识刷新模块图；增加源码级单例回归断言和真实浏览器 `/api/state` 网络验收。
- 规避：持有登录态、连接态或内存缓存的模块必须有唯一规范 URL；不能只比较文件路径，必须同时检查 query。共享模块升级时应提升入口缓存号，而不是给同一共享模块混用不同 query。

## 2026-07-15 v82.3 新增避坑：预填旧链接时粘贴可能把新链接追加在后面

- 现象：供应商点击“改链接”并粘贴新地址后，页面仍保持原链接。
- 原因：输入框预填旧 URL 但未自动选中；直接粘贴可能追加到旧 URL 后，旧解析逻辑又只取第一个 URL，最终仍提交旧值。
- 修复：修改模式打开时自动全选旧 URL，并在混合分享文本含多个 URL 时取最后一个 URL 作为本次新链接。
- 规避：带预填值的替换型表单应明确采用覆盖交互；解析分享文案时要覆盖“旧值 + 新粘贴值”的真实输入场景，不能只测单一 URL。

## 2026-07-15 v82.2 新增避坑：供应商操作列宽要按按钮真实占用反推

- 现象：表格已经禁止横向滚动，但最左侧的下载按钮仍被操作单元格裁掉；产品、发布人、平台三列又占用过多空白，视觉松散。
- 原因：后加载样式把操作列缩到固定宽度，却保留三个按钮原来的固定宽度、内边距和间距；按钮总占用超过单元格内容宽度，`overflow: hidden` 因而裁掉第一个入口。
- 修复：前移并固定产品、发布人、平台、观看量和状态列，收窄素材与空白列；按三个按钮宽度、两个间距和单元格内边距反算操作列，下载、备注与回传按钮同步改为紧凑尺寸。
- 规避：固定表格操作列时必须计算“按钮宽度总和 + 间距 + 单元格左右内边距”，并同时检查最后加载的样式覆盖；不能只看 `colgroup` 的声明宽度。

## 2026-07-15 v82.2 新增避坑：供应商回传链接不能依赖通用前端快照回写

- 现象：供应商子账号已经回传发布链接，创作端管理员仍显示“暂无链接”；供应商修改链接后数据分析也可能继续保留旧 URL。
- 原因：旧流程先在供应商本地状态里改资产，再通过通用集合保存回写；供应商快照只包含权限裁剪后的数据，且不能安全维护创作端数据分析集合，陈旧 `updatedAt` 还可能让新字段被服务端跳过。
- 修复：新增按交付资产执行的供应商专用原子接口；服务端校验真实角色与账号分配后，在同一事务中写入资产和当前分析链接。创作端进入发布清单或分析页时获取最新快照，供应商成功后只原位更新当前行。
- 规避：跨角色业务状态不能通过裁剪后的前端快照整集合回写；应使用服务端最小权限命令接口，并测试“已分配内容可见并可写、未分配内容不可见、创作端立即可见、陈旧客户端不能覆盖新状态”。

## 2026-07-15 v82.2 新增避坑：单图账号风格不能携带内容和版式指令

- 现象：用户只要求一张产品对比图，最终提示词却附带大段账号布局、人物、卡片、排版和负面约束，反过来限制模型对用户内容的发挥。
- 原因：单图模式把完整账号视觉模板直接拼到最终提示词；该模板同时包含颜色、画风、内容结构和排版规范，无法满足“风格只影响视觉、不干涉内容”的边界。
- 修复：单图模式只从账号配置抽取少量颜色与画风词，压缩成一句视觉参考；用户提示词原样保留，布局、人物、文案、排版、负面约束和内部说明全部不再拼接。单号和批量共用同一函数。
- 规避：风格继承必须区分视觉属性与内容指令；单图最终请求应断言用户原文存在、视觉参考不超过一句，且禁止出现布局、人物和内部约束词。

## 2026-07-15 v82.1 新增避坑：嵌套网格不能用硬最小宽度挤占相邻图数列

- 现象：批量图文账号行里“填写文案”按钮覆盖“每条图数”，两项看起来叠在同一个位置。
- 原因：外层已经为图数分配独立列，但内部模式、标题和文案按钮仍保留三段硬最小宽度；当内容列被账号操作区压缩时，内部网格溢出到相邻图数列。
- 修复：外层为内容和图数预留明确宽度，内部只保留固定模式列、可收缩标题列和紧凑文案按钮列；所有控件设置最小宽度为零并在自身列内截断。
- 规避：复杂表单行增加控件时，要同时检查外层网格和嵌套网格的最小宽度；“已有独立 grid-area”不代表子元素不会越界。

## 2026-07-15 v82.1 新增避坑：生成文案的专业语气要做输出层硬校验

- 现象：模型偶发使用“兄弟们”等直播式群体称呼，或一次返回过多标签；即使大部分正文相关，整体仍不像专业测评、教学或可信种草内容。
- 原因：旧提示只要求真实经验分享，没有明确禁止直播和夸张带货话术，也没有在输出层限制标签总数。
- 修复：提示词明确要求测评、教学、种草三类结构和可验证信息；输出层拦截群体称呼与夸张话术并触发重试，标签统一移到末行且限制为 4–7 个。
- 规避：生成质量验收不能只看相关性；还要批量断言语气禁用词、信息结构、标签数量和转义字符，避免偶发输出直接进入生产链路。

## 2026-07-15 v82 新增避坑：模型规划指令不能泄漏为最终图片提示词

- 现象：图卡提示词出现字面量 `\\n`、正文第几部分、信息密度要求和“不重复封面”等内部规划句；这些内容既不是画面，也会干扰生图模型。
- 原因：规划阶段要求模型同时返回内容分配和最终提示词，解析后没有区分“生成要求”与“可直接生图的画面描述”；模型偶发返回含未转义控制字符的 JSON，第一次解析失败或把转义文本原样显示。
- 修复：最终图卡只保留直接的画面内容、文字层级和视觉风格；统一解码转义换行并过滤规划元话术，标签在进入提示词阶段前剥离；不规范 JSON 只用更严格的一行 JSON 约束重试一次。
- 规避：图文验收不能只看接口成功；必须批量断言无字面量转义、无规划关键词、无标签泄漏，并分别覆盖仅标题、标题加正文、短正文扩写和长正文压缩。

## 2026-07-15 v82 新增避坑：图片数量控件必须同步真实槽位

- 现象：用户把图片数量从 4 张改成 2 张后，下方仍保留原来的图卡数量；批量账号的张数控件还会与创作模式按钮重叠。
- 原因：旧控件只保存脚本字段，没有同步重建 `artifacts.images.items`；批量行继续使用为旧控件设计的网格列宽，新增模式切换后共用同一列。
- 修复：数量变化立即裁剪或补齐真实图卡槽位并持久化；单图模式强制一张。批量行把“多 / 单”放到标题前并为模式、标题、文案、张数和参考图操作分配独立列。
- 规避：数量选择验收必须同时比较控件值、持久化字段和实际槽位长度；批量模式至少覆盖 2、4、7 张及单图路由，不能只看按钮文字。

## 2026-07-15 v82 新增避坑：发布清单操作不能靠横向滚动补救

- 现象：供应商下载按钮被相邻按钮遮挡；加入横向滚动后虽然能看到右侧操作，却造成整张表错乱和额外拉条。
- 原因：操作列按钮宽度、间距和表格最小宽度沿用旧布局，新增备注后总宽度超过容器；用 `overflow-x` 只是隐藏了列宽分配问题。
- 修复：收紧下载、备注和回传操作，给操作列明确但紧凑的宽度，素材名允许在自身列内截断，表格容器保持无横向滚动。
- 规避：表格增加操作按钮时先重新分配列宽；桌面表格的关键操作必须同时可见，不能把横向滚动条当作默认解决方案。

## 2026-07-15 v82 新增避坑：沟通入口必须同时校验角色与双向身份

- 现象：供应商点击备注没有反馈，管理员模拟供应商视角却可能看到发送入口；原文案“回复消息”也会让首次发言像只能回应已有内容。
- 原因：旧入口只有展示层，没有完整读取、发送、已读和权限闭环；视角切换被误当成真实供应商身份。
- 修复：创作端与真实供应商成员共享同一资产消息时间线，按当前成员区分自己的右侧气泡和对方消息；管理员模拟供应商视角只读且不显示发送入口，按钮和输入统一使用“发送消息”。
- 规避：跨角色沟通功能必须用创作者、管理员、供应商母账号、供应商子账号四种身份验证可见性、发送权限、未读状态和消息归属，不能只在一种视角里验证弹窗是否打开。

## 2026-07-14 v80 新增避坑：信息流不能把本地固定剧情当模型主输出

- 现象：不同标题生成的 A 面提示词仍高度相似，甚至反复出现同一句意义不明的固定台词；用户会误以为语言模型没有理解标题和文案。
- 原因：旧链路虽然调用语言模型补文案，但 A/B 视频提示词仍由本地固定剧情片段拼装，固定台词和固定镜头自然会跨任务重复。
- 修复：改为标题 → 发布文案/口播 → A/B 视频提示词 → B 面分镜提示词的顺序语言模型链路；增加随机创意引擎、上一版相似度规避、固定句拦截、A/B 差异和分时镜头完整性校验，失败时明确要求重新生成，不再静默回退固定模板。
- 规避：创意链路验收必须用同一标题连续生成至少两次，比较剧情、台词、道具和运镜；不能只检查“接口返回成功”，也不能把离线兜底文案直接展示为模型创作结果。

## 2026-07-14 v80 新增避坑：批量创建接口必须使用强类型入参与事务写入

- 现象：供应商母账号填写子账号后点击创建没有成功反馈，页面看起来像按钮失效。
- 原因：接口把 `items` 声明为普通列表，运行时元素是字典，却继续按数据模型对象调用序列化方法而抛出异常；旧写入还逐条提交，后项失败存在前项已落库的部分成功风险。
- 修复：为子账号项增加明确请求模型，服务端统一转为字典后进入事务；整批先校验必填项和用户名唯一性，再一次提交。前端同步增加提交中、成功、失败的原位反馈和按钮禁用态。
- 规避：批量写入必须测试“后项重复时前项也不落库”；前端不能只依赖短暂 toast，关键提交必须在当前弹窗里保留可读状态。

## 2026-07-14 v80 新增避坑：发布标签不能进入图片提示词语义

- 现象：图文工作台只填标题时直接生成图片提示词，没有先得到正文；正文末尾标签也可能被当成图像画面内容。
- 原因：旧一键生成把空正文当作可直接拆图的输入，没有执行文案补全阶段；图片提示词生成又直接接收完整发布正文。
- 修复：正文为空时先生成带标签的发布文案，再更新页面和生产单；传给图片提示词模型前单独剥离 `#标签`，最终发布正文仍保留至少 4 个标签。
- 规避：图文一键生成必须分别覆盖“只有标题”和“标题+正文”两条路径，并同时断言最终正文有标签、图片提示词无标签。

## 2026-07-14 v79 新增避坑：前端引用产品事实库必须显式导入

- 现象：信息流工作台点击生成时直接提示变量未定义，看起来像语言模型不可用，实际请求尚未发出。
- 原因：工作台代码新增了产品事实库读取，但模块导入列表没有同步加入该导出。
- 修复：补齐显式导入，并将入口文案改成“重新生成提示词”，让用户知道它会重算信息流提示而不是生成另一份发布脚本。
- 规避：新增模块级依赖后必须同时跑语法检查和真实页面点击；前端运行时未定义错误要先查模块依赖，不能先归因于上游模型。

## 2026-07-14 v79 新增避坑：剪辑播放不能同时维护两个主时钟

- 现象：剪辑预览持续跳动，点击暂停后仍可能继续前进；重复进入剪辑页后更明显。
- 原因：播放头用固定间隔累加，视频和音频又各自按媒体时钟推进；页面重绘后旧定时器也可能继续存活，多套循环同时改写预览状态。
- 修复：播放头统一改为动画帧加单个起始时间计算；重绘剪辑页前停止旧循环，暂停时同时取消动画帧并暂停视频、口播和 BGM；播放中不再因片段元数据更新反复重画时间轴。
- 规避：媒体编辑器只能选一个主时钟；页面重建、路由离开、暂停和播放结束都必须共享同一清理函数。

## 2026-07-14 v79 新增避坑：重新生成资源不能先删除旧资产

- 现象：数字人分段点击重新生成后，旧口播立刻消失；一旦新语音失败，页面只剩“未生成分段音频”。
- 原因：旧流程在新资产持久化完成前就清空或删除旧引用，失败路径又把生产单回退成估时状态。
- 修复：新音频生成成功后先写入生产单并持久化，再删除旧资产；失败和服务未配置路径只记录错误并保留旧分段音频。
- 规避：所有替换型生成都按“生成新资产 → 保存新引用 → 校验可读 → 删除旧资产”执行，失败路径不得破坏最后一份可用结果。

## 2026-07-14 v79 新增避坑：长下拉内部滚动不能触发全局关闭

- 现象：供应商全部账号、时间等长筛选刚滚动就关闭，用户无法到达后半段选项。
- 原因：增强下拉在窗口捕获阶段监听所有 `scroll` 并关闭菜单，菜单自己的滚动同样被当成页面滚动。
- 修复：菜单内部滚轮和触摸移动停止冒泡；全局滚动处理器识别当前菜单自身及其子节点并保持展开。
- 规避：浮层下拉既要处理外部页面滚动时关闭，也要单独验证内部滚动、触摸滚动、滚动条拖动和长列表末项可达。

## 2026-07-14 v76 新增避坑：右栏结果追加与整页重绘会叠加成溢出和跳动

- 现象：语音生成结果直接追加在右栏参数之后，按钮较多时会横向溢出；切换语音合成、音色设计、音色管理时整页重绘并立刻释放临时高度，视觉上仍会跳一下。
- 原因：结果播放器没有稳定容器和完整状态机，操作按钮也不允许换行；模式切换只在重绘前记录高度，下一帧就清除，短内容面板会让页面高度瞬时收缩。
- 修复：右栏顶部固定音频预览槽位，统一待生成、生成中、已生成和失败状态，操作按钮按可用宽度换行；模式重绘期间锁定工作区高度与滚动位置，等局部过渡完成后再释放。
- 规避：窄侧栏里的异步结果必须预留固定容器并覆盖空、加载、成功、失败四态；涉及整页节点替换的切换，验收时要连续检查工作区高度、页面横向宽度和滚动位置，不能只看最终静态画面。

## 2026-07-13 v75 新增避坑：MaaS 文生图与图生图不能共用同一 endpoint

- 现象：同一份有效私密配置下，无参考图请求被送入图生图入口并长时间超时；直接使用文生图入口可以正常返回图片，而文生图入口附带参考图又不能得到有效图片。
- 原因：服务端仅按供应商类型选择固定接口，没有在收集参考图后继续区分文生图与图生图。
- 修复：MaaS 分支在整理参考文件后选择接口；无参考图走文生图，有参考图走图生图。两类请求统一要求 JPEG base64 响应并关闭标识叠加，图生图继续携带图片数组与输入保真参数。
- 规避：图片适配器验收必须至少覆盖“无参考图、有参考图、响应解析”三项；不能只验证配置是否一致，也不能用修改私密环境配置掩盖路由错误。

## 2026-07-13 v75 新增避坑：WebGL 背景与多层背景模糊可能在悬停时产生黑块

- 现象：账号编辑弹窗常态显示正常，鼠标悬停平台或内容形式按钮后，按钮附近和弹窗局部会出现大面积黑色块。
- 原因：Galaxy WebGL 画布、遮罩层与弹窗自身的多层 `backdrop-filter` 在 Chromium 重新合成悬停图层时发生冲突；通用圆形填充悬停伪元素会进一步触发大范围重绘。
- 修复：账号弹窗使用独立不透明表面并关闭自身背景模糊，同时禁用该弹窗内分段按钮与次级按钮的扩张伪元素，只保留轻量边框和底色反馈。
- 规避：带 WebGL 背景的页面不能只截常态图；弹窗、抽屉和大面积模糊层必须补做按钮悬停截图，确认没有黑块、闪烁或相邻控件污染。

## 2026-07-13 v75 新增避坑：隐藏管理员按钮不等于完成权限收口

- 现象：创作者账号主页没有“编辑账号”，但侧边账号矩阵仍显示“创建账号”；如果只检查主页操作区，会漏掉另一条入口。
- 原因：管理员权限只应用在账号主页操作，没有覆盖上下文面板、空状态和账号弹窗的统一入口。
- 修复：账号矩阵与空状态按管理员权限渲染创建入口，账号弹窗函数本身再次校验管理员角色，避免通过全局事件绕过界面限制。
- 规避：权限验收要同时检查按钮可见性、直接函数 / 路由入口和服务端写入权限；至少用管理员与普通创作者各跑一次同页面快照。

## 2026-07-13 v74 新增避坑：并发额度不等于并行提交

- 现象：数字人并发常量已经改成 10，但点击“一键生成视频”后仍然只看到一条任务先提交，其余任务继续等待。
- 原因：调度器虽然一次选中了多个候选任务，提交阶段仍使用 `for ... await`，每个上游请求完成后才开始下一个；额度判断正确不代表请求实际并发。
- 修复：数字人与普通任务分别计算空槽，同一轮候选任务使用 `Promise.allSettled` 并行提交；单条失败继续走原有重试，不阻塞其他候选。
- 规避：以后修改并发上限必须同时检查“候选数量、提交方式、活跃任务计数”三层；不能只检查常量或 UI 上同时出现多少张卡片。
- 旧记录覆盖：2026-07-08 v54 中“OmniHuman 上限按 1 串行派发”的约束已被当前已确认的 10 并发额度替代，v74 起以本条为准。

## 2026-07-13 v74 新增避坑：参考音频不能作为 Seedance 唯一参考

- 现象：账号已经绑定参考音频，前端提示词也写了统一声线，但只提交音频时上游拒绝，容易误判为模型完全不支持音频参考，或更危险地在失败后静默去掉音频继续生成。
- 原因：Seedance 参考音频能进入请求，但上游要求它与图片或视频视觉参考共同出现，不能成为唯一参考模态。
- 修复：非数字人每段视频都携带同一参考音频，同时确保至少一条视觉参考进入提交；上游明确失败时保留错误，不允许无提示降级掉参考音频。
- 规避：参考能力必须以真实 providerRef 和轮询结果验收，不能只看前端选项、请求前对象或本地提示词；部署环境还必须确保音频和视觉资源具有上游可访问的 URL。

## 2026-07-13 v74 新增避坑：供应商隔离快照缺少原创作日期

- 现象：供应商发布清单只能显示计划发布日期或交付时间，无法显示创作端什么时候建立任务；旧交付尤其容易全部显示成同一天。
- 原因：供应商角色按权限不接收 `productions` 集合，而旧 delivered asset 也没有冗余保存 production 的 `createdAt`。
- 修复：新交付写入 `sourceCreatedAt`；旧交付由服务端在供应商快照中只读补全对应 production 创建时间，且不把完整 production 暴露给供应商。
- 规避：跨权限视图需要展示上游实体字段时，优先保存最小必要快照字段或在服务端做最小字段投影，不能为了一个日期把整个受限集合下发。

## 2026-07-12 v73 新增避坑：入口升级不等于 ES 子模块已刷新

- 现象：磁盘和服务端返回的 `chainCut.js` 已包含字幕删除、识别按钮和新布局，但浏览器刷新后仍显示旧剪辑台，容易误判为修改没有生效。
- 原因：入口 `main.js` 带新缓存号，但它继续用无版本 URL 引入 `studio.js`；创作链路又存在 `studio <-> chain*` 循环依赖，浏览器复用了旧模块图。
- 修复：`main -> studio -> chain* -> studio` 整组依赖统一使用同一个 `20260712-v73-4` 查询标识，不能只给最外层入口或单个叶子模块加版本号。
- 规避：以后修改创作链路时，部署验收必须同时检查入口 HTML、`main.js`、`studio.js` 和目标 `chain*.js` 的实际网络 URL；Playwright 应使用 `reload` 或直接 `goto`，不能把快捷键刷新是否生效当作事实。

## 2026-07-12 v73 新增避坑：Whisper 初始提示会误并字幕片段

- 现象：把完整口播脚本直接作为 Whisper initial prompt 后，约 15 秒的多句语音可能被识别成一条长字幕，时间轴虽有文本但无法跟真实说话位置对齐。
- 原因：初始提示会强烈影响解码，在短视频、静音间隔和既有脚本高度相似时，模型倾向于把提示内容整体补入同一识别段。
- 修复：先仅根据视频真实音轨做分段识别，再把每个识别段与脚本短句做后置模糊匹配和常见错字修正；识别失败时才回落到 FFmpeg 音频活动检测。
- 规避：字幕验收不能只看文字是否正确，还要抽查至少两个跨场景时间点，确认字幕出现时间、持续时间和烧录位置都与真实口播一致；旧成片必须主动点击“识别字幕”才会重跑新链路。
- 依赖边界：`whisper.cpp` 二进制与模型放在用户缓存或服务器私有运行目录，不提交模型、虚拟环境、音频中间文件或任何 API Key。

## 2026-07-12 新增避坑：批量账号操作不能只覆盖单账号分支

- 批量任务板同时存在单账号和多账号状态。只在 `matched.length === 1` 时移动资产按钮，会导致单账号看似通过、多账号实际仍保持旧布局。
- 账号专属参考图必须在每个账号行提供自己的“从资产选择”入口，按钮必须携带该账号 ID；不能用一个全局按钮再猜目标账号，也不能把账号专属素材误写到统一参考图。
- 账号名称附近只显示账号类型和平台；“从资产选择 / 取消选择”应作为该账号自己的操作固定在右侧，避免多账号时上下对应关系混乱。
- 视频账号没有“每条图数”输入，布局不得继续为空图数列预留宽度；正文应跨过空列延伸到右侧操作组前。
- 回归必须使用至少两个已选账号，检查每个账号的资产按钮、取消按钮、账号标记和目标账号 ID，不能只测单账号截图。

## 2026-07-12 新增避坑：首页指标不能只跳通用页面

- 首页等待上传、生成中、待审核、失败待重试等数字来自不同任务筛选条件，点击后不能统一跳到批量创作页，否则数字和实际落点会错位。
- 指标点击应先展示同一筛选集合的真实任务明细，再把 `activeAccountId` 和 `activeProductionId` 设置为用户选择的任务，按任务当前阶段进入对应单号工作台。
- 供应商待下载属于已交付资产，不是待处理 production；它的直达入口应进入发布清单，不能强行进入已结束的单号生成节点。

## 2026-07-12 新增避坑：启动文档长期并存会产生错误入口

- 旧本地预览文档仍描述 v3/v4、`4188`、静态 HTTP 服务和前端直连 Key，与当前 FastAPI `8787` 架构冲突。
- 启动和架构只维护 `自动化产品/运行与架构说明.md`；历史版本说明不再留在主目录制造第二真源。
- 当前必须通过 `start.command` 或 FastAPI 服务启动，不能使用 `file://`；敏感配置只能来自本地忽略文件或服务器环境。

## 2026-07-11 v71 合并记录

### 音色交互触发整页重绘

- 现象：拖动语速、切换或试听音色、收藏音色时，页面会闪一下，音色列表的滚动位置也可能改变。
- 根因：轻量交互完成后仍调用完整 `render`，导致语音页三栏和列表 DOM 被整体替换。
- 规避：参数只更新数值标签；音色选择、试听播放器和收藏态均原位同步，保留页面节点、列表节点和滚动位置。
- 验证：切换音色后页面和列表测试标记仍存在，列表 `scrollTop` 保持 `80`；收藏按钮立即切换为黑底白字。

### 批量模式分支与当前产品路径重复

- 现象：视频计划仍显示自动/自定义或标准模式选择，但当前生成流程实际只需要自定义文案，产品库也只作为参考。
- 根因：旧版前端编排字段和模式按钮仍保留，形成没有必要的决策入口；服务端没有对应的生成模式分支。
- 规避：前端统一将旧计划归一化为自定义模式，移除模式选择器和标准文案路径；保留产品事实信息作为参考，不修改接口与数据库结构。
- 验证：批量任务板中自动/标准模式按钮和产品模式选择器数量均为 `0`，历史计划打开后自动按自定义路径展示。

### 资产选择弹窗过大且缺少层次融合

- 现象：参考图选择弹窗接近整屏，纵向大图卡降低扫描效率，也与当前黑白轻量界面割裂。
- 根因：资产网格最小列宽 `128px`、缩略图比例 `3:4`，弹窗使用 `920px` 实色表面。
- 规避：共用弹窗统一为半透明磨砂表面；资产弹窗收至 `760px`，采用约 `82px` 方形缩略图与紧凑间距。
- 验证：浏览器计算样式确认弹窗背景透明度 `.82`、模糊 `28px`，网格仍完整保留选择、删除和确认操作。

### 供应商标签筛选连带 Dock 做整页过渡

- 现象：点击小红书、视频号等账号筛选时，底部 Dock 会短暂消失后重新出现。
- 根因：筛选调用 `document.startViewTransition`，浏览器会对整份文档创建旧/新快照；固定定位的 Dock 也被纳入切换，而不是只处理账号网格。
- 规避：供应商筛选不再启动整页 View Transition，仅让重新生成的 `.supplier-account-grid` 做局部淡入。
- 验证：点击平台筛选后的约 300ms 内连续采样 24 次，Dock 始终可见且尺寸非零。

### 供应商旧异步页面覆盖当前页面

- 现象：从供应商设置快速切到全部账号后，设置请求晚返回时可能把账号看板覆盖成旧页面或错误态，造成切页闪动和内容不稳定。
- 根因：三个供应商异步视图共用 `viewRoot`，请求完成后没有确认当前路由仍属于自己。
- 规避：每个异步结果写入前校验当前 `body.dataset.zone`；离开页面后，成功和失败结果都不再写入根节点。
- 验证：模拟设置请求延迟 240ms，在 20ms 后切到全部账号，最终标题和内容仍保持账号看板，Dock 持续可见。

### 隐藏辅助小字误伤药丸主标签

- 现象：初版隐藏交付和分析标签内的辅助小字时，“全部 / 仅回链”等主标签也暂时消失。
- 根因：药丸增强器会把主文本包装成 `.pill-label-content`，宽泛的 `.mode-tab > span` 规则同时命中了主文本和辅助文本。
- 规避：仅隐藏不带 `.pill-label-content` 的直接子级 `span`，并用可访问树检查按钮名称。
- 验证：数据分析四个状态标签均显示完整主文字，数量和解释小字不再占位。

## 2026-07-11 v70 合并记录

### 登录视觉层误带旧封面主体

- 现象：初版动态光束上又叠加了旧登录封面纹理，背景出现旧版大图，削弱黑白极简效果。
- 根因：把静态回退图同时用作常驻纹理层；回退素材不应在 WebGL 正常时参与合成。
- 规避：动态背景正常时只显示 WebGL 画布；静态封面仅在 WebGL 初始化失败时启用。
- 验证：桌面与移动端截图只显示黑白光束和当前登录内容，不出现旧封面主体。

### 增强下拉在黑色申请页对比不足

- 现象：申请账号中的角色选择被通用下拉增强器包装后，触发器沿用浅色页面文字色，在黑色背景上不清晰。
- 根因：登录页只覆盖了原生 `select`，没有覆盖增强器生成的 `.smart-select-trigger`。
- 规避：涉及主题切换的表单必须同时检查原生控件和增强后 DOM；登录作用域单独设置触发器、文字和箭头颜色。
- 验证：390px 移动端申请态中“创作成员”及下拉箭头清晰可见，字段无溢出。

## 2026-07-11 v69 合并记录

### 密度样式裁切音色控件

- 现象：音色分类标签和音色条目在窄栏中被遮挡，部分按钮看起来像被上一层覆盖。
- 根因：容器同时使用过低的固定高度和 `overflow: hidden`，内部内容增加后没有稳定的滚动区域。
- 规避：工具栏使用明确高度，列表区域单独纵向滚动；不要在包含交互控件的父容器上用裁切隐藏溢出。
- 验证：检查分类标签、首尾音色条目和操作按钮均完整可见，页面不产生横向溢出。

### 账号分配保存后整板闪动

- 现象：供应商管理员给自媒体账号分配子账号后，整个分配看板闪一下并跳回顶部附近。
- 根因：单项保存成功后重新渲染完整账号网格，原有 DOM、滚动位置和焦点被替换。
- 规避：保存期间只禁用当前选择器，成功后原地更新本地状态和当前控件；失败时恢复旧值，不重建整板。
- 验证：保存前后当前账号卡片保持同一 DOM 节点，滚动位置不变，选择器恢复可操作。

本文档用于让新的 Codex 会话接手“自动化产品 / 平台搭建”工作。不要在文档、提交说明、最终回复里暴露任何明文 API Key、服务器公网 IP、密码或账号凭据。

## 当前项目定位

这是一个面向小红书 / 视频号内容生产的自动化协作平台，核心链路包括：

- 单号创作：图文账号、素材视频账号、真人 / 数字人视频账号。
- 批量创作：按账号风格批量生成选题、口播、图片、视频、文案和发布清单。
- 整体资产：只放已经发布或管理员确认进入共享库的素材。
- 草稿箱 / 账号资产：未发布的生成过程素材、口播、视频、图片默认留在个人或账号草稿侧，避免污染整体资产库。
- 数据闭环：小红书链接回传后进入数据分析，后续给脚本、选题、素材生成提供优化方向。

本地固定预览端口：`http://localhost:8787/#/overview`。不要占用 `4173`。

## 2026-07-08 新增避坑：v57 自定义视频产品推断与默认 UI

- 单号视频默认“自定义文案”模式下不要显示产品选择框，也不要显示“创作内容”输入框。用户只填发布标题和发布正文，系统根据这两项生成口播、信息流 B 面、封面和视频提示词。
- 产品知识库不要删除。它仍用于标准生成、批量自动生产、品牌素材兜底、封面默认产品名、资产归类和账号默认产品；但自定义模式里只能作为后台推断和兜底，不允许抢用户标题 / 正文的主导权。
- 自定义模式推断产品时只看标题和正文中的明确产品词；识别不到就回退当前账号 / 当前生产单的默认产品。不要因为标题出现 Codex、WorkBuddy、Obsidian 等竞品词就强行替换成百度搭子。
- 信息流自定义模式要区分发布文案和视频线索。发布文案必须保留用户或模型生成的话题标签；视频提示词只吃口播 / 画面线索，不能把标签、发布正文结构或分镜图提示混进去。
- 信息流视频 prompt 可以保留 `0-3s / 3-7s / 11-15s` 这类时间分段；不能混入 `第一镜 / 第二镜 / 功能演示分镜结构` 等分镜图 prompt。固定负面约束只保留：`无字幕，不生成花字，不生成水印，不生成二维码。`
- 真人 / 数字人单号视频的发布标题是短文本区，用于和正文区域底部对齐；素材号 no-narration 布局需要单独覆盖标题、正文和封面提示词高度。注意 CSS 优先级，`#wsCopyTitle / #wsCopyBody` 的覆盖要比通用 textarea 规则更具体。
- 验证自定义视频时不要只看 UI 秒出结果。必须确认 LLM 实际回填：真人 / 数字人口播应为第一人称、明显长于发布文案、和发布文案不完全相同；素材号信息流 B 面应强相关用户标题 / 正文。
- 入口 HTML 缓存号必须随提交升级。本版对应 `20260708-v57`。

## 2026-07-08 新增避坑：v56 图文提示词清洗与单号视频自定义

- 图文图片提示词不能把内部结构定位写进最终画面文字。`封面图 / 内页 / 内页1 / 图1 / 第1张` 只用于系统拆图，不允许进入 `title`、`line` 或最终图片 prompt，否则图像模型会真的画出“内页：xxx”。
- 用户自定义标题里的 Codex、WorkBuddy、Obsidian 等同类工具名不能被统一替换成百度搭子。平替、对比、分工类选题要保留原工具名，再在提示词里表达它们和主产品的关系。
- 单号视频也要有“自定义文案 / 标准生成”切换，默认自定义。自定义模式下用户只填标题时，系统先按标题补正文，再生成口播、信息流脚本、封面和提示词。
- 单号视频自定义模式不要展示“创作内容”输入框，避免用户以为标题和创作内容都要填；只有标准生成模式才显示创作内容框。
- 自定义正文里的话题标签只属于发布文案和导出包，不属于口播或视频提示词。切分口播、生成信息流 B 面时必须先剔除 `#标签`。
- 视频交付包下载必须继续包含 `标题文案.txt`，并和视频文件一起打包，方便供应商拿到可发布标题和正文。
- 入口 HTML 缓存号必须随提交升级。本版对应 `20260708-v56`。

## 2026-07-08 新增避坑：v55 视频自定义文案与素材号 UI

- 批量创作里图文号、素材号、真人 / 数字人号默认都是“自定义文案”。默认状态下用户必须填写标题和正文；只有切回“标准生成”时才使用“创作内容”输入或四方向自动选题。
- 自定义文案不要再走默认选题池兜底。素材号要用用户标题/正文生成封面提示词、信息流发布文案和 B 面功能演示提示词；真人 / 数字人号要用用户正文切出口播镜头。
- 批量确认前要校验自定义文案是否为空。不要等后台异步跑到一半才发现用户没填标题/正文。
- 单号信息流工坊里，用户手动填写的标题是硬约束。一键生成脚本时可以发散 A 面，但标题和 B 面功能演示必须继续围绕用户填写的标题/正文。
- 素材号信息流 UI 采用两列紧凑布局：左侧标题/文案，右侧封面提示词、按钮和预览；不要再出现一侧大空白或把封面按钮挤在远离封面的地方。
- 入口 HTML 缓存号必须随提交升级。本版对应 `20260708-v55`。

## 2026-07-08 新增避坑：v54 数字人单并发、封面参考、资产清理与文案分镜 UI

- OmniHuman 数字人上游实际并发上限按 1 处理。前端一键生成多段时必须串行派发，不能同时提交多个数字人片段，否则会触发上游并发限制或网关超时。
- 数字人遇到 `Concurrent Limit`、504/TLB、网关超时或连接超时时，应按退避重试处理，并提示“上游限流/网关超时，请稍后重试”；不要误报为密钥未配置或让用户重新生成口播。
- 数字人 job 的 providerRef、status、progress、output/error 必须同步回 production 分段。刷新页面、换浏览器或服务器复测时，UI 不能只相信浏览器本地瞬时状态。
- 轮询要降频并去重。数字人真实任务建议 12-20 秒轮询一次，失败/成功后停止；不要 1 秒内重复轮询多个相同 providerRef。
- 重新生成信息流或数字人片段时，旧失败任务要标记为作废；否则新任务成功后，旧失败仍可能让整条 production 看起来失败。
- 数字人封面默认要带账号卡片里上传的“角色形象”参考图。封面生成若因角色参考图路径、请求体或上游图生图超时卡住，必须回写 failed/可重试，并允许降级为纯文本封面或提示用户移除参考图/上传封面。
- 数字人封面“生成中”不能无限挂着。页面加载时如果 loading 超过阈值且无结果，应自动恢复为 failed/可重试。
- 整体资产的账号级清理按钮必须是“先打包下载，再删除服务器图片文件和资产记录”。只能清理该账号共享图片，不要误删视频成片、发布记录、账号资料、草稿或其他成员数据。
- 整体资产不要再展示一长串历史标签筛选。默认只保留“全部 / 视频 / 图文”类型筛选，卡片标签也尽量只展示平台和视频/图文类型。
- 数据分析刷新、单条刷新和接口检测属于管理员操作；普通成员只能查看现有快照和打开发布链接。
- 视频文案分镜顶部控制台要保持紧凑：主题 / 标题 / 文案上方收窄，口播草稿和封面区域并排，封面提示词在左、封面预览在右，避免左侧或右侧大面积留白。
- 入口 HTML 缓存号必须随提交升级；服务器明明部署新代码但仍显示旧 UI 时，先查 HTML 资源版本号。

## 2026-07-08 新增避坑：v52 信息流字幕、标题发散与供应商清单

- 信息流智能混剪字幕只能从 A/B 视频提示词里的自然台词、吐槽、旁白和口播收束中抽取。不要用标题、发布文案或单独 `voiceover` 字段兜底，否则字幕会和真实视频内容不匹配。
- 信息流不是“先生成口播再生成视频”的链路。A/B 视频提示词应自由描述画面、人物动作和听到的说话内容；智能混剪负责从提示词里识别说话内容并生成字幕。
- 如果某个 A/B 分支没有任何可识别台词，宁可不给该段生成字幕，也不要把标题当字幕。更好的做法是在自然剧情分支里加入“角色说 / 吐槽 / 旁白 / 口播收束”等可拍出来的短句。
- 信息流标题需要足够发散。单号和批量都要有多种热标题结构，并引入随机 seed；同一类主题连续跑 20 次时不能只换产品名或固定句式。
- 批量信息流要和单号信息流共享同一逻辑：A 面夸张吸引人，B 面承接功能演示和分镜参考，A/B 各自根据 seed 变化，不能回到固定四段模板。
- 发布清单供应商视角里“形式”和“标签”表达重复，当前已删除“形式”列；后续不要再加回导致表格拥挤。

## 2026-07-08 新增避坑：v51 信息流、数据分析与部署缓存

- 信息流“生成视频”不能再用普通 `boards.units` 的空状态直接判断是否缺脚本；信息流自己的 `infoFlow.segments` 才是前 15 秒 / 后 15 秒脚本真源。已有两段提示词时，应先同步文本框，再重建单元并派发视频任务。
- 信息流后 15 秒功能演示分镜生成后，必须继续写回 `storyboardAssetIds`，并在 `buildMaterialUnits` 中进入后段视频 job 的 `refAssetIds`。否则用户会看到有分镜图，但视频没有参考它。
- 信息流 A 面可以夸张、自由、博眼球，但 A/B 必须共享同一个风格锚点；如果 A 面是动画/动漫风，B 面和功能演示分镜也要保持同一视觉风格。
- 发布文案提示词不要堆太多结构限制；只给主题、平台、产品、账号语气和已定内容，让语言模型自由组织标题和正文，底线是不要换题、不要把标题当正文第一句。
- 视频发布正文不要把标题作为第一段。生成、兜底和手动重生成都要清掉正文开头重复标题。
- 数据分析当前只稳定展示回链、快照、赞、藏、评和互动率；阅读量和质量分如果上游接口不稳定返回，不要放在主表格里误导用户。
- 服务器部署如果前端又显示旧“分镜工坊”或旧按钮布局，先检查入口 HTML 缓存号和静态资源版本是否同步到最新 commit；不要只重启服务。
- 本地数字人提示“角色图/音频不是公网 URL”时是正确拦截，不代表口播或角色图缺失。真实出片只能在服务器 `PUBLIC_BASE_URL` 和上游凭据齐备后验证。

## 2026-07-07 新增避坑：v49 提交完整依赖闭环

- 提交前必须以 commit 对象为准做前端模块加载检查。`api/ai.js` 引入 `normalizeCreativeTopicForMode` 时，`data/xhsTrendLibrary.js` 必须同 commit 导出该函数；只在本地工作区存在不算部署可用。
- 修复线上加载失败时，不能只改浏览器缓存或服务器静态资源；必须把缺失导出所在文件提交并推送到部署分支。
- 数字人部署验收需要 `/api/video/config` 暴露数字人配置状态、上游可达性、payload 模式和 `PUBLIC_BASE_URL` 配置状态。部署线程不能只看 Seedance 字段判断 OmniHuman 可用。
- 数字人真实验收顺序：前端能加载 -> `/api/video/config` 字段齐全 -> 角色图/分段口播音频经公开资源地址可访问 -> 提交返回 providerRef -> 轮询到真实输出。
- 如果服务端切到 JustOneAPI 代理，前端数据分析也必须同 commit 切到 `/api/analytics/justoneapi/*`；不要出现后端删除旧 `/api/analytics/fetch`、前端仍调用旧接口的半提交。
- 本地工作区很脏时，提交要显式列文件并跑 `git diff --cached --check`；不要 `git add .`。

## 2026-07-07 新增避坑：v48 数字人分段、口播一体化与部署前置

- 数字人分段 ID 必须稳定。`digitalSegmentsFromShots` 这类从分镜重建展示数据的函数不能每次都生成新 ID，否则按钮上的 `data-dh-video` 会指向旧片段，点击后误报“缺少数字人片段 / 先生成口播草稿”，即使页面上已经有音频和角色图。
- 数字人“生成视频”的正确前置顺序是：口播草稿 -> 分段口播音频 -> 角色图 -> 公网可访问 URL -> OmniHuman 提交。已有音频和角色图时，不应再提示用户去重新生成口播。
- 本地 8787 如果没有公网访问地址，数字人应明确提示角色图或音频不是公网 URL；这不是前端链路失败，也不能伪装为真实出片成功。
- OmniHuman 真实出片必须在服务器进程里同时满足：数字人接口配置可见、`PUBLIC_BASE_URL` 指向上游可访问的公开 HTTPS 地址、上传目录经 `/api/files/...` 能访问角色图和音频。`data:`、`blob:`、`localhost`、内网文件都不能直接提交。
- 部署线程验证数字人时，不能只看 `/api/video/config` 或按钮状态；必须提交 1 个分段任务、拿到 providerRef、轮询到真实输出，才算通过。
- 顶部视频创作控制台不要再做“左说明 + 右内容”的大两栏。说明文字应放到顶部，主题 / 标题正文 / 口播草稿 / 封面图在同一张卡片里满宽排布。
- 口播草稿是主题到数字人的核心输入，`一键生成 / 按口播生成文案 / 复制口播` 这类按钮应放在口播草稿框内，不要挂在右侧独立按钮列。
- 数字人主题兜底生成必须紧扣用户主题；如果语言模型不可用，也要按主题类别生成口播和文案，不能回落到“桌面文件乱 / 泛 AI 工具”这类默认无关文案。
- 服务器部署只允许更新代码和静态资源并重启服务；不得覆盖服务器数据库、上传目录、账号、资产库、发布清单、草稿、成员、数据分析、环境文件或认证缓存。

## 2026-07-04 新增避坑：v41 登录页、语音入口与数据分析部署

- 登录页已改回静态白色科技背景，不再引用 `main-login-bg.mp4`。后续不要为了视觉效果重新让登录页加载大视频，否则服务器首屏和弱设备会明显卡顿。
- 登录页 logo 使用 `自动化产品/assets/brand/xingzhen-login-logo.png`，该资产需要随代码一起部署；不要只在本机临时文件夹引用外部路径。
- “语音生成”入口必须排在侧边栏“数据分析”之后，并且是管理员专属。除了 CSS 隐藏按钮，路由层也要拦截非管理员手输 `#/voice`，避免普通成员进入管理型语音工作台。
- 数据分析本地验证中，`/api/analytics/resolve` 可直接解析小红书链接；`/api/analytics/fetch` 在本机 OpenCLI 可用时能返回真实结构化结果。服务器要可用必须让服务进程能找到 `opencli`，并且同一个运行用户完成小红书登录态授权。
- 浏览器里安装或登录了 OpenCLI 不等于服务器服务进程可用；服务器排查顺序应是：检查服务进程 PATH、检查 OpenCLI 命令可执行、检查该运行用户的小红书登录态、再用 `/api/analytics/fetch` 看真实失败原因。
- 数据分析抓不到阅读量时可以展示点赞、收藏、评论等真实可得互动指标，但不能回退模拟数据；失败也要保留真实原因。
- 不要再从前端入口导入本机 `js/local/devKeys.js` 注入开发 key。部署和本地验证都应走服务端环境文件或设置页；本地占位文件即使存在也不能写明文密钥。
- 服务器数据监测的 OpenCLI 查找已支持 `OPENCLI_BIN` / `AGENT_REACH_OPENCLI_BIN` 和常见安装路径。若线上仍提示未检测到 OpenCLI，优先在服务进程环境配置显式路径，而不是改前端数据分析逻辑。
- 语音生成页的功能切换条已经并入应用顶栏中间，与搜索 / 通知处在同一栏；不要再在页面内容区额外放一条大功能条。
- 图片生成 / 文件代理 / 视频合成等 URL 下载阶段不能硬编码 `follow_redirects` 或 `allow_redirects`。本地和服务器 `httpx` 版本可能不同，必须按 `AsyncClient.get` 方法签名动态选择重定向参数。
- 单个账号主页的“账号资产库”要同时展示：该账号下当前成员可见的未发布私有素材，以及整体资产库里归属该账号的已发布 / 共享素材；但不能把其他成员未发布私有素材暴露出来。

## 2026-07-03 新增避坑：图文联网参考与数字人口播

- 语音生成页已单独开入口，当前排在数据分析之后且仅管理员可见。页面必须是单屏工作台，不要做成长表单下滑页；二级能力用顶部小标签切换，列表内部自己滚动。
- MiniMax 音色设计接入服务端 `/api/tts/voice/design`，官方接口为 `POST /v1/voice_design`。按钮必须真调用接口，不能放装饰性按钮；本地验证要覆盖 `/api/tts/config`、`/api/tts/generate` 和 `/api/tts/voice/design`。
- 设计出来的音色存入 `voicePresets` 同步集合，按 `ownerId` 隔离；账号固定声线、数字人口播声线选择和语音生成音色库都读取同一套音色。试听音频只作为当前页面运行态，不写入服务器长期数据。
- 部署时 `voicePresets` 是新增集合，只能增量创建表/集合，不允许覆盖线上账号、资产、发布清单、草稿、任务、成员或分析数据；如果服务器缺少集合，应先备份数据库再更新代码并重启服务。
- 如果直接进入 `#/voice` 时音色库为空，先确认前端是否已刷新 `/api/tts/config`；语音页有自己的配置刷新兜底，避免进入页面时系统音色显示 0。

- 图文“联网参考小红书”命中参考后，不要再把参考强行改写成另一套新主题。参考标题和正文/摘要是第一依据，目标是保留 80% 以上的标题结构、选题关系、表达节奏和内容骨架。
- 如果参考标题本来是 `Obsidian+AI`、`AI办公`、`知识管理` 这类泛称，可以直接保留；如果参考里是 `WorkBuddy+Obsidian` 之类竞品/同类工具组合，默认弱化为 `AI+Obsidian`，除非用户明确做具体产品对比。
- 空创作内容随机只给四方向里的“方向词”，例如 `Codex 对比相关`、`Obsidian + AI 相关`、`周报效率相关`，不要随机成过细的长主题；后续脚本 / 文案再按方向展开。
- 参考原文抓不到时，左侧参考卡要保留原始标题、作者、互动和可用摘要，并提供打开 / 搜索原文入口；右侧改写不得假装拿到完整原文。

- 数字人/真人视频的随机创作内容仍必须从固定四方向选题库出发，不能回到自由随机短主题；空内容时也要先补成可执行视频 brief。
- 数字人口播脚本可以参考情绪起伏和真人停顿，但不能把 `{happy}`、`{/happy}`、`(clear-throat)` 等情绪/音效标记传给 TTS 或视频生成；进入口播、发布文案、TTS 前都要清洗。
- 数字人口播分段要尽量少切；只有口播变长时才多切。当前目标约 27 秒一段，单段不超过 30 秒，避免为了切段硬拆成过多小段。
- 声线 ID 识别只负责识别/测试，不应自动写入账号；只有用户点击“固定到账号”后才写入账号声线，并在 UI 上显示“已锁定”。
- 收藏声线要进入本地收藏列表，并在声线选择面板上方优先展示；收藏只影响本地选择效率，不改变账号固定声线。
- 视频发布标题和简介必须根据口播内容总结，不要另写一套泛泛图文文案。标题应有对象、门槛/收益/教程感和具体结果，类似“国产 Codex / AI Agent / 桌面智能体 + 上手/教程/讲清楚”的网感标题结构。

## 当前最高优先级未完成任务

### 0. 账号风格种子和小红书图文风格

2026-06-28 已升级账号风格种子到 `20260628-v9`：

- 账号总量固定为 80：50 个小红书图文账号 + 30 个视频号账号。
- 每个图文账号的风格字段按“整体风格：主风格为主、辅风格为辅；背景色、主文字、强调色；字体气质；封面/内页版式；信息结构；大留白、圆角卡片、清爽种草感；画面文字精简、大字号、清晰可读。”格式维护。
- 图文账号可以保留定位字段作为后台资料，但图文生成、图片 prompt 和图文发布文案不再使用账号定位决定内容方向；只使用本次创作内容、产品功能和账号创作风格。
- 10 个图文账号固定为简笔画 / 火柴人组图风：`XHS-08`、`XHS-10`、`XHS-13`、`XHS-23`、`XHS-28`、`XHS-33`、`XHS-36`、`XHS-43`、`XHS-46`、`XHS-49`。
- 图片 prompt 生成时 UI 展示字段可保留“整体风格”前缀，但底层要剥掉这些前缀，避免再次出现“图片风格：整体风格”的重复；最终图文图片 prompt 不应出现 `【账号定位：...】`。
- 后续更新账号风格时，不要只改颜色；必须同时区分字体气质、封面结构、内页叙事、数据结构和视觉元素。

### 1. 批量创作必须恢复成直接量产面板

用户明确不想要“聊天式问答”。批量创作入口的目标体验是：

- 用户输入“选两个图文”“创作两个图文号”“做两个图文”“找几个素材号”“量产 3 个账号”等，都应直接进入量产计划面板。
- 不要让机器人用聊天卡片解释“建议先处理哪个批次”。
- 批量创作不承担聊天问答，也不承担创建账号；只选择现有账号并生成量产任务板。
- 新会话进入批量创作时应直接显示量产任务板，而不是欢迎页或自由聊天页。
- 新建量产后消息区必须停在任务板顶部，不能自动滚到任务板底部。
- 自然语言输入只用于智能更新量产任务板，例如“帮我选择3个很久没发布内容的图文账号每一个创作3条内容”应识别为：久未发布优先、图文组、选择 3 个账号、每号 3 条、合计 9 条。
- 量产面板应包含：宣传产品、总创作内容、每个账号单独产品、每个账号单独创作内容、统一参考图、定制参考图、命中账号列表、确认执行。
- 全局“每号内容数”只是默认值；每个命中的账号行也要能单独调整本号条数，并且确认按钮和总量实时同步。
- 批量图文默认走“站内生成”路径，不默认站外回传。
- 批量工作台是独立量产界面，不能因为后台起草、站内出图、批次恢复、活跃 production 或 `returnTo` 状态，自动跳转到 `#/studio/images` 等单号图片工坊页面。只有用户明确点击“单号创作”或“进入单号工坊微调”时，才允许离开批量任务板。
- 量产任务板输入框、账号增减、参考图调整都会触发卡片局部重渲染；节点可能在 input / blur 事件中已经被别的渲染移走，替换 DOM 必须先判断节点仍有父节点，避免 `replaceWith` / `replaceChild` 竞态报错。
- 2026-06-30 新增避坑：用户说“最后10个图文号 / 倒数十个图文账号 / 前5个素材号”时，要按当前分组和排序后的账号序号选择，不能只取默认前 N 个。量产账号列表要显示稳定序号，便于用户按序号表达范围。
- 2026-06-30 新增避坑：量产账号列表内部有滚动条；点击底部账号选中/取消、调整单号条数/图数或重绘卡片时，必须同时保留消息流滚动和账号列表内部 `scrollTop`，不能跳回顶部。
- 2026-06-30 新增避坑：批量生产支持全局默认“每号内容数 / 默认图数”，也支持每个账号独立覆盖“本号条数 / 每条图数”。启动批次时要把单号图数写入 production 的 `script.imageCount`，小红书图文默认仍是 4 张，不要漏回 6 张。
- 2026-06-30 新增避坑：Agent 思考面板必须按 `sessionId` 隔离；后台会话起草时，其他新窗口 / 新会话不能显示上一会话的“思考中”步骤。
- 2026-06-30 新增避坑：重命名会话等输入弹窗内的 Backspace / Delete / Cmd+A 后删除必须只作用于输入框，不能冒泡到背景会话删除、关闭或切换逻辑。
- 2026-06-30 新增避坑：计划卡按钮、输入、参考图上传和账号点选不能只按当前 active session 找消息；多会话 / 多窗口下必须通过 `data-plan` / `data-mid` 在全部会话中找到所属消息。
- 2026-06-30 新增避坑：账号 / 标签点选应放在主点击委托的早期分支处理；如果放在后置第二个 click 监听里，容易被前面的分支、旧节点或重绘状态影响，导致“看得到但点选不生效”。
- 2026-06-30 新增避坑：in-app browser 未登录时工作台会被 `body.gated .app-shell { pointer-events:none }` 阻挡，页面在登录蒙层后面看得到但点不到；这种验证环境不能误判为账号按钮失效，真实点击验证要使用已登录浏览器。
- 2026-06-30 新增避坑：批量创作的手动账号池必须展示完整账号矩阵，而不是只展示 50 个图文号；当前固定为 80 个账号，包含 50 个小红书图文账号 + 30 个视频号账号。只有用户明确说“图文号 / 视频号 / 素材号 / 真人号”时，才按类型过滤。
- 2026-06-30 新增避坑：账号编号按账号创建顺序生成并保持全局一致，批量账号列表和单号创作左侧账号矩阵都使用同一套编号；新建账号应追加到末尾继续编号，不能因分组折叠、筛选或重绘重新从 1 开始。
- 2026-06-30 新增避坑：批量任务板的单号行包含多组带框控件，必须使用稳定网格对齐产品、条数、图数、内容、参考图状态和上传按钮；不要把参考图区做成独立随内容漂移的行，否则会出现用户截图里的参差和横向挤压。

回归测试必须覆盖至少 30 条不同中文表达，例如：

- 选两个图文
- 创作两个图文号
- 做两个图文号
- 做 3 个素材视频
- 找五个素材号
- 给我挑 4 个图文账号
- 随机选 2 个账号做图文
- 用 Dumate 做两个小红书图文
- 给学生党账号出 3 篇图文
- 给职场效率方向做 4 条视频

全部都应进入量产计划面板，而不是停留在聊天回答。

### 2. 批量参考图逻辑

批量创作需要两个参考图通道：

- 统一参考图：所有选中的账号都参考，最多 5 张，支持拖拽上传，也支持从资产选择。
- 定制参考图：单个账号专属参考，最多 3 张，支持拖拽上传，也支持从资产选择。
- “从资产选择”不要使用丑陋的原生多选框；应打开可预览图片的大资产弹窗，点击缩略图选择。
- 拖入 / 上传区域要和资产选择分开显示，不能和选择框挤在同一块里。
- 两者不冲突：最终单个账号最多 8 张参考图。
- 定制图走单独通道，不覆盖统一图；除非用户明确选择覆盖。
- 生成时要把统一图和定制图都传给站内图片 API，验证参考确实被使用。

### 3. 图片工坊链路

图文账号不再有“脚本”步骤，链路应简化为：

`图片工坊 -> 文案 -> 审核`

图片工坊要求：

- 不显示“脚本”节点。
- 站内生成时不要显示“出图回传”字样，避免误导。
- 图片占位框显示小红书竖版 3:4 的视觉比例。
- 但不要在后端强制裁剪、扩图或改尺寸。最终出图尺寸应由模型和提示词决定。
- 生成按钮尽量放在右侧，操作逻辑更清晰。
- 一键生成全部图片和单张生成都要有生成中特效。
- 失败时显示真实错误，不要只写 `Failed to fetch`。
- 站内生成默认使用图片 API；站外出图只是备选模式。
- 生成提示词时要分析总结用户创作内容，不要把用户输入原句硬塞进提示词和标题。
- 不要把内部结构词写进图片画面，例如“封面”“痛点引入”“问题引入”“图1”“第1张”等。
- 2026-06-29 新增避坑：图片正向 prompt 里不要出现“种草”“痛点”“共鸣”“构图”“封面”“首图”“步骤一”等给系统看的定位词。需要推荐时必须改写成具体功能、执行动作或可复用结果，例如“自动归类”“字段识别”“报告可用”。不要再把这些词硬塞进负面约束；底层应在生成前理解并清洗掉。
- 图文图片最终负面约束只允许保留：不出现页码、不出现二维码、图片右上角和左上角不要加入 logo，其他位置可以正常出现 logo。
- 参考图说明必须写在负面约束之前。服务端会统一清洗并重写最终负面约束，如果把参考图说明放在负面约束之后，会被服务端 guard 一并截掉。
- 图片标题和 UI 卡片标题必须使用清洗后的 prompt 标题，不要回退到脚本 `idea`，否则容易把“种草 / 痛点 / 构图 / 步骤一”带进预览标题和图片提示词。
- 站内提示词要更具体，但不要冗长到重复啰嗦。强调画面结构、信息层级、关键元素、可读文字和产品场景。
- 图片工坊重绘前必须先同步 `#imgBrief`、产品和张数到 production draft；拖入/选择参考图会触发重渲染，不能把用户已经写好的创作内容清空。
- 2026-06-29 新增避坑：如果创作内容里出现 Manus、Obsidian、WorkBuddy 等竞品 / 互补工具，最终每张图片 prompt 都必须继承这个对比、组合或分工关系，不能又退回成孤立宣传主产品。最终 prompt 应正向描述“双方职责、任务边界、组合流程”，不要把“不要孤立宣传 / 不要写错能力”这类限制句塞给图像模型。
- 2026-06-29 新增避坑：测评 / 对比 / 工具选择类选题不要做分数、星级、排名、打分表或评分卡。底层改成“适合谁 / 不适合谁 / 任务边界 / 真实证据 / 组合方式”；最终图片 prompt 不应出现评分、打分、星级、排行榜、分数等词。
- 2026-06-29 新增避坑：用户写了很多创作内容时，图文生成要先总结成信息节拍，再分布到每张图。每张图只讲一个观点 / 动作 / 证据，不要把整段用户输入塞进每张提示词。火柴人 / 简笔画 / 漫画账号要减少文字和表格密度，靠小人动作、表情、箭头和气泡讲解。
- 2026-06-29 新增避坑：单张“生成此图”只能生成该槽位；如果该槽位还没有提示词，应提示先生成图卡结构，不能暗中生成全部图卡。站内图片在后台生成完成时只保存结果，用户已经切到其他账号或页面时不能自动跳回当前工坊。
- 2026-06-30 新增避坑：如果用户点过“一键生成全部图片”，再点某一张“生成此图”，必须取消旧的全量生成队列；旧队列返回结果后不能继续把其他槽位置为 loading 或写入完成。单张生成只允许当前槽位 loading / 落库。
- 小红书图文生产默认张数为 4 张。用户手动调整后尊重用户设置；没有显式张数字段的旧链路和本地兜底也按 4 张。
- 2026-06-30 新增避坑：主题或脚本里带 Obsidian、Manus、WorkBuddy 等工具时，图片 prompt 不能只写“作参照”。必须具体写清如何结合使用或对比功能，例如 Obsidian 用于知识沉淀 / 双链 / Canvas，百度搭子用于本地桌面执行 / 文件整理 / 输出清单或报告；每张图要围绕这个分工或边界展开。
- 2026-06-30 新增避坑：批量图文即使只有一个统一创作方向，也不能让多个账号生成同款标题、首句、分点和发布文案。批量任务需要给每条 production 分配不同内容切口（痛点急救、工具分工、真实实测、模板收藏、避坑修正、前后对比、一人团队、冷静备忘等），并在文案生成时避开同批已出现的标题和首句。
- 2026-06-30 新增避坑：图片提示词要有“信息密度预算”。模型必须先把创作内容压缩成当前图数能承载的信息节拍；每张图只讲一个核心观点 / 动作 / 证据，最多 1 个大标题、1 句短解释、1 个具体动作或证据、最多 2 个短标签。内容太多时要取舍和平均分配，内容太少时补真实使用例子 / 结果证据 / 边界提醒，不要把长内容堆进单张图。

### 4. 图片 API 现状和坑

用户已确认：尺寸现在已经正常，水印也解决，模型也正确。

关键经验：

- `custom-imagemodel-gt` 对应 image-2。
- 正确路径应保持 image-2 / Responses 方式，追加 `logo_add: 0` 去水印。
- 之前“带水印但尺寸正常”的配置是正确模型，只缺 `logo_add: 0`。
- 不要再切换到错误模型或兜底模型；错误模型会出现 1:1、中文乱码、质量差、监控里没有图像模型消耗等问题。
- image-2 通常能按提示词里的尺寸出图，不需要平台自行扩图或裁剪。
- 文生图和参考图都已经可以跑通；后续改动必须同时验证无参考图和多参考图。
- 如果再次出现 1:1、乱码或无图像模型消耗，应优先检查是否走错 endpoint、模型或 fallback。
- 如果本地 UI 提示 `API 未通 (Failed to fetch)`，先查本地 8787 共享后端是否还在运行：`lsof -nP -iTCP:8787 -sTCP:LISTEN`、`curl http://127.0.0.1:8787/api/health`、`curl http://127.0.0.1:8787/api/image/config`。本地后端进程停掉时，语言模型、图片、语音都会表现为未接通；直接从 `自动化产品/start.command` 重启，不要先怀疑 Key 丢失。

### 5. 图片精修 / 原创度增强

生成图片或回传图片之后，在进入文案和审核前自动做轻量精修：

- 不裁剪，不强制改尺寸。
- 轻微增强画质、通透度、清晰度。
- 随机套用几组不同滤镜参数，例如轻微增饱和、轻微降饱和、微对比、暖亮、冷亮。
- 随机叠加小型矢量元素，放在四角或边缘，不遮挡主体和文字。
- 矢量元素要有多套形态、颜色、位置和组合，避免每张图都一样。
- 角落括号 / 角标只能随机出现在 0-2 个角，不要每张图四个角都有括号。
- 角落装饰属于发布前图片精修逻辑，不应该写进图片生成 prompt。
- 2026-06-30 新增避坑：批量站内生成和 Agent 上传补图都走 `orchestrator.js` 的发布前精修，单号图片工坊走 `chainBoards.js` / 资产入库精修；两边都必须保持 0-2 个随机角标和多形态装饰，不能固定四角括号。
- 不要加水印、二维码、平台标识。

### 6. 发布清单和资产库

发布清单需要显示产品标签：

- 例如 Dumate / 百度搭子 / 其他自定义产品。
- 下载文件命名里，产品标签要放在日期前面，便于供应商识别。
- 筛选和批量下载也应能按产品标签使用。
- 2026-06-29 新增避坑：供应商视角表格不要用横向滚动条兜底。素材名、账号名和标签要省略显示；标签默认只露少量关键标签，剩余用 `+N` 收纳，悬停显示完整标签；下载 / 回传链接按钮必须固定在同一行并与状态列对齐。
- 2026-06-29 新增避坑：供应商回传发布链接时可以填写可选备注，创作者要能在首页最新交付和发布清单详情看到这条备注，下载包里的 `标题文案.txt` 也要保留备注和发布链接。
- 2026-06-29 新增避坑：发布清单支持创作者回撤删除自己发布的内容，管理员可以删除所有内容；删除必须二级确认。回撤只删除发布清单交付包记录和对应分析链接，原始账号资产保留，相关 production 退回审核态便于重新定稿发布。
- 2026-06-30 新增避坑：供应商已经下载或已经回传发布链接的发布清单记录不能回撤。按钮可展示禁用原因（例如“已下载不可回撤”），创作者和管理员都不能绕过该限制。

整体资产库规则：

- 未发布的口播音频、视频、图片不要进入整体资产库。
- 未发布素材保留在草稿箱或账号资产里，个人可见。
- 只有发布后或管理员明确加入共享库，才进入整体资产。
- 口播音频不要误入 BGM 库。

### 7. 视频 / 数字人链路

真人账号默认走“数字人”模式，但 Seedance 模式仍要可切换。

数字人模式目标：

- 选题 / 创作内容 / 产品 -> 生成口播 -> MiniMax 分段生成音频 -> 统一角色图或分段角色图 -> 数字人 API -> 进入混剪。
- 每段口播预估拆分，不超过 30 秒。
- 数字人不需要复杂视频提示词，只需固定基础提示：“角色自然地讲述内容，动作自然，表情自然”。
- 数字人模式下隐藏旧 Seedance 分镜链路，不要再显示“一键编排出片”下面一堆 Seedance 卡片。
- 数字人分段卡片上提供：单段角色图、音频预览、一键生成视频、重新生成。
- 统一参考图默认是角色图；分段定制图可覆盖该段角色图。
- 数字人页面不需要“参考声线 / 上传参考声线”按钮，因为声线由 MiniMax 口播音频决定。

Seedance 模式目标：

- 点击 Seedance 切换按钮必须真的切换模式。
- 分段提示词才出现 Seedance 链路。
- 切换按钮要有明确滑动反馈，用户能感知模式切换。

混剪目标：

- MiniMax 口播音频必须进入音频轨。
- 字幕从口播生成并对齐，不要只显示字幕轨但没有真实音频。
- 录屏库、背景库、BGM 库后续用于自动混剪。
- BGM 随机或手动选择，默认音量低于口播。
- 字幕断句不要单字掉行。

### 8. MiniMax 语音

当前要求：

- 使用 B 端企业版 Key。
- 默认模型：`speech-2.8-hd`。
- 接入更多官方支持的 voice id。
- 删除旧的不支持 voice id。
- 增加声线收藏。
- 增加“固定到账号”，账号可保存默认音色。
- 生成后必须能预览音频。
- 错误提示区分：voice id 无效、网络不可达、endpoint 错误、认证失败。

### 9. 登录页和品牌视觉

登录页要使用星阵 logo 和更大气的视觉：

- 替换旧 ACGTV 标识。
- 不要暴露管理员账号密码。
- 用 CSS / Canvas 动画替代会闪烁的视频背景，避免视频循环闪屏。
- Logo 可以更大，更有品牌中心感。
- 登录框风格应贴合星阵 logo，保持功能不变。
- 已登录用户刷新页面不应闪现登录页再回到平台，应先读取本地登录态或显示轻量启动态。

账号卡片：

- 头像保持 1:1，不要变扁。
- 头像和账号名上边缘对齐更好看。
- 头像和风格预览图都仅管理员可拖拽修改。
- 风格预览图点击可放大查看。

### 10. 文案、标题和小红书违禁词

文案标题不能直接等于用户输入的创作内容。

参考资料：

- `/Users/macbookpro/Desktop/obsidian/知识库/百度/数据分析/Dumate小红书文案风格参考.md`
- `/Users/macbookpro/Desktop/obsidian/知识库/百度/数据分析/Dumate小红书竞品数据分析.md`
- 小红书违禁词语料文档：用户上传过 `小红书违禁词语料与注意事项(1).md`

要求：

- 标题更像真实小红书内容，不要强广告感。
- 可以捆绑同类高流量产品、热点和用户痛点，不一定直接把产品名放标题。
- 文案要有人味、专业、干货、有梗但克制。
- 不要过度呼吁下载、购买、冲、必备等强 CTA。
- 违禁词规避要写入底层生成逻辑；用户输入中有风险词时，要改写或规避，不要原样生成。
- UI 不要展示解释性提示，例如“底层已规避违禁词”。

### 11. 多人协作 / 同步

共享服务器模式必须以服务器为权威：

- 登录 / 刷新后服务器数据覆盖本地同步集合。
- 不允许 IndexedDB 旧账号、旧资产把服务器删除的数据复活。
- 删除账号、增加账号、修改账号，其他成员刷新或同步后必须能看到。
- 上传的图片、音频、视频二进制要服务端化，其他成员可预览和下载。
- 下载包不能只有 txt，必须包含真实文件。

### 12. 服务器部署准备

新服务器是百度智能云 BCC，不是阿里云 ECS。

已知配置：

- 平台：百度智能云 BCC。
- 地域：北京。
- 可用区：北京-D。
- 系统：Ubuntu 24.04 LTS amd64。
- 规格：8 核 32GB。
- 系统盘：40GB。
- 数据盘：约 100GB。
- 有弹性公网 IP。
- 管理员用户：root。
- 实例已创建并已付款；明文公网 IP、私网 IP、实例名和密码不要写入代码或文档。

部署前建议：

- 安全组开放：22、8787；正式域名后再开放 80/443。
- 建议挂载数据盘到 `/data/dumate-studio`。
- 安装：Node LTS、Python 3.10/3.11 venv、ffmpeg、nginx、git、unzip、build-essential。
- 使用 systemd 管理前端静态服务 / Python API 服务。
- `.env` 放服务端，不进 git。
- 生产 Key 必须由部署方直接预置到服务器环境变量 / `.env`，不要要求用户在页面、终端或聊天里手动粘贴 Key。当前部署先接入已提供的 B 端 MiniMax、公司 DeepSeek、image-2；其他模型或服务不是永久不接，等用户后续把本地 Key 更新齐后，最终也应由部署方统一写入服务器环境。
- 上传目录、SQLite/JSON 状态文件、日志目录都放数据盘并定期备份。
- 首次登录后建议关闭密码直连或至少改为密钥登录 + 强密码，并限制安全组来源；不要把凭据提交到 GitHub。
- 部署前先在本地确认：图片生成、参考图、多参考图、MiniMax、Seedance、批量创作入口、同步逻辑全部正常。
- 部署后验证：
  - `/api/health`
  - `/api/state`
  - `/api/files`
  - `/api/chat/completions`
  - `/api/image/generate`
  - `/api/video/config`
  - 两个浏览器 / 两个账号测试删除账号同步。

### 13. 已踩坑：批量创作自动跳单号工坊

2026-06-28 记录：

- 问题表现：页面停在批量创作任务板一段时间后，后台批量图文站内生成进度推进，界面会跳到单号图片工坊页面，例如 `#/studio/images`。
- 原因方向：批量任务生成的 production 与单号工坊共用 `activeProductionId` / `returnTo` / stage 路由，后台恢复或页面重渲染时可能把批量任务误当成单号工坊继续处理。
- 防线要求：`agent` 路由下默认锁住自动进入 `studio`；只有用户明确点击单号创作、账号入口或抽屉里的“进入单号工坊微调”时才放行。
- 批量确认执行前应清理 `activeProductionId` 和 `returnTo`，确认后继续留在 `#/agent` 并刷新量产任务板。
- 单号工坊抽屉从批量页进入时必须二次确认，避免用户点“查看/详情”后被误导到单号链路。

### 14. 账号风格与图片提示词种子

2026-06-28 记录：

- 账号定位和创作风格由 `自动化产品/js/data/accountProfilesSeed.js` 统一同步，当前种子来自飞书“全部账号风格”表：50 个小红书图文账号 + 30 个视频号账号。
- 服务器已经部署后，线上账号库属于生产数据。后续发布新版本时不能无条件用本地种子覆盖服务器账号数据；只能做受控迁移 / upsert，保留线上已编辑账号、上传头像、账号资产和发布状态。除非用户明确要求重置账号库，否则禁止清库重建。
- 小红书账号重点维护差异化 `账号风格` 和 `imagePromptTemplate`；`账号定位` 只作后台资料，不参与图文图片 prompt、图文脚本或图文发布文案的内容方向。大多数风格保持干净可读，但封面和内页应区分为文档截图、日报密集信息、大字封面、流程架构、简笔画故事、测评卡等不同方向。
- 视频号账号的 `styleProfile` 重点写口播节奏、语气、说话习惯和结尾方式，不重点写图文版式。
- 账号编辑弹窗不再展示“账号标签”chips，避免用户以标签维护量产逻辑；标签仅作为系统内部筛选辅助。
- 图文提示词固定结构：如有参考图先写“请根据上传的参考图”，然后写“生成小红书笔记风格3:4尺寸，【图片风格：...】，图片具体内容：【...】。负面约束：不出现页码，不出现二维码，图片右上角和左上角不要加入logo，其他位置可以正常出现logo。”
- 提示词里不要写内部分类词，例如“封面、痛点引入、问题引入、关键步骤、结果对比、总结收束、图1、第1张”；这些词也不要放进图文最终负面约束。
- 发布清单命名需要把产品标签插到日期前，例如 `...-百度搭子-20260628`；历史已发布记录启动时会按产品表静默补齐产品标签。
- 发布时计划发布时间为必填项；不填写不能定稿发布。
- 共享后端有“旧缓存大批量回推账号”的 409 保护；合法账号风格种子新增多账号时，不能走普通 `save("accounts")` 整集合同步，必须受控分片 upsert，避免被保护逻辑拦截。

## API 配置摘要

不要在代码仓库写明文 Key。应通过 `.env` 或服务端配置注入。

语言模型：

- 公司 DeepSeek 走腾讯 MaaS / TokenHub 兼容接口。
- 模型 ID：`deepseek-v4-pro-202606`。

图片模型：

- `custom-imagemodel-gt` 即 image-2。
- 保持之前能成功出图的 Responses / image-2 路径。
- 请求中加入 `logo_add: 0` 去掉“AI 生成”水印。
- 不要用错误 fallback 模型。

视频模型：

- Seedance 的 base / model / reference 文件挂载要严格按已验证路径。
- 失败时显示真实错误，包括鉴权、模型、参考图、网络、服务端返回。

语音模型：

- MiniMax 默认 `speech-2.8-hd`。
- B 端 Key 支持更多 voice id。
- 语音只在发布后进入共享素材库，未发布留草稿 / 账号资产。

## 下一个会话接手顺序

1. 继续从用户最新反馈处修 UI 细节，优先保持量产任务板稳定、紧凑、无聊天感。
2. 回归图片工坊：站内生成、无参考图、多参考图、生成中特效、真实错误、3:4 占位框。
3. 回归发布清单产品标签和命名。
4. 回归数字人 / Seedance 模式切换。
5. 检查登录页动画、logo、刷新不闪登录页。
6. 更新本文档，记录新坑和修复结果。

## 交付标准

每次交付前必须：

- 本地页面能打开，不白屏。
- 浏览器真实点击验证，不只跑命令。
- 图片生成无参考图成功。
- 图片生成多参考图成功。
- 批量创作入口不再进入聊天回答。
- 生成失败时有真实错误。
- 不破坏原 UI 风格和原功能。
- 不把明文密钥、公网 IP、服务器密码写入回复或文档。

## 2026-06-28 v1 更新记录

本次修复结果：

- 批量创作入口已恢复为量产计划面板；“创作两个图文号 / 做两个图文号 / 选两个图文”等不再进入聊天问答或创建账号分支。
- 30 条中文批量生产意图回归已通过，全部识别为量产计划。
- 批量参考图规则落地：统一参考图最多 5 张，每账号定制参考图最多 3 张，总计传入站内图片 API 时最多 8 张；支持拖拽、上传和从资产选择。
- 图片工坊默认走站内生成；无参考图与多参考图 API 均已在浏览器中验证成功。图片生成中显示 loading，失败显示真实错误，图文占位保持 3:4，不再出现“出图回传”误导文案。
- 图文批量默认站内生成已跑通：新批次 2 个图文账号共 12 张图全部生成完成，并进入待发布审核。
- 批量进度显示已修正：图文站内成图阶段显示为“生成中”，看板显示每条任务成图进度，避免误判为起草卡死。
- 发布清单已增加产品标签/产品列，交付命名继续保留账号、平台、内容类型和序号。
- 草稿阶段音频 / 视频 / 图片继续留在草稿或账号资产侧，只有发布交付后才进入共享发布素材视图。
- 数字人 / Seedance 模式切换已在工坊内验证；数字人模式隐藏旧 Seedance 分镜上传链路。
- 登录页已换星阵动画；已登录刷新时不会闪现登录页。
- 新部署目标继续以百度智能云 BCC 为准，不按旧阿里云方案执行。

本次遇到的问题：

- Playwright 自带 Chromium 缓存缺失，验证时改用本机 Chrome 可执行文件完成浏览器自动化。
- 图文批量真实站内出图耗时较长，默认 2 个账号 x 每号 6 张图会跑数分钟；状态显示已改为逐条成图进度。
- 历史会话中仍会保留旧版聊天回复文本，这是旧数据，不代表新入口逻辑。

## 2026-06-28 v4 更新记录

本次修复结果：

- 登录页 logo 改用用户提供的星阵单独 logo，并放大到 560px 显示。
- 浏览器 favicon 和侧栏入口小标改为同一张 `logo.png`，不再使用旧播放三角图标。
- 量产任务板默认入口改成未选择账号状态，确认按钮显示 0 条；用户自然语言指令触发时仍保留智能选号。
- 量产任务板增加“随机选 ≤10”按钮，可从当前账号条件池里随机选择最多 10 个账号。
- 随机选号后的单号明细排版收紧：第一行放账号、产品、条数、内容，定制参考图下放第二行，避免右侧上传区域挤出卡片。
- 前端资源版本号更新为 `20260628-xz4`，避免浏览器缓存旧 JS / CSS。

本次浏览器验证：

- `http://localhost:8787/?v=20260628-xz4b#/agent` 正常打开，无白屏。
- 新建量产后默认选中账号数为 0，确认按钮为“确认执行（0 条）”。
- 点击“随机选 ≤10”后选中 10 个账号，确认按钮为“确认执行（10 条）”。
- 1280px 视口、任务卡 564px 宽度下，10 条单号明细 `overflowRows=0`，不再横向溢出。
- favicon、侧栏小标、登录页 logo 均指向 `logo.png`，登录页 logo 计算宽度为 560px。

本次遇到的问题：

- 默认计划最初仍自动选择 3 个账号，根因是计划卡渲染时会把默认 goal“选择3个很久没发布...”当成真实用户意图重新归一化。已将默认 goal 改为中性的“新量产计划”，保留用户输入时的智能识别。
- 三栏工作台中间列实际只有约 564px，单号明细不能硬塞 5 列；后续新增字段也应优先采用两行或可折叠结构。

## 2026-06-28 v5 更新记录

本次修复结果：

- 浏览器 favicon 和侧栏小标改用用户新提供的透明星标图 `xingzhen-icon.png`，不再使用带白底或大留白的横向 logo。
- 登录页继续使用裁剪后的完整横向 `logo.png`，保持品牌文字展示。
- 量产任务板把“每号内容数”移到“随机选 ≤10”按钮旁边，形成同一组顶部工具，不再占用单号明细列。
- 单号明细主行统一为三列：账号名 / 产品选择 / 本次创作内容。
- 定制参考图行复用同一列线：定制参考图说明 / 已选状态 / 操作按钮组；所有带框控件统一高度。

本次浏览器验证：

- `http://localhost:8787/?v=20260628-xz6#/agent` 正常打开。
- favicon 和侧栏小标均指向 `./assets/brand/xingzhen-icon.png`。
- 10 个账号的产品框与内容框顶边差为 `0px`、高度差为 `0px`。
- 10 个账号的“从资产选择”和“拖入 / 上传”按钮顶边差为 `0px`、高度差为 `0px`。
- `overflowRows=0`，量产明细不横向溢出。

本次遇到的问题：

- 透明 PNG 源图本身可用，但小图标不能使用完整横向 logo，否则会因为大留白在 favicon / 侧栏里显得很小；小尺寸入口必须使用单独星标 icon。

## 2026-06-28 v8 更新记录

本次修复结果：

- 图片提示词组装增加脱壳清洗：先移除旧模板里的“请根据上传的参考图 / 生成小红书笔记风格 / 账号定位 / 图片风格 / 图片具体内容 / 负面约束”等外层结构，再统一生成最终 prompt，避免出现“账号定位：账号定位”和“图片具体内容里重复整段模板”。
- 图文提示词第一张标题优先使用本次主题，例如“周报写到头秃？”，不再默认回落成泛泛的“产品到底省在哪”。
- 账号风格种子升级到 `20260628-v8b`：继续固定为 50 个小红书图文账号 + 30 个视频号账号，其中 19 个数字人、11 个无数字人素材口播。
- 小红书 50 个图文账号的风格扩展为 20 类主视觉方向，包括真实办公截图、Agent 架构图、高密度备忘录、飞书文档截图、日报周报、测评评分卡、简笔画故事、项目看板、数据仪表盘等，配色和信息结构不再大面积雷同。
- `imagePromptTemplate` 改为“账号图文母版”，只描述画面语言、色彩、结构、素材和排版，不再存最终图片 prompt 成品，避免模型原样复制模板句子。
- 启动同步账号种子时会清理非种子账号和重复账号：共享账号应收敛到 80 个，不再出现 38/22 这类被旧缓存膨胀出的 视频号数量。

本次浏览器验证：

- `http://localhost:8787/#/overview` 和 `#/agent` 正常打开，无白屏。
- 共享状态验证账号总数为 80：小红书图文 50、视频号数字人 19、视频号素材 11，旧演示账号不再出现。
- 图片 prompt 本地坏样例验证通过：不再出现“账号定位：账号定位”、不再在“图片具体内容”里嵌套第二段“生成小红书笔记风格”。
- `/api/image/generate` 文生图成功：`custom-imagemodel-gt`、3:4、无参考图返回图片。
- `/api/image/generate` 参考图成功：传入 1 张本地参考图，`usedRefs=1`，返回图片。
- 新建最小批量“选择1个图文账号，每号1条，主题是用百度搭子整理乱文件夹”后，量产计划识别为 1 个账号 / 1 条；执行后站内图片生成 6/6，无错误，任务进入 review。

本次遇到的问题：

- v7 后共享后端里仍保留旧账号和重复账号，导致视频分组显示 38/22；修复方式是 v8b 种子同步时删除非种子 / 重复的受管账号，再分片 upsert 当前 80 个账号。
- 飞书旧字段里有 emoji 或半个 Unicode 代理字符时，后端写 SQLite JSON 会抛 `ValueError`，接口会误报为“未知集合”。账号种子必须在生成阶段清理 emoji、代理字符和不可写入 JSON 的符号。
- 控制台里可能留有修复前的 `/api/db/accounts` 400 历史请求；v8b 重新同步后的账号分片已全部 200。

## 2026-06-29 v12 更新记录

本次修复结果：

- 增加平台产品知识库种子 `productCatalogSeed.js`，用于图文、视频脚本、文案和随机选题的产品视角。
- 我们的产品库包含 `Dumate / 百度搭子` 和 `百度秒哒`；百度秒哒在用户可见名称里只显示中文名和短名“秒哒”，不显示英文名。
- 竞品 / 同类产品库包含 OpenClaw、Codex、Claude Code、Cursor、Windsurf、GitHub Copilot、TRAE、Obsidian、Manus、WorkBuddy，用于 AI 博主视角的教程、对比、测评、工具分工和合集选题。
- 产品种子采用 merge 方式同步：只补齐缺失字段和缺失产品，不覆盖用户已编辑的同 id 产品数据。
- 设置页导入导出加入 `products`，完整备份可以保留产品库。
- LLM 入口不再默认向所有产品注入 Dumate 事实；只有当前产品是 Dumate / 百度搭子时才注入 Dumate 产品事实。
- 随机选题增加“当前产品锁定”和产品能力边界纠偏：账号名、旧主题或历史素材里的其他产品名只能当旧数据，不得把当前产品改写成旧产品。
- 百度秒哒选题被限制在无代码应用生成、H5 / 页面、原型、小工具、数据表 / 后台等方向，不再误写成文件整理、桌面自动操作、PDF/Word/Excel 转格式、会议纪要等 Dumate 能力。
- 旧视频框架里的“百度搭子 Dumate / Dumate 产品教程短视频”改为“本次产品”，避免非 Dumate 链路被旧模板污染。

部署和数据规则：

- 服务器生产 Key 由部署线程直接配置到服务器环境变量 / 私有配置中，不要求用户手动粘贴到前端或仓库。
- 服务器后续会继续接入更多生产 Key；当前没有提供的 Key 不代表永远不接入，只是等待用户后续补齐。
- 版本更新和服务器部署时，不能覆盖线上已存在的账号数据、产品库数据、供应商数据、素材和发布清单；代码种子只能 merge / 补齐，不能无差别重置。
- 服务器部署仍以百度智能云 BCC 为准，不按旧阿里云方案执行；回复、文档、代码和提交信息都不能写入明文密钥、服务器密码、公网 IP 或私网 IP。

本次浏览器验证：

- `http://127.0.0.1:8787/?v=20260629-product-db-final#/agent` 正常打开，无白屏。
- 产品下拉包含 `Dumate`、`秒哒`、OpenClaw、Codex、Claude Code、Cursor、Windsurf、Copilot、TRAE、Obsidian、Manus、WorkBuddy。
- 页面正文不包含百度秒哒英文展示。
- 刷新后 console error 为 0。
- 真实 LLM 三轮“百度秒哒”随机选题返回：无代码 / 数据看板 / 原型方向，无 Dumate、百度搭子、文件整理、PDF/Word/Excel 等错产品或错能力污染。

本次遇到的问题：

- 第一次浏览器验证动态 import 使用了磁盘路径，产生了一条测试脚本自身的 `Failed to fetch dynamically imported module`，刷新页面后错误消失；页面服务根路径应使用 `/js/...`。
- 仅把产品名传给 LLM 不够，账号旧名称和历史默认主题会把模型重新拉回 Dumate；需要在随机选题里做“当前产品锁定 + 能力边界纠偏”。

## 2026-06-29 v13 更新记录

本次修复结果：

- “宣传产品”选择只保留百度自己的两个主产品：`Dumate / 百度搭子` 和 `百度秒哒`；竞品 / 同类产品不再出现在量产任务板、图片工坊、脚本页、分镜工坊的主产品下拉里。
- 竞品 / 同类产品继续保留在产品知识库里，只作为语言模型写图文、图片提示词、视频口播脚本时的对比、组合、分工和参考对象。
- 图片工坊点击“生成图卡结构与提示词”时改为两步：如果用户没有填写创作内容，先生成一段可执行创作 brief 并写回输入框；再根据该 brief 生成图卡结构和图片提示词。
- 用户没有写创作内容时，随机 brief / 随机主题可以主动带 1-2 个竞品或互补工具，例如 Obsidian + 百度搭子、Manus 与百度搭子的任务边界，让内容更像 AI 博主科普，不再孤立宣传。
- 用户明确写了创作方向时，优先服从用户方向；如果方向里提到竞品 / 同类产品，模型会按产品库识别它们的功能点，再和当前主产品做合理分工、对比或组合，不把竞品能力写成主产品能力。
- 创作 brief 截断改为按句号 / 分号 / 逗号收尾，避免半句话。

本次浏览器验证：

- `http://127.0.0.1:8787/?v=20260629-product-combo#/agent` 正常打开，无白屏。
- 量产任务板主产品下拉只显示 `Dumate` 和 `秒哒`，未出现 OpenClaw、Codex、Claude Code、Cursor、Windsurf、Copilot、TRAE、Obsidian、Manus、WorkBuddy。
- 真实 LLM 空内容测试：`generateCreativeBrief(Dumate)` 先生成创作 brief，包含 Manus 对比，完整句子收尾；再生成 6 张图卡结构和 6 条图片提示词，提示词中保留对比关系，未串到秒哒。
- 真实 LLM 明确方向测试：用户输入 `Obsidian + 百度搭子` 后，图卡结构和图片提示词均识别 Obsidian 的知识库 / 资料索引职责与百度搭子的本地文件 / 提取字段 / 周报职责，未串到秒哒。

本次遇到的问题：

- 只隐藏竞品选项不够，旧会话里可能残留竞品 `productId`；执行批量任务和渲染单号页面时都必须通过 `primaryProductById` 收敛回百度主产品。

## 2026-06-29 v16 更新记录

本次修复结果：

- 用户可见产品名统一为“百度搭子”，图文提示词、口播脚本、发布文案、产品下拉、旧账号名和旧任务标题都不再展示英文旧名。
- 内部稳定 id 仍保留原值，不为显示中文而重命名 `productId` / IndexedDB key / 服务端关联字段，避免破坏已有账号、任务、资产和产品库数据。
- 产品库 merge 会把历史产品缓存里的旧英文显示名、短名、选题角度清洗为“百度搭子”。
- 状态装载层增加可见字段清洗，旧 IndexedDB 或服务端快照里的账号名、标题、文案、提示词、通知、发布素材名会自动转中文；ID、URL、密钥、dataUrl 等内部或敏感字段不处理。
- 小红书合规清洗层会同步处理产品名，模型返回内容再过一次中文化，防止旧英文从口播或图片 prompt 回流。
- 定稿发布弹窗改为只选“计划发布日期”，不再精确到小时分钟；默认填今天，直接定稿发布会使用当天日期。
- 发布交付入口增加兜底日期，即使旧入口未传计划日期，也会自动使用当天日期。

本次验证：

- 本地 `/api/health` 正常，语言模型配置已接通。
- 真实 LLM 验证旧英文输入 + 旧产品缓存：生成 brief、3 张图片 prompt、发布文案里均无英文旧名，保留“百度搭子”和 Manus 分工关系。
- 发布默认日期验证：不传计划日期时自动写入当天日期，发布素材命名和标签均含“百度搭子”。
- 浏览器验证 `#/agent` 正常打开，无白屏；产品下拉显示“百度搭子 / 秒哒”；页面可见文本不再出现英文旧名；未发现日期时间输入控件。

本次遇到的问题：

- 只改产品种子不够，浏览器旧 IndexedDB 里可能仍有历史账号名或任务标题，会把旧英文显示回页面；必须在状态装载层做“用户可见字段清洗”。
- 深度清洗不能碰内部 id。第一次实现时把内部 `id` 也清洗成中文，导致产品查找失败；后续所有品牌名清洗都必须跳过 `id`、`*Id`、URL、密钥、dataUrl 等内部字段。
- 发布时间只需要日期，不需要时分；日期默认今天可以减少一次不必要输入。

## 2026-06-30 v20 更新记录

本次修复结果：

- 批量生产固定为量产任务板，不再显示“每号随机主题”切换。内容优先级为：单号创作内容 > 总创作要求 > 自动随机创作。
- 总创作要求为空时，不再按账号风格随机写一大段创作内容；只从“强对比选型 / 联动绑定生态 / 打工人效率场景 / 小白反转入门”四类短方向里选一个短选题，再交给后续链路展开。
- 同一批次里即使总创作要求相同，也必须按账号和批次序号做差异化：标题、首句、例子、图卡顺序、结论和文案架构都要不同。
- 图文内容编排增加信息密度预算：长内容少图时先压缩主线并均匀分配；短内容多图时补真实例子、证据和边界；每张图只讲一个核心点，不把所有信息塞进单图。
- 图片提示词清洗 `idea/visual/line`、时间码、用户指令、内部定位词和图文生产描述，避免“做成小红书笔记”“画面定位”“图片定位”等系统语言进入最终 prompt。
- Obsidian / Notion / 知识库类主题按“知识沉淀”处理，和百度搭子的关系应写成“桌面执行 + 知识沉淀”的组合流程，不要误写成周报或格式转换。
- 产品关系写短职责，不把产品库长能力清单塞进图片 prompt；Manus 可写云端通用任务，Obsidian 可写知识沉淀和双链。
- 批量单号行 UI 收紧为稳定网格，账号、产品、本号条数、每条图数对齐，创作内容输入独占一行，避免右侧任务看板存在时横向溢出。
- 本地正式入口是 `http://127.0.0.1:8787/#/agent`。4173 只是静态前端辅助入口；如打开 4173，需要前端自动发现 8787 后端，否则会显示 API 未通。

本次验证：

- `node --check` 通过：`ai.js`、`llm.js`、`orchestrator.js`、`cards.js`、`view.js`。
- `/api/health` 显示语言模型已配置；`/api/chat/completions` 真实返回正常。
- 真实 LLM 多轮压测通过：长内容 3 张图能压缩，短/中内容多图能扩展；统一创作要求下不同账号的标题、图卡和文案不再完全一致。
- 浏览器验证 `http://127.0.0.1:8787/#/agent` 正常打开，页面标题为“星阵 · 内容生产工作台”；布局指标无横向溢出；前端语言模型配置为同源 `/api/chat/completions` 且 server-managed。

本次遇到的问题：

- 测试时误打开 4173 会让用户误以为“本地不是 8787”。以后本地验证优先打开 8787；只有为了验证静态前端兼容时才打开 4173，并明确说明。
- 模型偶尔会把用户的“做成小红书笔记”等指令揉进脚本，需要在脚本后处理层清洗，不只靠提示词约束。
- 用 JSON 字符串模拟脚本会把 `idea/visual/line` 暴露给图片提示词测试；真实链路应使用 `shotsToText`，同时清洗层也要兜底过滤结构化字段。

## 2026-06-30 v21 更新记录

本次修复结果：

- 修复批量生产点击“确认执行”后无反应、任务看板变成 0/0 空列表、小机器人思考过程消失的问题。根因是批量差异化策略里 `accTag` 变量被误写成 `accountTag`，导致 `startBatch()` 在创建 production 前同步抛错，只留下空批次。
- 批量确认流程改为“启动中 → 批次创建成功 → 已执行”。如果启动失败，会自动回滚为待确认，不再留下死面板。
- 旧数据里“已执行但没有批次/进度”的孤儿量产卡会自动恢复为待确认；0 条空批次不再显示到右侧任务看板，避免出现退化的纯文本 0/0 列表。
- 批次创建后记录 `planMessageId`，后续可以判断量产卡和批次是否真正绑定。
- 发布清单序号改为按发布时间连续计算，不再依赖旧的 `pubSeq` 缓存字段，避免序号重复或跳乱。
- 发布清单创作端增加日期时间轴导航：按发布时间分组，可点击日期快速定位，也可折叠某一天的内容；每天之间用分割线区分。
- 创作端切到“供应商视角”时也显示“批量下载未下载”按钮；供应商表格序号同步使用同一套发布时间连续序号。

本次验证：

- `node --check` 通过：`agent/orchestrator.js`、`agent/view.js`、`agent/cards.js`、`views/deliveryView.js`。
- `git diff --check` 通过相关修改文件。
- 浏览器验证 `http://127.0.0.1:8787/#/agent`：新量产卡默认 0 个账号时会提示至少选择一个账号；选择账号后确认，右侧恢复节点式任务卡，小机器人显示“并发起草 1 个账号 · 1 条内容…”。
- 浏览器验证 `http://127.0.0.1:8787/#/delivery`：创作端出现日期导航和日期段；序号显示为连续编号；切到供应商视角后出现“批量下载未下载”，表格序号也连续。

本次遇到的问题：

- 批量启动同步错误会发生在语言模型调用之前，不应把所有“启动失败”都归因到 API 或额度。以后先看浏览器 console 的真实错误，再判断是否是后端/API 问题。
- 任务看板应保持节点式 `boardRow()` 卡片；出现“起草 0/0”的纯文本列表通常说明批次没有 production，应过滤或清理空批次。
- 发布清单展示序号必须从当前发布时间排序动态计算；旧字段只适合作为历史数据，不适合作为唯一展示序号。
- 工作区里已有的 `main.js`、`server/main.py`、`server/store.py` 修改是平台代码变更，不是可随意移动到下载文件夹的垃圾文件；处理前必须确认是否要纳入版本。

## 2026-06-30 v22 更新记录

本次修复结果：

- 单号创作左侧账号矩阵去掉“小红书/视频号”文字标签，改为序号颜色区分平台：小红书红色、视频号绿色，避免平台标签遮挡账号名称。
- 单号图片工坊生成图卡结构后的提示改为通用“已生成”，不再显示具体张数，避免旧缓存或默认值变化时出现“选 4 张却提示 6 张”的误导。
- 发布清单卡片元信息改为“固定标签区 + 详情省略区”，前三类标签占位稳定，长文件名、发布人、供应商状态等信息超出后省略，鼠标悬停可看完整内容。
- 发布清单计划日期展示只保留日期，不显示具体时间。
- 供应商视角支持点击素材行展开详情，可预览图片缩略图和文案内容；缩略图可点开大图。
- 批量创作参考图选择弹窗增加删除按钮，可快速删除普通上传图 / logo / 账号资产；已发布生成图不提供删除，避免破坏发布清单预览。
- 单号图片工坊的小参考图选择器同步增加删除普通参考图的入口。
- 启动和远端拉取后增加已发布图集自愈：历史发布图集里的子图如果没有标记共享，会自动补上“已发布生成图 / 共享素材 / 产品标签”，让整体资产库能看到已发布内容。
- 入口资源版本号更新到 `20260630-v22`，避免浏览器继续使用旧 CSS/JS。

本次验证：

- `node --check` 通过：`main.js`、`views/deliveryView.js`、`agent/view.js`、`views/chainBoards.js`。
- `git diff --check` 通过。
- 本地 `http://127.0.0.1:8787/api/health` 返回正常，语言模型配置已接通。
- 浏览器验证 `http://127.0.0.1:8787/#/studio`：账号矩阵 80 个账号，平台文字标签为 0；小红书红色序号 50 个，视频号绿色序号 30 个。
- 浏览器验证 `http://127.0.0.1:8787/#/delivery`：发布清单有固定标签区，日期不带时间；供应商视角行可展开，详情中可看到文案和 24 个图集缩略图。
- 浏览器验证 `http://127.0.0.1:8787/#/agent`：参考图选择弹窗正常打开，普通可删参考图按钮存在；已发布生成图不显示删除按钮。

本次遇到的问题：

- 发布后只创建图集交付资产不够，图集里的单张图片也必须标记为共享素材，否则供应商/创作者在整体资产库里看不到已经生成过的图。
- 参考图弹窗里不能对“已发布生成图”开放删除，否则会把发布清单详情预览依赖的子图删掉。
- 发布清单的标签和详情必须分区布局，不能让平台、产品、账号、日期、文件名全部挤在一条自由 flex 里。

## 2026-06-30 v23 问题记录

本次遇到的问题：

- 批量自动文案在空创作要求时容易退回同一套“资料整理/3 步整理”模板，标题和首句缺少小红书网感。原因是文案编辑规则只强调产品能力，没有把同批账号的标题类型、首句、骨架和例子做强制差异化。
- 批量生产里混入视频号后，继续展示“每条图数”会误导用户；视频链路应该是口播、数字人、混剪或素材视频，不是图文生图链路。
- 视频链路说明如果直接写成长标签，会在批量账号配置行里挤压输入框；这类说明应采用短胶囊展示，完整解释放到 hover title。
- 发布清单按日期折叠时如果直接重画整个列表，所有卡片会闪一下，体验不稳定；折叠应只切换当前日期段的 class 和高度动画。
- 整体素材库账号筛选一次性铺开 80 个账号会挤占页面；BGM 库和录屏库也不能和账号资产混为一类，它们属于视频剪辑素材库。

## 2026-06-30 v25 问题记录

本次遇到的问题：

- 图文提示词如果只在系统 prompt 里写“信息不要太密”，模型仍可能把总创作方向塞进每一张图；必须在后处理层把封面、普通讲解图、文档/表格型图的密度预算分开。
- “表格 / 文档 / 周报”等词如果出现在总主题里，不能让所有图片都自动走高密度。只有当前图明确是文档、表格、日报、报告等画面时，才允许文字稍多。
- 模型会把账号风格词（例如“简笔画、白底、大字标题、少文字、箭头和气泡”）误写进“本图只讲”的内容节拍，导致图像内容变空；后处理需要过滤风格词，只保留动作、场景和结果。
- 对外发布文案如果允许模型自然发挥，很容易把自家产品名写进标题、正文或话题标签。以后发布文案出口必须统一清洗自家产品名，改成“桌面智能体 / AI应用搭建工具 / 这个工具”等品类表达；图片 prompt 内部仍可保留产品名供图像模型识别界面与关系。

## 2026-06-30 v26 问题记录

本次遇到的问题：

- 图片信息密度不能靠往负面提示词里继续堆“不要出现...”解决；最终图片负面约束必须保持极短，只保留“不出现页码、不出现二维码、右上角和左上角不要加入 logo，其他位置可正常出现 logo”。密度、封面冲击力、内页节奏、工具分工都必须在正向提示词里编排清楚。
- 图文提示词必须先把用户意图压缩成每张图的单一信息节拍，再生成单图提示词；否则用户内容多时会把每张图塞满，用户内容少时又会变成空泛定位。
- 脚本对象不能直接 `String(script)` 传给图片提示词，否则会出现 `[object Object]` 或结构字段泄露；必须先把 shots / 文案转成可读文本。
- 可选联网热门参考不能阻断创作链路；OpenCLI 缺失、未授权、超时或失败时，要提示用户可在浏览器中配置 OpenCLI 后重试，并自动回退本地趋势库。

## 2026-07-01 v27 问题记录

本次遇到的问题：

- 热门标题参考如果只“学习结构”，标题会变得太安全、太像模板。小红书标题需要允许高比例改写热门样本：约 80% 保留钩子/句式，20% 替换成本次主题和工具关系；但公开标题、正文和 tag 仍不能出现自家产品名。
- 联网热门样本和本地趋势库只能作为内部取材与改写依据；样本标题、样本文案、参考说明不能被塞进最终图片 prompt，也不能变成对外文案里的“参考了某某”的解释。
- 图片提示词的问题主要在正向编排，不应继续往负面提示词里堆禁止项。最终图片负面约束继续保持极短；密度、点击入口、内页节奏、工具分工和竞品组合都必须靠正向描述解决。
- “封面/构图/种草/痛点/共鸣”等词如果出现在最终图片 prompt 里，会让图片模型把内部定位词画出来；这些词可以留在清洗规则里，但不能作为给模型的最终内容描述。
- 没有联网样本时，本地五大方向也不能只抽通用模板；需要先看主题里是否有 Obsidian、Codex、WorkBuddy、Manus、周报、资料、表格等语义，再生成不同标题和内容角度。
- 浏览器验证仍以本地 8787 为准。历史任务里可能保留旧失败节点，不能直接判断当前 API 未通；应同时看 `/api/health` 和图片配置检查。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 后续服务器部署时，联网热门能力应作为增强项：OpenCLI 不可用时提示配置或不再提醒，并自动回退本地趋势库完成创作。

## 2026-07-01 v28 问题记录

本次遇到的问题：

- 批量图文新链路已经合并为 `images -> review`，提示词和成图实际存放在 `artifacts.images.items`；如果继续按旧字段 `artifacts.imagePrompts` 检查，会误判为“没有生成提示词/图片”。
- 右侧任务看板里历史失败记录会让全页文本匹配误报“API 未通”。以后判断当前批次必须看最新 batch / production 的结构化状态，不能直接搜索整页是否含“失败”。
- 图片接口代理里 `httpx.AsyncClient.get()` 不能使用 `allow_redirects`，应使用 `follow_redirects`；否则站内图片链路会在本地/服务器运行时报错。
- 刷新或恢复时，图文任务如果处于 `images running/pending` 且已有 prompt 但缺 assetId，必须继续调用站内图片生成；只恢复状态不恢复生成 Promise 会让任务卡在“图文创作台中”。
- 账号种子不能只看 profile version 元数据；如果服务器 state 里账号数据为空但版本号相同，仍必须补齐 80 个账号，避免批量创作只剩空账号池。
- 图片 prompt 出口要清洗 `标题：/正文：/画面文字：` 等可见标签；否则模型会把“标题：”画进图片。清洗应放在最终 prompt 出口，不只依赖系统提示词。
- 风格短句压缩不能截断在“风为”这类半句上；被截断的风格词会影响图像模型理解，需要在 prompt 出口兜底补齐或去尾。

本次验证：

- `/api/health` 显示语言模型已配置；`/api/image/config` 显示 image-2 配置已接通且可达。
- 浏览器真实验证 `http://127.0.0.1:8787/?v=20260701-v28-final#/agent`：80 个账号正常，其中 50 个图文号、30 个视频号；新建量产默认 0 账号；选择 1 个图文号后确认执行，节点式任务看板出现。
- 批量图文真实跑通 1 条最小任务：按 4 张图生成，4/4 站内图片完成并进入待审；图片 prompt 未出现 `Dumate / DuMate / MIAODA`、未出现可见字段标签、未追加额外负面约束。
- 浏览器验证 `#/delivery`：发布清单无横向溢出，日期时间轴和供应商批量下载按钮可用，供应商行展开后能看到文案和图片序号。
- 浏览器验证 `#/assets`：已发布生成图进入共享素材库；BGM / 录屏归入剪辑素材库；账号筛选默认折叠，页面无横向溢出。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 后续服务器更新时继续保留服务器账号、资产、发布清单等业务数据，不要用本地空数据覆盖线上 state。

## 2026-07-02 v30 问题记录

本次遇到的问题：

- 登录页背景改回原版 8787 时，不能继续使用视频背景；视频素材容易影响性能和视觉判断。已改成 CSS 白色科技感动态背景，登录页 logo 使用本地透明星阵图。
- 浏览器 favicon 有缓存惯性，单纯替换图片文件可能仍显示旧图标；需要同步替换 `assets/brand/xingzhen-favicon.png` 并给 `link rel="icon"` 增加版本 query。
- 小红书联网搜索返回中如果包含 `desc/content/text/summary`，后端原解析器只保留标题、作者、互动和链接，导致前端只能“学结构”，文案缺少真实痛点和教程细节。已扩展解析字段，并在前端把可用摘要作为发布文案的一等输入。
- “重新生成文案”会复用旧 `trendPrep`，所以二次搜索后可能仍像没改。强制重写时必须刷新联网参考；联网开关变化时也要清掉旧参考和旧左右对比卡。
- 本地兜底文案里硬编码了“适合当一个小 SOP 留着”等收束句，导致即使语言模型偶发失败，也会回到固定模板。已删除这类固定句，并增加去模板化后处理。
- 离线版本也不能被本地趋势库限制成模板文。本地趋势只能作为灵感，最终文案应优先服从用户主题、账号语气和真实使用逻辑。
- 调试本地启动脚本时不要使用会展开环境变量的 shell trace；这会把本机环境变量打印到终端。以后只用无回显环境加载或脚本正常启动，不把任何敏感内容写入文档、提交或回复。
- 在 Codex 工具环境里，普通后台 `nohup` 进程可能随命令会话退出而被清理；本地 8787 需要脱离会话时，用 `subprocess.Popen(..., start_new_session=True)` 方式启动更稳。

本次验证：

- `node --check` 通过：`js/api/ai.js`、`js/data/xhsTrendLibrary.js`、`js/views/chainBoards.js`。
- `python3 -m py_compile 自动化产品/server/main.py` 通过。
- 本地 `http://localhost:8787/api/health` 返回正常，语言模型配置处于已接通状态。
- `assets/brand/xingzhen-favicon.png` 与用户提供的透明图标文件哈希一致。
- 本地请求 `assets/brand/xingzhen-favicon.png?v=20260702-v33` 返回 200，并带 no-cache 响应头。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 本次只重启本地 8787，不涉及服务器数据迁移；后续部署仍必须先备份并保护线上账号、资产、发布清单、数据分析和草稿。

## 2026-07-02 v31 问题记录

本次遇到的问题：

- 小红书数据分析不能只用裸链接读详情。OpenCLI 小红书详情通常需要搜索结果里带完整参数的链接；裸链接详情失败时，必须按标题重搜，能拿到真实搜索互动指标就回填搜索指标，不能整条直接模拟或吞掉真实失败原因。
- `agent-reach doctor` 显示小红书 OpenCLI 后端可用，不等于所有小红书详情都能读到全文或阅读量。搜索结果通常能拿标题、作者和点赞/互动；总阅读不可得时应显示 0 或不可得，并保留真实失败原因。
- 联网热门参考低于 100 互动时不应作为“热门参考”进入改写链路，否则会把低质量样本当爆款学习，文案质量会下滑。
- 图文发布文案生成不能被固定“三点清单 / SOP 留着 / 备忘录口吻”兜底模板锁死；联网和离线都要优先写成真实使用复盘、场景叙述、避坑提醒或有信息密度的教程。
- 图片提示词内容必须由最终发布文案和图卡脚本决定，账号风格只影响视觉效果；联网参考和账号名称不能改写图片内容主题。
- 图片标题不能用固定字数硬截断。首图要完整保留发布文案标题；内页标题也要保证是完整短句，不能出现“Obsidian安”这类半句。
- “账号定位”已从创建 / 编辑账号入口移除；新建账号不再写定位字段。历史数据中的定位字段只作兼容清洗兜底，不应出现在账号主页或生成提示词里。

本次验证：

- 代码检查以本地 8787 为准，重启前页面可能仍显示旧 503 或旧提示词逻辑；每次改完需要确认入口资源版本号已升级，并重启本地服务。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 本次为本地代码、文档和静态资源更新，不涉及服务器数据迁移；后续部署仍必须保护线上账号、资产、发布清单、数据分析和草稿。

## 2026-07-03 v32 问题记录

本次遇到的问题：

- 主版本 UI 调整前必须先复制整份主版本备份，避免和之前 UI 升级副本、其他线程未提交改动混在一起。本次备份目录为 `主版本备份-20260702-UI修改前`。
- 这轮用户明确要求“先不修改布局”，所以只能改配色、玻璃材质和静态背景接线；不要借机重做首页结构、左侧栏、批量创作三栏或业务流程。
- 登录背景视频接入时不能只加 `<video>` 标签；现有 JS 已经通过 `#loginBgVideo` 管控登录背景播放 / 暂停。如果忘记加 id，浏览器虽然可能 autoplay，但登录后暂停逻辑接不上。
- 登录页背景视频当前约 89MB，直接部署到服务器会增加首屏加载压力。后续部署前应把视频压成 web 版本，或确认静态服务支持缓存、range 请求和 poster 降级；不能为了上线速度让大视频拖慢登录页。
- 浏览器自动化验证带登录时，CLI 可能回显 fill 内容。后续带口令验证应优先复用已有浏览器会话、本地会话状态或后端健康接口，避免让敏感输入出现在日志、文档、提交或回复中。

本次验证：

- `git diff --check` 通过 UI 相关文件。
- 本地 8787 入口已加载 `20260702-v36` 资源版本；登录视频和降级图均能以静态资源返回 200。
- Playwright 截图验证登录页：小 logo 已移除，视频 / poster 背景下登录框无遮挡。
- Playwright 登录后截图验证首页与批量创作：普通页面卡片变为白色玻璃质感，批量生产工作台保留布局并切换为近黑玻璃 + 冷蓝高光。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 本次只涉及 UI 与静态资源，不涉及服务器数据迁移；后续部署仍必须先备份并保护线上账号、资产、发布清单、数据分析和草稿。

## 2026-07-03 v33 问题记录

本次遇到的问题：

- 只隔离 `productions / sessions / batches` 还不够。`assets` 如果作为全共享集合直接返回，虽然整体资产库页面会过滤已发布 / 共享素材，但批量参考图、账号素材、剪辑素材等入口仍可能从本地 state 里看到其他成员未发布私有素材。
- 管理员也不能看到其他成员未发布创作链路；因此服务端 `/api/state` 对未发布私有素材也要按 owner 过滤，不能只依赖前端页面隐藏。
- 数据分析和整体资产的共享边界要分清：数据分析记录、已发布素材、shared 素材全员可见；未发布 production、session、batch、job 和私有素材只给 owner。
- 本地验证多人权限时不要污染真实 8787 数据库；使用临时 SQLite 设置 `DATA_DB` 跑隔离用例，跑完删除临时库。

本次验证：

- 临时 SQLite 验证通过：成员 A 看不到成员 B 未发布 production / session / batch / private asset；管理员也看不到其他成员未发布创作态；已发布 production、shared / delivered assets、analyticsLinks 对所有成员可见。
- `jobs` 跟随可见 production 过滤，隐藏 production 下的 job 不会返回。
- 本地 8787 健康检查通过，入口资源已加载 `20260703-v37`；Playwright 可打开登录页、首页路由和批量创作路由。当前浏览器上下文未复用登录态，因此没有在自动化命令里输入口令，避免敏感输入进入日志。
- 本机没有 ffmpeg / ffprobe，登录背景视频未在本地重新压缩；服务器部署时要保留 poster 降级，并优先做 web 压缩、静态缓存或 range 请求支持，避免 89MB 视频拖慢首屏。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 后续服务器部署必须先备份线上数据库与上传目录，只更新代码和静态资源，不能用本地空 state 覆盖线上账号、资产库、发布清单、数据分析、草稿和成员数据。

## 2026-07-03 v34 问题记录

本次遇到的问题：

- 服务器验收真实 `/api/image/generate` 时返回 502，但模型配置、语言模型和语音测试均正常，说明不是密钥或网络连通问题。
- 根因是本地全局 `httpx` 较新，支持 `AsyncClient.get(..., follow_redirects=True)`；服务器依赖按 `server/requirements.txt` 固定为 `httpx==0.19.0`，旧版请求方法不支持 `follow_redirects` 参数，只支持 `allow_redirects`。
- 以后改服务端 HTTP 兼容性时，不能只用本机全局 Python 判断；要用项目 `.venv` 或 requirements 锁定版本检查方法签名。

本次验证：

- 已用项目 `.venv` 确认 `httpx 0.19.0` 的 `AsyncClient.get/post` 支持 `allow_redirects`，不支持 `follow_redirects`。
- 已把图片结果下载阶段改为 `allow_redirects=True`，不改接口、不改数据结构、不改图片模型配置。
- 本地编译和兼容性用例在 v34 版本记录中维护。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 本次只涉及服务端代码兼容补丁；服务器重新部署时仍必须保护线上账号、资产库、发布清单、数据分析、草稿和上传文件。

## 2026-07-03 v35 问题记录

本次遇到的问题：

- 服务器草稿箱删除后刷新又回来，不是部署覆盖数据，而是删除链路没有形成可靠的“远端已删”事实。前端之前本地先删并保存，远端 DELETE 失败会被静默吞掉，页面仍提示已删除；下次从服务器权威 state 拉取时，旧草稿自然又出现。
- 更隐蔽的复活风险来自旧浏览器缓存：A 成功删除某条 production 后，B 的旧 IndexedDB 里如果仍保留同 id，后续 B 整集合 upsert 时，服务端按 id upsert 会把已删 doc 当新 doc 插回。
- 因此只让前端 await DELETE 还不够，服务端必须记录删除墓碑；已删除 id 后续即便被旧快照 upsert，也必须跳过。
- 批量创作抽屉的“审核”页只显示成片 / 图片预览，不显示可编辑文案和联网参考，导致批量图文待审时必须跳回单号工坊才方便改文案。

本次解决办法：

- 服务端新增 `deleted_docs` 墓碑表；`delete_doc()` 删除 docs 后写入 tombstone；`upsert_docs()` 遇到 tombstone id 直接跳过，覆盖 `productions`、`jobs`、`sessions`、`batches`、`assets`、`accounts` 等同步集合。
- 前端关键删除路径改为 await 远端删除成功后再本地移除：草稿箱删除、单号创作页删除、Agent 批次删除、批次内单条删除、会话删除。远端失败时提示重新刷新 / 登录 / 稍后重试，不再假装删除成功。
- 批量创作详情抽屉的审核页新增可编辑“发布文案”面板，并在存在联网参考时显示参考标题、作者 / 互动、可用参考正文和“打开原文 / 搜索原文”入口；修改标题和正文会沿用原保存逻辑实时写入 production。

本次验证：

- `python3 -m py_compile 自动化产品/server/main.py 自动化产品/server/store.py 自动化产品/server/tests/test_store_tombstone.py` 通过。
- `python3 自动化产品/server/tests/test_store_tombstone.py` 通过：删除后旧快照再次 upsert 不会复活，同集合新 id 仍可正常写入。
- 关键删除调用点检查通过：`deleteProduction`、`deleteBatch`、`removeProductionFromBatch`、`deleteSession` 的 UI 调用点均已 await。
- 本次只改同步删除、抽屉审核显示和样式，不涉及服务器数据迁移；部署时仍只更新代码，不覆盖线上数据库、上传目录、账号、资产、发布清单、数据分析、草稿和成员数据。

### 注意

- 本版不包含任何密钥、服务器密码、公网 IP 或私网 IP。
- 服务器部署前继续先备份；部署后重点验收：删除草稿刷新不复活、旧浏览器刷新不复活、批量图文审核页可直接改标题 / 正文并查看参考文案。

## 2026-07-03 v36 问题记录

本次遇到的问题：

- 视频 / 数字人生产已有账号级 `voiceId / voiceName` 字段，但前端缺少“粘贴 voice_id 后识别是哪条声线”的入口；用户只能凭记忆填 ID，无法判断是否写错或是否被上游承认。
- 现有 `/api/tts/generate` 不适合作声线有效性测试，因为正式生成路径会在声线无效时回落到默认声线，容易误判“自定义 voice_id 可用”。
- 后端只知道系统预设声线，前端账号画像种子里还有克隆声线；因此“本地识别名称”和“上游有效性测试”必须分开：前端负责识别账号画像里的名称，后端负责严格测试 MiniMax 是否接受该 voice_id。
- 首页删掉账号矩阵后，数据问答如果仍独立占一张大卡，会造成首屏留白和下方待办 / 最新交付下压；最近交付原先用图标占位，也不利于快速识别内容。
- 本地一次性 shell 拉起 uvicorn 后会被当前执行环境清理，导致“健康检查刚通过、随后连接被拒绝”；本地验证需要用持久 session 跑 8787，或使用用户现有启动方式。

本次解决办法：

- 新增 `/api/tts/voice/lookup` 严格声线查询接口：只测试指定 voice_id，不回落默认声线；返回 configured / known / valid / durationMs / detail 等非敏感信息。
- 前端新增 `findKnownTtsVoice()` 与 `lookupTtsVoice()`：先查系统预设、当前账号、账号画像种子和小红书账号种子，再按需调用后端严格测试。
- 账号编辑、单号脚本页、分镜 / 数字人工坊都加入 voice_id 输入和“识别”按钮；识别到名称后可写回账号固定声线，仍保持非必填。
- 账号画像同步改为优先保留用户已手动设置的 `voiceId / voiceName`，避免后续种子同步覆盖固定声线。
- 首页移除“账号矩阵”模块；新建账号按钮移到顶栏；数据问答改为 hero 右侧小组件；统计 / 待办 / 最新交付整体压缩并拉高列表卡填充首屏；最新交付优先显示交付首图、账号头像或账号首张图片。

本次验证：

- `python3 -m py_compile 自动化产品/server/main.py 自动化产品/server/store.py` 通过。
- `node --check` 通过本次修改的关键 JS 文件。
- 直接调用本地新增 TTS 声线接口验证：目标 voice_id 上游返回有效；前端本地识别为 `Moss 沉稳声线`，并能列出关联账号。
- Playwright 走本地实际登录按钮路径验证首页：登录遮罩关闭、路由为 overview、账号矩阵不存在、数据问答在 hero 内、页面高度不溢出、最近交付 4 条均使用真实图片封面。
- Playwright 抽测视频账号编辑弹窗：`voice_id` 输入、声线名称输入和“识别”按钮均存在。

### 注意

- 本版只做本地代码更新和本地验证，没有触发服务器部署。
- 本版不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。
- 后续如果部署，仍必须只更新代码并保护服务器账号、资产库、发布清单、数据分析、草稿、上传目录和成员数据。

### v36 追加修复

- 联网参考在本地可用、服务器提示不可用时，优先判断服务器运行环境：后端实际检查的是服务进程能否找到并调用 `opencli`，以及服务器侧 OpenCLI 是否有小红书登录态。本机浏览器配置不会自动同步到服务器进程。
- 已把联网参考不可用提示改成服务器侧口径，避免误导为“只要本机浏览器配置 OpenCLI 就能让线上可用”。
- 空创作内容的本地随机选题收敛为四方向：强对比选型、联动绑定生态、打工人效率场景、小白反转入门；每个方向准备多条短选题，并加入运行时 seed，避免同一账号每次都出同一句。
- 文案生成与本地兜底改为更紧凑的真人经验写法：减少固定三点清单、压掉多余空行，要求正文包含具体场景、可执行方法和复核 / 边界判断。
- 主操作按钮与生成按钮从高饱和蓝紫渐变收敛为黑白灰毛边玻璃风，保留少量蓝色边缘光，避免页面按钮过于炫彩。

本次追加验证：

- `node --check` 通过 `api/ai.js`、`data/xhsTrendLibrary.js`、`main.js`、`views/overview.js`。
- `python3 -m py_compile 自动化产品/server/main.py 自动化产品/server/store.py` 通过。
- Node 抽样验证：空内容可从四方向里抽出不同短选题；离线文案无空行。

### 注意

- 本追加仍只修改本地代码和样式，没有执行服务器部署。
- 后续服务器部署仍只能更新代码和静态资源，不能覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。

## 2026-07-03 v39 问题记录

本次遇到的问题：

- 语音生成页首版视觉过重：大标题和过大的功能切换条占据首屏，音色库 / 文本输入 / 调试台不够像一个紧凑工作台。
- 空音色库提示文字贴边，顶部切换动效幅度过大；文本输入提示层在输入 / 粘贴时没有及时让开，造成“像是不支持粘贴”的错觉。
- 音色库只有选择、收藏、复制，缺少直接试听；用户无法在系统音色、收藏音色和我的音色之间快速判断哪条声音合适。
- MiniMax-M3 本地配置时，服务端最小调用一开始返回 401。原因不是代码没有读取 key，而是 `LLM_ENDPOINT` 使用了海外网关，和当前 key 所属网关不匹配；MiniMax 国内 / 海外网关混用时会出现“配置可见但调用 Unauthorized”。

本次解决办法：

- 语音生成页改为单屏极简工作台：左侧音色库、中间文本输入、右侧调试台；顶部只保留毛边玻璃小功能条和模型状态。
- 输入框改为实时同步输入 / 粘贴内容，聚焦或已有内容时隐藏打字提示；切换动效从弹跳改为轻滑，降低位移幅度。
- 音色卡片改为点击即选择并试听，同时保留独立试听按钮；收藏 / 复制按钮阻止冒泡，不会误触试听。
- 语言模型默认网关改为 MiniMax-M3 的国内 OpenAI 兼容地址；本地环境文件只保存非入库配置，文档和提交不写任何密钥。

本次验证：

- `node --check 自动化产品/js/views/voiceLab.js` 通过。
- 本地 8787 重新启动后，`/api/llm/config` 显示 MiniMax-M3 已配置，`/api/llm/test` 成功返回最小文本。
- 本地系统音色试听通过：`/api/tts/generate` 使用系统 voice_id 成功返回音频 data URL。

### 注意

- 服务器部署时不要覆盖线上数据库、上传目录、环境文件或认证缓存。
- 服务器侧 `LLM_ENDPOINT / LLM_BASE_URL / LLM_API_KEY` 必须属于同一 MiniMax 网关；如果线上也报 401，优先检查网关域名与 key 来源是否匹配，而不是先怀疑功能代码。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-06 v42 问题记录

本次遇到的问题：

- 线上数据监测和图文联网参考继续报“未检测到 OpenCLI / agent-reach 不可用”。原因不是前端按钮本身，而是服务器进程没有浏览器 Bridge、没有本地浏览器登录态，也无法直接继承开发机浏览器里的 OpenCLI 能力。
- 继续保留“联网参考 / 更新真实指标”的入口会造成用户误以为服务器已经能抓取小红书真实正文和指标，实际只会反复返回失败，增加产品臃肿和误导。
- 批量视频封面如果暴露上传、资产库、清空等多组按钮，会把“封面参考图”做成另一个复杂工坊；用户真正需要的是给一张参考图，系统按生成后的标题和文案自动出封面。
- 视频封面生成链路此前缺少独立封面稿：数字人 / 视频账号只能生成口播、分镜、音频和视频，发布前没有和标题文案强绑定的封面图位。

本次解决办法：

- 图文单号和批量创作的联网参考入口下线：前端不再显示联网参考开关，也不再触发小红书趋势搜索接口；旧状态里残留的联网开关会被强制按离线链路处理。
- 数据分析页下线“更新数据”动作，改为历史快照 / 复盘展示口径；发布回链只记录资产状态和已有快照，不再自动触发服务器不可用的抓取。
- 视频文案分镜工坊新增“封面图”模块，放在发布文案后面：封面提示词优先从发布标题、发布正文和产品名生成，可拖入 / 上传参考图；封面生成结果写入资产库并绑定当前生产单。
- 批量生产的视频账号只保留一个“封面参考图”按钮：可点选或拖入参考图，封面主题仍自动跟随批量生成后的标题和文案，不需要用户单独写封面需求。

以后避免方式：

- 服务器不具备浏览器登录态或 Bridge 能力时，不要把依赖本地浏览器的联网抓取能力作为线上可用功能展示。
- 上线前用服务器进程视角验证外部工具可用性；本机浏览器可用不等于服务器 FastAPI 进程可用。
- 以后新增批量参考能力时，优先保持“少入口 + 自动跟随内容”的形态，避免把批量生产面板变成多套独立配置。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-07 v47 问题记录

本次遇到的问题：

- 视频信息流链路里“发布文案”和“视频提示词”被混成了摘要：发布文案只输出一句简短说明，不能直接作为视频号 / 小红书发布稿。
- 后 15 秒功能演示提示词里出现“这是一条功能演示短视频”这种自指式口播，模型会按字面理解，无法生成自然功能介绍。
- 前后 15 秒提示词过于抽象，含有“要有概念稿”一类不可拍的描述，缺少具体物体、动作、运镜、环境音和功能步骤。
- 账号定位 / 账号视觉风格被注入信息流剧情提示词，容易把模型带偏到账号身份，而不是围绕本次标题、文案和产品功能生成画面。

本次解决办法：

- 将单号文案分镜和批量信息流的发布文案统一改成“标题 + 真实使用补充说明 + 具体场景动作 + 固定标签”的完整发布稿。
- 百度搭子视频文案强制补齐固定标签：`#AI工具 #AI提效 #codex #AI办公 #效率工具 #百度搭子`。
- 将 A 面改为可拍的夸张办公冲突：桌面、手机、资料、待办、人物动作和镜头移动都写清楚。
- 将 B 面改为真实功能演示：输入需求、任务拆解、资料整理、结果预览、修改入口和交付清单都在提示词里出现。
- 信息流提示词只保留角色外貌、穿搭、表情和声线锚点，不再把账号定位当作剧情信息。

以后避免方式：

- 新增视频提示词模板后先做关键词扫描，重点查自指式口播、抽象占位词和账号定位污染词。
- 发布文案和视频提示词分开验收：发布文案必须能直接发，提示词必须能直接拍。
- 视觉风格可以借账号风格，剧情和功能必须跟本次标题、文案和产品功能绑定。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-06 v45 问题记录

本次遇到的问题：

- Seedance 2.0 官方任务接口可以接受图片、视频、音频多模态参考，但平台原实现只把图片和音频真正传给后端，视频参考会在前端 / 后端链路中被忽略。
- JobRunner 原来最多只收集 9 个参考资产；如果同时有多张图、视频和音频，后面的素材可能在提交前被截掉。
- 本地 `data:`、`localhost` 视频 / 音频不能被 Seedance 上游拉取；如果不提前拦截，会让用户误以为“已经参考了素材”，实际任务可能失败或没有带参考。
- 旧视频 key 对 Seedance 2.0 返回模型未开通 / 无权限；换成与已开通模型同项目的本地私密 key 后，同一接口可以提交并生成成功。该问题属于环境 key / 项目权限不一致，不是 payload 结构错误。

本次解决办法：

- 前端 Seedance provider 允许图片、视频、音频三类参考进入 `/api/video/submit`。
- 服务端按 Seedance 能力拆分并裁剪参考素材：最多 9 张图、3 段视频、3 段音频；提交时分别写入 `reference_image`、`reference_video`、`reference_audio`。
- 对本地视频 / 音频参考增加明确错误提示，要求配置可被上游访问的公网 URL 或 `PUBLIC_BASE_URL`；图片继续保留文本降级，避免单号创作卡死。
- 用公开参考图片、公开视频、公开参考音频提交 Seedance 2.0 最小任务，最终轮询到 `succeeded` 并返回视频输出。

以后避免方式：

- 验证视频模型时要分三步：配置可见、任务提交成功、轮询到 `succeeded` 并有输出；只提交成功不等于最终生成完成。
- 浏览器控制台显示模型已开通，不代表本地或服务器进程里的 API key 属于同一项目；部署前必须用服务器进程视角跑一次最小提交。
- 不要把 API key、完整任务链接、服务器凭据写入文档、提交信息或回复；只记录模型名、接口类型和非敏感验证结论。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-06 v46 问题记录

本次遇到的问题：

- 服务器端此前无法稳定使用外部搜索 / 外部指标采集，核心原因是这些能力依赖服务器进程可见的外部工具或浏览器登录态；本地浏览器配置成功不等于线上 FastAPI 进程可用。
- 产品代码里残留的提示文案和注释仍会让人误以为图文、批量和数据分析还保留外部搜索能力，需要彻底从产品入口和提示中清掉。
- Seedance 2.0 与 OmniHuman 数字人不是同一套鉴权和接口。Seedance 已走 Ark 任务接口跑通；OmniHuman 走智能视觉 CV 签名接口，当前本地环境缺少 AK/SK 对，不能用 Ark 视频 key 直接替代。

本次解决办法：

- 图文、批量创作和数据分析均不再展示或触发外部搜索 / 外部指标采集能力；后端同步删除对应接口和工具调用。
- 代码关键词复查后，进一步清理残留提示，避免出现“仍可外部搜索或采集”的误导。
- 用本地 8787 复测 Seedance 2.0 多模态参考任务，确认公开图片、公开视频、公开音频可以随任务提交并最终生成成功。
- 用本地 8787 复测 OmniHuman 最小提交失败路径，确认缺 AK/SK 时后端直接给出明确错误，不会误提交或假成功。

以后避免方式：

- 上线前所有外部能力都必须从服务器进程视角验证：配置可见、提交成功、轮询成功、有输出四步缺一不可。
- 不要把依赖本地浏览器、OpenCLI 或临时登录态的能力作为服务器可用功能展示。
- 区分 Ark 视频任务 key 和智能视觉 AK/SK；新增模型接入时先确认官方接口类型，再落到环境变量和后端适配。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-06 v44 问题记录

本次遇到的问题：

- OmniHuman 1.5 数字人不是 Seedance/Ark 的同一条 Bearer 任务接口。官方调用文档走火山智能视觉 `CVSubmitTask / CVGetResult`，需要 AK/SK 签名、固定 `req_key`、公网可访问的 `image_url` 和 `audio_url`。
- 如果继续把数字人模型 ID 塞进 Ark 的 `/api/v3/contents/generations/tasks`，即使模型已在控制台开通，也可能得到“模型未开通 / 无权限 / 找不到模型”的误判。
- 当前本地环境只有 Seedance/Ark 视频 key，没有火山智能视觉 AK/SK，也没有 `PUBLIC_BASE_URL`；因此 OmniHuman 无法在本机完成真实出片验证。
- 当前本地视频 key 对标准 Seedance 2.0 和 fast 接入点均未返回可用权限；这更像 key / 项目 / 接入点权限不一致，而不是本次代码提交结构问题。

本次解决办法：

- 服务端新增 OmniHuman 专用 CV 签名通道：`/api/video/submit` 收到 `__digital_human__` 模型时，改走智能视觉提交；轮询时通过带前缀的 providerRef 路由到智能视觉查询。
- 后端缺 AK/SK、缺公网角色图、缺公网音频时直接返回明确错误，不再假装提交成功。
- 前端数字人工坊派发任务时改为按口播分段派发，每段携带角色图和对应分段音频，满足 OmniHuman 必填资源要求。
- 工坊按钮从“API 预留”改成真实派发；后续只要环境和公网资源齐备，就能进入实际提交链路。

以后避免方式：

- 区分三类视频接口：Seedance Ark 内容生成、智能视觉 OmniHuman 数字人、MiniMax TTS；不要用一个 API key 或一个 provider 配置混跑。
- 上线前用服务器进程视角检查环境变量：Seedance key、智能视觉 AK/SK、`PUBLIC_BASE_URL`、上传目录访问路径都必须分别验证。
- 数字人资源必须能被上游服务直接访问；本地 `data:`、`blob:`、`localhost`、内网文件都不能作为 OmniHuman 的 `image_url` 或 `audio_url`。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-06 v43 问题记录

本次遇到的问题：

- Seedance 控制台已开通模型，但平台本地服务仍可能沿用旧进程里的旧模型 / 旧环境变量；只改代码不重启 8787，`/api/video/config` 不会更新。
- 标准 Seedance 2.0 真实任务提交后可能长时间停在 running，不能只用前 1-2 分钟的轮询结果判断失败；需要继续查同一任务的最终状态。
- 数字人模型与 Seedance 2.0 不是同一个已验证入口。Seedance 2.0 已在 Ark 视频任务接口跑通，但 OmniHuman/OminiHuman 仍需要准确模型 ID 或独立接口，不能混在本次验收里一起承诺。

本次解决办法：

- 将服务端默认视频模型切到标准 Seedance 2.0，同时保留环境变量覆盖能力。
- 本地 8787 使用临时环境变量启动，确认平台自己的 `/api/video/submit` 可以提交 Ark/Seedance 任务。
- 对提交成功的任务继续轮询到终态，最终确认返回 `succeeded` 并有视频输出。

以后避免方式：

- 验证视频模型时分三层看：`/api/video/config` 只代表配置可见；`/api/video/submit` 成功代表上游接受任务；轮询到 `succeeded` 且有输出才代表真实跑通。
- 服务器部署时要重启服务进程并检查运行时环境变量，而不是只看代码默认值。
- 不要把 API key、任务完整回链、服务器凭据写入文档、提交信息或回复；只记录模型名、接口类型和非敏感验证结果。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-08 v51 问题记录

本次遇到的问题：

- 服务器部署线程验收时发现数字人前端预检会把刚上传的角色图 / 口播音频判断为非公网资源。根因是共享模式下素材已上传到服务端并写入文件地址，但同一浏览器里 `urlFor()` 仍优先返回本地 `blob:` 预览地址。
- 视频链路虽然文案已合并到文案分镜，但旧 `studio/copy` 路由和 stepper 文案还可能让部署验收误以为独立“文案”页仍存在。
- 视频发布清单需要以封面图作为第一眼预览；如果第一步没有生成或上传封面，发布后只能看到通用视频占位。
- 语音生成上游返回余额/额度不足时，原错误容易被误读为接口或服务不可用。

本次解决办法：

- `urlFor()` 在共享服务模式且素材有服务端文件地址时，优先返回服务端文件地址；普通离线预览仍保留本地 blob 兜底。
- 视频 flow 正式收敛为“文案分镜 → 剪辑 → 审核”，旧 `script / boards / prompts / render / copy` 路由都会归一到文案分镜。
- 工坊下一步、剪辑进审核、审核发布三处均要求视频已有封面图；交付快照保存 `coverAssetId`，发布清单视频卡片优先展示封面。
- TTS 错误提示区分上游余额/额度不足，部署时应优先检查服务器私有环境里的语音 key 和账户额度。

以后避免方式：

- 数字人真实验收必须检查前端提交到后端的资源 URL 类型：应是服务端文件地址或公网地址，不应是 `blob:`、`data:`、`localhost` 或内网文件。
- 链路合并不仅要改页面内容，还要同步改 flow、stepper、旧路由守卫、顶部面包屑、审核/发布前置条件。
- 发布清单的预览图、zip 内容和全链路回看必须都来自同一份最新 production 快照；回撤微调后重新发布要重新同步快照。

### 注意

- 本次只修改本地代码和文档，没有执行服务器部署。
- 后续如部署，只允许更新代码和静态资源；不得覆盖线上数据库、上传目录、账号、资产库、发布清单、数据分析、草稿、成员数据、环境文件或认证缓存。
- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。

## 2026-07-08 v57 接手记忆

本次稳定下来的规则：

- 自定义视频创作必须走 LLM：用户标题和正文是第一优先级，产品库只作为后台参考；模型不可用时直接报错，不使用本地模板兜底。
- MiniMax adaptive thinking 可能返回被截断的思考内容；当前只允许再用同一 LLM 关闭 thinking 重试一次，仍然不能回退到本地模板。
- 信息流发布文案要偏专业解析，不要口水开场；用户填写的标题不能被模型标题覆盖。
- 信息流 B 面的分镜提示只放进 storyboard prompts；视频 prompt 可以保留 `0-3s / 3-7s` 这种时间分段，但不得混入“第一镜 / 第二镜 / 功能演示分镜结构”这类分镜图提示。
- 视频 prompt 的负面约束固定为：无字幕，不生成花字，不生成水印，不生成二维码。不要再写“音效留到智能混剪”“无 BGM”“无口播 / 无人声”等会阻止上游自然生成声音的限制。
- 视频封面提示词以标题为主，只加一个随机风格和简短负面约束；不要把正文摘要塞进封面图。
- 批量生产任务板按“自定义 / 自动”和“图文 / 素材视频 / 真人视频”拆开，同一看板只运行一种内容；自定义模式逐账号填写标题和文案。

后续部署提醒：

- 部署只同步代码和静态资源，必须保护线上数据库、上传目录、账号、资产库、发布清单、草稿、成员、分析数据、环境文件和认证缓存。
- 服务器验收要确认静态资源版本、单号和批量功能都是最新 v57 行为，再验证 LLM、TTS、图片、Seedance、OmniHuman、JustOneAPI 等接口可用性。
- 数字人生成应保持并发 1，并观察是否仍触发上游并发限制；如果失败，先区分上游限流、资源 URL、公网访问、额度和本地代码问题。

### 注意

- 本记录不包含任何密钥、服务器密码、公网 IP、私网 IP、token 或账号凭据。
## 2026-07-23 v114 修复：本地视频工坊 sidecar 不能被旧启动器误停

- 现象：主平台 `8787` 健康且页面可打开时，视频工坊会突然显示“视频工坊服务未运行，请启动独立 sidecar（127.0.0.1:8765）后重试：ConnectError”。该错误发生后普通对话无法发送，但并非导演、Seedance、TTS 或项目数据本身失败。
- 原因：本机可同时存在 Finder / Terminal 的多个启动器。旧实现只在共享 PID 文件中记录 sidecar PID，任一旧启动器退出都会执行全局 stop，可能结束刚被新启动器接管的 `8765` sidecar；日志表现为 Uvicorn 正常 shutdown，而不是 Python 崩溃。主服务存活期间也没有独立健康守护。
- 修复：PID 文件改为 `PID + 启动实例标识`，退出清理只接受本实例记录的 sidecar；主服务后台运行并由同实例守护检查 sidecar，连续两次健康失败才重启，避免一次探测波动误杀任务。新增进程级回归，明确断言 launcher A 退出后 launcher B 的 sidecar 仍存活。
- 规避：本地双服务启动必须把“主服务、sidecar、PID 文件、退出 trap、守护进程”视为同一生命周期协议。不要用裸全局 PID stop，也不能把单次健康探测直接当作重启依据；排查 ConnectError 时先区分端口未监听、进程被优雅终止、上游模型错误和项目 pipeline 错误，禁止把 sidecar 生命周期问题误改成导演或视频生成逻辑。

## 2026-07-24 v117.13 问题记录：参考图不能在完整提示词写完后才补路由

- 现象：图文创作先只按标题和正文写完完整提示词，再在图片提交前追加统一 / 定制参考图。模型没有在提示词规划阶段理解附件的用途，统一图容易被无差别发送，单张定制图也可能无法改变该图的主题、构图与主体位置。
- 根因：参考图选择被放在生成任务提交阶段，视觉规划只修改附件列表和短注释，无法参与 MiniMax-M3 写同规格完整提示词；若把附件视觉细节再复述进 prompt，又会和真实附件重复，反而增加生图冲突。
- 解决：改成“视觉模型读取真实参考图与图卡草案 → 返回逐图附件路由及一句用途 / 位置 → MiniMax-M3 依据该规划和原有完整规格写提示词 → 仅提交已选附件”。定制参考携带目标图卡索引，服务端输出会过滤并强制保留该图的定制附件，禁止路由给其他图。
- 规避：没有明确视觉模型配置时不能宣称已看图、不能由文件名猜测视觉内容；只保留真实附件传图的回退，并在部署前只读核验视觉模型能力。图片和视频调用也不能伪装成 Token 或倒推历史账单，只记录后续成功调用的张数 / 任务数。
# 2026-07-23 v117.9 修复：发布清单不能让每个浏览器或发布人各自编号

- 现象：同一天由不同发布人交付时，发布清单会同时出现较大的全局编号和新的 `#001`；供应商子账号又只看到已分配的子集，若以可见列表重新计数就会和管理员、创作者端不一致。
- 原因：旧常规交付由浏览器 `state.ui.deliverSeq` 预分配，历史记录也只对缺失编号做只读投影；不同账号、不同旧缓存可能把个人序号写回，无法形成一个服务端唯一账本。
- 修复：服务端先对全部交付按 `deliveredAt → createdAt → updatedAt → id` 生成只读 `globalSeq`，所有角色均优先显示它；受控迁移再把历史 `pubSeq` 一次性校准为同一连续序列，迁移完成后所有新交付均由服务端账本领号。回推时服务端剥离 `globalSeq` / `projectedSeq`，并强制保留已存在交付的权威 `pubSeq`。
- 规避：发布序号属于全局业务账本，不能根据角色可见集合、发布人、页面筛选或本地缓存重新编号。历史校准必须在受保护部署中先做数据快照、只执行一次并验收四类角色；不能用本地 state 或整库同步覆盖线上资产。

## 2026-07-23 v117.9 修复：供应商数据助手不能用同高拉伸撑开左侧 KPI

- 现象：为了让供应商首页数据助手与左侧看板底部对齐，直接让同一 CSS Grid 行 `stretch` 后，右栏的长对话高度会被分摊给左侧内部网格，四张顶部 KPI 卡被异常拉高。
- 原因：左侧主列本身也是 Grid，默认 `align-content: stretch` 会把父行分配的剩余高度继续分散到内部自动行；右侧会话内容因此间接改变整个首页高度。
- 修复：左右列改为共享受限视口高度；左侧 `align-content: start` 保持 KPI、图表和最近操作的自然高度，右侧消息区 `minmax(0, 1fr)` 独立滚动。
- 规避：并排的看板需要同高时，要限制容器高度并确认嵌套 Grid 不会再分摊空余高度；长聊天记录只能滚动在消息容器内，不能参与页面或同级卡片的高度计算。
## 2026-07-24 v117.18 修复：有参考图时不能静默按泛化标题写图文

- 现象：用户上传“品牌 Logo、自媒体套件、视频生成思考过程、主界面”等统一参考图，只填写泛化标题时，正文仍可能写成桌面整理、天气查询等默认效率工具案例；四张图又可能各自携带全部参考图，提示词没有说明某一张素材应落在画面的什么位置。
- 原因：服务端把 `LLM_VISION_MODEL` 为空直接当作“没有视觉能力”，尽管当前部署的 MiniMax-M3 可以接收图片；前端随后静默返回空视觉摘要和均衡回退，先按标题写完泛化正文，再把附件当成装饰性输入。视觉失败没有阻断任务，因此用户看到的是貌似成功但没有真正看图的结果。
- 修复：独立视觉模型优先，未配置时仅对 MiniMax-M3 启用受控的图片理解回退。图文流程固定为“统一参考视觉摘要 → 正文 → 图卡 → 全部统一 / 单图定制参考的逐图编排 → 完整提示词与对应附件提交”；有参考图但任一步视觉读取或编排未完成时明确停止，禁止全量广播和标题模板降级。非 Logo 参考优先完整进入最相关的一页，提示词只补充其用途、位置和保留要求，不重复图像细节。
- 规避：是否“看过图”必须由真实多模态请求的成功结果证明，不能以文件名、空字符串或文本模型响应冒充。对“标题泛化、参考图具体”的任务，正文主题必须由视觉摘要锚定；每张图只携带已分配的附件子集，并以 10 轮以上包含多附件的确定性回归保护执行顺序。

## 2026-07-24 v117.18 修复：供应商下载记录应由数据助手在权限快照内回答

- 现象：供应商端问答只能回答交付数量、回传链接和播放量，无法回答“谁下载过”；界面加载期间也没有明确说明正在读取数据。
- 原因：已授权快照遗漏 `supplierDownloadedBy / supplierDownloadedAt`，模型提示词未声明下载记录能力；前端没有同一条消息的读取中状态。
- 修复：快照加入下载标记、时间和当前供应商层级可见的成员名称。父账号只可看到自己的子账号名称，子账号最多看到父账号的泛称；服务端先计算带交付编号的权威下载行，再交给 MiniMax-M3 组织答复。界面统一称“数据助手”，请求中显示“正在读取数据…”。
- 规避：扩展助手数据权限时必须先扩展服务端角色裁剪快照和确定性事实层，不能把原始全集或跨层级成员资料交给模型；加载态只能替换当前对话气泡，不能触发整页重绘。
