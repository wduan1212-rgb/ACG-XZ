# Project Memory - 平台搭建新会话交接

更新时间：2026-06-28

## 当前项目定位

- 本地项目：`/Users/macbookpro/Desktop/obsidian/知识库/百度/codex自动化产品2`
- 本地测试入口：固定使用 `http://localhost:8787/#/overview`
- 产品是小红书/视频号自动化工作台，覆盖批量创作、单号创作、图片工坊、分镜/视频工坊、混剪、整体资产、草稿箱、发布清单、数据分析、多人共享服务端。
- 当前原则：先本地跑通和浏览器验证，再交给部署线程。不要把 API key、服务器密码、公网 IP 写入前端、文档或最终回复。

## 未完成任务总览

### 1. 批量创作意图路由

- 恢复原来的量产计划面板，不要变成聊天问答式建议。
- 用户说“选两个图文 / 创作两个图文号 / 做两个图文 / 创建两个图文号 / 选几个素材号 / 找一些真人号 / 量产3个账号”等，都应直接进入量产计划。
- 只有明确说“新增账号/创建账号/账号名为...”时才走账号创建。
- 完成后至少用 30 条不同中文指令测试，不允许再出现“建议优先处理 imagesx4”这种聊天回复。

### 2. 批量创作 UI

- 保持简单直接：宣传产品、总创作内容、每个账号本次创作内容、统一参考图、定制参考图、账号选择。
- 去掉多余标签/风格策略入口，默认沿用账号风格。
- 统一参考图：所有选中账号共享，最多 5 张。
- 定制参考图：单账号单独通道，最多 3 张。
- 统一参考图 + 定制参考图总计最多 8 张；定制图优先，不覆盖统一图；必须支持拖拽上传。

### 3. 图片工坊

- 图文链路不再有脚本步骤，入口应是“图片工坊 -> 文案 -> 审核”。
- 默认站内生成，站外回传只是可选模式。站内模式不要出现“出图回传”文案。
- 预览框、待出图框使用小红书竖版 3:4 视觉比例，不要显示 9:16 误导。
- 一键生成全部图片按钮放右侧；单张生成/上传按钮也靠右对齐。
- 图片提示词要基于用户创作内容做分析总结，不要把用户原文硬塞进去。
- 不要把内部结构词“封面、痛点引入、问题引入、关键步骤、图1、第1张”等写进画面文字。
- 站内生成 Image2 已验证要走正确请求形态；尺寸、水印、参考图都不要再改坏。
- 已确认 watermark 用 `logo_add: 0` 解决；如果再次出水印，优先恢复“能出图但带水印”的请求形态，只补 `logo_add: 0`。

### 4. 图片精修 / 原创度增强

- 图片生成后、回传后，在进入文案/审核前自动轻量处理。
- 不裁剪、不强制扩图、不改最终尺寸。
- 做轻微画质增强、透明图层、饱和度/亮度/对比度差异化滤镜、小矢量角标或点线装饰。
- 矢量元素和滤镜参数要有多套随机组合，避免每张图叠加同样图形。

### 5. 发布清单

- 增加产品标签，例如 Dumate / 搭子 / 其他自定义产品。
- 下载包和素材命名里把产品标签放在日期前面，方便供应商识别。
- 只发布后的素材进入整体素材库；未发布的口播、视频、图片留在草稿箱或账号资产，不污染全局共享库。

### 6. 视频 / 数字人

- 真人视频默认数字人模式；Seedance 模式切换必须真的切换，并有滑动反馈。
- 数字人模式下不要显示旧 Seedance 分镜提示词链路。
- 数字人链路：创作内容/产品 -> 口播草稿 -> MiniMax 按不超过 30s 切段生成音频 -> 每段绑定统一角色图或单段角色图 -> 预留数字人 API -> 进入混剪。
- 数字人卡片需要“一键生成视频”和每段“重新生成”，不需要“参考声线/上传参考声线”按钮。
- Seedance 模式保留为独立模式，生成视频提示词时才出现分镜提示词。

### 7. MiniMax 语音

- 默认模型 `speech-2.8-hd`。
- 使用 B 端 key 后，加入可用声线列表，删除旧的无效 voiceId。
- 增加声线收藏、固定到账号功能。
- 未发布音频不要进入整体素材库。

### 8. 登录页 / 账号卡

- 登录页改用星阵 logo，logo 更大更大气。
- 登录背景不要用会闪烁的视频循环，改成 CSS/HTML 动画或无缝动画。
- 登录页不要暴露 admin 账号密码。
- 已登录用户刷新时不应闪现登录页。
- 账号头像保持 1:1，不要被压扁；头像上缘和账号名区域更协调。

### 9. 小红书违禁词

- 用户提供了违禁词语料，应在标题、文案、图片提示词、视频提示词生成时底层规避。
- 不要在 UI 里显示解释性提示；内部替换、弱化、改写即可。
- 注意不要过度误伤正常表达，修复后用含敏感词的输入做测试。

## API 和容易踩坑的配置

- DeepSeek/Tencent MaaS 语言模型：OpenAI-compatible chat completions endpoint，模型 `deepseek-v4-pro-202606`。key 只放服务端 `.env`，不要写入前端/文档。
- Image2/Tencent MaaS：模型 `custom-imagemodel-gt`。保持已跑通的请求形态；`logo_add: 0` 仅用于去水印。不要为尺寸问题做 canvas 扩图或裁切，模型本身能按提示词尺寸出图。
- Image2 调试结论：如果中文明显乱码、尺寸总是 1:1、控制台没有图像模型消耗，大概率请求走错模型/路径或回落到错误实现。
- MiniMax：默认 `speech-2.8-hd`；voice id 只使用 B 端支持列表。
- Seedance：以前多次失败来自 baseUrl、参考图、网络或请求体变化；改动后必须浏览器实测。
- 所有 key、token、服务器 IP、root 密码都不要进入 git、前端 bundle、Markdown 交接文档、截图说明。

## 新百度云 BCC 部署准备

- 新服务器是百度智能云 BCC，不是阿里云。
- 地域：北京；可用区：北京-D。
- 系统：Ubuntu 24.04 LTS amd64。
- 规格：8 vCPU / 32GB。
- 存储：系统盘 40GB，数据盘 100GB。
- 公网带宽：约 8 Mbps。
- 管理用户：root。公网 IP、私网 IP、密码不要写进文档或回复，需要从用户私下提供或控制台读取。

建议前置准备：

1. 安全组先开 22、8787；正式域名后开 80/443。
2. 数据盘挂载到 `/data/dumate-studio`，上传/数据库/日志放数据盘，不放系统盘。
3. 安装 Node LTS、Python 3.10/3.11 venv、ffmpeg、nginx、git、unzip、build-essential。
4. 准备 systemd 或 supervisor 管理前后端。
5. 服务端 `.env` 写入 LLM、Image2、MiniMax、Seedance 等 key；不要写进仓库。
6. 部署前先备份线上 state/db/uploads。
7. 部署后必须验证：`/api/health`、LLM 文案、Image2 文生图/参考图、MiniMax TTS、Seedance、上传/下载、多人同步、发布清单下载包。

多人共享部署重点：

- 服务器状态必须为权威源。
- 删除账号/增加账号后其他同学刷新或同步应能看到，不允许本地 IndexedDB 旧账号回推复活。
- 新上传资产必须走服务端文件接口，不能只在上传者本地 IndexedDB。

## 接手建议

1. 先修批量创作入口路由，因为用户当前最不满这里“变蠢、变成聊天”。
2. 用浏览器真实测试 30 条批量指令，确认全部进量产计划面板。
3. 再修图文/图片工坊的小 UI 和发布清单产品标签。
4. 最后整理部署线程需要的文件清单和验证步骤。
