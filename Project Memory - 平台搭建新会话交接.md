# Project Memory - 平台部署3号会话交接

更新时间：2026-07-01

本交接文档用于“codex自动化产品2 / 平台部署3号”继续接手。不要在回复、文档、代码、提交信息中写入任何明文 API Key、服务器密码、公网 IP、私网 IP 或账号凭据。

## 1. 当前项目与代码状态

- 本地项目路径：`/Users/macbookpro/Desktop/obsidian/知识库/百度/codex自动化产品2`
- 当前工作分支：`codex/v1-star-array-batch-image`
- 最新已推送提交：`ce58b9a v28 harden batch image flow`
- 本地固定预览端口：`http://127.0.0.1:8787/`
- 部署目标：百度智能云 BCC，不是阿里云。服务器敏感信息只允许在服务器环境变量或运维私密渠道中处理，不能写入仓库。
- 当前未纳入提交的本地文件包括：`.codex-lark-auth/`、`diagrams/`、`星阵背景.mp4`。其中 `星阵背景.mp4` 是下一波首页背景任务需要使用的素材，但 v28 尚未接入。

## 2. v28 已完成状态

### 批量创作

- 批量创作已恢复为量产计划面板，不走聊天问答。
- 新建量产默认 0 个账号，用户手动选择后再确认执行。
- 账号池固定为 80 个账号：50 个小红书图文账号 + 30 个视频号账号。
- 批量图文默认 4 张图，单号和批量都按账号风格 + 创作内容 + 产品信息生成。
- 批量图文新链路已合并为 `images -> review`，提示词和成图实际存放在 `artifacts.images.items`，不要再按旧字段 `artifacts.imagePrompts` 判断是否生成。
- 节点式任务看板已恢复，最新一次浏览器验证中可以看到任务节点、进度和待审状态。

### 图片与文案链路

- 图文文案已前置，图片 prompt 主要参考文案、产品信息和账号风格。
- 图片 prompt 出口会清理 `标题：/正文：/画面文字：` 等可见字段标签。
- 对外文案标题、正文、tag 不应出现自家产品名；图片 prompt 内部可以使用中文产品名帮助模型理解界面与功能关系。
- 图片负面提示词保持极短，只保留页码、二维码、左上角/右上角 logo 相关约束。不要继续堆大段负面提示词。
- 图片生成后进入发布前轻量精修；当前精修仍是平台内置逻辑，下一波要用用户提供的 `图像Skill.md` 替换最后一步精修策略。

### 发布清单与资产

- 发布清单已有产品标签、供应商视角、批量下载未下载、回传链接、供应商备注、展开预览。
- 供应商视角表格已做省略和 hover 收纳，避免横向滚动。
- 已发布生成图会进入共享资产库；BGM 和录屏已归入剪辑素材库，和账号资产区分。
- 发布清单回撤删除已有基础能力：已下载或已回传发布链接的记录不能回撤。

### 验证结果

- `/api/health` 本地正常，语言模型配置可用。
- `/api/image/config` 本地正常，图片 API 配置可达。
- 浏览器真实验证过 `#/agent`、`#/delivery`、`#/assets`：无白屏，主要区域无横向溢出。
- 浏览器真实跑通过 1 条批量图文最小任务：4/4 张站内图片完成并进入待审。

## 3. 下一波最高优先级任务

### 3.1 多人同步与可见性

用户在服务器上作为创作者发布内容后，其他同学刷新页面看不到新内容；退出账号再登录可以看到，说明大概率是前端刷新/同步策略或服务端 state 拉取时机问题。

需要排查：

- 刷新页面时是否真的重新请求服务器权威状态。
- IndexedDB / localStorage 是否覆盖了服务器新内容。
- 登录后全量 state、增量同步、发布清单和整体资产是否使用同一套服务端数据。
- 不能靠退出登录才能看到别人新发布的内容。

验收：

- A 用户发布内容后，B 用户直接刷新页面即可看到。
- 不同用户刷新后看到的发布清单、整体资产、账号公开素材一致。

### 3.2 发布账号显示

- 每一个账号发布内容都要能看到“发布者账号是谁”。
- 创作者端发布清单要显示发布者。
- 供应商端也要显示发布者，便于追责和沟通。
- 如果是管理员代发，也要区分“发布者 / 内容所属账号 / 管理员操作人”。

### 3.3 管理员与创作者草稿隔离

当前管理员登录后可以看到创作者账号的批量生产内容。需要改为：

- 管理员账号的单号创作链路、批量创作链路和草稿箱，只显示管理员自己的工作流。
- 管理员不能在创作工作台看到其他成员的未发布草稿、图片工坊、批量任务链路。
- 管理员仍可在发布清单、整体资产、数据分析等管理视角查看全局已发布或共享数据。
- 创作者之间也不能互看草稿箱和未发布任务链路。

验收：

- 管理员创建的批量任务只在管理员工作台可见。
- 创作者 A 的草稿不出现在创作者 B 和管理员的创作工作台里。
- 已发布内容按发布清单权限正常共享。

### 3.4 数据分析接入真实搜索/抓取

数据分析页不能继续展示模拟数据。用户会粘贴小红书链接，需要真实尝试抓取或搜索回传数据。

要求：

- 增加“更新数据”按钮，具备搜索条件的用户点击后触发真实抓取/搜索。
- 成功则写入大家都能看到的数据分析结果。
- 失败必须提示真实原因，例如无权限、链接不可访问、搜索服务未配置、超时、平台限制。
- 不要出现模拟阅读量、模拟趋势等误导信息。
- 如果总阅读不可得，可以换成收藏、评论、点赞、互动趋势、发布状态等可抓取字段。
- 阅读趋势可改成互动趋势，字段以真实可获得数据为准。

需要先做技术验证：

- 粘贴一个真实链接，确认当前本地/服务器是否能抓到标题、作者、互动数、发布时间、正文或评论等任何字段。
- 如果只能抓到部分字段，就只展示部分字段。
- 如果完全抓不到，展示“未获取到真实数据”而不是 mock。

### 3.5 联网搜索与文案改写链路

当前联网搜索看起来没有真正连接到文案改写，且本地不联网文案也偏短、偏模板、缺少人的口吻。

目标链路：

1. 如果开启联网搜索，先按用户创作方向和轻度账号风格搜索热门内容。
2. 保存原始参考标题、正文、标签，供用户查看。
3. 基于热门样本进行高比例改写，标题和文案不直接带自家产品名。
4. 改写后的文案进入发布文案区域。
5. 图片 prompt 再参考改写文案、产品信息和账号风格生成。

UI 要求：

- 在发布文案区域，如果开启联网搜索，改成左右两张卡：
  - 左侧：参考的热门标题 / 文案 / 标签。
  - 右侧：改写后的标题 / 文案 / 标签。
- 单号创作和批量创作都要支持。
- 批量创作可以多条内容参考同一爆款链接，但改写标题和文案必须明显不同。

文案要求：

- 标题不要直接带“百度搭子 / 百度秒哒”等自家产品名。
- 正文也不要直接带自家产品名，除非用户明确要求；更多使用“桌面智能体 / 这个工具 / AI 应用搭建工具”等说法。
- 文案要更像真人分享：更长一点、有利他性、有心得、有具体场景，不要每条都三段分点。
- 允许使用分点，但不能所有内容都固定成分点格式。

### 3.6 删除图文账号定位干预

- 图文账号的“账号定位”不再参与生成。
- 单号创作、批量创作、图片 prompt、发布文案都只使用：用户创作内容 / 联网搜索结果 / 产品信息 / 账号创作风格。
- 可以保留账号定位作为后台资料，但生成链路不要读取它。

### 3.7 图片 prompt 精简与参考图语句清理

需要删除图片 prompt 中这句干扰模型的话：

`综合参考产品界面层级、品牌色、截图质感和视觉密度；参考图里的旧标题和示例文案需要按本次主题重写`

原因：参考图不一定是风格参考，可能只是产品图、logo、界面或普通素材，这句话会误导图片模型。

新原则：

- 图片 prompt 主要来自改写后的文案、产品信息和账号风格。
- 参考图只作为图片 API 的参考输入，不要在文字 prompt 中过度解释。
- 正向 prompt 精准、清晰、画面化；不要继续增加负面提示词。
- 封面优先冲击感和点击率，信息简单、标题醒目；内页才承担解释、例子、流程和结果。

### 3.8 砍掉站外生成

- 将“站外生成 / 站外上传整段提示词”的功能整个砍掉。
- 平台只保留站内生成、上传补图、参考图选择和发布前处理。
- 删除 UI 文案、入口、状态和相关死分支，避免用户误解。

### 3.9 替换最后一步图片精修

用户提供了新的图像 Skill：

`/Users/macbookpro/Desktop/obsidian/知识库/百度/百度技能库/图像Skill.md`

要求：

- 用它替换现有最后一步图片精修策略。
- 不要直接把示例脚本原样塞进业务；要工程化为平台的图片后处理模块。
- 保持不裁剪、不强制扩图、不改变主体构图。
- 重点处理：元数据清理、轻度噪声/像素指纹重写、轻度锐化、轻度色彩/对比修复、合理导出。
- 注意不要破坏中文文字清晰度；过强颗粒、模糊、晕影都要克制。

### 3.10 首页背景与品牌图标

- 将首页背景换成项目根目录里的 `星阵背景.mp4`。
- 保留视频音频。
- 去掉现在首页 logo。
- 浏览器 favicon 换成用户给的 `2.png`：`/Users/macbookpro/Desktop/临时文件/常用/2.png`
- 部署到服务器后要确保视频不会导致首页卡顿；建议压缩/转码、懒加载或仅首页加载，不要全站常驻。

### 3.11 服务器数据保护

后续部署时：

- 不要用本地空 state 覆盖线上账号、资产、发布清单、数据分析结果。
- 服务器已有业务数据必须先备份。
- 账号、资产、发布记录、用户草稿隔离相关数据结构变更要写迁移逻辑。
- 所有生产 key 最终都应直接配置在服务器环境中，但不能写入仓库或文档。

## 4. 推荐执行顺序

1. 先修多人同步：刷新后拉取服务器权威状态，发布内容对其他用户即时可见。
2. 修权限隔离：管理员/创作者工作台和草稿箱互不串。
3. 修发布者展示：创作者端和供应商端都能看到发布者账号。
4. 砍掉站外生成，收敛图文链路。
5. 重构联网搜索 -> 参考卡 -> 改写卡 -> 文案 -> 图片 prompt 的链路。
6. 接真实数据分析抓取；不可得字段不展示模拟值。
7. 替换图片精修模块。
8. 接首页视频背景和 favicon，做服务器性能检查。
9. 全量浏览器验证：单号、批量、发布清单、供应商视角、资产库、数据分析、多人刷新。

## 5. 必测清单

- `#/agent`：批量创作能新建、选账号、联网/非联网、确认执行、节点式看板有反馈。
- `#/studio/images`：单号图文能生成文案、生成图片 prompt、站内生成图片、进入审核。
- `#/delivery`：发布清单显示发布者；供应商端显示发布者；批量下载未下载可用；回传备注可见。
- `#/assets`：已发布生成图进入共享资产；BGM / 录屏留在剪辑素材库；上传重复 logo 去重；参考图弹窗可删除用户上传的非发布依赖图。
- `#/analytics`：粘贴真实链接后只展示真实抓取结果；失败展示真实原因；不出现模拟数据。
- 多账号测试：A 发布后 B 刷新可见；管理员不能看到 A 的未发布创作链路和草稿。
- 服务器部署前后：账号总量、发布清单、资产库、用户草稿不丢失。

## 6. 交接给平台部署3号的提醒

- 这批任务有本地代码改动和服务器部署关注点。若部署线程需要本地改代码，应把具体文件和原因回传给代码修复线程，不要直接在服务器上临时改出不可追踪版本。
- 所有敏感配置只放服务器环境变量或私密配置文件，不写入仓库。
- 当前本地 v28 已推送，下一波应从 `ce58b9a` 之后继续。
